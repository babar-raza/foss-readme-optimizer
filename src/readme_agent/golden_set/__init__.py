"""Wave 8.6 (`OPS-011`): continuous measurement of the supervisor loop's own
decision quality -- a sibling to, not a replacement of, `OPS-004`'s older,
narrower golden-set concept (which covers only the single
`relationship_explained` prose job). See `scenarios.py`, `harness.py`,
`aggregation.py`."""
