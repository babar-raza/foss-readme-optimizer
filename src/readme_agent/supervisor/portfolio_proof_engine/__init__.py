"""Resumable portfolio proof engine: preflight/facts-only/canaries/fleet/failed-only modes,
a 15-stage read-only receipt model, and the executable 30-point acceptance/dashboard layer.

See `portfolio_proof_engine/contracts.py` for why this is a read-only observational layer over
`supervisor/portfolio_scheduler/` and `commands_supervision.py`, never a second execution engine.
"""
