# Strict next-action prompt for the VS Code optimizer agent

You are repairing `foss-readme-optimizer`. Follow this order exactly. Do not create a skill, a new
controller, a second ledger architecture, a new workflow, or a product-specific/PSD-specific code
branch. Reuse the current fact graph, composition ledger, check bridge, stage scheduler, review
clients, cache, and production workflow. Do not touch any product repository or perform a remote
write.

## Authority and starting checks

1. Start from GitHub main at exactly
   `685246a7a4dc014adcdcd3da5be8ca49498ee2ed`. Acceptance machinery is unchanged from the audit
   pin `6d112bbf`; `685246a7` adds only Decision #107 control-repo post-commit auto-push.
   Confirm the worktree and read `AGENTS.md` fully.
2. Read these audit files before editing:
   `work/owner_audit/acceptance_runner/REPORT.md`, `MATRIX.json`, and `REPRODUCTION.md`.
3. Record that the formal runtime denominator is 33. Never write “30/30” or “POC complete” unless
   the runtime registry actually has that denominator and every current contract proves it.
4. Run the official complete non-live gate first. The existing five failures are blockers; repair
   them without updating expected hashes merely to bless drift. Commit that repair independently.
5. Before any commit, prove `origin` is exactly the intended `babar-raza/foss-readme-optimizer`
   control repository. Decision #107 will auto-push the commit. Never install or invoke that hook
   in a product checkout. Log a bounded follow-up to exact-allowlist the control origin and durably
   report pending/rejected pushes; do not let that follow-up displace the acceptance sequence.

## Commit 1 — knowledge trust and candidate accountability

1. In `facts/aspose_knowledge_selection.py`, preserve verification/disposition per selected claim
   item. A field-level `FactRecordV2` must not become wholly verified because one item is verified.
2. Replace file-existence "corroboration" with semantic evidence resolution: support both `file`
   and `source_file`, bind the cited path plus symbol/line/snippet/polarity where present, and
   reject contradictory positive/limitation claims.
3. Add a regression using the real imported 3D/Python FBX contradiction. The output must not
   authorize FBX export as working while exporter methods are `NotImplementedError`.
4. Add a candidate knowledge-disposition gate: every selected accepted item is exactly one of
   rendered, preserved-equivalent, superseded, or omitted-with-verified-reason. Unaccounted items
   reject candidate promotion.
5. Use existing composition-plan/template seams to render accepted capabilities, formats,
   installation facts, limitations, troubleshooting, and SEO. Do not add a parallel renderer.
6. Run focused tests plus the official four gates. Commit with the required AI co-author trailer.

## Commit 2 — exact Aspose checks and fail-closed reconciliation

1. Replace the vendored Aspose checks with the exact 103-function attached version and copy only
   its required direct helper changes. Verify the function-name inventory mechanically.
2. Classify all 14 additions. Issue-draft-only checks may be not applicable to README acceptance;
   every README hard gate must either run or have a typed evidence-backed not-applicable reason.
3. Extend `aspose_checks_bridge.py` through its existing input-pool seam. Supply old/new README,
   old/new Markdown, repository root, knowledge texts, examples, badges, dependency snapshot, and
   disposition projections from existing typed evidence.
4. Project `ReadmeCompositionLedgerV1` and the existing document plan into the four canonical
   content/structure/code-example/badge disposition shapes. Do not build another ledger.
5. Change candidate-stage promotion so any reconciliation error, unclassified check, check
   execution error, or skipped applicable hard gate rejects promotion. Persist the failed attempt
   and reason, but do not promote it.
6. Add mutation tests proving every promoted blocker catches its named defect. Fixture clean-pass
   alone is insufficient.
7. Bind the vendored checks, classification, bridge, coverage, reconciliation, and disposition
   projections into the current stage-dependency manifest and acceptance artifact chain.
8. Run focused tests plus the official four gates. Commit separately.

## Commit 3 — ecosystem and README-only truth

1. Reuse current ecosystem parsers/resolvers to implement `DependencySnapshotV1` for .NET, Java,
   C++, TypeScript, and Go. Preserve Python/Rust behavior. Do not return fabricated empty buckets.
2. Populate native-system/proprietary-runtime limitations only from accepted evidence. Add direct,
   transitive, optional, dev/build, native, and proprietary tests per applicable ecosystem.
3. Add one generic `README_ONLY` repository-shape route based on zero package roots. It may produce
   an honest document using verified identity, scope, license, and approved links while marking
   install/API/example slots not applicable. It must never invent code, package, API, or executable
   examples. Use PSD only as a fixture, not as a family branch.
4. Obtain a narrow supplemental Aspose PSD evidence bundle if available; if unavailable, state that
   parity for PSD is independently specified, not copied from unproven evidence.
5. Run focused tests plus the official four gates. Commit separately.

## Commit 4 — Qwen review, autonomous repair, no-op, and blocked retry

1. Keep the existing merged review optimization, but on `finish_reason=length`, malformed tool
   arguments, or schema-invalid output, automatically invoke the existing separate blind and
   factual clients with their 3000/6000-token budgets. Use bounded retry; record every call.
2. Preserve deterministic revalidation and the two-repair-attempt limit. Never convert reviewer
   failure into acceptance.
3. After `AGENT_APPROVED`, immediately enter the existing approved-cache path once in the same
   repository transaction and prove `NO_OP_PROVEN` with zero provider calls and identical hashes.
4. Reuse cached blocked decisions only for `infra_external`. `agent_fixable` and transient LLM
   format failures must be repaired/retried autonomously within the existing attempt budget; they
   must not require `--retry-blocked`.
5. Add regressions reproducing the exact committed 4000-token truncation failure and proving the
   fallback, then the zero-call no-op.
6. Run focused tests plus the official four gates. Commit separately.

## Commit 5 — verified hosted runner using existing workflow

1. Modify the existing hosted profile/job; do not add a workflow. Hosted candidate proof must allow
   local evidence writes, verify local product facts, require independent review, and target
   verified `NO_OP_PROVEN`, while retaining zero product-remote writes.
2. Make `commands_lifecycle.py::cmd_runtime_matrix` use the governed platform priority.
3. Restore and save each repository's `runs/readme-poc` bundle across hosted runs using a
   repository-scoped content-addressed cache/artifact handoff. Missing expected candidate artifacts
   must fail, not warn, after candidate generation is claimed.
4. Keep `max-parallel: 1` for the first proof. Add measured runtime/resource output per repo and
   only raise to 2 after isolation is proven. Keep hard per-job timeouts and add a per-stage budget
   so one member cannot consume the entire 120-minute job invisibly.
5. Reproduce the workflow with `act`, then run the official four gates. Commit separately.

## Acceptance campaign — no code edits during proof

1. Pin pre-refresh commits for 3D/Note/Barcode, plus one genuinely unrefreshed repo, one README-only
   repo, and one representative for every remaining ecosystem.
2. For every representative require: complete item-level knowledge disposition; complete source
   reconciliation; 103-check inventory; no unclassified/error/skipped-applicable-hard-gate result;
   deterministic pass; independent approval; bounded repair history; immediate zero-provider-call
   no-op; identical artifact hashes on two runs.
3. Compare with the exact Aspose candidate where available. Record differences as factual,
   structural, voice/clarity, or policy; fix only systemic causal owners.
4. Run the hosted read-only proof. Then run the complete dynamic 33-repository campaign.
5. Regenerate `plans/status.md` from durable state. Claim success only if it reports 33/33
   current-contract `AGENT_APPROVED` and 33/33 `NO_OP_PROVEN`, the official suite is green, and the
   aggregate reconstructs from persisted artifacts.

After each commit, report exact files, tests, results, remaining blocker, and next command. Stop on
the first failed acceptance gate and repair the causal owner. Do not edit governance/status to
claim progress that runtime evidence does not prove.
