# Local README Assessment and Composition Proof

Task: `L8-LOCAL-README-ASSESSMENT-COMPOSITION`

Control-repository commit:
`b2679e455efca1c09e9f4f701a6186f0df7c80e7`

The live, read-only proof exercised the production composition capability against heterogeneous
representatives for C++, Go, Java, .NET, Python, Rust, and TypeScript. Each representative
produced a snapshot-bound assessment, nonempty agentic composition plan, deterministic document
plan, complete candidate README, native patch, fact-to-claim map, deterministic validation, and
independent bundle reconstruction.

`acceptance-manifest.json` records:

- seven verified representatives and zero failures;
- exact expected material-section decision coverage for every representative;
- valid native patches and document validation;
- accepted-fact-only claim grounding;
- repository-specific candidates;
- all prompt-injection controls passing;
- a clean, stable control tree for the entire run; and
- zero remote writes.

The representative bundle directories contain the original README, `ProductFactsV2`,
`ReadmeAssessmentV1`, agentic composition plan, deterministic document plan, candidate, patch,
claim map, validation result, and artifact checksums. `sha256sums.txt` is the inventory for this
promoted evidence directory.

Reproduce from the repository root with the command in `reproduction-command.txt`. The command
requires the configured read-only repository and live LLM gateway access used by the proof.
