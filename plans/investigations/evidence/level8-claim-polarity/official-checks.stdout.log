All checks passed!
571 files already formatted
Success: no issues found in 324 source files
........................................................................ [  3%]
.........................................................FFF............ [  7%]
........................................................................ [ 11%]
........................................................................ [ 15%]
........................................................................ [ 19%]
........................................................................ [ 23%]
........................................................................ [ 27%]
........................................................................ [ 31%]
........................................................................ [ 34%]
........................................................................ [ 38%]
........................................................................ [ 42%]
........................................................................ [ 46%]
........................................................................ [ 50%]
........................................................................ [ 54%]
........................................................................ [ 58%]
........................................................................ [ 62%]
........................................................................ [ 66%]
........................................................................ [ 69%]
........................................................................ [ 73%]
........................................................................ [ 77%]
........................................................................ [ 81%]
........................................................................ [ 85%]
.........................EEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE...EE..EE [ 89%]
EEEEEEEEEEEEEEEEEEEE.................................................... [ 93%]
........................................................................ [ 97%]
....................................................                     [100%]
=================================== ERRORS ====================================
_ ERROR at setup of TestBasicLoop.test_facts_stage_limit_stops_before_every_specialist _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_facts_stage_limit_stops_b0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44836F20>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_stage_limit_requires_lifecycle_tracking _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_stage_limit_requires_life0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F42DE9940>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_local_poc_records_snapshot_and_profile_before_later_stages _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_local_poc_records_snapsho0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F42DEB000>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_local_poc_repairs_revalidates_and_rereviews_before_accepting _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_local_poc_repairs_revalid0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F430B01A0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_heterogeneous_local_poc_members_share_the_real_supervisor_path _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_heterogeneous_local_poc_m0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44837540>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_bootstrap_then_planner_capability_then_stop_converges _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_bootstrap_then_planner_ca0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E30E0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_planner_calling_the_real_stop_capability_converges_without_dispatch _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_planner_calling_the_real_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E3A10>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_stop_is_rejected_while_deterministic_work_remains[implicit] _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_stop_is_rejected_while_de0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E3CB0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_stop_is_rejected_while_deterministic_work_remains[capability] _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_stop_is_rejected_while_de1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E03D0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_deterministic_backstop_ends_a_run_of_repeated_duplicate_calls_early _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_deterministic_backstop_en0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E0830>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_deterministic_backstop_ends_a_run_of_repeated_unknown_capability_calls_early _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_deterministic_backstop_en1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E0A60>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBasicLoop.test_planner_never_consulted_when_it_would_only_repeat_itself _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_planner_never_consulted_w0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E0C90>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestOrgRepoTrustBoundary.test_planner_supplied_org_repo_is_overridden_not_trusted _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_planner_supplied_org_repo0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E1010>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestCapabilityGap.test_unknown_capability_gap_alongside_independent_passed_branch _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_unknown_capability_gap_al0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E1B70>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRepair.test_execution_error_triggers_an_automatic_repair_that_recovers _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_execution_error_triggers_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E39A0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestDurableConvergence.test_second_call_with_unchanged_upstream_converges_with_zero_planning_calls _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_second_call_with_unchange0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E3AF0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistDrivenConvergence.test_upstream_commit_changes_but_tracked_content_unchanged_converges_without_planning _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_upstream_commit_changes_b0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E2350>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistDrivenConvergence.test_tracked_content_change_with_specialist_failure_stops_before_planning _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_tracked_content_change_wi0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A82F0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistSkipIntegration.test_a_skipped_domain_prevents_the_no_tracked_change_shortcut _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_a_skipped_domain_prevents0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A8590>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistSkipIntegration.test_default_off_never_invokes_the_skip_decision _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_default_off_never_invokes0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A8520>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestModelRouteDisablement.test_disabled_route_blocks_before_any_work _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_disabled_route_blocks_bef0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44836740>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestModelRouteDisablement.test_no_recorded_status_proceeds_normally _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_no_recorded_status_procee0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A8A60>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestModelRouteDisablement.test_enabled_status_proceeds_normally _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_enabled_status_proceeds_n0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A9630>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestNotOnboardedGate.test_missing_both_fields_blocks_before_any_clone_or_dispatch _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_missing_both_fields_block0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A9DA0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestNotOnboardedGate.test_missing_ecosystem_only_still_blocks _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_missing_ecosystem_only_st0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A9D30>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestNotOnboardedGate.test_missing_policy_profile_only_still_blocks _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_missing_policy_profile_on0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402ABF50>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestNotOnboardedGate.test_unregistered_ruby_ecosystem_is_explicitly_unsupported _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_unregistered_ruby_ecosyst0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E0F30>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestNotOnboardedGate.test_fully_onboarded_entry_is_unaffected_by_the_new_gate _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_fully_onboarded_entry_is_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E2350>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBaselineCloneFailureDegradesGracefully.test_clone_failure_returns_blocked_with_evidence_instead_of_raising _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_clone_failure_returns_blo0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E0A60>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestBaselineCloneFailureDegradesGracefully.test_clone_failure_with_evidence_disabled_still_returns_blocked _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_clone_failure_with_eviden0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E7930>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMultiDomainCoexistence.test_all_domains_land_in_the_same_run_without_collision _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_all_domains_land_in_the_s0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E42F0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMultiDomainCoexistence.test_second_run_all_domains_independently_report_no_change _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_second_run_all_domains_in0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E4440>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistResultsEvidence.test_specialist_results_json_written_on_the_shortcut_path _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_specialist_results_json_w0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E4910>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistResultsEvidence.test_specialist_results_json_written_on_the_full_loop_path _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_specialist_results_json_w1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E4D00>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRunManifestV2Evidence.test_manifest_json_carries_the_new_run_manifest_v2_fields _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_manifest_json_carries_the0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E5400>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRunManifestV2Evidence.test_requirement_ids_exercised_reflects_independent_verifications_own_map _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_requirement_ids_exercised0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E5BE0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestEvidenceCompletenessGate.test_all_four_files_present_on_the_full_loop_path _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_all_four_files_present_on0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E6AC0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestEvidenceCompletenessGate.test_all_four_files_present_on_the_shortcut_path _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_all_four_files_present_on1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E6BA0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestEscalationAlert.test_escalation_alert_appears_when_threshold_is_crossed _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_escalation_alert_appears_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E74D0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestEscalationAlert.test_no_escalation_alert_below_threshold _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_no_escalation_alert_below0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A91D0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistFailureIsolation.test_a_raising_specialist_does_not_abort_the_run _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_a_raising_specialist_does0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A9DA0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestSpecialistFailureIsolation.test_a_raising_specialists_error_stops_before_general_planning _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_a_raising_specialists_err0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E8BE70>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestDomainCoverageTracking.test_a_crashing_domain_is_durably_recorded_with_the_error_colon_convention _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_a_crashing_domain_is_dura0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E8B230>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestDomainCoverageTracking.test_domain_coverage_complete_gates_the_coarse_shortcut _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_domain_coverage_complete_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E888A0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestLockContention.test_lock_already_held_is_blocked_not_silently_ignored _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_lock_already_held_is_bloc0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E88C90>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRunLockContention.test_run_lock_already_held_blocks_before_any_specialist_dispatch _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_run_lock_already_held_blo0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E88FA0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRunLockContention.test_run_lock_is_released_after_converged_no_tracked_change _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_run_lock_is_released_afte0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E892B0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRunLockContention.test_first_freshness_shortcut_acquires_zero_locks _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_first_freshness_shortcut_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E88E50>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestLockReleaseFailureDoesNotDiscardResult.test_release_lock_failure_does_not_discard_a_successful_result _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_release_lock_failure_does0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E8AE40>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestLockReleaseFailureDoesNotDiscardResult.test_release_run_lock_failure_does_not_discard_a_successful_result _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_release_run_lock_failure_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F44E8ADD0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestPreCloneShortcut.test_matching_probe_skips_the_clone_entirely _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_matching_probe_skips_the_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402A9DA0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestTokenBudget.test_exceeding_the_budget_blocks_the_run __

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_exceeding_the_budget_bloc0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F402E4D00>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestTokenBudget.test_missing_usage_never_trips_the_breaker_or_crashes _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_missing_usage_never_trips0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40314050>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestPlannerFailureEvidence.test_planner_llm_failure_still_writes_a_complete_evidence_bundle _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_planner_llm_failure_still0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40314750>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestRequiredIndependentVerificationGate.test_missing_verifier_blocks_before_planner_call _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_missing_verifier_blocks_b0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40314B40>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMaxTurns.test_planner_receives_the_advertised_final_turn_and_can_stop _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_planner_receives_the_adve0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40314E50>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMaxTurns.test_a_planner_that_never_stops_is_blocked_as_repair_exhausted_not_silently_capped _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_a_planner_that_never_stop0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40315160>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMaxTurns.test_known_specialist_failure_precedes_repair_exhaustion _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_known_specialist_failure_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40314D00>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestMaxTurns.test_known_specialist_failure_never_spends_a_general_planner_turn _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_known_specialist_failure_1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40315EF0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestWriteCapableModeGate.test_write_capable_capability_blocked_when_mode_not_full _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_write_capable_capability_0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F403167B0>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestWriteCapableModeGate.test_write_capable_capability_not_mode_blocked_when_mode_full _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_write_capable_capability_1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40317690>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
_ ERROR at setup of TestWriteCapableModeGate.test_write_capable_dispatch_denied_permission_is_infra_external _

tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_write_capable_dispatch_de0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F40317700>

    @pytest.fixture
    def project(tmp_path, monkeypatch):
        _FakeCompositionForcedToolClient.calls = 0
        source = _init_source_repo(tmp_path / "source")
        _setup_project_root(tmp_path, str(source))
        monkeypatch.chdir(tmp_path)
        # Wave 7b: github_generated_surface_audit is now a second, always-run
        # specialist -- unlike readme_reconciliation (which only ever touches the
        # local baseline clone, itself a local file:// path in these tests), its
        # classify step makes a real GitHub API call. Faked here so the entire
        # offline suite stays network-free, matching this project's own
        # `@pytest.mark.live` convention for anything that genuinely needs the
        # network.
        monkeypatch.setattr(audit_github_generated_surfaces, "repo_summary", _fake_repo_summary)
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_contributors", lambda org_repo, token: []
        )
        monkeypatch.setattr(
            audit_github_generated_surfaces, "list_languages", lambda org_repo, token: {"Java": 100}
        )
        # Wave 7c: package_release_audit is a third always-run specialist,
        # dispatching audit_package_release_surfaces (a real GitHub API call) and
        # the existing check_install_path (a real Maven Central resolution) --
        # both faked here for the same reason as 7b's mocks above.
        monkeypatch.setattr(audit_package_release_surfaces, "list_releases", lambda org_repo, token: [])
        monkeypatch.setattr(
            check_install_path,
            "inspect_repo",
            lambda org_repo, check_install=True: {
                "presentation_report": type(
                    "FakeReport",
                    (),
                    {
                        "install_path_resolved": None,
                        "evidence": {"install_path_resolved": "not checked"},
                    },
                )()
            },
        )
        # Wave 7d: metadata_presentation is a fourth always-run specialist,
        # dispatching propose_metadata_changes (a real GitHub API call, distinct
        # from 7b's own `repo_summary` import) -- faked for the same reason.
        monkeypatch.setattr(
            propose_metadata_changes,
            "repo_summary",
            lambda org_repo, token: {
                "description": "An existing description",
                "homepage": "https://example.org",
                "topics": ["java"],
            },
        )
        real_collect_product_facts = propose_metadata_changes.collect_product_facts

        def _complete_fixture_product_facts(org_repo):
            """Keep unrelated supervisor tests focused on runtime behavior.

            The synthetic repository intentionally represents a complete product.
            Populate the four metadata-dependent facts that the repository-local
            V1-to-V2 migration cannot infer, while dedicated facts tests continue
            to cover missing/conflicting behavior.
            """

            result = real_collect_product_facts(org_repo)
            values = {
                "product.audience": ["Java developers"],
                "product.problems_solved": ["creating, reading, and modifying document files"],
                "product.capabilities": ["create documents", "read documents", "modify documents"],
                "product.formats": ["document files"],
                "product.limitations": ["Fixture scope is intentionally minimal"],
                "installation.verified_acquisition": {
                    "method": "source_build",
                    "outcome": "SOURCE_BUILD_VERIFIED",
                    "detail": "verified by the synthetic local fixture",
                },
                "example.minimal": {
                    "language": "java",
                    "class_name": "Example",
                    "code": "public class Example {}",
                    "verification": {"status": "verified_fixture"},
                },
                "product.compatibility": ["Java 11 or later"],
                "product.license": "MIT",
                "relationship.commercial_foss": "Open-source scope with a separate commercial edition.",
            }
            for fact in result["product_facts_v2"]["facts"]:
                value = values.get(fact["field"])
                if value is None:
                    continue
                fact["value"] = value
                fact["verification_state"] = "verified"
                fact["confidence"] = 1.0
            return result

        monkeypatch.setattr(
            propose_metadata_changes,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
        import readme_agent.supervisor.product_truth as supervisor_product_truth

        monkeypatch.setattr(
            supervisor_product_truth,
            "collect_product_facts",
            _complete_fixture_product_facts,
        )
>       monkeypatch.setattr(
            facts_provider,
            "resolve",
            lambda ecosystem, coordinate: ResolutionResult(
                found=True,
                detail=f"offline fixture registry: {ecosystem}:{coordinate}",
            ),
        )
E       AttributeError: <module 'readme_agent.facts.provider' from 'D:\\Users\\prora\\OneDrive\\Documents\\GitHub\\foss-readme-optimizer\\src\\readme_agent\\facts\\provider.py'> has no attribute 'resolve'

D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\tests\unit\test_supervisor_loop.py:567: AttributeError
================================== FAILURES ===================================
______ TestGetProductFactsCapability.test_execute_combines_both_sources _______

self = <test_capabilities.TestGetProductFactsCapability object at 0x0000027F429ED090>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F429288A0>
tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_execute_combines_both_sou0')

    def test_execute_combines_both_sources(self, monkeypatch, tmp_path):
        from readme_agent.profile.schema import DetectedEcosystem, RepositoryProfile
        from readme_agent.registry.models import (
            BlockPolicy,
            LicenseElement,
            LinkSpec,
            PolicyProfile,
            RelationshipElement,
            RequiredElements,
            WordLimit,
        )

        fake_entry = SimpleNamespace(
            org="acme",
            repo_name="widget",
            mode="full",
            family="widget",
            platform="java",
            ecosystem="java",
            policy_profile="acme-widget",
        )
        fake_policy = PolicyProfile(
            schema_version=2,
            policy_profile="acme-widget",
            required_elements=RequiredElements(
                license_mentioned=LicenseElement(detected_license="MIT"),
                products_org_link=LinkSpec(
                    url="https://products.acme.org/widget/java/",
                    family_url="https://products.acme.org/widget/",
                    label="Widget",
                ),
                products_com_link=LinkSpec(
                    url="https://products.acme.com/widget/java/",
                    family_url="https://products.acme.com/widget/",
                    label="Widget",
                ),
                relationship_explained=RelationshipElement(
                    min_sentences=2, talking_points=["open_source_scope"]
                ),
            ),
            secondary_links=[],
            block=BlockPolicy(
                word_limit=WordLimit(min=10, max=200),
                prohibited_terms=[],
                link_whitelist_domains=[],
            ),
        )
        fake_profile = RepositoryProfile(
            org_repo="acme/widget",
            detected_ecosystems=[
                DetectedEcosystem(
                    ecosystem="java",
                    manifest_path="pom.xml",
                    confidence=1.0,
                    evidence="found pom.xml",
                )
            ],
            unresolved_manifests=[],
        )
        monkeypatch.setattr(facts_provider, "require_listed", lambda org_repo: fake_entry)
        monkeypatch.setattr(facts_provider, "load_policy", lambda profile: fake_policy)
        monkeypatch.setattr(
            facts_provider, "get_or_build_profile", lambda entry, **kwargs: fake_profile
        )

>       result = get_product_facts.execute("acme/widget")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\unit\test_capabilities.py:748:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src\readme_agent\capabilities\get_product_facts.py:91: in execute
    return collect_product_facts(
src\readme_agent\facts\provider.py:315: in collect_product_facts
    local_candidates, local_verification = _local_verification_facts(
src\readme_agent\facts\provider.py:200: in _local_verification_facts
    acquisition = _acquisition_fact(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

entry = namespace(org='acme', repo_name='widget', mode='full', family='widget', platform='java', ecosystem='java', policy_profile='acme-widget')
source_revision = None, observed_at = '2026-07-27T13:41:05.167193+00:00'
local_verification = None
unavailable_detail = 'no product_truth.minimal_example configured for this policy profile -- nothing to compile/run locally yet'

    def _acquisition_fact(
        entry: ProductEntry,
        source_revision: str | None,
        observed_at: str | None,
        local_verification: LocalProductVerificationV1 | None,
        unavailable_detail: str,
    ) -> FactRecordV2:
        """Select one receipt-backed registry or isolated source acquisition."""

        if source_revision is None:
            return FactRecordV2(
                fact_id=descriptive_fact_id("installation.verified_acquisition", "missing-revision"),
                field="installation.verified_acquisition",
                value={
                    "method": "unresolved",
                    "outcome": "BLOCKED_LOCAL_VERIFICATION",
                    "detail": "immutable source revision is required for acquisition truth",
                    "truth_eligible": False,
                },
                source=FactSourceV2(
                    source_type="mechanical_repository",
>                   location=f"repository://{entry.org_repo}",
                                             ^^^^^^^^^^^^^^
                    retrieved_at=observed_at,
                ),
                verification_state="blocked",
                authoritative_owner="repository-owner",
                confidence=0.0,
                affected_surfaces=SURFACE_DEPENDENCIES["installation.verified_acquisition"],
            )
E           AttributeError: 'types.SimpleNamespace' object has no attribute 'org_repo'

src\readme_agent\facts\provider.py:55: AttributeError
______ TestGetProductFactsCapability.test_execute_exposes_package_roots _______

self = <test_capabilities.TestGetProductFactsCapability object at 0x0000027F429ED1D0>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F4359B070>
tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_execute_exposes_package_r0')

    def test_execute_exposes_package_roots(self, monkeypatch, tmp_path):
        """Wave 11.3 (`FACT-010`): additive -- `package_roots` was already
        computed inside `profile` (Wave 11.1) but never exposed here."""
        from readme_agent.profile.schema import PackageRoot, RepositoryProfile
        from readme_agent.registry.models import (
            BlockPolicy,
            LicenseElement,
            LinkSpec,
            PolicyProfile,
            RelationshipElement,
            RequiredElements,
            WordLimit,
        )

        fake_entry = SimpleNamespace(
            org="acme",
            repo_name="widget",
            mode="full",
            family="widget",
            platform="java",
            ecosystem="java",
            policy_profile="acme-widget",
        )
        fake_policy = PolicyProfile(
            schema_version=2,
            policy_profile="acme-widget",
            required_elements=RequiredElements(
                license_mentioned=LicenseElement(detected_license="MIT"),
                products_org_link=LinkSpec(
                    url="https://products.acme.org/widget/java/",
                    family_url="https://products.acme.org/widget/",
                    label="Widget",
                ),
                products_com_link=LinkSpec(
                    url="https://products.acme.com/widget/java/",
                    family_url="https://products.acme.com/widget/",
                    label="Widget",
                ),
                relationship_explained=RelationshipElement(
                    min_sentences=2, talking_points=["open_source_scope"]
                ),
            ),
            secondary_links=[],
            block=BlockPolicy(
                word_limit=WordLimit(min=10, max=200),
                prohibited_terms=[],
                link_whitelist_domains=[],
            ),
        )
        fake_profile = RepositoryProfile(
            org_repo="acme/widget",
            package_roots=[
                PackageRoot(
                    path=".",
                    ecosystem="java",
                    manifest_path="pom.xml",
                    confidence=1.0,
                    evidence="found pom.xml",
                )
            ],
        )
        monkeypatch.setattr(facts_provider, "require_listed", lambda org_repo: fake_entry)
        monkeypatch.setattr(facts_provider, "load_policy", lambda profile: fake_policy)
        monkeypatch.setattr(
            facts_provider, "get_or_build_profile", lambda entry, **kwargs: fake_profile
        )

>       result = get_product_facts.execute("acme/widget")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\unit\test_capabilities.py:835:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src\readme_agent\capabilities\get_product_facts.py:91: in execute
    return collect_product_facts(
src\readme_agent\facts\provider.py:315: in collect_product_facts
    local_candidates, local_verification = _local_verification_facts(
src\readme_agent\facts\provider.py:200: in _local_verification_facts
    acquisition = _acquisition_fact(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

entry = namespace(org='acme', repo_name='widget', mode='full', family='widget', platform='java', ecosystem='java', policy_profile='acme-widget')
source_revision = None, observed_at = '2026-07-27T13:41:05.594749+00:00'
local_verification = None
unavailable_detail = 'no product_truth.minimal_example configured for this policy profile -- nothing to compile/run locally yet'

    def _acquisition_fact(
        entry: ProductEntry,
        source_revision: str | None,
        observed_at: str | None,
        local_verification: LocalProductVerificationV1 | None,
        unavailable_detail: str,
    ) -> FactRecordV2:
        """Select one receipt-backed registry or isolated source acquisition."""

        if source_revision is None:
            return FactRecordV2(
                fact_id=descriptive_fact_id("installation.verified_acquisition", "missing-revision"),
                field="installation.verified_acquisition",
                value={
                    "method": "unresolved",
                    "outcome": "BLOCKED_LOCAL_VERIFICATION",
                    "detail": "immutable source revision is required for acquisition truth",
                    "truth_eligible": False,
                },
                source=FactSourceV2(
                    source_type="mechanical_repository",
>                   location=f"repository://{entry.org_repo}",
                                             ^^^^^^^^^^^^^^
                    retrieved_at=observed_at,
                ),
                verification_state="blocked",
                authoritative_owner="repository-owner",
                confidence=0.0,
                affected_surfaces=SURFACE_DEPENDENCIES["installation.verified_acquisition"],
            )
E           AttributeError: 'types.SimpleNamespace' object has no attribute 'org_repo'

src\readme_agent\facts\provider.py:55: AttributeError
_______ TestGetProductFactsCapability.test_execute_allows_disabled_repo _______

self = <test_capabilities.TestGetProductFactsCapability object at 0x0000027F429863F0>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000027F436251D0>
tmp_path = WindowsPath('C:/Windows/Temp/pytest-of-prora/pytest-232/test_execute_allows_disabled_r1')

    def test_execute_allows_disabled_repo(self, monkeypatch, tmp_path):
        """Decision #40: mode == "disabled" means push access is unverified,
        not "excluded from analysis" -- this read-only capability runs
        against every registered repo regardless of mode."""
        from readme_agent.profile.schema import RepositoryProfile
        from readme_agent.registry.models import (
            BlockPolicy,
            LicenseElement,
            LinkSpec,
            PolicyProfile,
            RelationshipElement,
            RequiredElements,
            WordLimit,
        )

        fake_entry = SimpleNamespace(
            org="acme",
            repo_name="widget",
            mode="disabled",
            family="widget",
            platform="java",
            ecosystem="java",
            policy_profile="acme-widget",
        )
        fake_policy = PolicyProfile(
            schema_version=2,
            policy_profile="acme-widget",
            required_elements=RequiredElements(
                license_mentioned=LicenseElement(detected_license="MIT"),
                products_org_link=LinkSpec(
                    url="https://products.acme.org/widget/java/",
                    family_url="https://products.acme.org/widget/",
                    label="Widget",
                ),
                products_com_link=LinkSpec(
                    url="https://products.acme.com/widget/java/",
                    family_url="https://products.acme.com/widget/",
                    label="Widget",
                ),
                relationship_explained=RelationshipElement(
                    min_sentences=2, talking_points=["open_source_scope"]
                ),
            ),
            secondary_links=[],
            block=BlockPolicy(
                word_limit=WordLimit(min=10, max=200),
                prohibited_terms=[],
                link_whitelist_domains=[],
            ),
        )
        fake_profile = RepositoryProfile(
            org_repo="acme/widget", detected_ecosystems=[], unresolved_manifests=[]
        )
        monkeypatch.setattr(facts_provider, "require_listed", lambda org_repo: fake_entry)
        monkeypatch.setattr(facts_provider, "load_policy", lambda profile: fake_policy)
        monkeypatch.setattr(
            facts_provider, "get_or_build_profile", lambda entry, **kwargs: fake_profile
        )

>       result = get_product_facts.execute("acme/widget")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\unit\test_capabilities.py:914:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
src\readme_agent\capabilities\get_product_facts.py:91: in execute
    return collect_product_facts(
src\readme_agent\facts\provider.py:315: in collect_product_facts
    local_candidates, local_verification = _local_verification_facts(
src\readme_agent\facts\provider.py:200: in _local_verification_facts
    acquisition = _acquisition_fact(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

entry = namespace(org='acme', repo_name='widget', mode='disabled', family='widget', platform='java', ecosystem='java', policy_profile='acme-widget')
source_revision = None, observed_at = '2026-07-27T13:41:05.633218+00:00'
local_verification = None
unavailable_detail = 'no product_truth.minimal_example configured for this policy profile -- nothing to compile/run locally yet'

    def _acquisition_fact(
        entry: ProductEntry,
        source_revision: str | None,
        observed_at: str | None,
        local_verification: LocalProductVerificationV1 | None,
        unavailable_detail: str,
    ) -> FactRecordV2:
        """Select one receipt-backed registry or isolated source acquisition."""

        if source_revision is None:
            return FactRecordV2(
                fact_id=descriptive_fact_id("installation.verified_acquisition", "missing-revision"),
                field="installation.verified_acquisition",
                value={
                    "method": "unresolved",
                    "outcome": "BLOCKED_LOCAL_VERIFICATION",
                    "detail": "immutable source revision is required for acquisition truth",
                    "truth_eligible": False,
                },
                source=FactSourceV2(
                    source_type="mechanical_repository",
>                   location=f"repository://{entry.org_repo}",
                                             ^^^^^^^^^^^^^^
                    retrieved_at=observed_at,
                ),
                verification_state="blocked",
                authoritative_owner="repository-owner",
                confidence=0.0,
                affected_surfaces=SURFACE_DEPENDENCIES["installation.verified_acquisition"],
            )
E           AttributeError: 'types.SimpleNamespace' object has no attribute 'org_repo'

src\readme_agent\facts\provider.py:55: AttributeError
=========================== short test summary info ===========================
FAILED tests/unit/test_capabilities.py::TestGetProductFactsCapability::test_execute_combines_both_sources
FAILED tests/unit/test_capabilities.py::TestGetProductFactsCapability::test_execute_exposes_package_roots
FAILED tests/unit/test_capabilities.py::TestGetProductFactsCapability::test_execute_allows_disabled_repo
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_facts_stage_limit_stops_before_every_specialist
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_stage_limit_requires_lifecycle_tracking
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_local_poc_records_snapshot_and_profile_before_later_stages
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_local_poc_repairs_revalidates_and_rereviews_before_accepting
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_heterogeneous_local_poc_members_share_the_real_supervisor_path
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_bootstrap_then_planner_capability_then_stop_converges
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_planner_calling_the_real_stop_capability_converges_without_dispatch
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_stop_is_rejected_while_deterministic_work_remains[implicit]
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_stop_is_rejected_while_deterministic_work_remains[capability]
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_deterministic_backstop_ends_a_run_of_repeated_duplicate_calls_early
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_deterministic_backstop_ends_a_run_of_repeated_unknown_capability_calls_early
ERROR tests/unit/test_supervisor_loop.py::TestBasicLoop::test_planner_never_consulted_when_it_would_only_repeat_itself
ERROR tests/unit/test_supervisor_loop.py::TestOrgRepoTrustBoundary::test_planner_supplied_org_repo_is_overridden_not_trusted
ERROR tests/unit/test_supervisor_loop.py::TestCapabilityGap::test_unknown_capability_gap_alongside_independent_passed_branch
ERROR tests/unit/test_supervisor_loop.py::TestRepair::test_execution_error_triggers_an_automatic_repair_that_recovers
ERROR tests/unit/test_supervisor_loop.py::TestDurableConvergence::test_second_call_with_unchanged_upstream_converges_with_zero_planning_calls
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistDrivenConvergence::test_upstream_commit_changes_but_tracked_content_unchanged_converges_without_planning
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistDrivenConvergence::test_tracked_content_change_with_specialist_failure_stops_before_planning
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistSkipIntegration::test_a_skipped_domain_prevents_the_no_tracked_change_shortcut
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistSkipIntegration::test_default_off_never_invokes_the_skip_decision
ERROR tests/unit/test_supervisor_loop.py::TestModelRouteDisablement::test_disabled_route_blocks_before_any_work
ERROR tests/unit/test_supervisor_loop.py::TestModelRouteDisablement::test_no_recorded_status_proceeds_normally
ERROR tests/unit/test_supervisor_loop.py::TestModelRouteDisablement::test_enabled_status_proceeds_normally
ERROR tests/unit/test_supervisor_loop.py::TestNotOnboardedGate::test_missing_both_fields_blocks_before_any_clone_or_dispatch
ERROR tests/unit/test_supervisor_loop.py::TestNotOnboardedGate::test_missing_ecosystem_only_still_blocks
ERROR tests/unit/test_supervisor_loop.py::TestNotOnboardedGate::test_missing_policy_profile_only_still_blocks
ERROR tests/unit/test_supervisor_loop.py::TestNotOnboardedGate::test_unregistered_ruby_ecosystem_is_explicitly_unsupported
ERROR tests/unit/test_supervisor_loop.py::TestNotOnboardedGate::test_fully_onboarded_entry_is_unaffected_by_the_new_gate
ERROR tests/unit/test_supervisor_loop.py::TestBaselineCloneFailureDegradesGracefully::test_clone_failure_returns_blocked_with_evidence_instead_of_raising
ERROR tests/unit/test_supervisor_loop.py::TestBaselineCloneFailureDegradesGracefully::test_clone_failure_with_evidence_disabled_still_returns_blocked
ERROR tests/unit/test_supervisor_loop.py::TestMultiDomainCoexistence::test_all_domains_land_in_the_same_run_without_collision
ERROR tests/unit/test_supervisor_loop.py::TestMultiDomainCoexistence::test_second_run_all_domains_independently_report_no_change
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistResultsEvidence::test_specialist_results_json_written_on_the_shortcut_path
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistResultsEvidence::test_specialist_results_json_written_on_the_full_loop_path
ERROR tests/unit/test_supervisor_loop.py::TestRunManifestV2Evidence::test_manifest_json_carries_the_new_run_manifest_v2_fields
ERROR tests/unit/test_supervisor_loop.py::TestRunManifestV2Evidence::test_requirement_ids_exercised_reflects_independent_verifications_own_map
ERROR tests/unit/test_supervisor_loop.py::TestEvidenceCompletenessGate::test_all_four_files_present_on_the_full_loop_path
ERROR tests/unit/test_supervisor_loop.py::TestEvidenceCompletenessGate::test_all_four_files_present_on_the_shortcut_path
ERROR tests/unit/test_supervisor_loop.py::TestEscalationAlert::test_escalation_alert_appears_when_threshold_is_crossed
ERROR tests/unit/test_supervisor_loop.py::TestEscalationAlert::test_no_escalation_alert_below_threshold
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistFailureIsolation::test_a_raising_specialist_does_not_abort_the_run
ERROR tests/unit/test_supervisor_loop.py::TestSpecialistFailureIsolation::test_a_raising_specialists_error_stops_before_general_planning
ERROR tests/unit/test_supervisor_loop.py::TestDomainCoverageTracking::test_a_crashing_domain_is_durably_recorded_with_the_error_colon_convention
ERROR tests/unit/test_supervisor_loop.py::TestDomainCoverageTracking::test_domain_coverage_complete_gates_the_coarse_shortcut
ERROR tests/unit/test_supervisor_loop.py::TestLockContention::test_lock_already_held_is_blocked_not_silently_ignored
ERROR tests/unit/test_supervisor_loop.py::TestRunLockContention::test_run_lock_already_held_blocks_before_any_specialist_dispatch
ERROR tests/unit/test_supervisor_loop.py::TestRunLockContention::test_run_lock_is_released_after_converged_no_tracked_change
ERROR tests/unit/test_supervisor_loop.py::TestRunLockContention::test_first_freshness_shortcut_acquires_zero_locks
ERROR tests/unit/test_supervisor_loop.py::TestLockReleaseFailureDoesNotDiscardResult::test_release_lock_failure_does_not_discard_a_successful_result
ERROR tests/unit/test_supervisor_loop.py::TestLockReleaseFailureDoesNotDiscardResult::test_release_run_lock_failure_does_not_discard_a_successful_result
ERROR tests/unit/test_supervisor_loop.py::TestPreCloneShortcut::test_matching_probe_skips_the_clone_entirely
ERROR tests/unit/test_supervisor_loop.py::TestTokenBudget::test_exceeding_the_budget_blocks_the_run
ERROR tests/unit/test_supervisor_loop.py::TestTokenBudget::test_missing_usage_never_trips_the_breaker_or_crashes
ERROR tests/unit/test_supervisor_loop.py::TestPlannerFailureEvidence::test_planner_llm_failure_still_writes_a_complete_evidence_bundle
ERROR tests/unit/test_supervisor_loop.py::TestRequiredIndependentVerificationGate::test_missing_verifier_blocks_before_planner_call
ERROR tests/unit/test_supervisor_loop.py::TestMaxTurns::test_planner_receives_the_advertised_final_turn_and_can_stop
ERROR tests/unit/test_supervisor_loop.py::TestMaxTurns::test_a_planner_that_never_stops_is_blocked_as_repair_exhausted_not_silently_capped
ERROR tests/unit/test_supervisor_loop.py::TestMaxTurns::test_known_specialist_failure_precedes_repair_exhaustion
ERROR tests/unit/test_supervisor_loop.py::TestMaxTurns::test_known_specialist_failure_never_spends_a_general_planner_turn
ERROR tests/unit/test_supervisor_loop.py::TestWriteCapableModeGate::test_write_capable_capability_blocked_when_mode_not_full
ERROR tests/unit/test_supervisor_loop.py::TestWriteCapableModeGate::test_write_capable_capability_not_mode_blocked_when_mode_full
ERROR tests/unit/test_supervisor_loop.py::TestWriteCapableModeGate::test_write_capable_dispatch_denied_permission_is_infra_external
3 failed, 1787 passed, 34 deselected, 62 errors in 495.44s (0:08:15)
Plan structure clean (59 warning(s)).
check_verifiers_are_wired: every verify_* function has a call site in src/
Prompt hygiene clean: 10 active prompt(s)
Level-8 requirement coverage current: 425 rows
Checked 157 IMPLEMENTED rows.
  0 row(s) with a semantic closure finding.
  86 row(s) with informational-only findings (no test path cited).
  71 row(s) fully clean.

=== Tree precondition (start) ===
TREE CLEAN

=== ruff check ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe -m ruff check .
--- ruff check: OK

=== ruff format --check ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe -m ruff format --check .
--- ruff format --check: OK

=== mypy src ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe -m mypy src
--- mypy src: OK

=== pytest -q (full non-live suite) ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe -m pytest -q
--- pytest -q (full non-live suite): FAILED (exit 1)

=== validate_plan_structure.py ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\scripts\governance\validate_plan_structure.py
--- validate_plan_structure.py: OK

=== check_verifiers_are_wired.py --check (PRODSYS-P2-T1) ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\scripts\governance\check_verifiers_are_wired.py --check
--- check_verifiers_are_wired.py --check (PRODSYS-P2-T1): OK

=== check_prompt_hygiene.py ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\scripts\governance\check_prompt_hygiene.py
--- check_prompt_hygiene.py: OK

=== build_level8_requirement_taskcard_coverage.py --check (PRODSYS-P1-T3) ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\scripts\governance\build_level8_requirement_taskcard_coverage.py --check
--- build_level8_requirement_taskcard_coverage.py --check (PRODSYS-P1-T3): OK

=== traceability_matrix.py --check ===
$ D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\.venv\Scripts\python.exe D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer\plans\investigations\tools\traceability_matrix.py --check
--- traceability_matrix.py --check: OK

=== actionlint (workflow syntax) ===
--- actionlint not found on PATH -- skipped (install it for a real syntax check)

=== Tree precondition (end) ===
TREE CLEAN

============================================================
Tree state: TREE CLEAN
At least one required official check failed -- see output above.
