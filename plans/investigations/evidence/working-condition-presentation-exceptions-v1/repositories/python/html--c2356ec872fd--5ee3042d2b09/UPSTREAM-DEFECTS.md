# Upstream defects — aspose-html-foss/Aspose.HTML-FOSS-for-Python

Source revision: `c2356ec872fd7d64c14a0ae8cc043eea1a03847e`. Items below failed deterministic
verification and are hidden from the generated README (working-condition
presentation). They need fixes in the product repository or richer evidence.

- `installation.verified_acquisition` is blocked: exact public Python imports/example executed from the immutable source tree; package installation is blocked by an invalid build backend
