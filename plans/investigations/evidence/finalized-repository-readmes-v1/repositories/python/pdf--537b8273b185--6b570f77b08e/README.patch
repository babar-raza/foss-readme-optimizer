diff --git a/README.md b/README.md
index 7cdabb7..2745800 100644
--- a/README.md
+++ b/README.md
@@ -1,68 +1,148 @@
 # Aspose.PDF FOSS for Python
 
-[![CI](https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python/actions/workflows/ci.yml/badge.svg)](https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python/actions/workflows/ci.yml)
-[![Python 3.11+](https://img.shields.io/badge/python-3.11%2B-blue.svg)](https://www.python.org/downloads/)
-[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
+[![Version: 0.1.0a0](https://img.shields.io/badge/Version-0.1.0a0-blue)](https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python/tree/537b8273b185e4f7440b201cacad56567e55b2f0) ![Platform: Python](https://img.shields.io/badge/Platform-Python-blue) ![Requires: Python >=3.11](https://img.shields.io/badge/Requires-Python%20%3E%3D3.11-blue) [![License: MIT](https://img.shields.io/badge/License-MIT-blue)](LICENSE) [![Contributors: aspose-pdf-foss/Aspose-PDF-FOSS-for-Python](https://img.shields.io/github/contributors/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python.svg)](https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python/graphs/contributors)
+
+Aspose.PDF FOSS for Python is an open-source library for developers using Python. It reads PDF files and writes PDF files, PNG files, and TIFF files.
+
+## Navigation
+
+- [At a Glance](#at-a-glance)
+- [Key Capabilities](#key-capabilities)
+- [Installation](#installation)
+- [Quick Start](#quick-start)
+- [Additional Examples](#additional-examples)
+- [API Reference](#api-reference)
+- [Documentation and Resources](#documentation-and-resources)
+- [Scope and Limitations](#scope-and-limitations)
+- [Development and Testing](#development-and-testing)
+- [Security](#security)
+- [Contributing](#contributing)
+- [License](#license)
+
+## At a Glance
+
+```mermaid
+flowchart LR
+  subgraph Inputs["Inputs and Formats"]
+    I1["PDF files"]
+  end
+
+  PRODUCT["Aspose.PDF FOSS for Python"]
+
+  subgraph Capabilities["Core Capabilities"]
+    direction LR
+    subgraph CapabilityColumn1[" "]
+      direction TB
+      C1["Create, load, save, merge, and inspect PDF documents"]
+      C2["Add and edit text and images, including text replacement and redaction"]
+      C3["Extract text, images, and attachments"]
+      C4["Concatenate, extract, insert, delete, and append PDF pages"]
+      C5["Render PDF pages to PNG or TIFF images"]
+      C6["Create and manage interactive form fields"]
+      C1 ~~~ C2
+      C2 ~~~ C3
+      C3 ~~~ C4
+      C4 ~~~ C5
+      C5 ~~~ C6
+    end
+    subgraph CapabilityColumn2[" "]
+      direction TB
+      C7["Add, update, and remove PDF annotations"]
+      C8["Encrypt, decrypt, optimize, and compress PDF documents"]
+      C9["Run heuristic PDF/A and PDF/UA validation"]
+      C10["Resource limit configuration"]
+      C11["XMP metadata handling"]
+      C12["Digital signature support"]
+      C7 ~~~ C8
+      C8 ~~~ C9
+      C9 ~~~ C10
+      C10 ~~~ C11
+      C11 ~~~ C12
+    end
+  end
+  style CapabilityColumn1 fill:none,stroke:none
+  style CapabilityColumn2 fill:none,stroke:none
+
+  subgraph Outputs["Outputs"]
+    O1["PDF files"]
+    O2["PNG files"]
+    O3["TIFF files"]
+  end
+
+  I1 --- PRODUCT
+  PRODUCT --- Capabilities
+  Capabilities --- Outputs
+```
 
-Aspose.PDF FOSS for Python is an open-source Python library for creating,
-reading, editing, rendering, and validating PDF documents.
+## Key Capabilities
 
-The package is implemented in Python and ships type information. The project is
-currently in alpha, so APIs and feature coverage may evolve before the first
-stable release.
+- **Create and manage PDF documents** - Load, save, merge, and inspect files throughout the document lifecycle. Available through the public `Document` API.
+- **Edit text and images in PDF documents** - Replace or redact text and update visual page content.
+- **Extract images and attached files from PDF files** - Retrieve text, embedded images, and file attachments.
+- **Concatenate, extract, insert, delete, and append PDF pages** - Reorder pages or move selected pages between documents. Available through the public `Page` API.
+- **Render PDF pages to PNG or TIFF images** - Produce PNG and TIFF image output from individual pages. Available through the public `Page` API.
+- **Create and manage interactive form fields** - Work with interactive data-entry controls and their values. Available through the public `Form` and `Field` APIs.
+- **Add, update, and remove PDF annotations** - Manage annotations as document content changes. Available through the public `Annotation` API.
+- **Encrypt, decrypt, optimize, and compress PDF documents** - Protect document content through encryption while controlling file size. Available through the public `Document` API.
+- **Run heuristic PDF/A and PDF/UA validation** - Check archival and accessibility conformance profiles.
+- **Configure PDF resource limits** - Control resource-use limits during document processing.
+- **Work with XMP metadata and low-level PDF objects** - Parse and serialize metadata packets while inspecting low-level PDF objects. Available through the public `XmpPacket`, `parse_xmp`, `serialize_xmp`, and `CosExtractor` APIs.
+- **Work with PDF digital signatures** - Support document-signing workflows.
 
-## Features
+<details>
+<summary>View Detailed Capabilities</summary>
 
-- Create, load, save, merge, split, and inspect PDF documents
 - Add Standard-14 or embedded Unicode text, including shaped bidirectional
   text, plus images, lines, rectangles, annotations, attachments, and form data
-- Extract text, images, attachments, metadata, and bookmarks
-- Render pages to PNG or TIFF
-- Replace or redact text in supported content streams
-- Encrypt and decrypt documents with RC4 or AES
-- Create and inspect PDF signatures
-- Optimize streams, images, fonts, and unused objects
-- Apply configurable resource limits when processing untrusted PDFs
-- Work with XMP metadata and low-level PDF objects
-- Perform heuristic PDF/A and PDF/UA checks and conversions
-
-See the [supported features](supported-features.md) document for the
-detailed capability matrix and known limitations.
-
-## Requirements
-
-- Python 3.11 or newer
-- `cryptography`
-- `asn1crypto`
 
-Optional extras add Pillow-based image support, Brotli-based WOFF2 decoding,
-and HarfBuzz/bidi complex-text layout:
 
-```bash
-python -m pip install 'aspose-pdf-foss-for-python[images,woff2,text-layout]'
-```
+</details>
 
 ## Installation
 
-Install a published prerelease:
+Install the package directly from its source repository:
 
 ```bash
-python -m pip install --pre aspose-pdf-foss-for-python
+git clone https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python.git
+cd Aspose-PDF-FOSS-for-Python
+git checkout --detach 537b8273b185e4f7440b201cacad56567e55b2f0
+python -m pip install .
 ```
 
-Install the latest source checkout for development:
+Use source installation for the `aspose-pdf-foss-for-python` distribution.
+
+Optional dependency groups declared in `pyproject.toml`:
+- `dev`: `python -m pip install ".[dev]"`
+- `fuzz`: `python -m pip install ".[fuzz]"`
+- `images`: `python -m pip install ".[images]"`
+- `text-layout`: `python -m pip install ".[text-layout]"`
+- `woff2`: `python -m pip install ".[woff2]"`
+
+Required runtime dependencies declared in `pyproject.toml`: `cryptography>=42`, `asn1crypto>=1.5`.
+
+- optional capability: `python -m pip install Pillow`
+
+<details>
+<summary>View Dependency and Extra Details</summary>
+
+- `cryptography`
+
+
+- `asn1crypto`
+
+
+Optional extras add Pillow-based image support, Brotli-based WOFF2 decoding,
+and HarfBuzz/bidi complex-text layout:
+
 
 ```bash
-git clone https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python.git
-cd Aspose-PDF-FOSS-for-Python
-python3 -m venv .venv
-source .venv/bin/activate
-python -m pip install -e '.[dev]'
+python -m pip install 'aspose-pdf-foss-for-python[images,woff2,text-layout]'
 ```
 
-## Quick Start
 
-### Create a PDF
+</details>
+
+## Quick Start
 
 ```python
 from aspose_pdf import Document
@@ -70,7 +150,7 @@ from aspose_pdf import Document
 with Document() as document:
     page = document.pages.add()
     page.add_text(
-        "Hello from Aspose.PDF FOSS!",
+        "Hello from Aspose.PDF FOSS for Python!",
         x=72,
         y=720,
         font_size=18,
@@ -78,29 +158,30 @@ with Document() as document:
     document.save("hello.pdf")
 ```
 
-For Unicode outside the Standard-14 encodings, provide an embeddable
-TrueType/OpenType font as bytes, a path, or a `FontDescriptor`:
+## Additional Examples
+
+Expand this section to view examples for creating a PDF, exporting a document to PDF, reading a document, and extracting text, plus 1 more workflow.
+
+<details>
+<summary>View additional examples and results</summary>
+
+### Create a PDF
 
 ```python
+from aspose_pdf import Document
+
 with Document() as document:
     page = document.pages.add()
     page.add_text(
-        "Latin Č · Кириллица · Ελληνικά · 漢字",
+        "Hello from Aspose.PDF FOSS!",
         x=72,
         y=720,
-        font="NotoSans-Regular.ttf",
+        font_size=18,
     )
-    document.save("unicode.pdf")
+    document.save("hello.pdf")
 ```
 
-The writer creates a subset Type0/CID font, two-byte character codes,
-`/ToUnicode`, widths, and the required CID-to-glyph mapping. A character that
-the supplied font cannot represent raises `FontEmbeddingException` instead of
-silently writing `.notdef`.
-
-Install the `text-layout` extra and pass `TextLayoutOptions` for OpenType
-shaping, bidirectional runs, ordered font fallback, and width-constrained line
-layout:
+### Export a Document to PDF
 
 ```python
 from aspose_pdf import Document, TextLayoutOptions
@@ -123,10 +204,7 @@ with Document() as document:
     document.save("complex-text.pdf")
 ```
 
-Logical text is retained for extraction while glyphs are painted in their
-shaped visual order.
-
-### Read a document
+### Read a Document
 
 ```python
 from aspose_pdf import Document
@@ -137,12 +215,7 @@ with Document("input.pdf") as document:
     print(document.info)
 ```
 
-`Document(source, password=..., limits=...)` accepts a path, raw bytes, or a
-binary stream and is equivalent to `Document().load_from(source, ...)`. A
-missing file, non-PDF data, or a missing password raises; neither form ever
-hands back a silently empty document.
-
-### Extract text
+### Extract Text
 
 ```python
 from aspose_pdf import PdfExtractor
@@ -153,17 +226,7 @@ with PdfExtractor() as extractor:
     print(extractor.get_text())
 ```
 
-### Merge PDF files
-
-```python
-from aspose_pdf import PdfFileEditor
-
-with PdfFileEditor() as editor:
-    if not editor.concatenate(["part-1.pdf", "part-2.pdf"], "merged.pdf"):
-        raise RuntimeError(editor.last_exception)
-```
-
-### Render a page
+### Render a Page
 
 ```python
 from aspose_pdf import Document
@@ -173,53 +236,331 @@ with Document() as document:
     document.pages[0].save_as_image("page-1.png", dpi=144)
 ```
 
-## Feature Boundaries
 
-This project aims to fail explicitly when an operation is unsupported, but PDF
-is a large format and coverage is not yet complete.
+</details>
 
-- Page rendering is best effort and does not implement every PDF graphics
-  feature.
-- PDF/A and PDF/UA validation is heuristic, not certification-grade.
-- OCR and layout reflow are not implemented.
-- Signature-chain, revocation, and timestamp validation have documented
-  limitations.
-- Compatibility modules may expose names whose operations are not implemented.
+## API Reference
 
-Review [supported-features.md](supported-features.md) before relying
-on the library for compliance-sensitive or security-sensitive workflows.
+The package declares 152 public exports across 20 export namespaces. Package namespaces include `aspose_pdf`, `aspose_pdf.annotations`, `aspose_pdf.clustering`, `aspose_pdf.engine.data`, `aspose_pdf.engine.primitives`, `aspose_pdf.generated`, `aspose_pdf.security`.
 
-## Development
+<details>
+<summary>View public API by namespace</summary>
 
-Activate the project virtual environment and install development dependencies:
+### Aspose.PDF Namespace (`aspose_pdf`)
 
-```bash
-source .venv/bin/activate
-python -m pip install -e '.[dev]'
-```
+| Type | Description |
+| --- | --- |
+| `AF_RELATIONSHIPS` | `AF_RELATIONSHIPS` is a verified public type exported by the `aspose_pdf` namespace. |
+| `Action` | `Action` is a verified public type exported by the `aspose_pdf` namespace. |
+| `Annotation` | Annotation: generate, retrieve property, and set property. Includes 10 additional members. |
+| `AnnotationCollection` | Annotation collection: add, clear content, and delete. Includes 2 additional members. |
+| `AnnotationFlags` | Annotation flags. Inherits from `IntFlag`. |
+| `AnnotationType` | Enumerates annotation type values. Values include `CARET`, `CIRCLE`, and `FILE_ATTACHMENT` and 21 more. |
+| `ByteArrayDataSource` | Byte array data source: read content, write output, and access to data. Inherits from `DataSource`. |
+| `CertificationLevel` | Enumerates certification level values. Values include `FORM_FILLING`, `FORM_FILLING_AND_ANNOTATIONS`, and `NO_CHANGES` and 1 more. |
+| `DataSource` | Data source: read content and write output. |
+| `Destination` | `Destination` is a verified public type exported by the `aspose_pdf` namespace. |
+| `Document` | Document: add, auto, and change. Includes 21 additional members. |
+| `Field` | Field: remove content, access to field type, and access to name. Includes 1 additional member. |
+| `FieldType` | Enumerates field type values. Values include `CHECKBOX`, `COMBOBOX`, and `LISTBOX` and 4 more. |
+| `FileDataSource` | File data source: read content and write output. Inherits from `DataSource`. |
+| `FileFontSource` | File font source: retrieve font definitions. Inherits from `FontSource`. |
+| `FileSpecification` | File specification: save output, access to contents, and access to creation date. Includes 6 additional members. |
+| `FitBDestination` | Fit b destination: access to page. Inherits from `Destination`. |
+| `FitBHDestination` | Fit bh destination: access to page and access to top. Inherits from `Destination`. |
+| `FitBVDestination` | Fit bv destination: access to left and access to page. Inherits from `Destination`. |
+| `FitDestination` | Fit destination: access to page. Inherits from `Destination`. |
+| `FitHDestination` | Fit h destination: access to page and access to top. Inherits from `Destination`. |
+| `FitRDestination` | Fit r destination: access to bottom, access to left, and access to page. Inherits from `Destination`. Includes 2 additional members. |
+| `FitVDestination` | Fit v destination: access to left and access to page. Inherits from `Destination`. |
+| `FolderFontSource` | Folder font source: retrieve font definitions. Inherits from `FontSource`. |
+| `FontDescriptor` | Font descriptor: retrieve font bytes, matches, and access to has font data. |
+| `FontEmbeddingException` | Signals a font embedding condition; derives from `AsposePdfException`. |
+| `FontRepository` | Font repository: add, clear content, and find. Includes 5 additional members. |
+| `FontSource` | Font source: retrieve font definitions. |
+| `Form` | Form: add, flatten, and generate. Includes 8 additional members. |
+| `FormType` | Enumerates form type values. Values include `from_string`, `DYNAMIC`, and `STANDARD`. |
+| `GoToAction` | Go to action: access to destination. Inherits from `Action`. |
+| `GoToRAction` | Go to r action: access to destination and access to file. Inherits from `Action`. |
+| `JavaScriptAction` | Java script action: access to script. Inherits from `Action`. |
+| `LaunchAction` | Launch action: access to file. Inherits from `Action`. |
+| `LinkAnnotation` | Link annotation: generate, retrieve property, and set property. Inherits from `Annotation`. Includes 10 additional members. |
+| `MarkupAnnotation` | Markup annotation: generate, retrieve property, and set property. Inherits from `Annotation`. Includes 10 additional members. |
+| `MemoryFontSource` | Memory font source: retrieve font definitions. Inherits from `FontSource`. |
+| `MergeOptions` | Merge options: add, access to inputs, and access to outputs. Inherits from `PluginOptions`. Includes 2 additional members. |
+| `Merger` | Merger: process. Inherits from `PdfPlugin`. |
+| `NamedAction` | Named action: access to name. Inherits from `Action`. |
+| `NamespaceProvider` | Namespace provider: retrieve namespace URI, retrieve prefix, and retrieve URI. Inherits from `XmpNamespaceProvider`. Includes 4 additional members. |
+| `OperationResult` | Operation result: is, save output, and to. Includes 2 additional members. |
+| `OptimizationOptions` | Optimization options: to, access to allow reuse page content, and access to compress fonts. Includes 11 additional members. |
+| `OptimizeOptions` | Optimize options: add, access to inputs, and access to outputs. Inherits from `PluginOptions`. Includes 2 additional members. |
+| `Optimizer` | Optimizer: process. Inherits from `PdfPlugin`. |
+| `Page` | Page: traverse with a visitor, add, and draw. Includes 14 additional members. |
+| `PageCollection` | Page collection: add, clear content, and contains. Includes 6 additional members. |
+| `PdfAValidateOptions` | PDF a validate options: add, access to font lookup directory, and access to inputs. Includes 3 additional members. |
+| `PdfLoadLimits` | PDF load limits: unlimited, access to max codec work bytes, and access to max compression ratio. Includes 14 additional members. |
+| `TextLayoutOptions` | Text layout options: access to alignment, access to direction, and access to fallback fonts. Includes 5 additional members. |
+| `XmpPacket` | XMP packet: add, retrieve data, and retrieve array. Includes 18 additional members. |
+| `PdfExtractor` | PDF extractor: bind, close, and dispose. Includes 11 additional members. |
+| `PdfSignature` | PDF signature: validate, access to byte range, and access to contact info. Includes 10 additional members. |
+| `StructureElement` | Structure element: add, move, and remove content. Includes 8 additional members. |
+| `ValidationResult` | Validation result: access to certification level, access to errors, and access to is valid. Includes 8 additional members. |
+| `UnsignedContent` | Unsigned content: add, remove content, and reset. Includes 7 additional members. |
+| `PdfFileEditor` | PDF file editor: add, append content, and close. Includes 6 additional members. |
+| `ValidationOptions` | Validation options: to, access to allow self signed, and access to check revocation. Includes 6 additional members. |
+| `PdfAValidationResult` | PDF a validation result: add, to, and access to errors. Includes 5 additional members. |
+| `RasterizedPage` | Rasterized page: retrieve pixel, save output, and to. Includes 5 additional members. |
+| `PdfUaValidationResult` | PDF ua validation result: add, to, and access to errors. Includes 4 additional members. |
+| `Plugin` | Enumerates plugin values. Values include `CONVERTER`, `EDITOR`, and `EXTRACTOR` and 4 more. |
+| `XmpField` | XMP field: access to is URI, access to language, and access to name. Includes 4 additional members. |
+| `TaggedContent` | Tagged content: add, element, and move. Includes 3 additional members. |
+| `PluginOptions` | Plugin options: add, access to inputs, and access to outputs. Includes 2 additional members. |
+| `XmpArray` | XMP array: add, remove content, and access to items. Includes 2 additional members. |
+| `XmpProperty` | XMP property: add, remove content, and access to field. Includes 2 additional members. |
+| `RevocationStatus` | Enumerates revocation status values. Values include `GOOD`, `NOT_CHECKED`, and `REVOKED` and 1 more. |
+| `TrustStatus` | Enumerates trust status values. Values include `BROKEN`, `SELF_SIGNED`, and `TRUSTED` and 1 more. |
+| `UnsignedContentAbsorber` | Unsigned content absorber: extract, retrieve extracted, and has. Includes 1 additional member. |
+| `XYZDestination` | Xyz destination: access to left, access to page, and access to top. Inherits from `Destination`. Includes 1 additional member. |
+| `XmpStruct` | XMP struct: add, retrieve data, and access to fields. Includes 1 additional member. |
+| `ValidationMode` | Enumerates validation mode values. Values include `AUTO`, `OFFLINE`, and `ONLINE`. |
+| `ValidationStatus` | Enumerates validation status values. Values include `INVALID`, `UNKNOWN`, and `VALID`. |
+| `PdfUaValidateOptions` | PDF ua validate options: add and access to inputs. |
+| `StreamDataSource` | Stream data source: read content and write output. Inherits from `DataSource`. |
+| `ValidationMethod` | Enumerates validation method values. Values include `LTIP` and `PKCS7`. |
+| `PdfAValidator` | PDF a validator: process. |
+| `PdfPlugin` | `PdfPlugin` exposes the verified public `process` operation in the `aspose_pdf` namespace. |
+| `PdfUaValidator` | PDF ua validator: process. |
+| `ResultContainer` | Result container: access to result collection. |
+| `Splitter` | Splitter: process. Inherits from `PdfPlugin`. |
+| `SystemFontSource` | System font source: retrieve font definitions. Inherits from `FontSource`. |
+| `TextExtractor` | Text extractor: process. Inherits from `PdfPlugin`. |
+| `URIAction` | URI action: access to URI. Inherits from `Action`. |
+| `PdfResourceLimitException` | Signals a PDF resource limit condition; derives from `PdfValidationException`. |
+| `SplitOptions` | Split options: add, access to inputs, and access to outputs. Inherits from `PluginOptions`. Includes 2 additional members. |
+| `TextExtractorOptions` | Text extractor options: add, access to inputs, and access to outputs. Inherits from `PluginOptions`. Includes 2 additional members. |
+| `UnsupportedFeatureException` | Signals an unsupported feature condition; derives from `AsposePdfException`. |
+| `parse_xmp` | `parse_xmp` is a verified public type exported by the `aspose_pdf` namespace. |
+| `serialize_xmp` | `serialize_xmp` is a verified public type exported by the `aspose_pdf` namespace. |
+
+### Aspose.PDF.Annotations Namespace (`aspose_pdf.annotations`)
+
+| Type | Description |
+| --- | --- |
+| `Annotation` | The `aspose_pdf.annotations` namespace re-exports `Annotation` from the primary `aspose_pdf` namespace. |
+| `AnnotationCollection` | The `aspose_pdf.annotations` namespace re-exports `AnnotationCollection` from the primary `aspose_pdf` namespace. |
+| `AnnotationFlags` | The `aspose_pdf.annotations` namespace re-exports `AnnotationFlags` from the primary `aspose_pdf` namespace. |
+| `AnnotationType` | The `aspose_pdf.annotations` namespace re-exports `AnnotationType` from the primary `aspose_pdf` namespace. |
+| `LinkAnnotation` | The `aspose_pdf.annotations` namespace re-exports `LinkAnnotation` from the primary `aspose_pdf` namespace. |
+| `MarkupAnnotation` | The `aspose_pdf.annotations` namespace re-exports `MarkupAnnotation` from the primary `aspose_pdf` namespace. |
+| `Name` | `Name` is a verified public type exported by the `aspose_pdf.annotations` namespace. |
+
+### Aspose.PDF.Attachments Namespace (`aspose_pdf.attachments`)
+
+| Type | Description |
+| --- | --- |
+| `FileSpecification` | The `aspose_pdf.attachments` namespace re-exports `FileSpecification` from the primary `aspose_pdf` namespace. |
+
+### Aspose.PDF.CGM Namespace (`aspose_pdf.cgm`)
+
+| Type | Description |
+| --- | --- |
+| `CgmLoadOptions` | `CgmLoadOptions` is a verified public type exported by the `aspose_pdf.cgm` namespace. |
+
+### Aspose.PDF.Clustering Namespace (`aspose_pdf.clustering`)
+
+| Type | Description |
+| --- | --- |
+| `Cluster` | `Cluster` is a verified public type exported by the `aspose_pdf.clustering` namespace. |
+| `ClusterCollection` | `ClusterCollection` is a verified public type exported by the `aspose_pdf.clustering` namespace. |
+| `DataPoint` | `DataPoint` is a verified public type exported by the `aspose_pdf.clustering` namespace. |
+
+### Aspose.PDF.Color Namespace (`aspose_pdf.color`)
+
+| Type | Description |
+| --- | --- |
+| `Color` | `Color` is a verified public type exported by the `aspose_pdf.color` namespace. |
+| `GradientAxialShading` | `GradientAxialShading` is a verified public type exported by the `aspose_pdf.color` namespace. |
+| `Point` | `Point` is a verified public type exported by the `aspose_pdf.color` namespace. |
+
+### Aspose.PDF.Document Namespace (`aspose_pdf.document`)
+
+| Type | Description |
+| --- | --- |
+| `Document` | The `aspose_pdf.document` namespace re-exports `Document` from the primary `aspose_pdf` namespace. |
+
+### Aspose.PDF.Drawing Namespace (`aspose_pdf.drawing`)
+
+| Type | Description |
+| --- | --- |
+| `Rectangle` | `Rectangle` is a verified public type exported by the `aspose_pdf.drawing` namespace. |
+
+### Aspose.PDF.Exceptions Namespace (`aspose_pdf.exceptions`)
+
+| Type | Description |
+| --- | --- |
+| `AsposePdfException` | `AsposePdfException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `DeprecatedFeatureException` | `DeprecatedFeatureException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `FontEmbeddingException` | The `aspose_pdf.exceptions` namespace re-exports `FontEmbeddingException` from the primary `aspose_pdf` namespace. |
+| `IncorrectCMapUsageException` | `IncorrectCMapUsageException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `InvalidPasswordException` | `InvalidPasswordException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `InvalidPdfFileFormatException` | `InvalidPdfFileFormatException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `InvalidValueFormatException` | `InvalidValueFormatException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `PdfException` | `PdfException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `PdfIOException` | `PdfIOException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `PdfParseException` | `PdfParseException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `PdfResourceLimitException` | The `aspose_pdf.exceptions` namespace re-exports `PdfResourceLimitException` from the primary `aspose_pdf` namespace. |
+| `PdfSecurityException` | `PdfSecurityException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `PdfValidationException` | `PdfValidationException` is a verified public type exported by the `aspose_pdf.exceptions` namespace. |
+| `UnsupportedFeatureException` | The `aspose_pdf.exceptions` namespace re-exports `UnsupportedFeatureException` from the primary `aspose_pdf` namespace. |
+
+### Aspose.PDF.Facades Namespace (`aspose_pdf.facades`)
+
+| Type | Description |
+| --- | --- |
+| `PdfExtractor` | The `aspose_pdf.facades` namespace re-exports `PdfExtractor` from the primary `aspose_pdf` namespace. |
+| `PdfFileEditor` | The `aspose_pdf.facades` namespace re-exports `PdfFileEditor` from the primary `aspose_pdf` namespace. |
+
+### Aspose.PDF.Font Registry Namespace (`aspose_pdf.font_registry`)
+
+| Type | Description |
+| --- | --- |
+| `FontDescriptor` | The `aspose_pdf.font_registry` namespace re-exports `FontDescriptor` from the primary `aspose_pdf` namespace. |
+| `FontRegistry` | `FontRegistry` is a verified public type exported by the `aspose_pdf.font_registry` namespace. |
+
+### Aspose.PDF.Font Repository Namespace (`aspose_pdf.font_repository`)
+
+| Type | Description |
+| --- | --- |
+| `FileFontSource` | The `aspose_pdf.font_repository` namespace re-exports `FileFontSource` from the primary `aspose_pdf` namespace. |
+| `FolderFontSource` | The `aspose_pdf.font_repository` namespace re-exports `FolderFontSource` from the primary `aspose_pdf` namespace. |
+| `FontDescriptor` | The `aspose_pdf.font_repository` namespace re-exports `FontDescriptor` from the primary `aspose_pdf` namespace. |
+| `FontRepository` | The `aspose_pdf.font_repository` namespace re-exports `FontRepository` from the primary `aspose_pdf` namespace. |
+| `FontSource` | The `aspose_pdf.font_repository` namespace re-exports `FontSource` from the primary `aspose_pdf` namespace. |
+| `MemoryFontSource` | The `aspose_pdf.font_repository` namespace re-exports `MemoryFontSource` from the primary `aspose_pdf` namespace. |
+| `SystemFontSource` | The `aspose_pdf.font_repository` namespace re-exports `SystemFontSource` from the primary `aspose_pdf` namespace. |
+
+### Aspose.PDF.Forms Namespace (`aspose_pdf.forms`)
+
+| Type | Description |
+| --- | --- |
+| `Field` | The `aspose_pdf.forms` namespace re-exports `Field` from the primary `aspose_pdf` namespace. |
+| `FieldType` | The `aspose_pdf.forms` namespace re-exports `FieldType` from the primary `aspose_pdf` namespace. |
+| `Form` | The `aspose_pdf.forms` namespace re-exports `Form` from the primary `aspose_pdf` namespace. |
+| `FormType` | The `aspose_pdf.forms` namespace re-exports `FormType` from the primary `aspose_pdf` namespace. |
+| `InvalidFormTypeOperationException` | `InvalidFormTypeOperationException` is a verified public type exported by the `aspose_pdf.forms` namespace. |
+| `UnsignedContent` | The `aspose_pdf.forms` namespace re-exports `UnsignedContent` from the primary `aspose_pdf` namespace. |
+| `UnsignedContentAbsorber` | The `aspose_pdf.forms` namespace re-exports `UnsignedContentAbsorber` from the primary `aspose_pdf` namespace. |
 
-Run lint and tests:
+### Aspose.PDF.Generated Namespace (`aspose_pdf.generated`)
+
+| Type | Description |
+| --- | --- |
+| `annotations` | Public function for annotations operations. |
+
+### Aspose.PDF.Geometry Namespace (`aspose_pdf.geometry`)
+
+| Type | Description |
+| --- | --- |
+| `Matrix3D` | `Matrix3D` is a verified public type exported by the `aspose_pdf.geometry` namespace. |
+| `Point3D` | `Point3D` is a verified public type exported by the `aspose_pdf.geometry` namespace. |
+| `Rectangle` | The `aspose_pdf.geometry` namespace re-exports `Rectangle` from the primary `aspose_pdf.drawing` namespace. |
+
+### Aspose.PDF.Graphics Namespace (`aspose_pdf.graphics`)
+
+| Type | Description |
+| --- | --- |
+| `GraphicElementCollection` | `GraphicElementCollection` is a verified public type exported by the `aspose_pdf.graphics` namespace. |
+| `GraphicsAbsorber` | `GraphicsAbsorber` is a verified public type exported by the `aspose_pdf.graphics` namespace. |
+| `InvalidOperationException` | `InvalidOperationException` is a verified public type exported by the `aspose_pdf.graphics` namespace. |
+
+### Aspose.PDF.Engine.Simple PDF Namespace (`aspose_pdf.engine.simple_pdf`)
+
+| Type | Description |
+| --- | --- |
+| `SimplePdf` | Simple PDF: add, append content, and auto. Includes 21 additional members. |
+| `CosExtractor` | Cos extractor: attach, detect encryption, and encryption. Includes 21 additional members. |
+
+### Aspose.PDF.Engine.Encryption Namespace (`aspose_pdf.engine.encryption`)
+
+| Type | Description |
+| --- | --- |
+| `EncryptionUtils` | Encryption utils: compute, decrypt, and derive. Includes 12 additional members. |
+
+### Aspose.PDF.Engine.Incremental Update Namespace (`aspose_pdf.engine.incremental_update`)
+
+| Type | Description |
+| --- | --- |
+| `IncrementalUpdate` | Incremental update: add, build, and find. Includes 12 additional members. |
+
+### Aspose.PDF.Logical Structure Namespace (`aspose_pdf.logical_structure`)
+
+| Type | Description |
+| --- | --- |
+| `StructureTypeStandard` | Enumerates structure type standard values. Values include `DIV`, `DOCUMENT`, and `P` and 12 more. |
+
+</details>
+
+## Documentation and Resources
+
+- **[Full API reference](https://reference.aspose.org/pdf/python/)** - the complete browsable reference for the public API.
+- Found a bug or have a feature request? [Open an issue](https://github.com/aspose-pdf-foss/Aspose-PDF-FOSS-for-Python/issues) on GitHub.
+
+## Scope and Limitations
+
+- Page rendering supports common page content; it is not represented as complete PDF graphics coverage.
+- PDF/A and PDF/UA checks are heuristic signals, not certification-grade conformance.
+- OCR is not implemented, and layout reflow remains outside the prerelease scope.
+- The lightweight signature check does not perform full PKCS#7 certificate-chain validation.
+- Compatibility surfaces may name features that are unavailable and must fail explicitly.
+- The documented feature set is bounded by the active test suite rather than every exposed compatibility name.
+
+The package manifest classifies this release as **Alpha**. The distribution includes the [`src/aspose_pdf/py.typed`](src/aspose_pdf/py.typed) type marker.
+
+Review [`supported-features.md`](supported-features.md) for the repository's detailed implementation boundaries.
+
+This repository contains [Aspose.PDF FOSS for Python](https://products.aspose.org/pdf/). For requirements beyond the FOSS scope described above, explore the [full-featured Aspose.PDF Enterprise Edition](https://products.aspose.com/pdf/). It is a separate product, so features and APIs may differ.
+
+## Development and Testing
+
+The repository includes 187 test files, 4 source-bound validation commands.
+
+<details>
+<summary>View development and testing resources</summary>
+
+### Tests
+
+- [`tests/conftest.py`](tests/conftest.py)
+- [`tests/helpers_make_pdfs.py`](tests/helpers_make_pdfs.py)
+- [`tests/test_acc01_font_embedding.py`](tests/test_acc01_font_embedding.py)
+- [`tests/test_aes_key_derivation.py`](tests/test_aes_key_derivation.py)
+- [`tests/test_agl.py`](tests/test_agl.py)
+- [`tests/test_algorithm_2b_v5r6.py`](tests/test_algorithm_2b_v5r6.py)
+- [`tests/test_annotation_appearance_decorations.py`](tests/test_annotation_appearance_decorations.py)
+- [Browse all test files](tests)
+
+### Focused Commands and Repository Scripts
 
 ```bash
-python -m ruff check src/
-python -m pytest -q
+scripts/build.sh
 ```
 
-Build and validate the distributions:
-
 ```bash
-python -m build
-python -m twine check dist/*
+scripts/check.sh
 ```
 
-The convenience scripts run the standard checks:
+```bash
+python -m pip install -e .
+```
 
 ```bash
-scripts/check.sh
-scripts/build.sh
+python -m pytest tests
 ```
 
-## Repository Map
+
+</details>
+
+<details>
+<summary>View Repository Layout Details</summary>
 
 | Path | Description |
 | --- | --- |
@@ -231,18 +572,8 @@ scripts/build.sh
 | `scripts/` | Local check and build commands |
 | `.github/workflows/` | CI and publishing workflows |
 
-## Contributing
 
-Issues and pull requests are welcome. Please:
-
-1. Keep changes focused.
-2. Add tests for new behavior and bug fixes.
-3. Write code comments and docstrings in English.
-4. Run `python -m ruff check src/` and `python -m pytest -q`.
-5. Document public API changes and important limitations.
-
-When reporting a parser or rendering problem, include a minimal PDF that can be
-shared publicly whenever possible.
+</details>
 
 ## Security
 
@@ -251,6 +582,7 @@ PDF files are untrusted binary input. Loading uses a generous default
 decoded streams, page content, images, and rasterization. Customize the policy
 when an application needs tighter limits:
 
+
 ```python
 from aspose_pdf import Document, PdfLoadLimits, PdfResourceLimitException
 
@@ -267,6 +599,7 @@ except PdfResourceLimitException as error:
     print(f"PDF rejected: {error}")
 ```
 
+
 The same `limits=` argument is accepted by `Document.load_from()` and
 `Document.open_streaming()`; lazy decoding continues to use the document's
 shared budget. `PdfLoadLimits.unlimited()` disables every safeguard and should
@@ -274,11 +607,35 @@ only be used for trusted input in an environment with external resource
 controls. These limits reduce known parser and allocation risks but are not an
 exhaustive DoS sandbox; isolate highly hostile workloads at the process level.
 
+
 If you discover a security issue, follow the [security policy](SECURITY.md) and
 use GitHub private vulnerability reporting instead of opening a public issue.
 
-## License
 
-Aspose.PDF FOSS for Python is licensed under the [MIT License](LICENSE).
+## Contributing
+
+Issues and pull requests are welcome. Please:
+
+
+1. Keep changes focused.
+
+
+2. Add tests for new behavior and bug fixes.
+
+
+3. Write code comments and docstrings in English.
+
+
+4. Run `python -m ruff check src/` and `python -m pytest -q`.
+
+
+5. Document public API changes and important limitations.
+
+
+When reporting a parser or rendering problem, include a minimal PDF that can be
+shared publicly whenever possible.
+
+
+## License
 
-Copyright © 2026 Aspose Pty Ltd.
+This project is available under the [MIT License](LICENSE). It permits use, modification, distribution, and commercial use when the license and copyright notice are retained.
