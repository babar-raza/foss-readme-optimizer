# Agent Handover

## 1. Handover Snapshot

- Repository: `D:/Users/prora/OneDrive/Documents/GitHub/foss-readme-optimizer`
- Branch: `main`
- Content checkpoint: `923bbbe278354c704f8a13f5a7944e6e60135cf3`
- Executable authority: `plans/investigations/control/level8-autonomous-mission-task-graph.yaml`
- Graph SHA-256: `a60e1a073ab401ff3c9754c4ea1541a89e83181d7e612240347aabdff8c4fdbb`
- Durable mission state: version 689; no graph drift
- Current goal: `GOAL-V0-VERIFIED-PYTHON-POC`
- Current task: `L8-VPY-01-NOTE-VERIFIED-CANARY`, `IN_PROGRESS`
- Current mission projection: 2/31 current `FACTS_READY`, 0/31 current candidates,
  deterministic approvals, agent approvals, or no-op proofs; raw historical lifecycle counts are
  facts 17, candidate 9, deterministic 9, agent-approved 2, no-op 2.
- Working tree at capture: 51 modified tracked files, 17 untracked paths, no staged paths, and no
  repository-owned process. This is valuable unfinished candidate work and must remain in place.
- Exact next action: finish Note from its current verified fact bundle. Recompile its candidate,
  close source-claim accountability findings, run deterministic validation, independent review,
  and the identical no-op proof, then commit the finalized Note artifact. Page is next and must
  refresh FACTS_READY because its last facts run preceded the final example-selector change.
- Overall state: `PARTIAL`, safely resumable, no product effect authorized.

This snapshot becomes historical after its handover commit. Live Git and durable supervisor state
always override narrative files.

## 2. Ultimate Goal

Deliver the full `plans/idea.md` system for every dynamically discovered and admitted Aspose FOSS
repository: derive repository-verified product truth, generate a professional and consistent but
product-specific GitHub presentation, validate every material claim, obtain independent approval,
open only authorized draft proposals, recover durably, and earn independently reproducible Level
5, Level 7, and Level 8 proof.

Python is first, then .NET, Java, C++, TypeScript, Rust, and Go. The immediate deliverable is the
first finalized verified Python README, followed by all Python repositories and then the complete
portfolio. No partial batch is the POC.

## 3. Current Mission and Scope

The active mission is `LEVEL8-CENTRAL-REPOSITORY-PRESENTATION`. Its current bounded scope is
verified Python README qualification using the sole `supervise` runtime. Every repository must
progress through immutable snapshot, verified facts, structured plan, candidate, deterministic
validation, independent review, bounded repair, and unchanged no-op proof.

Non-goals at this boundary are product-repository writes, PR creation, merges, default-branch
writes, GitHub App production installation, trusted-README closure, a second controller, or reduced
factuality. The current diagnostic previews are not final deliverables.

## 4. Authority and Reference Map

| Reference | Role | Status |
|---|---|---|
| `plans/idea.md` | Product outcome and ordered gates | Highest product authority |
| `plans/master.md` | Architecture, decisions, sequence, maturity gates | Current governed design |
| `plans/requirements.md` | Normative acceptance obligations | Binding acceptance authority |
| `plans/GOVERNANCE.md` | Evidence, task, safety, and multi-agent rules | Binding governance |
| `AGENTS.md` | Repository-local execution and safety rules | Binding operator instructions |
| `plans/investigations/control/level8-autonomous-mission-task-graph.yaml` | Sole executable task graph | Graph hash above |
| Supervisor Git-ref state | Claim, task, lifecycle, and transition authority | Version 689 at capture; always re-read |
| `runs/multi-agent/L8-VPY-01-NOTE-VERIFIED-CANARY/execution-plan.json` | Current Note role/path leases | Supporting runtime record |
| `runs/multi-agent/L8-VPY-02-PAGE-PDF-VERIFIED-CANARIES/execution-plan.json` | Page/PDF and example-value lane record | Supporting runtime record |

## 5. Exact Plan

### Phase A - Note verified canary

- Entry: current Note facts at revision `6d97a522a9ed24708687911f1aabb76e2dea2da7`.
- Steps: reuse facts; compile the accepted global contract; account for every inherited and
  generated claim; repair factual/prose defects; run deterministic validation; run a separate
  independent reviewer; prove identical rerun/no new LLM call/no effect; store and commit the
  finalized README plus facts, plan, diff, review, and no-op evidence.
- Exit: current `AGENT_APPROVED` and `NO_OP_PROVEN`, zero unresolved material claims, zero effects.
- Status: open. Facts are current; the existing preview is non-final.

### Phase B - Page and PDF verified canaries

- Page must rerun only FACTS_READY because the final example-selector dependency changed after its
  successful PS-to-PDF facts run. PDF facts are current at revision
  `737d26451ed5c58e53017e3c9460e834f99d20ed`.
- Compile each candidate through the same global contract, factual accountability, deterministic
  validation, independent review, repair, and no-op proof.
- Exit: both finalized and committed locally. No product effect.

### Phase C - Remaining Python and portfolio

- Complete every runtime-loaded Python repository, then .NET, Java, C++, TypeScript, Rust, and Go.
- Close full-registry Gate A only when every admitted repository is current, agent-approved, and
  no-op proven. Gate B is a separate human-acceptance state.
- Then prove the same runtime under `act`, disposable staging, fresh-authorized Gate C draft PRs,
  hosted GitHub App operation, all presentation surfaces, Level 5, Level 6, the 30-day Level 7
  window, and the 90-day Level 8 window.

## 6. Work Completed

### Verified complete

- Commit `923bbbe2` added an AST-only, ecosystem-extensible example-value contract. Python quick
  starts must perform a meaningful product operation and produce an observable result. Repository
  author order is preserved after that gate; shortest executable fragments no longer win.
- The same slice preserves stronger same-revision repository limitations when an agent draft
  contributes an empty list.
- `69 passed` across `test_agentic_drafting.py`, `test_draft_product_truth_capability.py`, and
  `test_example_value.py`.
- Repository-wide Ruff check, Ruff format check, mypy `src`, and `git diff --check` passed at the
  checkpoint.
- Note facts are current and contain an isolated repository-authored image-extraction workflow that
  creates output files.
- PDF facts are current and contain the isolated create-page/add-text/save `hello.pdf` workflow.
- Page's PS-to-PDF workflow was independently executed in the Docker verifier and passed.

### Implemented but unverified or partial

- Page's recorded fact bundle contains the correct PS-to-PDF workflow but is stale against the
  final repository-order selector dependency. It must be refreshed at its graph-authorized turn.
- Note, Page, and PDF diagnostic previews pass the current visual contract, but independent review
  correctly rejected finality: source-claim counts were 99, 44, and 62, and no unchanged no-op
  proof existed.
- The larger presentation/claim-accountability implementation is preserved in 51 modified and 17
  untracked paths. It is not a green coherent commit.
- The previous complete non-live run had 36 failures in legacy `test_supervisor_loop.py` fixtures.
  Focused repaired seams pass; the complete suite has not been rerun after this slice.

## 7. Current Working State

No repository-owned process remains. The last command boundary was a successful Note bounded
`FACTS_READY` run with exact LLM accounting (`provider_calls=1`, `cache_reuse=0`) and no later
capability. Immediately before it, PDF reached the same stage with one call. Page reached the same
stage earlier, but its dependency is now stale due the selector integration described above.

Do not discard the dirty tree. Inspect overlapping diffs before editing. The current claim remains
`L8-VPY-01-NOTE-VERIFIED-CANARY`; do not steal or bypass it.

## 8. Remaining Gaps

| Gap | Severity | First failing boundary | Permanent action and proof |
|---|---|---|---|
| `VPY-NOTE-CLAIMS` | P0 | source-claim accountability | Reconcile or authoritatively correct every material Note source claim; deterministic and independent acceptance |
| `VPY-NOTE-NOOP` | P0 | unchanged rerun | Same source/facts/template/prompts produce identical candidate with no unnecessary LLM call or effect |
| `VPY-PAGE-STALE-FACTS` | P0 after Note | facts dependency | Bounded Page FACTS_READY refresh under current selector; exact manifest/hash proof |
| `VPY-PAGE-PDF-FINAL` | P0 | candidate/review | Complete source accountability, deterministic validation, independent review, and no-op proof |
| `LEGACY-SUPERVISOR-FIXTURES` | P0 before coherent integration commit | full non-live regression | Migrate legacy fixtures to the provenance-complete production seam without weakening verifier enforcement; complete runner green |
| `PORTFOLIO-GATES` | P0/P1 | dynamic denominator | Continue the durable graph through Python, remaining ecosystems, Gates A/B/C, hosted operation, and maturity windows |

## 9. Ordered Execution Queue

1. Run live mission status and confirm the active Note claim and zero process owners.
2. Reuse Note facts; do not spend another facts/LLM call while dependency hashes match.
3. Compile the Note candidate and run exact claim-accountability validation.
4. Repair the first failing Note claim/prose boundary in bounded slices; after two ineffective
   equivalent attempts, record a first-principles review and change mechanism.
5. Run deterministic validation, independent review, and identical no-op proof.
6. Commit the finalized Note README/evidence to `main`; refresh these same handover files.
7. Evaluate. When `L8-VPY-02` is eligible, refresh Page FACTS_READY, then finalize Page and PDF.
8. Continue all Python, then the remaining platform order, without creating another plan.
9. Before any product push, show all product READMEs and obtain fresh exact what/why/where approval.

Only the supervisor may later fan out isolated repository lanes. Shared state/evidence aggregation
remains serialized.

## 10. Decisions and Constraints

- One control branch: `main`; no reset, restore, clean, broad stash, force push, or overwrite.
- The sole runtime is `readme-agent supervise`; the sole executable graph is the Level-8 graph.
- Existing README content is valuable evidence, not verified truth. Validate every material claim.
- Global README contract: no emojis or visitor-visible comments; consistent professional title and
  badge row; full product names; detailed non-directional Mermaid; list TOC; collapsed secondary
  detail; factual MIT/notice prose; contextual governed links; Enterprise Edition terminology.
- Preserve factuality, protected content, push-blocking, allow-list, redaction, idempotency,
  lifecycle evidence, and independent review.
- No product write, PR, GitHub App expansion, or manual effect without its required fresh authority.
- Use battle-tested libraries and sibling implementations before hand-rolling.
- Every commit includes `Co-Authored-By: Codex <noreply@openai.com>`.

## 11. Tests, Proof, and Evidence

- Focused green: `69 passed` for product truth and example value.
- Static green: Ruff check, Ruff format check, mypy `src`, `git diff --check`.
- Wider focused run before the final fixture repair: `150 passed, 1 failed`; the sole failure was the
  obsolete setup-only “good” fixture. That exact test passed after repair. Do not call the entire
  151-test aggregate rerun verified because its later aggregate output channel did not return.
- Complete non-live gate: not current-green; last known boundary was 36 legacy supervisor-loop
  failures.
- Independent preview review:
  `runs/multi-agent/independent-verification/L8-VPY-02-PAGE-PDF-VERIFIED-CANARIES/`.
- Current facts:
  `runs/readme-poc/aspose-note-foss__Aspose.Note-FOSS-for-Python/6d97a522a9ed24708687911f1aabb76e2dea2da7/facts/product-facts.json`,
  `runs/readme-poc/aspose-page-foss__Aspose.Page-FOSS-for-Python/dac5d70e0f91949a780f2e98dfbb12314a5fbc70/facts/product-facts.json`, and
  `runs/readme-poc/aspose-pdf-foss__Aspose-PDF-FOSS-for-Python/737d26451ed5c58e53017e3c9460e834f99d20ed/facts/product-facts.json`.

## 12. Risks and Uncertainty

- Diagnostic previews can look good while failing source accountability; never promote on visual
  quality alone.
- Page facts are intentionally classified stale after the final selector change.
- Note's selected observable example is image extraction rather than the earlier thin load-only
  snippet. Reader quality still requires independent review in the final candidate.
- The dirty tree is large and overlapping; stage only coherent reviewed files.
- The complete regression suite remains the integration bottleneck. Do not weaken production
  verifier enforcement to make legacy fixtures pass.

## 13. Receiving Agent Startup Steps

1. `cd D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer`.
2. Read `AGENTS.md`, this file, `state.json`, `plans/idea.md`, the relevant master/requirements
   sections, and the active graph task.
3. Run `git status --short --branch`, `git rev-parse HEAD`, and inspect repository-owned processes.
4. Run the durable mission `status` command in `CONTINUE.md`; evaluate only on reported graph drift.
5. Confirm/reuse the unexpired Note claim.
6. Inspect the current Note facts and dirty candidate seams before editing.
7. Execute the Note candidate/accountability boundary first; do not rerun facts without dependency
   invalidation.
8. Run focused tests, deterministic validation, independent review, no-op proof, static gates, then
   the complete non-live gate before coherent integration closure.
9. Record checksum-complete evidence, reconcile the same task/requirements/logs, commit to `main`,
   refresh this handover, evaluate, and continue.

## 14. Closure Standard

The mission is complete only when every mandatory durable task is `CLOSED`, all current
requirements have production-like proof, every registry repository passes verified Gates A and B,
authorized publication/runtime/presentation gates pass, safety/recovery/idempotency remain intact,
the 30-day and 90-day windows finish, and an independent audit awards Level 8. A candidate, a
platform, a commit, or a green focused test is never mission closure.
