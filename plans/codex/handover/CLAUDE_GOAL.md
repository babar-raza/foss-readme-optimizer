/goal

Deliver Python as the first complete production-admitted `verified_repository_presentation`
platform in `D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer`. Continue from live
Git and durable supervisor state under `AGENTS.md`, `plans/idea.md`, `plans/master.md`,
`plans/requirements.md` plus its typed catalog, `plans/GOVERNANCE.md`, and the sole executable graph
`plans/investigations/control/level8-autonomous-mission-task-graph.yaml`. Read
`plans/codex/handover/HANDOVER.md` and verify its snapshot before acting.

Correct and freeze the global README presentation contract; regenerate, independently verify,
display, and obtain explicit human acceptance for every dynamically admitted Python README; then
prove complete fresh-process zero-provider-call transaction no-op, canonical GitHub Actions,
full `act`, disposable staging, GitHub App analysis/effect isolation, recovery, deduplication,
authorization, and Python production admission. Do not execute any non-Python platform task until
`L8-VPY-05-PRODUCTION-ADMISSION` is evidence-backed `CLOSED`. Afterwards reuse the same verified
contract and production system in `data/platform_priorities.json` order.

Use only the existing mission graph, durable Git-ref state, supervisor, task execution focuses,
requirements, and evidence contracts. Create no replacement plan, queue, controller, state store,
or trusted-lane execution. Preserve factuality, existing-source accountability, safety, independent
non-authoring review, checksum evidence, idempotency, recovery, allow-list and push-blocking. Do not
write a product repository or default branch. A later product effect requires a complete accepted
platform plus fresh exact what/why/where human authorization.

Resume only the graph-selected task and repository. Repair the first causal boundary, use focused
then required integration proof, independently verify, promote exact accepted bytes, show each
README immediately, transition the same task with evidence, evaluate, and continue. Two equivalent
failures or 15 minutes without material narrowing require a recorded first-principles tactic change.
Do not stop for tests, commits, checkpoints, context/session limits, dirty unrelated work, or an
agent-fixable failure. Stop only at a genuine unavailable external-authority/infrastructure/manual
gate or after the complete objective is independently proven requirement by requirement.
