# Continue the Verified Repository-Presentation Mission

Work in `D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer` on control-repository
`main`. Read `AGENTS.md`, then verify this handover against live Git and durable mission state.
Preserve the dirty tree; do not reset, restore, clean, stash, force, or create a control branch.

Authority is `plans/idea.md` → `plans/master.md`/decision catalog → requirement catalog →
`plans/investigations/control/level8-autonomous-mission-task-graph.yaml` → durable supervisor state.
Create no competing plan, controller, queue, or state.

The historical snapshot is HEAD `e8f4de70160fb18d61d5ea5ee70802eac0174cc7`, state version 790,
graph `ecc3c4567a448a9796c500a3916e8d6c026d9c18fdce33bc2c35212fbc270df1`, active task
`L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES`, and immediate goal `DELIVERY-PY-PDF-CURRENT`. Re-read
live state; never treat these values as current after change.

The umbrella mission does not authorize broad work. Follow only the exact `next_immediate_goal`,
`next_immediate_outcome`, and `next_repository_scope` printed by mission `status`. Current order is
PDF → Page → Note → Aspose.3D → remaining Python → .NET → Java. Each is a separate durable goal.

On explicit user resume:

1. Check branch, HEAD, tree, repository-owned processes, and goal state.
2. Run mission `status`; evaluate only on graph drift.
3. Claim/reclaim the printed task through the same controller.
4. Execute the PDF bounded canary with the active task and observer binding shown in `HANDOVER.md`.
5. Reuse valid cached stages and repair only the first PDF-blocking boundary.
6. Run focused proof, deterministic validation, independent review, promotion, and unchanged no-op.
7. Show the exact accepted README immediately; broad regression waits for its declared boundary.
8. Transition with checksum-complete evidence, evaluate, and continue with the next printed goal.

The runtime rejects an unbound repository, graph drift, wrong task/observer, expired claim, or
exhausted approach before repository work. Do not bypass this guard. Two equivalent ineffective
attempts or 15 minutes without narrowing require a recorded first-principles replan that changes the
causal approach. Nonblocking discoveries go to backlog and cannot enlarge the current task.

No product write or PR is authorized. Do not request GitHub App work at this local boundary. Do not
call cached/historical artifacts current until the durable current contract accepts them.
