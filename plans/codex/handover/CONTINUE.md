# Continue the Verified Repository-Presentation Mission

Resume `D:\Users\prora\OneDrive\Documents\GitHub\foss-readme-optimizer` as the sole operator on
`main`. Live Git and supervisor state override this historical snapshot.

Authority, in order:

1. `plans/idea.md`
2. `plans/master.md`
3. `plans/requirements.md`
4. `plans/investigations/control/level8-autonomous-mission-task-graph.yaml`
5. supervisor Git-ref durable state
6. `plans/codex/idea-fidelity-to-level-8-autonomous-execution-plan.md` as supporting detail only

The active executable goal is `GOAL-V0-VERIFIED-PYTHON-POC`. Trusted-lane goals are preserved with
`execution_required: false`; they must not be selected or allowed to satisfy verified acceptance.
The immediate task is `L8-VPY-00-GOLDEN-TEMPLATE`.

Start exactly here:

```powershell
git status --short --branch
git rev-parse HEAD
.venv/Scripts/readme-agent supervise `
  --mission-task-graph plans/investigations/control/level8-autonomous-mission-task-graph.yaml `
  --mission-action status `
  --mission-observer Codex `
  --durable-state
```

If graph drift is reported, run `evaluate`. If VPY-00 is eligible and no unexpired claim exists,
claim it through the supervisor. Inspect existing presentation contracts, renderer, validators,
and tests before adding code. Do not create another template engine if a current public seam can
be extended.

VPY-00 must convert
`plans/investigations/evidence/readme-presentation-contract-v1/aspose-note-foss-python-golden.md`
into a reusable `RepositoryPresentationTemplateV1` with compact, standard, and extended profiles.
Treat the sample as structural reference only. Bind all product name, API, package, format,
feature, command, example, link, license, limitation, and relationship slots to verified
repository facts. Prove exact structural reproduction where applicable, a different-product
case, cross-product leakage rejection, unsupported-slot rejection, and dependency-complete cache
invalidation.

After VPY-00 closes, continue without prompting:

1. `L8-VPY-01-NOTE-VERIFIED-CANARY`
2. `L8-VPY-02-PAGE-PDF-VERIFIED-CANARIES`
3. `L8-VPY-03-ALL-PYTHON-VERIFIED-POC`
4. the remaining verified portfolio and Gate A/B/C tasks
5. hosted runtime and remaining presentation surfaces
6. Level 5, Level 6, the 30-day Level-7 proof, and the 90-day Level-8 proof

Use this loop continuously:

```text
verify authority and live state
-> reconcile graph drift and claim lease
-> claim/reclaim the highest-priority ready task
-> implement the smallest complete task slice
-> focused proof
-> integration, regression, safety, recovery, idempotency, and live-like proof
-> independent verification
-> repair the first failing boundary
-> checksum-complete redacted evidence
-> update the same task, requirements, logs, and handover
-> commit coherent work directly to main
-> evaluate and continue
```

Two ineffective attempts or 15 minutes at the same boundary require a first-principles review
before a third approach. Cache expensive results only when keys include all correctness
dependencies. Do not lower acceptance, use trusted prose as verified truth, stop on an
agent-fixable failure, create a competing plan/controller, or close on implementation alone.

Preserve existing work. Never reset, restore, clean, force-push, or overwrite without inspection.
Never write a product repository without fresh exact what/why/where approval. Never auto-merge,
write a default branch, or expose a target-write credential to analysis.

The visible chat goal may still display a stale blocked trusted objective because the goal API
cannot mutate an unfinished objective. It is not execution authority. Do not falsely complete it;
follow the live mission graph until the UI owner can replace it.
