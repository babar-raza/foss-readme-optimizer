# Project log — 2026-08-27

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-27 | master | — | — | L8-PORT-01 | Fixed a `format_direction_contradiction` false positive on restrictive "limited to" framing; kept a regression test proving table-row scanning is intentional, not part of the bug. |
| 2026-08-27 | requirements | — | VER-013, RDM-031 | L8-PORT-01 | Logged a fourth pre-existing test failure found via stash-isolation; corrected RDM-031's evidence doc to rule out `section_cluster_authoring.py` as the code-fence normalization gap's source. |
| 2026-08-27 | master requirements | — | RDM-030 | L8-PORT-01 | Fixed RDM-030's first collision mechanism: `role_sentence()`'s empty-suffix family-name fallback replaced with the fixed word "base" so it can no longer coincide with a sibling type's explicit-prefix subject. |
| 2026-08-27 | master requirements | — | RDM-030 | L8-PORT-01 | Fixed RDM-030's second, independent collision mechanism (`ID`/`Id`, case-only): disambiguated at the rendering layer by appending distinguishing member names to any group still colliding after casefolding. |
| 2026-08-27 | requirements | — | RDM-030 | L8-PORT-01 | Live-verified both RDM-030 fixes against `aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET`: the duplicated-description failure no longer reproduces. |
| 2026-08-27 | requirements | — | RDM-029 | L8-PORT-01 | Pinpointed RDM-029's two remaining scope gaps: fenced code examples have no resolution-authoring path at all, and non-format-direction claim drops are untouched by the existing fix. Stays PARTIAL. |
| 2026-08-27 | requirements | — | OPS-014 | L8-PORT-01 | Logged `portfolio-proof`'s registry-heal gap: it unconditionally sets `no_registry_heal=True` with no override, discovered via three identical `source_scan_stale` failures. Workaround confirmed (plain `supervise --registry`), not fixed. |
| 2026-08-27 | requirements | — | RDM-032 | L8-PORT-01 | Logged RDM-032: bounded review's namespace-scoping regex searches a table unit's own text for its owning heading, which is always a separate, earlier unit — dead code, silently falling back to the generic capped fact sample every time. |
| 2026-08-27 | requirements | — | RDM-033 | L8-PORT-01 | Logged RDM-033: `section_cluster_authoring.py`'s retry loop didn't catch `LLMTruncatedResponseError`, so a cut-off forced-tool-call response failed closed instead of retrying with a more concise prompt. |
| 2026-08-27 | requirements | — | RDM-032 | L8-PORT-01 | Attempted RDM-032 fix reverted before committing: making the namespace match succeed made the real failure worse (1 oversized unit became 2), because the unscoped-when-matched projection is itself unbounded. Recorded the true mechanism and regression. |
| 2026-08-27 | master | — | RDM-033 | L8-PORT-01 | Fixed RDM-033: `section_cluster_authoring.py` now catches `LLMTruncatedResponseError` and retries once with an explicit "write more concisely" repair hint before failing closed. |
| 2026-08-27 | requirements | — | RDM-033 | L8-PORT-01 | Recorded RDM-033's fix landing (commit 95b162a7e) with its two new regression tests. |
| 2026-08-27 | requirements | — | RDM-029 | L8-PORT-01 | Live-traced RDM-029 gap 1's exact branch through `resolve_source_claims()`: fenced examples fall through to a hardcoded allowed-obligation gate that excludes `additional_examples`, then to a deliberate no-op — confirmed by direct trace, not inference. |
| 2026-08-27 | requirements | — | RDM-029 | L8-PORT-01 | Hypothesized RDM-029's three gaps share one root cause upstream in provenance recording — later directly refuted (see 19:07 entry below). |
| 2026-08-27 | master requirements | — | RDM-029 | L8-PORT-01 | Landed RDM-029's partial fix: extended `deferred_unverified_obligation_detail_resolution()`'s allowed-obligation set to include `additional_examples`/`dependency_requirements`, closing the confirmed permanent gap for repositories whose core section is independently verified. |
| 2026-08-27 | requirements | — | RDM-029 | L8-PORT-01 | Live-verified the RDM-029 partial fix against `aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET`: landed safely but did not clear this specific repository, whose `candidate_core_present` is also False for the same obligation — a further, deeper layer. |
| 2026-08-27 | requirements | — | RDM-029 | L8-PORT-01 | Refuted the prior shared-root-cause hypothesis with direct data: a real `deferred_verification` resolution for the exact claim already exists in two persisted runs, revealing composition-level LLM nondeterminism rather than a uniform missing-binding gap. |
| 2026-08-27 | master | — | RDM-032 | L8-PORT-01 | Fixed RDM-032 correctly: threaded the section's heading-through-unit text in as a second namespace-match input, and capped the scoped projection's classes/functions at 24 entries (reporting completeness truthfully) instead of the earlier, reverted unbounded version. |
| 2026-08-27 | requirements | — | RDM-032, RDM-028 | L8-PORT-01 | Marked RDM-032 IMPLEMENTED after live-verifying it against its original demo repository: the fix is correct and present elsewhere in the portfolio, but that specific repository's blocker turned out to be the already-known, already-backlogged RDM-028 (a monolithic API Method Index table), not a namespace-scoping gap — cross-referenced rather than conflated. |
| 2026-08-27 | requirements | — | FACT-018 | L8-PORT-01 | Corroborated FACT-018 with a third ecosystem instance (`aspose-pdf-foss/Aspose.PDF-FOSS-for-Java`, TIFF output) while investigating `format_direction_contradiction` as a possible high-leverage linter bug — it is not; the linter is working as designed against a narrow, honest evidence source. |
| 2026-08-27 | requirements | — | RDM-028 | L8-PORT-01 | Investigated implementing RDM-028 directly; found and logged a real regression risk before writing code — a naive row cap could silently drop exactly the rows the API Method Index exists to satisfy `document_validation.py`'s protected-content check for. Not implemented; sharpens RDM-028's own "needs a genuine curation-cutoff decision" framing. |
| 2026-08-27 | governance | — | — | L8-PF-00 | Production recovery planning sprint: independently verified an external review's architectural/CI/state/quality claims against live code and state (8 parallel read-only investigation agents, direct command verification); separated a recurring pinned-hash CI-drift process gap from a distinct, real, structural rerun-inconsistency problem in the live portfolio pipeline. See `plans/investigations/production-recovery-sprint-2026-08-27.md`. |
| 2026-08-27 | master | — | — | L8-PF-00 | Refreshed `plans/master.md`'s Status section to live `mission status` numbers (denominator 34, `no_op_proven` 1/34, `graph_drift: false`, 2026-08-27) — the prior 2026-08-24/31-denominator snapshot had drifted from truth; preserved as historical narrative, not deleted. |
| 2026-08-27 | master | #109, #110, #111, #112, #113 | — | L8-PF-00 | Appended Decisions #109-113 to the Decision Ledger (`plans/master.md` + `plans/decisions/catalog.jsonl` via `scripts/retrofits/append_production_recovery_decisions_109_113.py`): pinned-content-hash enforcement, universal LLM-judgment-surface ratcheting, per-repository-scoped composition-plan invalidation, a durable/portfolio-shared ratchet state tier, and CAS classification hardening. Wired into the Build Checklist and into `L8-PF-00`/`L8-PF-03`/`L8-PF-04`/`L8-PORT-01`'s existing `acceptance_checks`, not new top-level items. |
| 2026-08-27 | requirements | #109, #110, #111, #112, #113 | GOV-032, LLM-023, CORE-039, SAFE-020, SAFE-021 | L8-PF-00 | Appended 5 new requirement rows for Decisions #109-113 via `scripts/retrofits/append_production_recovery_requirements.py`; refreshed `plans/requirements.md`'s stale summary counts (497→514 total; the drift itself is GOV-032's own first proof point) and the Level-8 graph's `requirement_catalog` sha256/count pointer. |
| 2026-08-27 | master requirements | #113 | SAFE-021 | L8-PF-00 | Autonomous execution phase, task 1 of the production-recovery queue: implemented Decision #113/SAFE-021 for real. `GitStateBackend.save()` now re-fetches and structurally compares the remote ref on any unmatched push-failure stderr before raising, matching a genuine non-fast-forward outcome regardless of git's exact wording. Two new regression tests; SAFE-021 marked `IMPLEMENTED`. |
| 2026-08-27 | master requirements | #110 | LLM-023 | L8-PF-00 | Autonomous execution phase, task 2: implemented Decision #110/LLM-023. New `verification/prose_quality_cache.py` mirrors the claim-disposition ratchet pattern, wired into `verify_prose_quality.execute()`. Proves zero new provider calls on an identical rerun and exactly one fresh call after a contract-version bump. LLM-023 marked `IMPLEMENTED`. |

## Entries

- [master] **2026-08-27 — `format_direction_contradiction` false positive on restrictive "limited to" framing**

The verb-noun scan for `output`/`input` nouns only excluded explicit negations (`not`/`cannot`/`doesn't`/...), not restrictive framing, so a limitation statement like "Output is limited to PDF files." was flagged as if it were a positive capability claim. Live-verified against `aspose-note-foss/Aspose.Note-FOSS-for-Python`'s real blocked candidate, one of two apparent false-positive sources found there. A second candidate fix (excluding Markdown table rows from scanning) was reverted after `test_format_direction_lint_rejects_api_name_inference_without_functional_fact` proved it wrong — table-row scanning is intentional, catching an API description that infers an unverified format role even inside a table. Kept as a regression test (`test_table_row_format_claims_are_still_caught`) alongside the real fix.

- [requirements] **2026-08-27 — VER-013: a fourth pre-existing failure, and an RDM-031 correction**

`test_public_candidate_quality_registry.py::test_checks_source_hash_matches_recorded_version` found pre-existing via the same stash-isolation method, while regression-testing the format-direction fix above — a version-pinning tripwire whose hashed module set predates this session entirely. Also corrected RDM-031's evidence doc: `section_cluster_authoring.py` cannot be the code-fence normalization gap's source, since it unconditionally rejects any authored unit containing a fence; the true introduction point is still open.

- [master requirements] **2026-08-27 — RDM-030 fixed (mechanism 1): the family-noun fallback collision**

`aspose-pdf-foss/Aspose.PDF-FOSS-for-Java` reproducibly failed "API reference contains duplicated descriptions." `role_sentence()`'s empty-suffix fallback (a bare class whose own name equals its role suffix, e.g. `class Font`) used the product family noun as its subject — the same noun an unrelated, explicit-prefix sibling type (`PdfFont` stripping to "Pdf") independently canonicalizes to, rendering two genuinely distinct, differently-documented real types to the byte-identical sentence. Replaced the family-noun fallback with the fixed word "base", which no real prefix-derived subject can ever equal by construction. Verified against the live collision plus a full regression sweep (85 passed; 2 new-looking failures confirmed pre-existing via git-stash isolation, added to VER-013).

- [master requirements] **2026-08-27 — RDM-030 fixed (mechanism 2): the `ID`/`Id` case-only collision**

Mechanism 1's fix did not clear `aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET`'s live blocker. Live re-verification found a second, independent collision: `ID` and `Id` are two real, distinct .NET classes differing only by letter case; both fall through `role_sentence()`'s generic fallback with already-different rendered text, but `presentation_template.py`'s duplicate check casefolds before comparing, so they still collided there. Fixed at the rendering layer instead (`role_sentence()` has no visibility into sibling types): `verified_template_api_reference.py` now assembles every namespace's rows before final rendering and disambiguates any group still colliding after casefolding by appending each row's own distinguishing declared member names. Verified the real 660-row .NET reference now renders 660 unique descriptions; full unit suite clean of new regressions.

- [requirements] **2026-08-27 — RDM-030: live-verification proof against `.NET`**

`portfolio-proof --mode fleet --retry-blocked --only "aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET"` against commit `e654a706e` (both fixes landed) no longer reproduces "API reference contains duplicated descriptions" — the repository now advances past presentation compilation into the `presentation_plan` review stage, blocked there on a separate, unrelated set of findings.

- [requirements] **2026-08-27 — RDM-029: two remaining scope gaps pinpointed**

Live re-check on `aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET` (still 13 blocking claims after the prior RDM-029 fix) found the recurring claim-accountability blocker across TypeScript/Rust/.NET is the already-tracked RDM-029, not a new defect. Two precise gaps: (1) fenced code examples are categorically excluded from every edit path in `verified_source_policy.py`, so a fenced example demonstrating an unauthorized format direction never gets a resolution authored for it (8/10 previewed blocking claims for this repo); (2) non-format-direction claim drops are untouched by the existing fix at all (2/10). Both need new, narrow resolution-authoring functions, not a gate change — sized design work, so RDM-029 stays PARTIAL.

- [requirements] **2026-08-27 — OPS-014: `portfolio-proof`'s registry-heal gap**

`portfolio-proof` (all modes) can never refresh its own registry revision: `build_registry_supervise_namespace()` unconditionally sets `no_registry_heal=True` with no override flag. Discovered live while restoring the mission's first `NO_OP_PROVEN` milestone — three `--only`-scoped retries all failed identically with `source_scan_stale` once the cached revision crossed its 24h TTL. Confirmed workaround: running the plain `readme-agent supervise --registry ... --only <repo>` (heal enabled by default) refreshes the cache immediately, after which `portfolio-proof` succeeds again. Logged as backlog, not fixed.

- [requirements] **2026-08-27 — RDM-032: bounded review's namespace scoping is dead code**

`_bounded_fact_payloads()` tries to avoid busting its packet budget on a namespace table's cited `api.public_surface` fact by narrowing it to the one namespace the table belongs to, detected via a regex searching the table unit's *own* rendered text for its owning `### X Namespace (`Y`)` heading. That heading is always a separate, earlier unit — the regex can never match, so the scoping optimization silently falls back to the complete, unscoped fact every time, invisible until a repository's unscoped fact happened to exceed budget (`aspose-3d-foss/Aspose.3D-FOSS-for-.NET`, oversized-unit failure).

- [requirements] **2026-08-27 — RDM-033: recurring section-authoring truncation**

`section_cluster_authoring.py`'s retry loop only caught `(ValidationError, SectionAuthoringAcceptanceError)`, not `LLMTruncatedResponseError` — a forced-tool-call response cut off mid-JSON (`finish_reason == "length"`) failed closed immediately instead of retrying with guidance to write more concisely, even though the loop already supported up to three attempts.

- [requirements] **2026-08-27 — RDM-032: reverted fix attempt and the true mechanism**

Implemented direction 1 (thread the heading text through so the regex can match) and shipped it against real data: unit-tested cleanly, but a live retry against `aspose-3d-foss/Aspose.3D-FOSS-for-.NET` made the failure *worse* — one oversized unit became two. Root cause: `composition_fact_payloads()` already runs every fact through a deliberately lossy, capped summary before namespace scoping runs; making the scoping regex match swapped that small, generic, capped sample for a complete, uncapped, namespace-accurate one — for a namespace large enough, "complete and accurate" is bigger than "capped and generic," the opposite of what scoping exists to achieve. Reverted via `git checkout` before committing rather than ship a regression.

- [master] **2026-08-27 — RDM-033 fixed: retry on truncation with a concise-rewrite hint**

`section_cluster_authoring.py` now catches `LLMTruncatedResponseError` inside its existing retry loop and, if attempts remain, rebuilds the messages with an explicit repair hint asking for the same facts covered more concisely before retrying — only failing closed once all `_MAX_LOGICAL_ATTEMPTS` are exhausted. Two new regression tests cover the recovery path and the exhausted-retries fail-closed path.

- [requirements] **2026-08-27 — RDM-033: fix landing recorded**

Recorded commit `95b162a7e`'s landing with its two new tests (`test_truncated_response_triggers_a_concise_retry_then_succeeds`, `test_truncated_response_exhausts_retries_and_fails_closed`).

- [requirements] **2026-08-27 — RDM-029 gap 1: exact branch traced, live**

Live-traced all 10 of `aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET`'s previewed blocking claims through `assess_material_claims()`+`classify_source_claim_risk()` against the real vendor source. 8 (fenced C# examples) have `obligation_id="additional_examples"`; `resolve_source_claims()` only special-cases `obligation_id=="primary_example"`, so these fall through to `accepted_obligation_bindings()` (returns `None` for this repo), then to `deferred_unverified_obligation_detail_resolution()` (immediately returns `None`, `"additional_examples"` not in its hardcoded allowed set), then to a deliberate no-op — confirmed by direct trace, not inference. The one function built for correct per-example verification is Python-fence-specific by construction and explicitly rejects a C# fence; generalizing it is multi-ecosystem feature work, not attempted.

- [requirements] **2026-08-27 — RDM-029: a shared-root-cause hypothesis (later refuted)**

Traced `aspose-font-foss/Aspose.Font-FOSS-for-Python` (33-34 consecutive `escalation_alert` failures, the most-stuck repository in the portfolio). Its one blocking claim has a third obligation, `major_capabilities`, which IS in the allowed set and whose capability-specific guard tests True — ruling it out as the blocker. By elimination, hypothesized the actual blocker must be `candidate_content_provenance` not carrying a matching binding, the same upstream condition already suspected for gap 1, and that all three gaps might share one root cause. Explicitly flagged as unconfirmed, from two data points, not a live trace.

- [master requirements] **2026-08-27 — RDM-029 partial fix landed: `additional_examples`/`dependency_requirements` admitted**

Extended `deferred_unverified_obligation_detail_resolution()`'s hardcoded allowed-obligation set to include `additional_examples` and `dependency_requirements`, closing the confirmed permanent gap (gap 1) for repositories whose core section is independently fact-bound. Unit-tested with two new tests covering both obligations' resolve/defer paths.

- [requirements] **2026-08-27 — RDM-029 partial fix: live-verification result**

The fix landed safely and is unit-verified correct via a different repository (`aspose-font-foss/Python`, where `candidate_core_present` is independently True for `additional_examples`), but a live retry against the exact repository that motivated it (`aspose-pdf-foss/Aspose.PDF-FOSS-for-.NET`) still showed all 8 targeted fenced-example claims blocking. `candidate_core_present` is apparently also False for this repository's own candidate — a further, deeper, not-yet-investigated layer. Status stays PARTIAL.

- [requirements] **2026-08-27 — RDM-029: shared-root-cause hypothesis directly refuted**

Tested the prior hypothesis directly against real persisted data rather than by elimination: `candidate_content_provenance` *is* persisted per-run (`planning/readme-document-plan.json`), contrary to the hypothesis's own caveat. Loading it for `aspose-font-foss/Aspose.Font-FOSS-for-Python` and calling `accepted_obligation_bindings("major_capabilities", ...)` directly returns **accepted, not `None`**. The same file's persisted claim-accountability record for this exact claim shows `currently_accountable: true` with a valid `deferred_verification` resolution in two independent snapshots, despite a 33-34 run failure streak on live fleet passes — not consistent with a deterministic missing-binding gap; consistent with composition-level LLM nondeterminism, where only some LLM-authored phrasings satisfy the accountability engine's own `survives_in_candidate` check. Revised recommendation: test retry-cadence recovery before writing more per-obligation resolution code.

- [master] **2026-08-27 — RDM-032 fixed correctly: scoped, capped, no unbounded projection**

Corrected version of the reverted attempt above. Threads `section_text` (the unit's owning heading through the unit itself) in as a second namespace-match input, alongside the original `unit_text` (kept for backward compatibility with existing single-argument callers). Caps the scoped projection's `classes`/`functions` at 24 entries each and reports `projection_complete_for_namespace` truthfully rather than always claiming completeness — bounding the worst case instead of comparing against the generic payload (empirically confirmed the wrong criterion: the generic fallback is always smaller by construction, so a size comparison against it would have silently disabled scoping entirely, even for small, harmless namespaces). 88 focused/regression tests pass across `test_bounded_review_packets.py` and seven sibling review test files.

- [requirements] **2026-08-27 — RDM-032 marked IMPLEMENTED; cross-referenced RDM-028**

Live retry against `aspose-3d-foss/Aspose.3D-FOSS-for-.NET` (the repository that originally demonstrated the symptom) still fails identically after the fix landed. Direct inspection of the persisted `bounded-review-plan.json` shows why: that repository's oversized unit is section `api-method-index`, whose own raw rendered text is 123,617 characters — already over the 120,000 budget before any facts payload — the already-known, already-backlogged RDM-028 finding, not a namespace-scoping gap RDM-032 was ever designed to fix. Confirmed RDM-032's own target pattern is real and present elsewhere in the portfolio (`aspose-html-foss/Aspose.HTML-FOSS-for-Python`'s `asposehtmldom` namespace table, required budget 133,095), so the fix has genuine, verified value even though it cannot single-handedly clear this specific demo repository.

- [requirements] **2026-08-27 — FACT-018 corroborated: a third ecosystem instance**

Investigated `format_direction_contradiction` as a possible high-leverage linter bug (it recurs across 8+ repositories). Found it working as designed: `aspose-pdf-foss/Aspose.PDF-FOSS-for-Java`'s candidate accurately describes a real, verified `TiffDevice` class, but the selected `product.formats` fact lists only GIF and BMP as verified outputs — not TIFF, JPEG, or PNG, even though `JpegDevice`/`PngDevice` are equally real classes in the same verified API surface. Root cause confirmed directly in `facts/format_direction.py::_verified_example_literal_directions()`: the compiled-io-literal evidence source scans exactly one verified example's source code for output file-path literals — whatever formats that one example happens to write become "verified"; every other real, working format is invisible to this fact by construction, not because it is unsupported. This is FACT-018's already-diagnosed mechanism recurring in a third ecosystem (after Python instances in `aspose-note-foss` and `aspose-3d-foss`), not a new gap — no new requirement logged, no fix attempted.

- [requirements] **2026-08-27 — RDM-028: protected-content interaction risk flagged before implementation**

Investigated implementing RDM-028's API Method Index row cap directly — it looked tractable, with an existing design direction already stated in the requirement and a confirmed sole-blocker repository. Traced the interaction with `facts/protected_content.py` before writing code: every inline-code span in the vendor's original README is captured as its own hashed `technical_terminology` fragment, and `document_validation.py`'s protected-content check requires every one to survive somewhere in the candidate with zero tolerance — exactly the requirement this table's own docstring says it exists to satisfy. A naive "keep the first N rows" cap could silently drop exactly the rows this table exists to protect, trading a clearly-diagnosed oversized-unit failure for a harder-to-trace protected-content-loss failure in a different validator. Does not invalidate RDM-028's diagnosis; sharpens its own "needs a genuine curation-cutoff decision" framing — any real fix must first partition rows into "sole surviving match for a real protected-content fragment" versus genuinely optional extra coverage, not a uniform top-N slice. Not implemented.

- [governance] **2026-08-27 — Production recovery planning sprint: independent verification and root-cause synthesis**

A planning-only sprint (no implementation) independently re-verified an external review's claims about this project's architecture, CI, state authority, controller fragmentation, evidence staleness, and README quality against live code/state, using 8 parallel read-only investigation agents plus direct command verification (`gh run list`, live `mission status`, `ruff`/`mypy`, `validate_plan_structure.py`, `validate_compact_authority.py`, a scratch CAS-classifier probe). Key correction to the reviewed baseline: `graph_drift` is now `false` and portfolio progress is nonzero (23/34 facts-ready, 1/34 no-op-proven) — not the reported "0/34 everywhere." Separated two conflated problems: (1) CI redness, which traces entirely to pinned/recorded content-hashes left stale by verified code changes, never a behavioral regression or LLM-nondeterminism; and (2) a real, structural rerun-inconsistency problem in the live portfolio pipeline, caused by one uncached LLM judgment surface (prose-quality) and, dominantly, a composition-plan reuse gate (`document_template_hash()`) that hashes ~50 files/4 globs globally with no per-repository scoping, so any unrelated fix invalidates every repository's cached plan at once — amplified by the fact that the ratchet state making accepted verdicts reproducible lives only in gitignored, machine-local `runs/`, restored by nothing (zero `actions/cache` usage in any workflow). Full findings, evidence, and the resulting Decisions #109-113 are in `plans/investigations/production-recovery-sprint-2026-08-27.md`. One original hypothesis (CAS classifier fragility as the literal cause of a "raw git push rejection") did not survive a direct reproduction attempt this sprint and was revised down to a defense-in-depth recommendation rather than silently dropped.

- [master] **2026-08-27 — Status section refreshed to live mission-status numbers**

`plans/master.md`'s Status section had drifted from live truth: it still read "0/31 processable repositories accepted" against a 31-denominator dated 2026-08-24, while a fresh read-only `supervise --mission-action status` run this session showed denominator 34, `facts_ready` 23/34, `no_op_proven` 1/34, and `graph_drift: false`. Replaced the leading paragraph with the live numbers (dated 2026-08-27, pointing at the new investigation report) and kept the prior 2026-08-24/33-denominator paragraph beneath it verbatim, explicitly marked as historical narrative rather than current — no detail dropped, per this project's own surgical-revision convention.

- [master] **2026-08-27 — Decisions #109-113 appended; Build Checklist and Level-8 taskcards wired**

Appended five new Decision Ledger entries covering the production recovery sprint's core findings: #109 (pinned-content-hash consistency mechanically enforced, never hand-remembered), #110 (every LLM-authored judgment surface ratcheted, closing the one gap found in the prose-quality check), #111 (composition-plan reuse invalidation scoped to per-repository dependency instead of one global glob hash), #112 (convergence-critical ratchet state durable and portfolio-shared instead of disposable local-only `runs/`), and #113 (CAS post-push-failure classification hardened as defense-in-depth, with its originally-hypothesized root cause honestly downgraded after a direct reproduction attempt did not confirm it). Added via `scripts/retrofits/append_production_recovery_decisions_109_113.py`, matching the catalog's existing per-record convention. Wired into the Build Checklist (one new item for #109, extensions to the existing "First sealed transaction," "Minimal graph runner," and "Portfolio proof" items for #110-113) and into the `acceptance_checks` of taskcards `L8-PF-00-CAMPAIGN-AUTHORITY-RECONCILIATION`, `L8-PF-03-SEALED-CANDIDATE-NO-OP`, `L8-PF-04-MINIMAL-GRAPH-RUNNER`, and `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY` — no new top-level taskcards authored, consistent with Decision #93's compact-authority cap.

- [requirements] **2026-08-27 — GOV-032, LLM-023, CORE-039, SAFE-020, SAFE-021 added; stale summary counts refreshed**

Appended five new requirement rows (one per Decision #109-113) via `scripts/retrofits/append_production_recovery_requirements.py`, following the established native-authored-record convention (`legacy_row_sha256` = `sha256("native-authored:<id>")`). While doing so, found `plans/requirements.md`'s own summary counts had drifted from the catalog (497 vs. the then-current 509, before this sprint's 5 additions) — refreshed to the live 514-total breakdown and the Level-8 graph's `requirement_catalog` sha256/record_count pointer, and left a dated note in `requirements.md` itself flagging that this summary is not yet mechanically regenerated (exactly GOV-032's own first proof point). Also found, but deliberately left un-regenerated as out-of-scope implementation work, an eighth-generation instance of the same drift pattern: the graph's own `requirement_coverage` pointer (`record_count: 497`) was already stale before this sprint even started.

- [master requirements] **2026-08-27 — SAFE-021/Decision #113 implemented: structural CAS classification fallback**

First real implementation task of the autonomous execution phase (queue order: R5 first, cheapest to verify and a stated prerequisite for R4). `GitStateBackend.save()` (`src/readme_agent/state/git_backend.py`) previously trusted `_is_non_fast_forward()`'s hardcoded stderr-substring match as the sole way to classify a push failure as CAS staleness; anything unmatched raised `StateBackendError` even if the remote ref had genuinely moved. It now falls back to a structural re-fetch-and-compare on any unmatched push failure: if the ref's current SHA differs from what this save started from, it returns `stale` with the real current version, exactly like a matched rejection would; if the ref is unchanged, it still raises (proving the fallback can't mask a genuine auth/network failure). Two new unit tests added to `tests/unit/test_git_state_backend_fetch.py` (`test_save_falls_back_to_structural_recheck_on_unmatched_push_rejection`, `test_save_still_raises_when_push_fails_and_ref_did_not_move`); full state-layer regression suite (`test_git_state_backend_fetch.py`, `test_state_backend.py`, `test_state_cas.py`, `test_domain_state.py`, `test_state_git_backend_local_parallel.py` — 86 tests combined) passes with no regressions; ruff/ruff-format/mypy clean. `SAFE-021` marked `IMPLEMENTED` with this evidence.

- [master requirements] **2026-08-27 — LLM-023/Decision #110 implemented: prose-quality judgment surface ratcheted**

Task 2 of the autonomous execution phase. `verify_prose_quality`/`check_prose_quality()` was the one LLM-authored judgment surface in the codebase with zero caching — every other surface (claim disposition, bounded review, section authoring, trusted-fidelity review) already persists an accepted verdict keyed by content hash plus a deliberately-bumped contract-version literal. New `src/readme_agent/verification/prose_quality_cache.py` mirrors the simplest existing precedent (`readme/claim_accountability_llm_disposition.py`'s per-repo ratchet — no revision-scoped directory needed, since the verdict is a pure function of `final_text` plus `PROSE_QUALITY_CONTRACT_VERSION`). Unlike the claim ratchet (accept-only persistence, to allow retry against changing candidate text), this cache persists every verdict, flagged or not, since a fixed `final_text` gives a retry nothing new to observe. Wired into `capabilities/verify_prose_quality.py::execute()` via a `cache_path=` keyword parameter, deliberately undeclared in the tool schema, matching the module's own existing `client=` convention (test/wiring-only, never offered to the dispatcher). Two existing tests (`test_execute_with_no_client_never_flags`, `test_execute_with_a_fixture_client_flags_and_corroborates`) updated to inject an isolated `tmp_path` cache file rather than touching the real `runs/readme-poc/` tree. Two new tests prove the actual contract: `test_execute_reuses_a_cached_verdict_with_zero_new_calls` (a second call against an exhausted `FixtureForcedToolClient` still returns the identical verdict) and `test_execute_reruns_after_the_contract_version_changes` (bumping the contract-version literal forces exactly one fresh call). Eight more direct unit tests in new `tests/unit/test_prose_quality_cache.py` cover key stability, corruption handling, atomic writes, and multi-key coexistence. 151 tests across the affected files pass; ruff/ruff-format/mypy clean. `LLM-023` marked `IMPLEMENTED` with this evidence.
