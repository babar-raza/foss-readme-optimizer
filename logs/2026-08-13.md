# Project log — 2026-08-13

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-13 | master governance mission-graph | #102 | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Added governance decision 102 (typed external-blocker dispositions satisfy a platform-cohort gate task for downstream-sequencing only) and amended `L8-VPY-03`'s closeout_rules/acceptance_checks to match; independent review found and fixed two gaps (stale decision count, missing PSD disposition record); a `GOV-032` backlog attempt was reverted after it broke 5 real tests. |

## Entries

- [master requirements governance mission-graph] **2026-08-13 — Added governance decision 102 and amended `L8-VPY-03`'s closeout wording so its closure can honestly reflect the current Python checkpoint.**
  Live `mission evaluate` confirmed the recovered `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` claim
  (durable state 1188 -> 1190) still could not close: its own `closeout_rules` require "every
  current Python repository" to be repository-verified, independently approved, and no-op-proven,
  and only 10 of 13 are; HTML/PSD/TeX carry genuine, product-owner-reviewed, non-agent-fixable
  external blockers (decision 101 exception for HTML, PSD deferred, TeX excluded — see
  2026-08-12's `master mission cohort external-blocker` and `master governance evidence
  html decision-101` entries). Because `L8-VPY-04-PRODUCTION-TRANSPORT` and everything after it in
  the mission graph (`L8-VPY-05`, then `L8-VNET-01`, the first non-Python task) depends on
  `L8-VPY-03` reaching status `CLOSED` — confirmed by reading `mission_control.py` directly:
  `_DEPENDENCY_SATISFIED = _TERMINAL_SUCCESS = {"CLOSED"}`, and `closeout_rules` is schema-only
  prose the code never parses or evaluates — the *entire* requested non-Python portfolio was
  graph-blocked behind a literal "all 13" reading that three permanently non-agent-fixable
  upstream defects (not writable by this project without separate, unauthorized product-repo
  pushes) could never satisfy.

  Asked the product owner how to proceed (three options: amend the graph's closeout semantics via
  a new decision; pursue the one repairable blocker, HTML's `pyproject.toml`, through a separately
  authorized product push; or record the conflict and stop). Chosen: amend the graph closeout
  semantics.

  Added **decision 102** (`plans/decisions/catalog.jsonl`, `plans/master.md`'s Decision Ledger): a
  platform-cohort gate task may close, solely to unlock the mission controller's `CLOSED`-only
  dependency check on later tasks, once every repository in its scope is `NO_OP_PROVEN` or carries
  an accepted, human-reviewed typed disposition (a decision-101 exception, or a deferred/excluded
  external-blocker record with owner, evidence, and resume predicate). It explicitly never
  reclassifies a typed-disposition repository as `NO_OP_PROVEN` and never satisfies Gate A/B or
  full-registry closure — decision 101 already forecloses that independently, and decision 102
  does not reopen it. Applying decision 102 to a specific gate task requires updating that task's
  own `closeout_rules`/`acceptance_checks` text in place, so the evidence later submitted to close
  it asserts something true instead of restating the old, stricter, currently-unmet sentence.

  Amended `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` in
  `plans/investigations/control/level8-autonomous-mission-task-graph.yaml`: its first
  `acceptance_checks` bullet and its `closeout_rules` now read "NO_OP_PROVEN or an accepted typed
  external-blocker disposition per governance decisions 101 and 102" instead of an unqualified
  "every current Python repository ... no-op-proven"; `failure_reroute` gained one clause stating
  this task's own `CLOSED` status unlocks downstream sequencing only and does not assert POC
  completion. Avoided writing `#102`/`#101` inline (a space immediately before `#` starts a YAML
  comment and silently truncated the line, caught by re-parsing the file with `yaml.safe_load`
  before trusting the edit — fixed by spelling out "governance decision(s) N" instead). Verified
  structurally with the real CLI, not just YAML syntax: `readme-agent supervise --mission-action
  status` correctly refused with "durable mission state is stale against the loaded task graph"
  against the new `graph_sha256`; `--mission-action evaluate` reconciled it cleanly
  (`graph_drift: false`, state 1190 -> 1191, `L8-VPY-03` still correctly shown open, not
  falsely closed by the edit alone).

  Attempted to log **`GOV-032`** (`plans/requirements/catalog.jsonl`, status `BACKLOG`) for a
  separate, unrelated finding: `plans/investigations/evidence/l8-vpy-03-python-external-blockers/
  README.md` (dated 2026-08-01) documents only two blockers (HTML, TeX) against a stated
  twelve-repository denominator; PSD is missing entirely and the denominator is stale against the
  current 13-repository/3-blocker state. **Reverted**: a full pytest run surfaced 5 real, newly
  broken `TestCompactAuthoritySourceBinding` failures. Root cause:
  `scripts/governance/validate_compact_authority.py`'s `_source_catalog_errors()` filters decisions
  by `if record["decision_id"] in source_decision_ids` before comparing them against the original
  migration commit (why decision 102 caused zero failures), but has no analogous filter for
  requirements — it does a strict full-list reconstruction match, so any new requirement row breaks
  it regardless of content. This is a genuine, pre-existing gap in that validator (requirements
  cannot currently be appended post-migration the way decisions can); fixing it is out of scope for
  this task, so `GOV-032`, the `requirements.md` count edits, and the resulting graph-hash
  references were fully reverted (`plans/requirements/catalog.jsonl`, `plans/requirements.md`, and
  `plans/investigations/control/level8-deferred-task-catalog.jsonl` are byte-identical to the
  pre-session committed state; `requirement_catalog.sha256`/`requirement_coverage.sha256` back to
  their original values). `TestCompactAuthoritySourceBinding` (6/6) and the full plan-structure
  validator reconfirmed clean after the revert.

  Ran independent, non-authoring review of the decision-102 amendment: **CONDITIONAL PASS**, no
  dishonesty or scope creep found, but two concrete gaps — `plans/master.md`'s Decision Ledger
  header still said "101 stable decisions" after decision 102 was added (fixed, now 102); PSD had
  no dedicated disposition record comparable to HTML's `data/working_condition_exceptions.json`
  entry or TeX's `report/findings/tex/python/upstream-issues.md` (fixed: added
  `report/findings/psd/python/upstream-issues.md`, consolidating the already-established
  2026-08-09/2026-08-12 findings into the canonical location decision 101 routes a
  missing-source repository to). Both fixes independently re-verified (grep-confirmed exact text;
  file existence and content checked directly).

  The actual `L8-VPY-03` `CLOSED` transition (with real `MissionContributionEvidenceV1`/
  closeout-control evidence matching the amended `acceptance_checks` text, a fresh complete
  non-live pytest receipt, and freshly computed scoreboard/multi-agent-admission hashes) is still
  pending — this entry covers the graph/decision amendment and its review/correction only, not its
  execution. No product-repository write occurred; no repository's own lifecycle state changed.
