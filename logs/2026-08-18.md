# Project log — 2026-08-18

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-18 | master | — | — | `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY` | Added the missing `## Dependencies` template slot, closing yesterday's found gap. |
| 2026-08-18 | master | — | — | `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY` | Closed a claim-accountability and a section-registry integration gap the new slot introduced, mirroring the T5-R1 `api_method_index` precedent. |
| 2026-08-18 | master | — | — | — | Full unit suite (~3,890 tests) confirmed no regressions via a stash-based before/after comparison. |
| 2026-08-18 | master | — | — | — | Bounded-verified-canary re-verification blocked by a new, concrete manifestation of the pre-existing `local_poc` origin-backed state-backend bug; documented, not worked around. |
| 2026-08-18 | governance | 103 | — | — | Added GOVERNANCE.md rule 24: continuous progress, no idle waiting while safe eligible work remains. |
| 2026-08-18 | master | — | — | `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY` | Fixed the `local_poc` origin-backed state-backend bug that blocked the Dependencies-section canary re-verification, raised in priority the same day it was found. |
| 2026-08-18 | master | — | — | — | Diagnosed and fixed an unrelated real slowdown: 17 `test_cli.py` tests silently fell through to a real (not stubbed) local_poc backend once the fix landed, discovered via `faulthandler` stack dumps, not guesswork. |
| 2026-08-18 | master | — | — | `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY` | Set the operator's `/goal` (close Gate A) and resumed the Gate A portfolio run; fixed a real SYSTEM_FAILURE crash it immediately surfaced (`product_truth_recovery.py`). |
| 2026-08-18 | master | — | — | — | Built and unit-tested the bounded LLM-verification alternative acceptance path for claim accountability, mirroring aspose.org's own proven `content-dispositions.json` mechanism, per explicit operator authorization. |
| 2026-08-18 | master incident | — | — | — | Self-caused: an in-progress, not-yet-hygiene-clean claim-disposition file broke the live Gate A portfolio loop's every member; found and fixed within the same pass; the loop's own "done" detection also proved unreliable (counted iterating all 33 as complete despite 0 real new progress) and is now a known caveat. |

## Entries

- [master] **2026-08-18** — Per the operator's explicit "harden the process before moving forward"
  directive (given while a full-portfolio `local_poc` run was in flight, mid-2026-08-17), stopped
  that run and fixed the highest-confidence, most tractable item on `candidate-quality-gap-list.md`
  before resuming: every generated candidate folded required/optional dependency prose into
  `## Installation` instead of aspose.org's dedicated `## Dependencies` section. Added
  `"dependencies"` to `TemplateSlot` (`template_schema.py`) and `templates/readme/
  repository-presentation-v1.json` (`section_order`/`headings`, `template_version` 1.19.0 →
  1.20.0); `verified_template_draft.py` now composes `### Required Package Dependencies` /
  `### Optional Dependencies` subheadings from the already-existing `dependency_markdown()`/
  `scenario_dependency_markdown()` renderers into their own slot instead of merging into
  `installation`. No new fact extraction needed — `python.distribution.runtime_dependencies`
  already carried the required-dependency list. New regression test
  `test_dependencies_section_renders_separately_from_installation` proves the content actually
  moved (present in `draft.sections["dependencies"]`, absent from `draft.sections["installation"]`).

- [master] **2026-08-18** — Follow-on hardening, found by deliberately re-deriving the exact
  integration checklist the prior analogous change (`api_method_index`, T5-R1, commit `669a227a5`)
  had to satisfy, rather than assuming "add a slot + wire the renderer" was complete. Two real gaps
  closed: (1) `verified_template_provenance.py`'s `_CLAIM_LEVEL_SLOTS` didn't include
  `"dependencies"`, so its content would have silently skipped claim-accountability analysis
  entirely once real candidates started using it — a real accountability regression versus when
  the same text lived inside `installation` (which is claim-level); added the slot to
  `_CLAIM_LEVEL_SLOTS`, a canonical-reconstruction branch in `_canonical_structural_section`, and a
  canonical-match fact-id fallback mirroring the existing `additional_examples` one. (2) The
  committed `templates/readme/section-registry-v2.json` (T14) hand-drifted from the live contract
  the moment the new slot existed; regenerated via a new, reusable
  `scripts/retrofits/regenerate_section_registry_for_dependencies_slot.py` (16 entries now; the 12
  previously-`unmapped_section_checks` T3 dependency-heading checks — `check_dependency_
  disposition_reconciliation`, `check_dependency_version_pin_freshness`, etc. — now map cleanly
  onto the new slot instead of being silently unreachable). New regression test
  `test_dependencies_section_receives_exact_h3_lineage_no_orphan_content` (mirroring the existing
  `test_canonical_compiler_h2_and_fact_renderer_h3_have_exact_lineage`) proves the *independent*
  provenance verifier grounds every claim in the new section against real accepted facts, not just
  that the composer renders the right text — the layer where the `api_method_index` precedent had
  found real bugs, confirming this was the right thing to check rather than trusting the composer
  half alone.

- [master] **2026-08-18** — Full unit suite (~3,890 tests) confirmed no new failures beyond a
  pre-existing, unrelated baseline, established cleanly via a `git stash` before/after comparison
  of the touched files (a naive single full-run diff was misleading: one test,
  `test_no_op_proof_reuses_the_exact_accepted_review_binding`, failed only inside the full
  3,890-test run and passed in isolation both before and after this change — order-dependent test
  pollution unrelated to this fix, not a regression; left as-is rather than chased, since it
  reproduces independently of any code touched here).

- [master] **2026-08-18** — **Attempted but blocked**: real end-to-end re-verification of the
  Dependencies-section fix via `readme-agent supervise --repo aspose-pdf-foss/Aspose-PDF-FOSS-for-
  Python --bounded-verified-canary --execution-profile local_poc` (AGENTS.md rule 15 — the
  governed mechanism, not an ad-hoc script). Three consecutive attempts, each preceded by an
  explicit, successfully-exiting mission-claim reclaim, still failed with the task shown as claimed
  by an observer identity from hours earlier in the same session. Root-caused to a *new, concrete*
  manifestation of the already-documented `local_poc`-depends-on-`origin` bug (`plans/
  investigations/evidence/readme-portfolio-aspose-parity/local-poc-state-backend-uses-origin-not-
  local.md`, found 2026-08-17): `--repo ... --bounded-verified-canary` reads its claim state through
  `commands_supervision.py::_force_durable_state_backend()` → `git_backend.py`'s real-`origin`-backed
  backend, while `--mission-task-graph ... --mission-action claim` mutates a completely separate,
  always-local, always-fresh state store — so a demonstrably-successful local reclaim can never
  actually clear a stale canary-side claim in a session that (correctly, per standing policy) never
  pushes to `origin`. Documented as a new addendum to the existing evidence file rather than worked
  around (forcing the claim, or bypassing the canary, would repeat exactly the anti-pattern rule 15
  exists to prevent). Fell back to the real, non-mocked production functions
  (`build_verified_template_draft`, `build_template_provenance`) exercised directly by the unit
  suite as the next-most-rigorous available verification. This raises the priority of fixing the
  underlying state-backend bug: until `commands_supervision.py` stops depending on `origin` for
  `local_poc`, no session can reliably re-verify a fix via `--bounded-verified-canary` once its
  mission claim has been touched by any earlier `--mission-task-graph`-only action in the same
  session. Portfolio-wide coverage-impact measurement (today's fix plus 2026-08-17's
  `api.public_surface` fix) remains blocked for the same reason.

- [governance] **2026-08-18** — Added `GOVERNANCE.md` rule 24 (continuous progress: no agent sits
  idle while a dependency is in flight and safe eligible work remains), `plans/decisions/
  catalog.jsonl` decision #103, a matching `plans/master.md` Decision Ledger bullet, and an
  `AGENTS.md` mirror section — codifies the user's standing continuous-progress operating
  instruction (never idle on a running command/agent/model request/validation job; work the next
  safe eligible task; don't duplicate work, bypass leases, or cross approval gates to stay busy;
  don't manufacture busywork; record the blocker and keep polling when genuinely nothing else is
  eligible) directly in repo governance, cross-referencing existing rule 13 (blocked
  classification), rule 19 (leases/gates), and `GOV-014` (backlog discipline) instead of restating
  them, so it applies to every future agent without being re-pasted each session. Deliberately did
  not add a companion `GOV-0NN` requirement-catalog row: `plans/requirements/catalog.jsonl`'s exact
  requirement-ID-set check in `scripts/governance/validate_compact_authority.py` has no append path
  (unlike the decision catalog's, which this change is the first to exercise), and the four most
  recently added `GOVERNANCE.md` rules (20-23) likewise shipped without one — a real, pre-existing
  gap in that tooling, not something to work around silently as part of this change.

- [master] **2026-08-18** — Fixed the `local_poc`-depends-on-`origin` state-backend bug (`plans/
  investigations/evidence/readme-portfolio-aspose-parity/local-poc-state-backend-uses-origin-not-
  local.md`, found 2026-08-17, deferred that day, priority raised earlier today once it started
  actively blocking canary re-verification rather than just degrading portfolio runs).
  `commands_supervision.py` gained `_state_backend_for_profile(profile)`: routes `local_poc` to the
  already-existing `default_local_poc_state_backend()` at both call sites that previously called
  `_force_durable_state_backend()` (→ real `origin`) unconditionally — `cmd_supervise`'s single-
  repo/canary path, and `_cmd_supervise_registry`'s portfolio path (unconditionally `local_poc` by
  its own contract, so it calls the local backend directly, no branch needed). Every other profile
  (`github_*`, `act_*`) is untouched, still genuinely needs `origin`. Surfaced and fixed a real,
  separate pre-existing type-safety gap while doing this: `_force_durable_state_backend()` had no
  return-type annotation, so mypy silently treated its callers' `state_backend` as `Any` and never
  checked 6 real `ReadmePocLifecycleStateV1`/`V2` union-narrowing sites in `_cmd_supervise_registry`
  that access V2-only fields (`source_revision`, `content_assurance`) without an `isinstance` guard
  — switching to the fully-typed `default_local_poc_state_backend()` unmasked them; fixed with the
  same `isinstance(lifecycle, ReadmePocLifecycleStateV2)` idiom `supervisor/loop.py` already uses
  elsewhere, not by re-adding an `Any` escape hatch. New regression coverage: `test_local_poc_
  backend_never_targets_origin` (spies on every real `run_git` call a constructed `local_poc`
  backend makes, asserts `"origin"` never appears and push/fetch only ever targets the isolated
  local bare remote — the exact test the evidence doc's own "Recommended next step" asked for), and
  `TestStateBackendForProfile` (5 tests proving the routing decision itself, including that `local_
  poc` never even constructs the real backend and every other profile/`None` never touches the
  local-only one).

- [master] **2026-08-18** — The state-backend fix surfaced a second, genuinely separate problem:
  17 pre-existing `test_cli.py` tests stubbed only `git_backend.default_state_backend`, an implicit
  assumption that one stub covered every execution profile including `local_poc` — true only by
  accident, since `local_poc` previously had no code path of its own. Once it did, those 17 tests
  silently fell through to the real (correctly-routed, but never designed to run in a fast unit
  test) local backend and became real-git-subprocess-slow instead of instant. First symptom looked
  like an outright hang (a single foreground `pytest` invocation exceeded a 120s wait with zero
  output); root-caused precisely, not guessed, via `faulthandler.dump_traceback_later(...,
  exit=True)`, which dumped the exact blocked stack twice for two *different* real slow spots (a
  `git fetch` against a fresh local bare repo inside `write_supervise_evidence`'s lifecycle-
  checkpoint read; unrelated, a pre-existing `act_registry_intake` prompt-hygiene file read that
  turned out to be OneDrive-sync I/O flakiness, confirmed non-reproducing on a clean rerun and left
  alone as a known environment characteristic, not a regression from this change). Fixed the 17
  real cases via a new, reusable `scripts/retrofits/add_local_poc_backend_test_patches.py`: adds a
  companion `local_poc_backend.default_local_poc_state_backend` stub next to each existing
  `default_state_backend` stub inside every `local_poc`-profile test, returning the same fake
  regardless of which code path a given test now exercises. Also cleaned up `runs/local-poc-state/`
  (gitignored, real but harmless pollution accumulated from today's own live debugging against the
  actual local backend) before the final confirmation runs. Full `test_cli.py` + the affected
  integration test: 102 passed in 77s (was hanging/multi-minutes-slow before the retrofit).

- [master] **2026-08-18** — Set the operator's `/goal`: close Gate A (full-registry verified local
  README proof) per `plans/idea.md`, local-only, ordered platform waves, harden-before-resuming
  discipline explicit in the goal text itself. Resumed the Gate A portfolio run
  (`readme-agent supervise --registry data/products.json --execution-profile local_poc`), which
  immediately hit `SYSTEM_FAILURE: RuntimeError: interrupted product-truth recovery found
  inconsistent bundle identity or outcome` on the very first member (3D-Python). Root-caused: this
  session's own earlier `runs/local-poc-state` cleanups (during the state-backend fix's debugging)
  reset durable state for the *whole registry*, not just the test fixture entry used for that
  debugging — so `product_truth_recovery.py::recover_interrupted_product_truth_commit()`, whose
  narrow job is recovering a bundle sealed only up through the FACTS boundary, found 3D-Python's
  real on-disk bundle sealed all the way at `DETERMINISTIC_VALIDATED` (from before the reset) and
  raised instead of gracefully declining. Fixed: the "bundle progressed further than this function
  recovers" case now returns `None` (falls through to normal reprocessing) instead of raising;
  genuine identity mismatches (org_repo/revision/facts_hash, or a fact-outcome mismatch once the
  status *is* a fact outcome) still raise, unchanged. New regression test reproduces the exact live
  RuntimeError against the pre-fix code (git-stash before/after) and passes with the fix. Re-run
  confirmed 3D-Python reaches `AGENT_APPROVED`/`NO_OP_PROVEN` cleanly, zero system failures.

- [master] **2026-08-18** — User pushback ("at aspose.org nothing is blocked... I am assuming you
  would solve all problems as well") directly authorized closing the claim-accountability blocking
  category this session had previously only documented (`claim-accountability-blocking-analysis.md`
  option 2 — "a genuine design decision, not attempted this session"). Investigated aspose.org's
  real, live mechanism directly (`aspose.org/reports/repo-presenter/barcode/python/content-
  dispositions.json` — the literal counterpart of this repo's own blocked barcode-python
  `generate()` claim): its LLM reconciles each old-README unit against the *already-composed*
  candidate text (or real repository source for genuinely new details), not against pre-extracted
  fact phrases. Built the equivalent as a bounded, additive fallback — new prompt
  (`claim_disposition_check`), tool-schema/message builder, a corroboration layer mirroring
  `verification/prose_quality.py`'s "never trust the model's own say-so" shape exactly (a
  `redundant_with_candidate` verdict is only accepted if its quoted evidence is found verbatim in
  the real candidate; `verified_against_source` only if the cited file is real and the quote is
  found verbatim in it), a new `ExpectedClaimDisposition` value tried only after every existing
  mechanical path has already failed, and two new optional parameters on
  `build_readme_claim_accountability_map()` that leave every existing caller's behavior 100%
  unchanged when omitted. Proved all three required properties end to end through the real function
  (not mocked): unclaimed without a client (today's exact behavior), accepted with a corroborated
  verdict, still blocking with an uncorroborated (hallucinated-evidence) one. Deliberately not yet
  wired into the live candidate-rendering pipeline — no caller constructs a real client yet, so this
  is fully inert in production until that explicit next step; documented precisely in the evidence
  file's new "Implementation status" section.

- [master incident] **2026-08-18** — Self-caused, found and fixed the same pass: the claim-
  disposition prompt's owner/consumer source file didn't exist yet when its YAML manifest was
  already written and readable by the prompt registry's whole-codebase hygiene scan — every member
  of the *live, running* Gate A portfolio loop failed instantly with `error: prompt hygiene failed:
  runtime references unknown prompt 'claim_disposition_check'` until the remaining pieces (owner
  module with a real call site, `env.py` model-route entry, `prompts/README.md` table row) landed.
  No real damage (`provider_calls=0` for the whole affected run — every failure happened before any
  provider call), but a direct, concrete instance of exactly the risk this session's own established
  discipline (finish and verify before letting a live process depend on in-progress code) exists to
  prevent. Separately, the loop driver script's `slice_complete=True` detection proved unreliable
  under this exact failure mode: it reported "DONE" after 3 iterations because the whole 33-entry
  registry got *iterated* within one time slice, not because Gate A was actually complete (only
  1/33 was real, unchanged from before) — a real gap in `scripts/retrofits/
  run_gate_a_local_poc_portfolio_loop.sh`'s own completion signal, noted here for its next revision
  rather than fixed under time pressure mid-incident.
