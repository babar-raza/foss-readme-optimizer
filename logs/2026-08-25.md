# Project log — 2026-08-25

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-25 | requirements links backlog | — | `CORE-035`, `CORE-036`, `CORE-037`, `EVID-006` | L8-PF-05-SEVEN-ECOSYSTEM-CANARIES | Fixed link-governance false positive on code-span sample URLs; logged four mission-controller hardening findings. |
| 2026-08-25 | requirements backlog mission-graph | — | `CORE-038` | L8-PF-05-SEVEN-ECOSYSTEM-CANARIES | Root-caused a C++ false-positive stub claim; reconciled the requirement catalog after the prior BACKLOG additions. |
| 2026-08-25 | requirements facts cmake | — | `VAL-019`, `VAL-020` | L8-PF-05-SEVEN-ECOSYSTEM-CANARIES | Added CMake-derived development-command facts, unblocking build/test claims for non-Python repositories. |
| 2026-08-25 | requirements evidence readme | — | `RDM-027` | L8-PF-05-SEVEN-ECOSYSTEM-CANARIES | Root-caused duplicate Key Capabilities content from LLM-authored and preserved source bullets colliding. |
| 2026-08-25 | requirements backlog invalidation-cost | — | `CORE-035` | L8-PF-05-SEVEN-ECOSYSTEM-CANARIES | Recorded CORE-035's cache-invalidation blast radius being hit twice in one session, with a batching mitigation. |

## Entries

- [requirements links backlog] **2026-08-25 — Repaired PF05-CXX-LINK-001: a C++ code-example URL literal was governed as a navigational link** Root cause: `contextual_validation.py` rejected the `aspose-cells-foss/Aspose.Cells-FOSS-for-Cpp` candidate because `https://docs.aspose.org/cells/cpp/` appeared "non-linkable" -- but it is a string literal argument inside a verified C++ `hyperlinks.Add()` example, not a navigational link, and GitHub's renderer never linkifies fenced or inline code either. `readme/document_link_hygiene.py` already protected code spans for its own unwrap pass, but `links/occurrences.py` had no equivalent exclusion, so its five callers (`contextual_validation`, `contextual_selection`, `verified_source_policy`, `verified_template_link_budget`, `document_link_hygiene`) disagreed about code-embedded URLs. Fix: extracted the shared protected-span logic into new module `links/code_spans.py`, applied it at `links/occurrences.py::find_aspose_link_occurrences`, and refactored `document_link_hygiene.py` onto the same helper instead of a duplicate regex pair. Regression tests: `tests/unit/test_contextual_link_selection.py::test_aspose_url_inside_code_fence_is_not_a_governed_link` (positive) and `::test_same_unbound_url_in_prose_still_fails_final_validation` (negative control), plus new `tests/unit/test_link_occurrences.py`. Also logged four non-blocking production-hardening findings from an independent mission-controller assessment as `BACKLOG` rows per GOV-014: CORE-035 (fact-verification cache's `_COMMON_FILES` invalidation blast radius), CORE-036 (`GitStateBackend.save()` missing a read-after-write confirmation, following the 2026-08-24 saved/saved CAS anomaly), CORE-037 (mission-status staleness-drift check against `plans/master.md`), and EVID-006 (hardcoded 900.0-second placeholder timings in `reduce_real_receipts.py`). Commit `e2db75462`.

- [requirements backlog mission-graph] **2026-08-25 — Root-caused the C++ DateTime "unimplemented stub" false positive and reconciled the requirement-catalog graph** Resuming L8-PF-05-SEVEN-ECOSYSTEM-CANARIES past PF05-CXX-LINK-001 (`e2db75462`), the bounded C++ canary hit a new `contradiction_capability_symbol` finding on `DateTime`. Root cause: vendored `scripts/pipeline/extraction/scout.py::_detect_cpp_stubs()` flags any C++ `function_definition` whose `compound_statement` body is empty as an unimplemented stub, without checking for a sibling `field_initializer_list` -- misclassifying the idiomatic initializer-list-does-the-work constructor pattern. Verified against the real upstream `Aspose.Cells.Foss.Cpp/src/DateTime.cpp:77` at the exact snapshotted revision: the flagged constructor is fully implemented via its member-initializer list. Not fixed directly: `scout.py` is byte-integrity checked against a pinned `source_commit` manifest (`tests/unit/test_aspose_org_vendored_source.py::test_vendored_source_rejects_changed_bytes`), and any edit would shift `generator_sha256`, invalidating every cached native-language knowledge-generation bundle portfolio-wide (the already-logged CORE-035 blast radius). Logged as CORE-038 (BACKLOG). Separately, the prior session's four new BACKLOG rows (CORE-035..037, EVID-006) had drifted `plans/requirements/catalog.jsonl`'s hash away from the mission graph's frozen baseline, blocking every mission-controller action with "requirement catalog hash mismatch"; backfilled their `legacy_line`/`legacy_row_sha256` provenance fields following the existing `backfill_know_legacy_provenance_fields.py` convention and reconciled `level8-autonomous-mission-task-graph.yaml` via `build_level8_requirement_taskcard_coverage.py` (489 -> 494 rows). Verified `--check` clean with `graph_drift: false` and the 111-test mission control/graph regression suite passing. Commit `5659fdd58`.

- [requirements facts cmake] **2026-08-25 — Repaired missing CMake development-command evidence blocking every non-Python PF05 ecosystem** PF05's C++ canary blocked on two `unjustified_loss` claims covering the source README's own build/test instructions (the GoogleTest FetchContent detail, the `samples/` build). Root cause: those claims classify as `development_commands`, which `classify_source_claim_risk()` treats as `mandatory_fact_resolution` -- but the only collector behind `development.commands` was Python-only (`curated_repository_guidance.repository_development_commands` delegated straight to `curated_python_development`), so a C++ repository had no command evidence at all and the section degenerated to a bare file list, strictly worse than the source. This affected every non-Python ecosystem in PF05's scope. Fix: added `facts/curated_cmake_development.py`, deriving configure/build/test commands purely from CMake manifest text -- `ctest` only when `enable_testing()` plus a registered test, a `FetchContent` dependency only when its URL pins an exact version, `add_executable(${PROJECT_NAME} ...)` resolved through the manifest's own `project()` call -- nothing executed, `execution_verified` stays false. Verified against the real repository at the canary's snapshotted revision: recovers exactly what the README claims (GoogleTest v1.14.0, `ASPOSE_CELLS_FOSS_TESTS_USE_SYSTEM_GTEST`, CMake 3.16) and excludes the library-only project. Registered the module in the fact-acceptance contract, which invalidated every repository's facts including the sealed Python deliverable (the already-logged CORE-035 blast radius); regenerated to `AGENT_APPROVED` then `NO_OP_PROVEN`. Regression tests: `tests/unit/test_cmake_development_commands.py`, `tests/unit/test_verified_source_claim_omissions.py`. Also logged VAL-019/VAL-020 (BACKLOG) for two unit tests confirmed pre-existing-failing on clean `main` via stash-and-rerun. Commit `8cb2a6cd8`.

- [requirements evidence readme] **2026-08-25 — Root-caused PF05-CXX-DUPLICATE-001: Key Capabilities rendered two overlapping capability lists** With the link and CMake fixes landed, the C++ canary cleared intake, facts, and claim accountability and stopped on duplicate capability content: the candidate's Key Capabilities section carried both the LLM-authored cluster (`section_authoring_overlay.authored_unit_markdown`) and the source README's own preserved backticked bullets, with three headings byte-identical across the two. Both halves are individually correct -- `verified_template_draft.py:503` already replaces rather than appends the deterministic rows, and the surviving source bullets are `accepted_fact`/`survives=True` -- so no earlier gate caught the overlap; nothing reconciles the two producers. Noted the DateTime contradiction (CORE-038) is entangled with this: its "available" side sits inside the preserved source block, so a naive fix could silence that finding without curing the underlying false limitation claim. Logged as RDM-027 (BACKLOG) with three candidate repair directions, constrained to keep every surviving source bullet's claim accountability intact. Full write-up: `plans/investigations/evidence/portfolio-proof-pf05-seven-ecosystem-canaries/cxx-duplicate-capabilities-2026-08-26.md`. Commit `8e695dd1f`.

- [requirements backlog invalidation-cost] **2026-08-25 — Recorded the realized CORE-035 invalidation cost after it hit twice in one session** The cache-invalidation blast radius CORE-035 describes was realized twice in this session: registering `curated_cmake_development.py` in the fact-acceptance contract, then a later edit to that same file, each invalidated every repository's facts and regressed the sealed Aspose.3D Python deliverable, forcing a full regeneration back to 1/33 both times. Updated CORE-035's `acceptance_evidence` in `plans/requirements/catalog.jsonl` with this realized history and the practical mitigation until the contract is properly scoped: batch every edit to a contract-registered fact module and regenerate accepted deliverables once at the end, rather than after each edit. Commit `10175c2b0`.
