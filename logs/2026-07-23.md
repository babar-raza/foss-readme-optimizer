# Project log — 2026-07-23

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-07-23 | requirements governance docs | #37 | `GOV-021`, `LLM-018`, `LLM-019` | Wave 9.3 | Wave 9.3 closed: AGENTS.md/safety-model.md/surface-model.md stale passages fixed, OWN-004 over-extension corrected, LLM-018/019 citations fixed at all 4 sites, decision #37 amended in place, plans/status.md+roadmap.md+changelog.md created, master.md Status section trimmed. |
| 2026-07-23 | supervisor runtime requirements | — | `RUN-002`, `RUN-005`, `RUN-006`, `RUN-008`, `RUN-009`, `ORC-007` | Wave 9.4, Wave 9.5 | Wave 9.4: ExecutionProfileV1 (5 profiles), --execution-profile flag, --domain bypass closed under github_* profiles, permission-class enforcement threaded into supervise_repo(). Wave 9.5: TriggerRecordV1 dedup mechanism wired into cmd_supervise() via GITHUB_RUN_ID. |
| 2026-07-23 | effect-ledger idempotency capabilities | #58 | `EFF-006`, `PRL-009` | Wave 9.6 | EFF-006 fixes fresh_fingerprint-only idempotency key; new EffectIdentityV1 hashes final_text; PRL-009 backlogs the separate open_presentation_pr branch-reuse gap this exposed. |
| 2026-07-23 | convergence freshness supervisor | #59 | `FRESH-001`, `FRESH-002`, `FRESH-003`, `FRESH-004`, `FRESH-005`, `FRESH-006`, `VER-007` | Wave 9.7 | New SurfaceFreshnessContractV1/no_change_gate_holds() close the Git-SHA-only no-op gap for 4 non-git surfaces + pending-effect/open-proposal/trigger conditions; GOV-024 contingency reconfirmed. |
| 2026-07-23 | profile ecosystems repository-intelligence | #60 | `ECO-004`, `ECO-005` | Wave 11.1 | New find_all_manifest_roots()/find_all_manifest_roots_from_tree() + PackageRoot/package_roots close ECO-004; richer per-root schema and 8-fixture-archetype ask deferred to ECO-005 (BACKLOG). |
| 2026-07-23 | ecosystems resolver capabilities package-verification | #61 | `PKG-001`, `PKG-002`, `PKG-003`, `PKG-004`, `PKG-005`, `PKG-006` | Wave 11.2 | PyPI/npm/NuGet/Go-proxy/Conan/vcpkg resolvers all live-verified; new verify_package_acquisition reports per-package-root outcomes; PKG-006 backlogs real install/build execution. |
| 2026-07-23 | facts product-facts capabilities | #62 | `FACT-010`, `FACT-011`, `FACT-012`, `FACT-013` | Wave 11.3 | New facts/schema.py + facts/changeset.py (FACT-011/012); get_product_facts.py exposes package_roots; FACT-013 backlogs the unbuildable handoff-dependent ingestion modes (decision #37). |
| 2026-07-23 | capabilities schema dispatcher | #63 | `CAP-008`, `CAP-009` | Wave 11.4 | New input_model/output_model fields, OrgRepoRef/OrgRepoOnlyInputV1 retrofitted onto 3 capabilities; CAP-009 backlogs profile-compatibility filtering due to a real vocabulary-mismatch blocker. |
| 2026-07-23 | readme claim-verification reconciliation | #64 | `RDM-024`, `RDM-025` | Wave 12.1 | New find_claim_conflicts() (RDM-024) proven against the real, live-fetched cells/java README; RDM-025 backlogs render-pipeline integration citing two concrete architectural blockers (decision #11 hash exclusions, prepare_readme_candidate's load-bearing fragility). |
| 2026-07-23 | supervisor specialist-selection cli | #65 | `ORC-003`, `ORC-008`, `AGT-008` | Wave 12.2 | New CLI flag closes the confirmed gap where Wave 8.6's dynamic mechanisms had zero production effect; AGT-008 corrected BACKLOG->PARTIAL (2/3 criteria already built); ORC-008 backlogs unused Task fields. |
| 2026-07-23 | verification specialists truth-audit | #66 | `VER-008` | Wave 12.3 | Zero code changes. Direct verification found independent_verification already runs strictly last with same-run sibling visibility; VER-008 names the real gap (no repair-task mechanism fits a cross-domain finding) rather than a shallow fix. |
| 2026-07-23 | evidence supervisor manifest | #67 | `EVID-001`, `EVID-002`, `EVID-003`, `EVID-004`, `GOV-012`, `SAFE-014`, `SAFE-017` | Wave 13.1 | New evidence/manifest_v2.py + write_run_manifest_v2() replace the ad hoc manifest.json dict at all 4 evidence call sites; requirement_ids_exercised gives GOV-012/SAFE-014 their first real evidence. |
| 2026-07-23 | authorization effect-class cli | #68 | `AUTH-001`, `AUTH-002`, `AUTH-003`, `AUTH-004`, `AUTH-005`, `AUTH-006` | Wave 13.2 | Built additive EffectClass taxonomy + AuthorizationRecordV1 schema/fail-closed loader + diagnostic authorization-validate CLI, live-verified against all 3 pilots; enforcement wiring deferred to Wave 13.3. |
| 2026-07-23 | authorization effect-class enforcement cli | #69 | `AUTH-004`, `AUTH-007` | Wave 13.3 | Wired authorized_for() into open_presentation_pr via effect_ledger.py's dispatch_gated_effect(), scoped to just that capability after source verification showed commit_readme_write's local-only effect fits no EffectClass; presented the real-record policy fork to the user, who chose mechanism-only (no self-authored grant), so the one repo with a real PR precedent is now correctly blocked pending a human-filed record (AUTH-007). |
| 2026-07-23 | model-routing llm-gateway dispatcher | #70 | `LLM-020`, `LLM-021`, `LLM-022` | Wave 13.4 | Generalized the hardcoded supervisor_planning model-route check to every capability declaring model_route, wired at the real dispatch_tool_call() chokepoint after finding the 3 real routed capabilities dispatch from their own specialist nodes, not through the general planner; gated the LLM_MODEL global override behind an explicit debug flag; verified and closed the LLM-017/VAL-016 reconciliation task as already-accurate, no mismatch found. |
| 2026-07-23 | golden-set model-routing ci auto-disable | #71 | `OPS-011`, `OPS-012`, `OPS-013` | Wave 13.5 | Built golden_set/auto_disable.py's real threshold-based route-disable mechanism, a new readme-agent golden-set-run CLI command, and a workflow_dispatch-only golden-set-run.yml workflow (schedule deferred, GOVERNANCE rule 10); live-verified against the real gateway (4/5, 0.8 pass rate, correctly stayed enabled, surfaced one real planner weakness); corrected OPS-011 to PARTIAL after finding 2 of its 6 named metrics still uncovered. |
| 2026-07-23 | dependencies sbom ci reproducibility act | #72 | `DEP-001`, `DEP-002`, `DEP-003`, `DEP-004`, `DEP-005`, `DEP-006` | Wave 13.6 | Built SBOM generation (pip-audit, fixing real pip/setuptools CVEs), an explicit direct-dependency fix (langchain-core), and a new dependency-check.yml workflow proving a clean install from the locked dependency set on Python 3.11/3.12/3.13; live-verified via real local act runs against Docker, which surfaced and led to fixing two genuine cross-platform bugs (a non-deterministic os.walk() bound and a CI-env-leaking unit test) exactly matching the wave's own guard against runner-dependent behavior. |
| 2026-07-23 | governance decision-ledger authorization gov-018 gov-023 | #33, #44 | `GOV-018`, `GOV-023`, `GOV-026` | Wave 13.7 | Verified GOV-018/GOV-023's actual text before amending anything and found the sprint plan's mapping wrong: GOV-018 bundles an unrelated live-proof clause with the real push-approval clause, and GOV-023 is about master.md edit authorization, not product-repo writes -- which also meant this session's own wave-closure practice was running on an undocumented standing-directive exception. Surfaced both findings to the user before amending; amended decision #33's push-clause only (superseded by AuthorizationRecordV1) and decision #44 (ratifying the bounded standing-directive exception actually in use), both in place per GOVERNANCE rule 4, plus new GOV-026 naming the mechanism. |
| 2026-07-23 | truth-audit verification incident | — | `VER-009` | Foundation correction | Repeated full-project audit found real heterogeneous runs falsely reporting convergence/exit 0 despite unresolved specialist verification failures; terminal classification now fails closed on specialist `ERROR:` results, with regression coverage and a new audit/handoff bundle. Production-like rerun remains required. |

| 2026-07-23 | wave-0 truth-consolidation semantic-traceability | — | `VER-001`, `ORC-005` | Level-8 Wave 0 | Replaced path-existence-only traceability with semantic test/evidence/status checks and corrected the self-contradictory `VER-001` closure claim from `IMPLEMENTED` to `PARTIAL`; 86 older closure rows remain visible as evidence debt rather than being bulk-downgraded. |

| 2026-07-23 | wave-0 fresh-clone reproduction dependencies verification | — | `DEP-004`, `OPS-010` | Level-8 Wave 0 | Fresh clone from committed state installed from the lock and passed Ruff, format, mypy, 1,174 non-live tests, plan validation, and actionlint; the first attempt exposed and led to fixing source/spec split, CRLF-sensitive contract hashing, and a split log-index commit. |

| 2026-07-23 | level-8 wave-0 authority reroute mission-control | — | `GOV-023` | Level-8 Wave 0 | Isolated the six-section master approval gate as a three-attempt external blocker, split safe Wave 0 work into durable child taskcards, and hardened the supervisor so rerouting unlocks only those children while Wave 1 remains gated. |
| 2026-07-23 | level-8 wave-0 candidate-artifact disposition | — | `GOV-017`, `GOV-023` | Level-8 Wave 0 | Inventoried and preserved all five dirty plan candidates; classified the master edit as valuable but gated, the idea edit as non-structural, and the three untracked files as disallowed redundant/stale/generated candidates. Nothing was staged, overwritten, deleted, or accepted as execution authority. |
| 2026-07-23 | level-8 wave-0 semantic closure | — | `GOV-009`, `RDM-001`, `CORE-001`, `CORE-006`, `OPS-003`, `SAFE-005`, `MEM-002`, `PRL-001`, `AUTH-003` | Level-8 Wave 0 | Consumed all 85 semantic closure findings: 76 closures preserved through requirement-specific proof, nine literal overclaims downgraded to PARTIAL, 224 focused tests passed, and the matrix reached zero high-confidence findings without touching the gated status candidate. |
| 2026-07-23 | level-8 wave-0 master-consolidation governance | #26, #32, #33, #37, #73, #74, #75, #76 | `L8-001`, `L8-002`, `L8-003`, `L8-004`, `L8-005`, `L8-006`, `L8-007`, `L8-008`, `L8-009`, `L8-010`, `L8-011`, `L8-012`, `L8-013` | Level-8 Wave 0 | Reconciled the six approved master sections into one Waves 0-8 sequence, preserved decisions through #72, added decisions #73-76, and added thirteen normative Level-8 production/maturity gates. |
| 2026-07-23 | master requirements wave1 level8 | #73 | `OPS-010`, `CAP-002`, `CAP-008`, `ORC-005`, `VER-001`, `VER-009`, `L8-002` | Wave 1 | Canonical runtime, typed contracts, fail-closed heterogeneous proof, and descendant cleanup are verified; Wave 2 is active. |
| 2026-07-23 | wave2 reconciliation level8 | #73 | `RUN-009`, `RUN-010`, `RUN-011`, `RUN-012`, `RUN-013`, `OPS-015`, `OPS-016`, `OPS-017`, `EVID-005` | Wave 2 | Wave 1 is durably CLOSED at state 48; Wave 2 restartable Actions runtime is claimed at state 49 with no prior-wave overclaim. |
| 2026-07-23 | requirements runtime evidence | #74 | `RUN-002`, `RUN-005`, `RUN-006`, `RUN-007`, `RUN-009`, `RUN-010`, `EVID-004`, `L8-001`, `L8-003`, `L8-004`, `L8-005`, `L8-013` | Level-8 Wave 2 | Built the restartable Actions runtime, fixed recovery/checkpoint/retry/inventory defects, and retained PARTIAL statuses pending hosted GitHub App/dead-man proof. |
| 2026-07-23 | requirements incident | #74 | `RUN-007`, `RUN-009`, `L8-005` | Level-8 Wave 2 | Independent review closed terminal-evidence false-success and actionable-backlog false-green boundaries and narrowed restart evidence to what it proves. |
| 2026-07-23 | requirements incident | #74 | `RUN-009`, `L8-003` | Level-8 Wave 2 | Full-suite stale-SHA failure led to complete status-vocabulary support, check-only modes, and regenerated mission coverage. |
| 2026-07-23 | requirements incident | #74 | `L8-001`, `RUN-002` | Level-8 Wave 2 | First hosted production run passed recovery/matrix, failed closed on absent App credentials, and exposed the deprecated app-id input. |
| 2026-07-23 | external blockers continuation | #44, #74 | `L8-001`, `L8-003`, `L8-004`, `L8-005` | Level-8 Wave 2 | Durable state 53 records Wave 2 as `BLOCKED_EXTERNAL`: the local implementation and independent audit pass, while publication authority, installed-App credentials, dead-man infrastructure, hosted supervisor execution/recovery, and later master closure approval remain external. Safe repository-local Wave 3 preparation continues through the same controller and task graph. |
| 2026-07-23 | level-8 wave3 product-truth foundation | #75 | `OWN-001`, `OWN-002`, `OWN-010`, `OWN-011`, `OWN-014`, `OWN-015`, `FACT-001`–`FACT-007`, `FACT-009`, `FACT-010`, `RDM-025`, `L8-006`, `L8-007` | Level-8 Wave 3 local child | Built and locally proved ProductFactsV2, precedence/conflicts, dependent-surface gating, ownership, protected-content, and secret-free example boundaries; real Cells/Java false-coordinate proposal is blocked while parent Wave 3 remains gated. |
| 2026-07-23 | level-8 wave3 independent-audit repairs | #75 | `FACT-003`, `FACT-005`, `L8-006` | Level-8 Wave 3 local child | Independent review rejected two P0 factuality bypasses and one P2 contract contradiction; selected-fact citation enforcement, non-forgeable metadata fact derivation, negative controls, and module-map truth repairs closed all three before final acceptance. |

## Entries

- [truth-audit verification incident] **2026-07-23 — Repeat full-project truth audit and
  false-convergence correction.** Reconciled the dirty candidate tree, requirements, master,
  roadmap, workflows, registry, source, tests, and July 23 evidence; an independent adversarial
  pass reached the same `NOT PRESENTABLE`, Level-3 conclusion. The heterogeneous portfolio log
  showed .NET, Python, and C++ runs returning `CONVERGED_NO_CHANGE`/exit 0 while their
  `readme_presentation` specialist recorded verification rejection and a blocked candidate.
  Root cause was traced to `supervisor/convergence.py::final_status()` considering only planner
  task-graph failures, not specialist results maintained outside that graph.

  Added `VER-009`; passed the complete specialist result map into all terminal-classifier call
  sites; unresolved `ERROR:` results now produce deterministic `BLOCKED`/
  `specialist_failed:<domain>:<status>` outcomes. Added direct convergence and end-to-end
  specialist-exception regression tests. Created
  `plans/investigations/full-project-truth-audit-2026-07-23.md` and its machine-readable
  `execution-handoff.json`. No product remote was written. The requirement remains `PARTIAL`
  until the previously false-success heterogeneous cases are rerun under production-like
  conditions.

- [requirements governance docs] **2026-07-23 — Doc repair, decision 37 amended in place, OWN-004 scope correction, doc-set split** Wave 9.3 (Authoritative Document Repair, Decision #37 Amendment, Doc-Set Split) of the
convergence-sprint plan, continuing autonomous execution per explicit user direction.

**Doc repair, verified-exact stale passages fixed** (all confirmed against live source before
editing, not assumed): `AGENTS.md:227-228`'s "no mutating capability yet" and its rule 14's
"no-op until the first mutating capability is registered" -- both false today, corrected to name
`commit_readme_write`/`open_presentation_pr` as the real, live-proven capabilities that already
satisfy these gates. `docs/safety-model.md`'s push-blocking section described "this tool never
issues a write verb" and framed `open_presentation_pr` as a future hypothetical ("if a
gated_effector capability is ever built") -- corrected to name the real, already-live-proven
exception (a structurally separate, deliberately-never-neutered PR clone) without weakening the
underlying push-blocking guarantee for the default work clone every other capability shares.

**`GOV-021` fixed and reclassified `IMPLEMENTED`**: `docs/repository-presentation-surface-model.md`'s
Class D section described a full bidirectional ack/reject/rerun state machine, contradicting
decision #37's own finding (no real receiving counterparty exists). Corrected to describe the real
one-way `HandoffFindingV1` record.

**`OWN-004` scope correction** (a fix to an over-extension, not a weakening): `specialists/
package_release_audit.py`'s docstring asserted `OWN-004`/`OWN-013` "forbid any renderer or write
path here" for any release/package-related finding -- but `OWN-004` only forbids touching the
*actual* release/package registry, never README presentation text describing it (`OWN-002`/
`OWN-008` already classify README content as repository-file-managed). Corrected the docstring and
the matching passage in `docs/repository-presentation-surface-model.md` to state this distinction
explicitly -- this is the doctrine fix Wave 12.1's future claim-correction mechanism depends on.

**`LLM-018`/`LLM-019` citation sweep, all 4 named sites fixed, both reclassified `IMPLEMENTED`**:
the stale `gpt-oss` "1/10 valid" figure (real evidence: a 0.4-0.8 swing across reruns) and the
stale "~96k token ceiling" figure (real, live-proven ceiling: ~71,069 tokens) were corrected in
`plans/master.md` (two Decision Ledger passages, decisions #26/#27), `plans/investigations/
full-surface-current-state.md`, `src/readme_agent/env.py`'s `JOB_MODEL_ROUTING` comment, and
`plans/requirements.md`'s own `LLM-016` row.

**Decision #37 amended in place** (`GOVERNANCE.md` rule 4 -- historical reasoning retained
unchanged, conclusion sharpened): repository inspection/recon is now stated as the primary, active
arbiter for any objectively, mechanically checkable claim -- not a passive fallback used only when
a handoff is absent -- precisely because decision #37's own finding means no responsive product-
agent counterparty will ever arrive to adjudicate a conflict instead. Cites the real, concrete
motivating failure: the live PR against `aspose-cells-foss/Aspose.Cells-FOSS-for-Java#1` left a
known-broken Maven coordinate untouched even though this project's own resolver had already
verified it. Product-agent prose stays authoritative only for what recon cannot verify (intent,
roadmap, subjective positioning, licensing choice itself). `DOC-006` stays `RESEARCH-GATED` until
Wave 11 builds it.

**Doc-set split**: `plans/status.md` (new, generated -- `traceability_matrix.py` extended to also
write it: requirement-status counts, Build Checklist wave state, latest decision number,
implementation-truth-matrix summary) and `plans/roadmap.md` (new, hand-maintained, seeds Waves
9.4-15) created. `plans/changelog.md` (new) is a one-line stub pointing at `logs/README.md` --
deliberately not a second changelog, since decision #44 already relocated history there.
`plans/master.md`'s 251-line Status section (pure historical narrative, every paragraph already
duplicated by its own numbered Decision Ledger entry, decisions #37 through #54) replaced with a
short pointer into `plans/status.md`/`plans/roadmap.md`/`logs/`, per `GOVERNANCE.md` rule 1
(master.md holds current state, never history).

Full official-checks suite re-run clean after every change in this phase (ruff/ruff-format/mypy/
pytest/validate_plan_structure/actionlint all green).

- [supervisor runtime requirements] **2026-07-23 — Execution profiles built and enforced (9.4); trigger identity/dedup built and wired (9.5)** Wave 9.4 (Execution Profiles & Single Official Runtime) and 9.5 (Fail-Closed Durable State,
Checkpoints & Trigger Identity/Dedup) of the convergence-sprint plan, continuing autonomous
execution.

**Wave 9.4**: Built `supervisor/execution_profile.py::ExecutionProfileV1` -- 5 typed profiles
(`local_inspect`, `local_dry_run`, `github_observe`, `github_proposal`, `github_apply`), each
declaring required state, allowed permission classes, evidence/verification requirements, valid
triggers, and rollback. Added `--execution-profile` to the `supervise` CLI subparser.
`commands.py::cmd_supervise()` now: (1) rejects `--domain` outright under any `github_*` profile,
before any registry heal/preflight/clone runs -- exit 2, a usage error; (2) forces a real durable-
state backend when a profile requires one, regardless of whether `--durable-state` was also
passed; (3) threads a new `allowed_permission_classes` parameter into
`supervisor/loop.py::supervise_repo()`/`_dispatch_and_record()` (additive, every existing
caller/test unaffected) so a `github_observe` run structurally cannot dispatch a
`local_write`/`remote_write` effect even against a `mode: full` repo -- closing a real gap where
only the `mode` check governed this before. All three GitHub workflows updated to pass an explicit
profile; `readme-agent-run.yml` relabeled compatibility/diagnostic-only in its own name (decision
#46's doctrine, now structural). CLI help text for `run`/`run-registry`/`profile-registry` also
relabeled. 18 new tests across `tests/unit/test_execution_profile.py` and
`tests/unit/test_cli.py::TestExecutionProfileFlag`; the full `test_supervisor_loop.py` suite (49
tests) re-run clean after the permission-threading change to `loop.py`.

**Wave 9.5**: Built `state/schema.py::TriggerRecordV1` (a stable dedup identity: prefers
`manual_request_id` > `workflow_run_id` > `delivery_id` > a schedule-window fallback) and
`state/trigger.py::record_trigger()`/`is_duplicate_trigger()` -- same load->patch-on-fresh-copy->
CAS-save->retry-on-stale shape as `save_domain()`, bounded to 200 records per repo. Wired into
`cmd_supervise()` via new `env.py::github_run_id()`/`github_event_name()` helpers: a re-dispatched
GitHub Actions run with the same `GITHUB_RUN_ID` now short-circuits to a deduplicated result
instead of re-executing `supervise_repo()` a second time. 7 new tests in
`test_state_backend.py::TestTriggerRecording` plus one CLI-level end-to-end test.

Both `RUN-005` and `RUN-006` move `BACKLOG` -> `PARTIAL`, with an honest, specific accounting of
what's actually covered versus what stays open: `RUN-005`'s fail-closed guarantee is confirmed
(not newly built) for state-backend *construction* failures (`StateBackendError` already
propagates as a clean CLI error, never a silent degrade) but NOT for `supervise_repo()`'s own
internal best-effort state-read fallbacks during a run -- that remains separate follow-up work.
`RUN-006`'s trigger-identity/dedup half is real and tested; "recover accepted-but-unfinished work
after runner loss" (a durable, checkpointed intake queue) is a materially larger undertaking, not
built this pass -- named explicitly as new row `RUN-009` (`BACKLOG`) rather than left unstated, per
`GOV-014`. New rows: `RUN-008` (`IMPLEMENTED`, execution profiles), `ORC-007` (`IMPLEMENTED`, the
`--domain` bypass closure). `RUN-002` corrected `PLANNED`->`PARTIAL` (workflow_dispatch/schedule
proven; workflow_call/repository_dispatch type-represented, not exercised).

Full official-checks suite re-run clean after every change in both phases.

- [effect-ledger idempotency capabilities] **2026-07-23 — Wave 9.6: candidate-byte-aware effect identity, fixing the confirmed idempotency-key collision** Wave 9.6 (candidate-byte-aware effect identity) closed, per the autonomous
execution directive. Direct trace confirmed the sprint plan's flagged bug:
`capabilities/effect_ledger.py::idempotency_key()` hashed `org_repo` +
`facts_hash` + `fresh_fingerprint`, where `fresh_fingerprint` is the
pre-render upstream baseline hash (`readme/facts.py::
compute_tracked_content_hash()`, computed before the LLM ever runs) --
never a hash of the rendered candidate/`final_text`. Two different
generated candidates against an unchanged upstream collided on one key,
so the ledger's `already_applied` cache hit silently discarded the second,
newly-verified candidate.

Fixed with a new `capabilities/effect_identity.py::EffectIdentityV1`
(`candidate_byte_hash = sha256(final_text)`, `upstream_surface_hash` =
`fresh_fingerprint`, `product_facts_hash` reuses `facts_hash` wholesale --
`facts_hash` already folds in `policy_content_hash`/`prompt_content_hash`/
`GENERATION_SCHEMA_VERSION`/`VALIDATION_RULESET_VERSION`, so naming those
again would add no discriminating power; a deliberate, source-grounded
narrowing from the plan's original longer field list, logged rather than
silently done). `idempotency_key()` now builds this typed identity and
hashes its canonical JSON whenever `final_text` is declared in a
manifest's `idempotency_inputs` -- fully backward compatible for every
other capability's plain per-field selection (proven by a dedicated test).
`commit_readme_write.py`/`open_presentation_pr.py` both now declare
`final_text` in `idempotency_inputs`.

New `EFF-006` -> IMPLEMENTED, proven by `tests/unit/test_effect_identity.py`
(9 tests) and two new classes in `tests/unit/test_effect_ledger.py`
(`TestCandidateAwareIdempotencyKey`, `TestDispatchGatedEffectCandidateAwareness`)
covering all 6 of the plan's named regression scenarios at both the pure
idempotency_key() and full dispatch_gated_effect() level.

Scope correction found during this same verification pass, logged rather
than silently absorbed into a bigger fix: the ledger fix fully closes the
gap for `commit_readme_write` (the local write always applies `final_text`
directly to the file). For `open_presentation_pr`, the ledger now
correctly attempts a real second `execute()` call for a differing
candidate, but that capability's own pre-push `find_open_pr()` dedup
(`PRL-001`) still keys on `facts_hash[:12]` alone -- it finds the first
candidate's still-open PR and returns `already_open` without ever pushing
the repaired content. This is the same visible outcome as before this fix
(a stale PR persists), just reached by a different code path, so nothing
regresses -- but it is a real, separate, pre-existing gap, now named
explicitly as new `PRL-009` (BACKLOG) rather than silently expanding this
phase's scope to redesign GitHub branch-naming semantics.

Verification: ruff check/format, mypy (154 source files), and the full
`run_official_checks.py` suite (ruff, mypy, pytest -q, validate_plan_structure,
actionlint) re-run clean after all changes. `validate_plan_structure.py`
reports the file structurally clean (35 pre-existing over-length-row
warnings, informational only, unrelated to this change).

Decision Ledger #58 added to `plans/master.md`. `plans/status.md` and
`matrix.json` to be regenerated as part of this entry's own commit-adjacent
housekeeping.

- [convergence freshness supervisor] **2026-07-23 — Wave 9.7: per-surface freshness contracts and the corrected 7-condition NO_CHANGE gate** Wave 9.7 (per-surface freshness contracts, corrected NO_CHANGE convergence)
closed. Closes the "repository-level no-op based only on Git SHA"
prohibited outcome named in the sprint plan.

Direct trace confirmed the live gap: `supervisor/loop.py`'s coarse
pre-specialist-tier shortcuts could return CONVERGED_NO_CHANGE from a
Git-SHA/control-plane/domain-coverage match alone, permanently skipping
the entire specialist tier for as long as upstream stayed unchanged --
including the four domains (github_generated_surface_audit,
package_release_audit, metadata_presentation, visual_preparation) whose
own live queries are the *only* way this project ever observes a package
publish, a topics edit, a new release, or a stale social-preview image.

Built: new `state/schema.py::SurfaceFreshnessContractV1` +
`state/freshness_contract.py` (DEFAULT_SURFACE_CONTRACTS for exactly those
4 non-git-tracked domains; is_due_for_recheck()/any_surface_due_for_recheck()/
refresh_surface_contracts()); new SupervisorStateV1.surface_freshness field
(additive, safe default). New `supervisor/convergence.py::
no_change_gate_holds()` composes the original is_fresh() (Git revision /
control-plane fingerprint / domain coverage) with 4 new conditions:
has_pending_effect(), has_open_proposal_needing_reconciliation(),
has_unfinished_trigger(), and the surface-freshness check -- replacing the
bare is_fresh() call at both of loop.py's coarse-shortcut call sites (the
pre-clone probe and the post-clone check). loop.py now loads the full
prior RunStateV1 (new _load_prior_run_state()) and refreshes
surface_freshness from real specialist_results at both write sites where
the tier actually ran; the pre-clone probe's own write (which never runs
the tier) carries the prior value forward unchanged.

Scope correction, verified not assumed: the sprint plan named 11 surfaces
(README, community files, description, homepage, topics, releases,
packages, GitHub-generated audits, visuals, social preview, open
proposals). Direct trace found README/community files already covered by
the existing Git-revision comparison, and open proposals better modeled as
a boolean condition than a TTL contract (nothing to periodically recheck
until PRL-002 populates open_proposals for real, which it does not yet --
still PARTIAL). Narrowed to the 4 genuinely-missing surfaces plus 3
non-TTL conditions, logged as new FRESH-001 through FRESH-006 rather than
a partial build of all 11 originally-named surfaces.

New VER-007 -> IMPLEMENTED for the composed 7-condition gate itself.

GOV-024's own Wave 9.7 contingency confirmed, no new code needed:
compute_control_plane_fingerprint()'s docstring already states it hashes
prompt_registry.content_hash() covering the supervisor's own
supervisor_turn prompt, and Wave 9.3 already reclassified GOV-024 to
IMPLEMENTED on this exact basis -- re-verified true here, nothing left
open.

Verification: new tests/unit/test_freshness_contract.py (24 tests) and new
classes in tests/unit/test_convergence.py, including
TestNoChangeGateHolds's 6 named negative scenarios (package publish /
description change / topics change / new release / merged PR / stale
social preview -- none may shortcut to NO_CHANGE) plus positive/backward-
compat cases. Full run_official_checks.py suite re-run clean (1019
passed, 18 deselected), including the complete test_supervisor_loop.py
(49 tests) and test_convergence.py (36 tests) with zero regressions.

Decision Ledger #59 added to plans/master.md.

- [profile ecosystems repository-intelligence] **2026-07-23 — Wave 11.1: multi-root package detection closes the one-manifest-per-ecosystem monorepo gap** Wave 11.1 (Repository Profile Graph -- multi-root package detection)
closed. This is the first Wave 11 phase, unblocked by Wave 9's full
closure (decisions #55-#59) lifting decision #46's AUD-001-007 freeze.

Direct trace confirmed a real, already-self-documented gap:
`inspection/file_inventory.py::resolve_manifest_candidates()`'s own
docstring already named "one manifest path per ecosystem" as the extent
of this project's monorepo support -- `remaining.discard((ecosystem,
pattern))` stops looking for that ecosystem the moment one match is
found. A multi-module Maven/Gradle tree, a multi-.csproj .NET solution,
or an npm/Yarn workspace with more than one manifest of the same
ecosystem had every match after the first silently discarded.

Fixed, additively: new `find_all_manifest_roots()`
(inspection/file_inventory.py, filesystem walk) and
`find_all_manifest_roots_from_tree()` (inspection/tree_paths.py,
git-tree-API) -- both new, separate functions sharing their sibling's
exact traversal/exclusion rules (NOISE_DIRS, the same bounded walk),
never touching `resolve_manifest_candidates()`/
`find_manifest_paths_from_tree()`, which stay "first match wins" for
every existing caller. New `profile/schema.py::PackageRoot`
(path/ecosystem/manifest_path/confidence/evidence) + additive
`RepositoryProfile.package_roots`, populated by both
`profile/detector.py::build_profile()` (clone-based) and
`profile/cached.py::_build_profile_via_api()` (git-tree-API-based) --
full parity between both profiling paths, not just one.

Scope correction, verified not assumed: the sprint plan's original ask
named a richer per-root schema (languages/build-systems/package-managers/
registries/test-systems/examples/docs/release-mechanism) plus 8 checked-in
fixture archetypes and `SharedSurface`/`UnresolvedFinding` records.
Narrowed to the detection layer alone (does a root exist, where, which
ecosystem) -- per-root fact-parsing would add N real GitHub-API-fetch
costs per repo on the tree-API path, a cost this phase did not sign up to
add unmeasured. Synthetic tmp_path-constructed test scenarios used instead
of checked-in fixture directories, matching this project's own existing
test-authoring convention rather than the 8 elaborate archetypes
literally. New ECO-004 -> IMPLEMENTED; new ECO-005 (BACKLOG) names the
deferred richer schema honestly.

Verification: 20 new tests across test_inspection.py (7),
test_tree_paths.py (7), test_profile.py (4 + 1 schema test),
test_profile_cached.py (2 new/extended) -- multi-.csproj, multi-module
Maven at different depths, mixed-ecosystem roots, single-root regression,
and noise-directory exclusion all covered. A grep confirms zero new
org-repo-specific branches introduced anywhere (Wave 11.1 did not touch
supervisor/loop.py). Full run_official_checks.py suite re-run clean
(1039 passed, 18 deselected).

Decision Ledger #60 added to plans/master.md.

- [ecosystems resolver capabilities package-verification] **2026-07-23 — Wave 11.2: multi-registry acquisition verification -- 5 new live resolvers + verify_package_acquisition** Wave 11.2 (Multi-Registry Acquisition Verification) closed. Five more live
resolvers added to ecosystems/resolver.py, all live-verified over the real
network before any code was written (matching this project's own
live-proof-over-assumption discipline, GOVERNANCE.md rule 10), plus a new
per-package-root capability.

ecosystems/resolver.py gained: PyPI (python), npm (typescript), NuGet
(net), the Go module proxy (go), and Conan Center/vcpkg (registered under
"cpp_conan"/"cpp_vcpkg", not a direct "cpp" key). Every URL/status-code
shape was live-checked 2026-07-23 against a real known-good package and a
deliberately nonexistent name:
  - PyPI/npm/NuGet: straightforward 200/404, exactly as expected.
  - Go proxy: required live-discovering its "!"-escaping convention for
    uppercase letters in module paths -- an unescaped mixed-case module
    (github.com/PuerkitoBio/goquery) 404s; the escaped form
    (github.com/!puerkito!bio/goquery) resolves. Found live, not assumed
    from the golang.org/x/mod/module docs alone.
  - Conan Center and vcpkg: neither has a package-existence REST API at
    all (both are community-curated, git-hosted recipe indices, not
    centralized binary hosts). The real, working check turned out to be
    the recipe/port path itself as a raw GitHub file fetch
    (raw.githubusercontent.com/conan-io/conan-center-index/.../recipes/
    zlib/config.yml -> 200; .../microsoft/vcpkg/.../ports/zlib/vcpkg.json
    -> 200; made-up names -> 404 for both) -- discovered live after two
    guessed API endpoints both 404'd.

cpp deliberately has no direct "cpp" resolver key: two independent,
unrelated registries with no manifest signal indicating which a repo
actually uses would make a direct key an ECO-003-violating guess.

orchestrator.inspect_repo()'s existing generic resolve(ecosystem,
manifest) dispatch (already ecosystem-agnostic, unchanged) automatically
gained python/typescript/net/go coverage the moment these were
registered -- zero wiring changes needed there. check_install_path.py's
manifest metadata (supported_build_systems/package_managers/registries)
updated to reflect the real, broader coverage.

New capabilities/verify_package_acquisition.py (PKG-005): reports one
outcome per Wave 11.1 PackageRoot (REGISTRY_VERIFIED/NOT_PUBLISHED/
CAPABILITY_GAP/BLOCKED_NETWORK/NOT_APPLICABLE), not one repo-wide boolean
the way check_install_path.py's install_path_resolved is -- a multi-root
repository's second module's broken coordinate is never hidden behind the
first module's correct one. A cpp root always reports CAPABILITY_GAP,
never a guessed registry. New ResolutionResult.blocked: bool field
(additive, default False) distinguishes a genuine "not found" from a real
network failure.

Scope correction, verified not assumed: PKG-006 (BACKLOG) names
INSTALL_VERIFIED/SOURCE_BUILD_VERIFIED as deliberately not built -- those
would require actually invoking a package manager (pip install/
npm install/nuget restore/a real C/C++ build) against arbitrary resolved
coordinates, a materially larger and different risk (real command
execution against untrusted input) than every other outcome's read-only
HTTP GET. Not expanded into PKG-005 unmeasured.

Verification: new scripts/retrofits/prove_multi_registry_resolvers_live.py
(kept as the executable record, all 12 live checks passing against the
real network); 12 new tests in test_verify_package_acquisition.py; new
resolver tests in test_ecosystem_resolver.py including the blocked-field
tests; 6 existing test_capabilities.py assertions updated for the 23rd
registered capability (hardcoded counts/sets). Full run_official_checks.py
suite re-run clean (1068 passed, 18 deselected).

Decision Ledger #61 added to plans/master.md.

- [facts product-facts capabilities] **2026-07-23 — Wave 11.3: typed ProductFactsV1, a facts diff mechanism, and package_roots exposed via get_product_facts** Wave 11.3 (ProductFactsV1/ProductChangeSetV1, corrective conflict-mode
facts ingestion) closed.

New facts/schema.py::ProductFactsV1/PackageCoordinateFactV1 (FACT-011): a
typed, honestly-scoped product-facts record covering exactly what this
project can genuinely derive today (identity, license, package
coordinates, relationship talking points, secondary links) -- not the
full decision #22 field list (audience/capabilities/formats/verified-
example/docs/release-info/limitations all remain unbuilt, FACT-002 stays
correctly PLANNED, unchanged). ProductFactsV1.from_capability_results()
combines get_product_facts.py's own dict output with an optional
verify_package_acquisition.py result, merged by package-root path --
deliberately does not call either capability itself (decision #26(b)'s
"one capability never reaches into another's dispatch path") and does not
change either capability's declared side_effect_class (get_product_facts
stays read_only_local, zero new network calls added).

get_product_facts.py additively gained a package_roots output key: Wave
11.1's multi-root package graph was already computed inside its own
profile object but never surfaced there until now (zero new cost, same
already-fetched profile).

New facts/changeset.py::ProductChangeSetV1/diff_product_facts() (FACT-012):
a pure diff between two ProductFactsV1 snapshots, catching the concrete,
load-bearing case of a package-root verification outcome flipping (e.g.
REGISTRY_VERIFIED -> NOT_PUBLISHED, a real yanked/removed package). Feeds
FACT-010's own "surgical diff evidence" acceptance criterion -- that row's
text updated to cite it; its status stays PLANNED since the dependency-map
half (which presentation section depends on which fact) and any actual
triggering wiring are still Wave 12.1's job.

Scope correction, verified not assumed: new FACT-013 (BACKLOG) names the
sprint plan's original 3-mode ingestion ask (handoff+repo-verification /
repo-only reconstruction / conflict-mode against a handoff source) as not
built beyond repo-only reconstruction. Decision #37's own amendment
(Wave 9.3) already established no real, responsive external
handoff-receiving system exists to integrate with -- building ingestion
code paths for an unreachable input source would be untested, unreachable
code, not a real capability. The concrete corrective mechanism decision
#37 actually calls for -- recon evidence as the primary arbiter for
checkable claims -- is exactly what FACT-011 (typed facts) and PKG-005
(live acquisition verification) together provide as inputs to Wave 12.1's
future semantic-reconciliation correction pass.

Verification: 16 new tests (test_facts_schema.py: 9, test_facts_changeset.py:
7) plus one extension to test_capabilities.py::TestGetProductFactsCapability.
Full run_official_checks.py suite re-run clean (1081 passed, 18 deselected).

Decision Ledger #62 added to plans/master.md.

- [capabilities schema dispatcher] **2026-07-23 — Wave 11.4: input_model/output_model typing + dispatcher structural validation -- Wave 11 fully closed** Wave 11.4 (Capability contract typing & dispatcher hardening) closed --
this is the final Wave 11 phase, so Wave 11 (11.1-11.4) is now fully
closed.

New capabilities/schema.py::CapabilityManifest.input_model/output_model:
type[BaseModel] | None (additive, defaults None -- every pre-existing
capability completely unaffected). to_tool_schema() prefers input_model.
model_json_schema() when declared, stripping the model's own
docstring-derived "description" key first (would otherwise leak an
internal-implementation-detail explanation into the LLM-facing schema --
function.description from purpose stays the one authoritative
description).

New dispatcher.py::dispatch_tool_call() check: when a manifest declares
input_model, model_validate(arguments) runs before the executor,
converting a Pydantic ValidationError into a clear rejected_invalid_
arguments outcome instead of an opaque execution_error surfacing deep
inside require_listed()/clone_baseline().

New OrgRepoRef: a real, reusable Pydantic-validated str type requiring the
"org/repo" shape (must contain exactly one slash, non-empty segments).
Shared OrgRepoOnlyInputV1 input model built on top of it, retrofitted onto
exactly this session's own three org_repo-only capabilities
(get_product_facts, profile_repository, verify_package_acquisition) -- not
all 22+ registered capabilities, per the sprint plan's own explicit "no
breaking rewrite of all at once" instruction.

Scope correction, verified not assumed: while scoping the plan's "reject
incompatible profiles/stale state refs" ask, found a real, concrete design
blocker, not just deferred for effort. CapabilityManifest's
supported_build_systems/supported_package_managers/supported_registries
fields (populated so far only by check_install_path.py/
verify_package_acquisition.py, Wave 11.2) use an invented registry-name
vocabulary ("maven_central", "pypi") with no declared mapping to
RepositoryProfile's own ecosystem-key vocabulary ("java", "python") --
building a profile-compatibility filter now would need a third
translation table or first reconciling the two vocabularies, neither of
which this phase signed up to do unmeasured. "Stale state ref" has a
similar unresolved design question: which of RunStateV1's several
revision fields (state_version, upstream_revision_at_accept,
last_observed_upstream_revision) a dispatch-time staleness check should
key off depends on calling context, not yet settled. New CAP-009
(BACKLOG) names both blockers explicitly rather than hacking around them.

New CAP-008 -> IMPLEMENTED.

Verification: new tests across test_capabilities.py (TestOrgRepoRef +
new TestCapabilityManifestSchema cases) and
test_capability_dispatcher.py::TestDispatchInputModelValidation. Full
run_official_checks.py suite plus test_specialists.py/test_orchestrator.py
re-run clean (1093 passed, 18 deselected), zero regressions.

Decision Ledger #63 added to plans/master.md. Wave 11 (11.1-11.4) is now
fully closed.

- [readme claim-verification reconciliation] **2026-07-23 — Wave 12.1: real-evidence claim-conflict detector against the actual cells/java PR regression, deliberately narrowed from a full renderer rewrite** Wave 12.1 (Semantic README reconciliation structural model & claim
verification) closed -- deliberately narrowed after a deep investigation
found the full sprint-plan ask (a complete SectionModel/recompile
rewrite of the renderer, 8 golden-set fixtures) carries real, unresolved
architectural risk that a single pass should not rush.

Direct investigation confirmed the sprint plan's own diagnosis of
readme/renderer.py::render_missing_elements() exactly: a pure function of
a 4-boolean GapReport that never receives the rest of the README and can
only splice inside one owned span. Live-refetched the real
aspose-cells-foss/Aspose.Cells-FOSS-for-Java README (2026-07-23) and
confirmed it still carries the identical, unresolving
<groupId>org.aspose</groupId><artifactId>aspose-cells-foss</artifactId>
Maven coordinate the original PR shipped untouched.

Built: new readme/claim_verification.py::find_claim_conflicts() --
cross-checks README install-command text (Maven/pip/npm/NuGet shapes)
against Wave 11.3's ProductFactsV1.package_coordinates, producing a
ClaimConflictFinding only for a positively-confirmed NOT_PUBLISHED
outcome, never for CAPABILITY_GAP/BLOCKED_NETWORK (couldn't check) or
REGISTRY_VERIFIED. The named regression proof uses the REAL, live-fetched
README text (not a synthetic fixture) -- confirms a finding is correctly
produced for the exact real, currently-broken coordinate. New RDM-024 ->
IMPLEMENTED.

Scope correction, verified not assumed -- two real blockers found while
scoping the full rewrite, not a deferral for effort:
1. readme/facts.py::compute_facts_hash() deliberately excludes README
   content and gap_report (decision #11) -- a claim conflict depends on
   live network state with zero local-file/facts_hash correlate, exactly
   the blind spot Wave 9.7 solved at the supervisor level
   (state/freshness_contract.py) but with no render-level equivalent yet.
   Wiring this into orchestrator.prepare_readme_candidate() today would
   either force a live network call on every offline render (a real,
   unmeasured architecture change) or silently miss conflicts on the skip
   path.
2. prepare_readme_candidate() is one of this project's most heavily
   load-bearing functions (idempotency/durable-skip/span-stripped
   re-detection/hash exclusions all precisely balanced, confirmed by
   direct reading, not assumed) -- correctly integrating a new signal is
   real, separate design work.

New RDM-025 (BACKLOG) names this explicitly, plus the further question of
what corrected text should actually say when a coordinate doesn't resolve
(needs product-aware judgment, not automated here). The full SectionModel/
8-golden-fixture recompile rewrite the sprint plan originally asked for is
NOT built this pass -- a deliberate, evidence-grounded narrowing, not a
silent gap.

Verification: 12 new tests (test_claim_verification.py), including the
real-regression case against the live-fetched README, multi-root
isolation, and per-ecosystem (java/python/typescript/net) coverage. Full
run_official_checks.py suite re-run clean (1105 passed, 18 deselected).

Decision Ledger #64 added to plans/master.md.

- [supervisor specialist-selection cli] **2026-07-23 — Wave 12.2: --enable-dynamic-planning wires real specialist-selection/repair clients into the shipped CLI for the first time** Wave 12.2 (real task-graph planning & dynamic specialist selection)
closed.

Direct verification confirmed the sprint plan's own "verified correction
to scope" exactly: supervise_repo() has accepted specialist_selection_
client/repair_planner_client/enable_specialist_skip since Wave 8.6, but
commands.py::cmd_supervise() never constructed real client instances or
passed them through -- meaning the fully-built, unit-tested dynamic
specialist-skip and repair-alternative-selection mechanisms had zero
effect in any shipped CLI/GitHub-Actions run. This is the literal
"specialist selection available only to Python tests" guard confirmed as
the actual current state, not a hypothetical.

Fixed: new --enable-dynamic-planning CLI flag (never a default, matching
--durable-state's own convention) constructs real LivePlannerClient
instances job-routed via env.llm_model_for_job("specialist_selection")/
("repair_capability_selection") -- both already-live-tested routing
entries (LLM-016) that simply had no caller before this. ORC-003's own
row updated with this evidence (status unchanged, PARTIAL -- the flag is
unit-proven, not yet exercised in a real live pilot run, named honestly).

AGT-008 reclassified BACKLOG -> PARTIAL: direct source verification found
2 of its 3 acceptance criteria already built (get_domain_findings
capability registered; DOSSIER_TOKEN_BUDGET already gates on real
usage.prompt_tokens, not a hardcoded constant or client-side tokenizer
estimate) -- only the third (a required, bounded planner_summary per
specialist) is genuinely still missing, named precisely rather than the
whole row staying incorrectly BACKLOG.

Scope correction, verified not assumed: new ORC-008 (BACKLOG) confirms
Task.depends_on/TaskGraph.validate_acyclic() already exist (the sprint
plan's claim about these was accurate) but declines to add the 7 further
Task fields (expected_outputs/required_evidence/applicable_requirement_ids/
affected_surfaces/priority/parallelizable/stop_conditions) this pass -- no
planner-prompt-building code anywhere reads or acts on them yet, so adding
them now would be exactly the unused-field/half-finished-implementation
pattern this project's own conventions warn against.

Verification: 4 new tests (test_cli.py::TestEnableDynamicPlanningFlag) --
flag defaults, real client construction with distinct job-routed models,
and the regression proof that omitting the flag changes nothing. Full
run_official_checks.py suite re-run clean (1109 passed, 18 deselected).

Decision Ledger #65 added to plans/master.md.

- [verification specialists truth-audit] **2026-07-23 — Wave 12.3: verification-ordering claim confirmed already true; real gap (verifier findings produce no corrective action) named honestly -- Wave 12 fully closed** Wave 12.3 (verification reordering & final-acceptance gate) closed with
zero code changes -- a deep investigation found the "verification
reordering" half of the sprint plan's ask already true in production, and
precisely named the real remaining gap instead of building a shallow
wiring fix for it. Wave 12 (12.1-12.3) is now fully closed.

Direct source verification confirmed specialists/independent_verification.py
already runs strictly after every other specialist domain within the same
pass: specialists/registry.py::_SPECIALISTS registers it last, DEPENDS_ON
covers all 9 sibling domains, and _build()'s own import-time gate (already
tested via test_dependency_registered_earlier_does_not_raise/
test_dependency_not_yet_registered_raises/
test_real_registry_satisfies_its_own_declared_dependencies) would fail
loudly at import time if this ever regressed. Since the specialist tier
dispatches domains sequentially, with each domain's own record node
writing through to the backend before the next domain starts,
independent_verification.run()'s own backend.load(org_repo) at its own
turn genuinely observes every sibling's SAME-run state, not stale
prior-run data -- confirmed by direct reading of independent_
verification.py's own _classify_node(). This is NOT the gap the sprint
plan's text assumed.

The real, deeper gap, found by continuing the investigation rather than
stopping at "ordering is fine": details["adversarial_findings"]/
details["completeness"] are recorded as evidence but nothing turns them
into an automatic corrective action, because supervisor/repair.py::
create_repair_task()'s existing shape (retry the same capability, same
arguments) does not fit a cross-domain finding at all -- there is no
single capability to retry when the finding is "readme_presentation
committed a change despite an unresolved cross_surface_validation
inconsistency." Closing this for real needs either a new capability
shaped to address a cross-domain finding, or a preventive gate inside
readme_presentation's own graph that checks for a pending unresolved
sibling finding before committing -- genuinely new design work, not a
two-function wiring fix.

New VER-008 (PARTIAL) documents both halves precisely: the ordering claim
verified true (with citations to already-existing tests, no new tests
needed), and the corrective-action gap named honestly as still open.

No source code changed this phase. The value delivered is a corrected,
evidence-grounded understanding of what's actually built versus what the
sprint plan assumed -- exactly the kind of finding this project's own
truth-closure discipline exists to produce, and exactly the discipline
that prevented a shallow, cosmetic "wiring fix" that wouldn't have
actually closed the real gap.

Full run_official_checks.py suite re-run clean (1109 passed, 18
deselected, unchanged from Wave 12.2 since no code was touched).

Decision Ledger #66 added to plans/master.md. Wave 12 (12.1-12.3) is now
fully closed.

- [evidence supervisor manifest] **2026-07-23 — Wave 13.1: RunManifestV2 unifies supervise_repo()'s evidence manifest, closing a flagged duplication** Wave 13.1 (evidence unification, RunManifestV2) closed.

New evidence/manifest_v2.py::RunManifestV2 (pydantic) + evidence/writer.py::
write_run_manifest_v2() replace supervisor/loop.py::_write_supervise_
evidence()'s own ad hoc manifest.json dict literal -- closing a
duplication that function's own docstring already flagged ("duplicates
evidence/writer.py's .tmp + os.replace pattern rather than reusing its
private _atomic_write_json"). Reuses the writer's existing redaction/
atomic-write/checksum helpers -- no parallel writer.

Wired into all 4 of supervise_repo()'s evidence-writing call sites
(pre-clone probe, clone-failure, CONVERGED_NO_TRACKED_CHANGE shortcut,
full planner-loop completion). Each populates control_plane_fingerprint/
upstream_revision/domain_coverage_complete/surface_freshness from whatever
it genuinely has locally available -- domain_coverage_complete=True set
only at the one call site where it's directly, locally derivable from the
same condition that reached that branch (the NO_TRACKED_CHANGE shortcut,
where every domain's own NO_CHANGE status was just confirmed), None
elsewhere rather than an inferred guess across a helper-function boundary.

New requirement_ids_exercised: dict[str, bool] reuses independent_
verification's own already-built requirement_map (Wave 8c) rather than
recomputing it -- the first real (if partial) closure of GOV-012's
"evidence records requirement IDs" ask. SAFE-014's requirement-ID half is
closed the same way; its decision/phase-traceability half stays open,
named honestly. SAFE-017 stays PLANNED, not silently upgraded: the new
field doesn't correlate a specific validation failure to its requirement
ID, only exercised-without-error at the whole-run level.

New EVID-001 through EVID-003 -> IMPLEMENTED/PARTIAL; EVID-004 (BACKLOG)
names the two still-unpopulated fields (authorization_record_id awaiting
Wave 13.2/13.3's authorization mechanism; trigger_dedup_key awaiting
commands.py threading TriggerRecordV1 through to supervise_repo()) with
the schema already declaring them -- matching this project's own
"declare the field, populate it honestly later" convention rather than a
speculative unused field.

Verification: 8 new tests (test_manifest_v2.py: 2, test_evidence_writer.py::
TestWriteRunManifestV2: 4, test_supervisor_loop.py::TestRunManifestV2Evidence:
2). Full test_supervisor_loop.py suite (51 tests) and full
run_official_checks.py suite re-run clean (1116 passed, 18 deselected).

Decision Ledger #67 added to plans/master.md.

- [authorization effect-class cli] **2026-07-23 — Wave 13.2: effect-class taxonomy and authorization-record schema** Wave 13.2 closed: effect-class taxonomy and authorization-record schema/loader.

Built the additive authorization layer this project's Execution Readiness model needs before Wave 13.7
can evolve GOV-018/GOV-023 for real:

- `authorization/schema.py`: `EffectClass` (8 named classes: STATE_REF_WRITE, EVIDENCE_WRITE,
  PR_BRANCH_PUSH, PR_CREATE_OR_UPDATE, DEFAULT_BRANCH_WRITE, REPOSITORY_SETTINGS_WRITE,
  PACKAGE_PUBLICATION, MANUAL_UI_PREPARATION) -- additive alongside, never a replacement for,
  `capabilities/schema.py::PermissionClass`'s existing `side_effect_class`. `AuthorizationRecordV1`
  reuses Wave 11.4's `OrgRepoRef` shape validation for its `repository` field rather than a second
  ad hoc check.
- `authorization/registry.py`: `load_authorization_record()`/`authorized_for()`, mirroring
  `registry/loader.py::load_policy()`'s fail-closed shape, plus one addition beyond that precedent --
  a filename/`repository`-field cross-check catching a real copy/paste hazard (a record authored for
  one repo filed under another's filename).
- Config lives at `config/authorization/<org>__<repo>.yml`, reusing `paths.py`'s own established
  `f"{org}__{repo}"` naming convention, sibling to (never merged with) `config/policies/*.yml`. The
  directory is created empty -- no real record filed for any repo yet, deliberately: granting real
  authority is a human-authored, asynchronously-reviewed act per the Execution Readiness model, not
  something this phase unilaterally creates.
- New `readme-agent authorization-validate --repo <org/repo>` CLI command -- purely diagnostic, never
  itself grants authority. Live-run against all 3 real pilots (aspose-cells-foss/Aspose.Cells-FOSS-for-Java,
  aspose-3d-foss, aspose-pdf-foss), each correctly and honestly reporting "no authorization record filed."

New/updated requirements: AUTH-001 through AUTH-006, all IMPLEMENTED for their stated scope. AUTH-004 is
IMPLEMENTED only for the loader mechanism itself -- the row's own text explicitly defers wiring
`authorized_for()` into real capability dispatch (open_presentation_pr.py/commit_readme_write.py/
effect_ledger.py) to Wave 13.3, not silently claimed done here.

20 new tests: `test_authorization_registry.py` (15: load/no-file/malformed-yaml/malformed-schema/
mismatched-repository-field, authorized_for coverage/expiration/explicit-now, schema validation via
pydantic.ValidationError not bare Exception), `test_cli.py::TestAuthorizationValidateCommand` (5: flag
parsing, missing flag, no-record honest report, real-record coverage report, malformed-record exit 2).

Full official-checks suite re-run clean: 1136 passed (1116 from Wave 13.1's close + 20 new), ruff
check/format, mypy, validate_plan_structure.py, actionlint all OK.

Decision Ledger: #68.

- [authorization effect-class enforcement cli] **2026-07-23 — Wave 13.3: authorization enforcement cutover** Wave 13.3 closed: authorization enforcement cutover, plus a real user-decided policy fork.

Before writing any code, re-verified this wave's own load-bearing premise directly against source
(`open_presentation_pr.py`, `commit_readme_write.py`, `effect_ledger.py::dispatch_gated_effect()`)
rather than trusting the sprint plan's literal text. Found the plan's assumption that both write
capabilities need the same authorization gate does not survive that check: none of
`authorization.schema.EffectClass`'s 8 values describes `commit_readme_write`'s actual effect (a
commit into a disposable local clone, never pushed -- "no remote effect exists to roll back" per its
own long-standing docstring). Forcing a fit onto an existing value, or inventing a new one, would be
taxonomy scope creep for zero external blast radius. Scope narrowed accordingly:

- Only `open_presentation_pr` (the one real `remote_write` capability) is wired, declaring
  `effect_classes=["PR_BRANCH_PUSH", "PR_CREATE_OR_UPDATE"]`.
- `commit_readme_write` deliberately declares no `effect_classes` -- documented in its own docstring,
  not silently omitted.

Mechanism built:
- New additive `CapabilityManifest.effect_classes: list[str]` field (empty-by-default, mirrors
  `allowed_domains`'s own "empty = unscoped" convention). Kept as bare `str`, not the `EffectClass`
  Literal type, to avoid a `capabilities/schema.py` <-> `authorization/schema.py` import cycle
  (authorization/schema.py already imports `OrgRepoRef` from capabilities/schema.py). Membership is
  validated once, at capability-registration time, by `registry.py::_build()` -- the same pattern
  already established for `allowed_domains`.
- `effect_ledger.py::dispatch_gated_effect()` now checks `authorization.registry.authorized_for()` for
  every declared effect class -- ahead of `precheck()`, before the lock is acquired or any pending
  entry is written -- returning a new `blocked_pending_authorization` outcome. A no-op for every
  capability that doesn't declare `effect_classes` (everything except `open_presentation_pr` today).
  Checked in addition to `mode != 'full'`, not instead of, per the plan's own instruction.
- `authorization_dir` is passed explicitly at the call site (re-reading the module attribute at call
  time), avoiding the same bound-default-parameter hazard already found and fixed once in
  `commands.py::cmd_authorization_validate` during Wave 13.2.

The real policy fork: the cutover's own "no regression against the real PR precedent" verify condition
needs an actual `AuthorizationRecordV1` filed for `aspose-cells-foss/Aspose.Cells-FOSS-for-Java`. Self-
authoring one under this same agent's commit authority would collapse the intended two-layer design
(mode + authorization) back into one layer the agent alone controls -- exactly the "infers authorization
from mode == 'full' alone" failure AUTH-004 exists to close. This was presented to the user directly as
a genuine fork rather than assumed or silently resolved either way. The user chose: build the mechanism
only, no real record.

Concrete, correct-not-regressive consequence: `open_presentation_pr` is now blocked
(`blocked_pending_authorization`) for every repo, including the one with a real merged-PR precedent,
until a human files a real record. Logged honestly as new BACKLOG row AUTH-007, not silently left
implied by AUTH-004's own IMPLEMENTED status.

Requirements: AUTH-004 -> IMPLEMENTED (full wiring: loader + enforcement, not just the loader). New
AUTH-007 -> BACKLOG (the deferred real-record/no-regression proof).

11 new tests: `test_capabilities.py` (5: 1 schema-default for `effect_classes`, plus
`TestRegistryEffectClassEnforcement`'s 4 -- unknown effect class rejected at build time, known effect
class accepted, the real `open_presentation_pr`/`commit_readme_write` capabilities' declared
effect_classes verified directly), `test_effect_ledger.py::TestDispatchGatedEffectAuthorization` (6: no
record blocks before any pending write, a covering record allows dispatch, a record missing one of
several declared effect classes still blocks, an expired record still blocks, a capability declaring no
effect_classes is unaffected, and a live negative-control run against the real `open_presentation_pr`
capability + the real `aspose-cells-foss/Aspose.Cells-FOSS-for-Java` org_repo + the real, genuinely-empty
`config/authorization/` directory, with zero monkeypatching -- the block fires before any clone/push is
attempted, no network I/O beyond one local file-existence check).

Full official-checks suite re-run clean.

Decision Ledger: #69.

- [model-routing llm-gateway dispatcher] **2026-07-23 — Wave 13.4: model-routing governance wiring** Wave 13.4 closed: model-routing governance wiring.

Generalized the one hardcoded supervisor_planning model-route check (Wave 8.6) to every real
capability that declares model_route.

Live-verification-before-code found the real call graph more scattered than the sprint plan's brief
text implied: the 3 real model_route-declaring capabilities (verify_prose_quality,
compare_against_presentation_standard, review_visual_asset_accuracy) each dispatch via a plain
dispatch_tool_call() call made directly inside their own specialist LangGraph node
(readme_presentation.py::_verify_node, presentation_benchmarking.py::_classify_node,
visual_preparation.py::_review_node) -- never through supervisor/loop.py::_dispatch_and_record(),
which is only the general planner's own dispatch path. The gate therefore had to live in
capabilities/dispatcher.py::dispatch_tool_call() itself, the one real chokepoint every dispatch of
these capabilities actually crosses.

Mechanism built:
- New optional `state_backend: StateBackend | None = None` keyword param on dispatch_tool_call() --
  a no-op unless BOTH manifest.model_route is declared AND a live backend is supplied, so every
  pre-existing caller/test is unaffected by default.
- Checks state_backend.load_model_route_status(manifest.model_route); a durably-recorded "disabled"
  status now rejects with a new, distinct `rejected_model_route_disabled` outcome, before the executor
  ever runs.
- Threaded through all 3 real specialist call sites (each now reads `backend` from
  config["configurable"], the same established pattern their own _record_nodes already use).
- Also threaded through effect_ledger.py::dispatch_gated_effect()'s two internal dispatch_tool_call()
  calls and _dispatch_and_record()'s own general-planner dispatch path, for completeness -- a no-op
  today (no mutating capability declares model_route), but future-proofed rather than left a silent
  gap for whenever one does.

Separately: env.py::llm_model_for_job()'s global LLM_MODEL override now requires an explicit,
additional READMEAGENT_DEBUG_MODEL_OVERRIDE=1 opt-in. Previously a bare LLM_MODEL env var silently
won over the evidence-selected per-job routing table unconditionally -- which would have silently
masked exactly the kind of disabled-route enforcement this same wave just built (a stale/forgotten
LLM_MODEL value could substitute a different model for a job a golden-set run had just disabled for
cause).

Entry-gate reconciliation task (per the sprint plan's own instruction): checked the claimed
LLM-017/VAL-016 status mismatch ("code exists, apparently never run against real data") before
extending anything. Direct verification found the premise already stale -- LLM-017 is already
accurately BACKLOG, VAL-016 already accurately PLANNED. No mismatch existed to log. Recorded honestly
as new LLM-022 (BACKLOG) rather than silently absorbed as a no-op step -- the real embedding-wiring
gap LLM-017 describes remains open and is unrelated to this wave's own per-job-routing-governance
scope.

Requirements: LLM-020 (dispatcher-level model-route gate) -> IMPLEMENTED. LLM-021 (debug-flag-gated
override) -> IMPLEMENTED. New LLM-022 (LLM-017/VAL-016 reconciliation, found already accurate) ->
BACKLOG.

11 new tests: test_capability_dispatcher.py::TestDispatchModelRouteGate (8: disabled route blocks
before execute runs, enabled route proceeds, no recorded status proceeds (None = enabled, the
documented default), no state_backend supplied is a no-op even for a routed capability, a capability
with no model_route is unaffected even with a disabled backend passed, plus 3 tests confirming the
real verify_prose_quality/compare_against_presentation_standard/review_visual_asset_accuracy
capabilities each declare their expected model_route), test_env.py::TestLlmModelForJob (3 new: override
applies only with the debug flag, debug flag alone without LLM_MODEL is a no-op, a non-"1" debug-flag
value does not enable the override) plus 1 existing test rewritten to match the corrected default
behavior (a bare LLM_MODEL no longer silently overrides the routing table).

Two pre-existing test fakes needed updating for the new call shape, found via the full regression run
rather than left silently broken: test_specialists.py::_FakeStateBackend was missing
load_model_route_status() (added, returning None = enabled, matching the documented default); a
monkeypatched dispatch_tool_call lambda in test_supervisor_loop.py::TestWriteCapableModeGate didn't
accept the new state_backend keyword (added).

Full official-checks suite re-run clean: 1158 passed (1147 + 11 new).

Decision Ledger: #70.

- [golden-set model-routing ci auto-disable] **2026-07-23 — Wave 13.5: live golden-set-from-actions + auto-disable closure** Wave 13.5 closed: live golden-set-from-actions + auto-disable closure.

Turned Wave 8.6's offline-only golden-set scoring harness into a real, CI-invokable, auto-disabling
monitor.

Mechanism built:
- New `golden_set/auto_disable.py::evaluate_and_disable()`: durably flips a job's ModelRouteStatusV1
  to disabled when a real run's pass rate crosses PASS_RATE_FLOOR = 0.5 -- a real, evidence-derived
  floor, not a guess (LLM-018's own characterized 0.4-0.8 swing for an already-unrouted model; 0.5
  sits below even that model's worst measured swing, so it only fires for an unambiguous regression
  in an already-trusted route). Never re-touches an already-disabled record -- idempotent, defers
  entirely to a human re-enable via the existing model-route-enable command.
- New `readme-agent golden-set-run --job <job>` CLI command (commands.py::cmd_golden_set_run): wires
  a real LivePlannerClient (routed via env.llm_model_for_job(job)) through the existing harness, then
  through the new auto-disable check. Exit code IS the CI signal -- 1 when a route was just disabled
  this run, so degradation shows as a failed workflow run, not just a log line a human has to notice.
- New `.github/workflows/golden-set-run.yml` -- workflow_dispatch only, deliberately NOT scheduled
  yet. This is a real, deliberate scope correction from the sprint plan's own "schedule + dispatch"
  text: GOVERNANCE.md rule 10 ("prove it in production before automating") is the same discipline
  readme-agent-supervise.yml and measure-clone-latency.yml are themselves still following -- this is
  the first time `readme-agent golden-set-run` runs anywhere outside unit tests, so it doesn't get a
  cron trigger yet either. New OPS-013 tracks the eventual schedule: addition.

Live-verified for real, not just unit-tested: ran `readme-agent golden-set-run --job
supervisor_planning` against the real gateway. Scored 4/5 (0.8 pass rate) -- correctly stayed enabled
(above the 0.5 floor), confirmed by real exit code 0, and surfaced one genuine, previously-unknown
planner weakness (stop_decision_accuracy_everything_unchanged: the live qwen3-next model called
profile_repository instead of stopping) -- logged here as an honest new finding, not swept aside.

Deliberately did NOT attempt a live seeded-bad-scenario proving a real threshold breach flips real
durable state. Reasoning: doing so would durably disable this project's own real, currently-working
supervisor_planning route in production until a human manually re-enabled it -- a real, if reversible,
degradation of live operating capability, with no benefit over the mechanism already being proven
against a fake backend in tests (TestEvaluateAndDisable). Considered scope boundary, logged honestly.

Requirement reconciliation: OPS-011's own row names 6 metrics (tool-selection accuracy, verifier
agreement, repair success rate, stop-accuracy, hallucinated-capability rate, latency/cost per run).
Direct re-check against what's actually built found only 4 of 6 covered -- no "verifier agreement"
metric exists anywhere in this pipeline, and "latency/cost per run" is captured only per-scenario
inside a golden-set run, never aggregated across real production runs the way
aggregate_production_metrics() does for hallucinated-capability-rate/repair outcomes. Corrected OPS-011
to PARTIAL, not the sprint plan's own suggested IMPLEMENTED -- an honest downgrade from what the plan
assumed, not a silent pass-through.

Requirements: OPS-011 BACKLOG -> PARTIAL (corrected, not the plan's literal IMPLEMENTED ask). New
OPS-012 (auto-disable mechanism) -> IMPLEMENTED. New OPS-013 (schedule: trigger, deferred) -> BACKLOG.

10 new tests: test_golden_set.py::TestEvaluateAndDisable (5: above floor is a no-op, below floor
disables and persists, empty results is a no-op since pass_rate=None is never "crossed the floor", an
already-disabled route is left untouched, evidence_ref is recorded when supplied),
test_cli.py::TestGoldenSetRunCommand (5: flag parsing, missing-flag exits nonzero, a deterministically-
constructed all-scenarios-correct run built from the real scenario corpus's own expected/forbidden
fields exits 0 and never touches the route, a deterministically-wrong run exits 1 and disables, an
already-disabled route is left untouched on a second bad run).

Full official-checks suite re-run clean: 1168 passed (1158 + 10 new). actionlint confirmed
golden-set-run.yml parses cleanly alongside the existing 7 workflows.

Decision Ledger: #71.

- [dependencies sbom ci reproducibility act] **2026-07-23 — Wave 13.6: dependency & runner reproducibility** Wave 13.6 closed: dependency & runner reproducibility.

Built the SBOM/lockfile/CI-reproducibility mechanism the plan asked for, and -- because its own
required `act` proof step was actually run rather than treated as a formality -- found and fixed two
genuine, previously-undetected "runner depends on an accidental global environment" bugs, exactly the
class of defect this wave exists to guard against.

SBOM + vulnerability scanning:
- New `pip-audit>=2.7` declared as a real dev dependency in pyproject.toml (not an ad hoc script).
- Generated a real CycloneDX SBOM: plans/investigations/evidence/dependency-reproducibility-2026-07-23/
  sbom.cyclonedx.json.
- Found and fixed 6 real CVEs in the local dev venv's own bootstrap pip (24.3.1 -> 26.1.2).
- Found and fixed a real setuptools CVE (79.0.1, PYSEC-2026-3447) on a fresh Python 3.11 image,
  reproduced directly via `docker run python:3.11` after act's own run surfaced it. Both fixes are now
  proactive steps inside the new CI workflow itself (`pip install --upgrade pip setuptools`), not just
  a one-time manual fix in this session's own venv.

Explicit direct dependencies:
- Direct-import audit (grep every import root in src/ against pyproject.toml's declared deps) found one
  real gap: `langchain_core`, imported directly by every specialist module
  (`from langchain_core.runnables import RunnableConfig`), was undeclared -- working only because
  langgraph happened to pull it in transitively. Added `langchain-core>=1.0` as an explicit direct
  dependency. Regenerated requirements-lock.txt to match.

New CI workflow:
- `.github/workflows/dependency-check.yml`: a real clean install from the LOCKED dependency set
  (requirements-lock.txt), never ci.yml's own fresh unpinned `pip install -e ".[dev]"` resolve, across
  the full Python 3.11/3.12/3.13 matrix, then the real test suite, then an advisory
  (continue-on-error) pip-audit SBOM/scan step uploaded as an artifact.
- workflow_dispatch only, deliberately not on push/pull_request yet -- GOVERNANCE.md rule 10 ("prove it
  in production before automating"), the same first-run discipline every other new workflow in this
  repo has followed (readme-agent-supervise.yml, measure-clone-latency.yml, golden-set-run.yml). New
  DEP-006 tracks the eventual promotion to push/PR triggers.

Live-verified for real: ran `act` locally (Docker) against ci.yml AND the new dependency-check.yml,
across all 3 Python versions (3.11.15, 3.12.13, 3.13.14). Every step green, including the SBOM/audit
step, after applying the pip/setuptools fixes above -- not merely linted with actionlint, actually
executed end to end under real Linux containers.

Two real bugs found and fixed along the way, both invisible on the local Windows dev machine, both
surfaced only because the plan's own required act proof was actually run:

1. `inspection/file_inventory.py`'s `_MAX_FILES_SCANNED`-bounded `os.walk()` traversal was never
   sorted -- os.walk()'s own traversal order is filesystem-dependent, never guaranteed. A real test
   (test_walk_stops_at_max_files_scanned_bound) passed on Windows but failed under the same act
   (Linux/Docker) run, because Linux's own directory-entry order reached the real manifest before the
   50-file bound tripped. This is a genuine production-correctness bug, not just test flakiness: on a
   real oversized repo, which manifest a bounded scan finds (or silently misses) depended on which
   OS/filesystem performed the scan. Fixed by sorting dirs/files at both os.walk() call sites
   (_find_manifest_paths()'s _bounded_walk(), find_all_manifest_roots()) -- makes the walk's own
   effective order deterministic across every OS. Added a new regression test
   (test_bound_is_reached_deterministically_regardless_of_os_walk_order) that monkeypatches os.walk to
   yield a deliberately reversed order and confirms the bound still trips on the same files regardless
   -- would have failed deterministically pre-fix (a reversed top-level order visits the manifest
   directory first).

2. `test_cli.py::TestExecutionProfileFlag::test_github_profile_forces_a_durable_state_backend_without_
   the_flag` didn't mock env.github_run_id()/env.github_event_name(), unlike its own sibling test which
   already did. Under a real Actions/act runner, the real ambient GITHUB_EVENT_NAME=push (from ci.yml's
   own trigger) leaked in, and TriggerRecordV1.event_type's Literal (workflow_dispatch/schedule/
   repository_dispatch/cli_manual) correctly rejected "push", crashing a test that has nothing to do
   with trigger identity. Confirmed via direct check this is a genuine test-isolation bug, not a schema
   gap: every real workflow that actually dispatches a github_*-profile/durable-state supervise call
   (readme-agent-supervise.yml, readme-agent-portfolio.yml) triggers only on schedule/workflow_dispatch,
   both already valid literal values. Fixed by mocking env.github_run_id() to return None, matching real
   local-CLI use.

Requirements: DEP-001 through DEP-005 -> IMPLEMENTED. New DEP-006 (push/PR trigger promotion, deferred
per GOVERNANCE rule 10) -> BACKLOG.

1 new test: test_inspection.py::test_bound_is_reached_deterministically_regardless_of_os_walk_order.

Full official-checks suite re-run clean: 1169 passed (1168 + 1 new).

Decision Ledger: #72.

- [governance decision-ledger authorization gov-018 gov-023] **2026-07-23 — Wave 13.7: Decision Ledger amendment -- GOV-018/GOV-023 authorization-scoped autonomy** Wave 13.7 closed: Decision Ledger amendment -- GOV-018/GOV-023 evolve to authorization-scoped
autonomy, with a real, verified correction to the sprint plan's own premise before anything was
amended.

Before writing any amendment, verified the actual current text of GOV-018 and GOV-023 directly
against master.md's Decision Ledger and requirements.md, rather than trust the sprint plan's own
assumed mapping. Found the mapping was wrong in a way that mattered:

- GOV-018 (decision #33) actually bundles two unrelated things: (a) "prove it in production, not
  just unit tests" (GOVERNANCE rule 10 -- unrelated to autonomy, must stay untouched), and (b)
  "nothing may commit or push to a managed remote without the user's separate, explicit, per-instance
  approval" -- only this second clause is what the sprint plan actually meant to target.
- GOV-023 (decision #44) is not about real-world writes at all -- it's "before editing any section of
  plans/master.md, an agent MUST... proceed only on the user's fresh, explicit, per-instance
  go-ahead... a standing or implied yes does NOT satisfy this." That's about editing the planning
  document itself, a different concern from external-repo authorization.

This second finding was consequential: by its own literal text, this session's own practice this
whole sprint -- appending a new Decision Ledger entry at the close of every wave under the user's
standing "execute the plan e2e autonomously, keep docs/logs updated" directive, without re-asking
per-instance each time -- reads as non-compliant with GOV-023 if taken literally. Rather than
silently continue an undocumented departure or silently assume it was fine, this was surfaced to the
user directly as a real fork before proceeding, since it touches both a P0 safety requirement and this
session's own conduct. The user chose: amend GOV-018's push-clause only, correctly scoped, plus
separately ratify GOV-023 to name what has actually been happening, rather than leave it silent.

Amendments made (both in place, per GOVERNANCE.md rule 4 -- decisions are append-only in numbering,
revisable in content; no new decision number was minted):

- Decision #33 (GOV-018): a new dated paragraph amends ONLY the per-instance push-approval clause,
  describing the AuthorizationRecordV1 model Wave 13.2/13.3 built and live-proved: a human grants/
  renews a bounded record asynchronously (a reviewable config change, never a runtime prompt); every
  effect-class-declaring capability dispatch is checked via authorized_for() before even a pending
  ledger entry is written; an out-of-scope action produces a blocked/CapabilityGap finding, never
  invented authority. Explicitly notes: no real record has been filed for any repo yet (decision #69),
  so this amendment changes nothing about what can actually happen today -- open_presentation_pr
  remains blocked for every repo exactly as before, only the FORM of the eventual human approval act
  has changed. The live-proof clause (the decision's own namesake, "prove it in production") is
  completely untouched.

- Decision #44 (GOV-023): a new dated paragraph names the real governance gap found while drafting
  this very amendment -- an explicit, task-scoped standing directive that names a specific plan/scope
  and explicitly asks for docs/logs to be kept current DOES satisfy the per-instance go-ahead, but only
  for the routine, append-only class of edit that directive implies (a new Decision Ledger entry
  documenting a wave's own already-completed, tested, verified work, plus the accompanying routine
  Status/Build-Checklist updates). Does NOT extend to deleting/rewriting an existing decision's
  substance, restructuring a section, or anything outside the routine wave-closure pattern -- those
  still need a fresh, per-instance go-ahead, unchanged.

Requirements.md updates:
- GOV-018's row text revised to match decision #33's amendment (push-clause only; live-proof clause
  preserved verbatim).
- GOV-023's row text revised to match decision #44's amendment (bounded standing-directive exception,
  scoped precisely).
- New GOV-026: names the replacement mechanism formally (EffectClass/AuthorizationRecordV1/
  authorized_for()/dispatch_gated_effect() enforcement/authorization-validate CLI), cites all of it as
  built, tested, and live-proven (the real negative-control run from Wave 13.3), and explicitly notes
  AUTH-007's own still-open gap (no real record filed yet) rather than implying this row means the
  mechanism has been positively exercised end-to-end.

No source code changes this wave -- purely a governance/documentation amendment, verified against live
source (the actual decision/requirement text) before anything was written, exactly like every code
wave this session.

Full official-checks suite re-run clean (no test count change expected -- no code touched).

Decision Ledger: #33 and #44 amended in place (no new decision number).

- [wave-0 truth-consolidation semantic-traceability ver-001] **2026-07-23 — Level-8
  Wave 0 opened: semantic closure gate and first status correction.** Reconciled the current
  candidate tree before extending it, preserved every source change, and replaced the
  implementation-truth matrix's path-existence-only check with semantic checks for cited pytest
  symbols/suites, parseable and checksum-addressed evidence artifacts, and acceptance prose that
  contradicts its row status. The first run checked 151 `IMPLEMENTED` rows: 55 satisfied the new
  machine-verifiable floor, 10 carried informational-only findings, and 86 lacked the evidence
  precision required for closure. Those 86 are evidence debt, not automatically false; they were
  not bulk-downgraded.

  One P0 row was provably false on its own terms: `VER-001` was `IMPLEMENTED` while its acceptance
  evidence explicitly said “Still honestly unmet” and `ORC-005` records the still-callable
  unverified `orchestrator.run_repo()` mutation path. Corrected `VER-001` to `PARTIAL`; no source
  implementation claim was removed. `tests/unit/test_traceability_matrix.py` proves the semantic
  resolver, suite handling, JSON-evidence hashing/parsing, and contradiction detection. The
  `plans/master.md` consolidation remains intentionally paused pending the user's fresh approval
  for Mission, Status, Decision Ledger, Architecture, Build Checklist, and Verification Checklist.

- [wave-0 fresh-clone reproduction dependencies verification] **2026-07-23 — Level-8
  Wave 0 committed-baseline reproduction passed.** Created a `--no-local` fresh clone at commit
  `674285a7c1fd9df1308944f549a94b36f29f9c11`, built its repo-root `.venv`, installed the exact
  `requirements-lock.txt` set (SHA-256
  `75fbac57d40f6e6d1a66cf58de06f850e087c40e82a673f2657d35ff80fb9e70`), and installed the
  project editable without dependency re-resolution. The official suite passed: Ruff, format,
  mypy, 1,174 non-live tests (18 live deselected), plan validation, and actionlint.

  The first fresh-clone attempt correctly rejected three dirty-tree-hidden integration defects:
  `PKG-005` source/spec split, checkout-line-ending-sensitive generation hashes, and a log-index
  count committed before its shard. The source/spec and shard splits were closed by coherent
  follow-up commits; generation hashing now normalizes Git checkout line endings and has a
  regression test. Evidence:
  `plans/investigations/evidence/wave-0-fresh-clone-reproduction-2026-07-23/official-checks.json`.

- [level-8 wave-0 authority reroute mission-control] **2026-07-23 — Wave 0 isolated its
  structural-plan authority blocker without stopping safe work.** Split the broad plan-truth task
  into a gated master-amendment child, semantic closure-evidence work, candidate-artifact
  disposition, and current-HEAD fresh-clone reproduction. Recorded three materially different
  failed authority checks and the exact action/resume condition on the blocked child. The graph
  loader now enforces that external-blocker standard, and the dependency model permits rerouted
  child work while keeping Wave 1 gated on all four outcomes. Focused verification passed:
  13 mission-control tests, 34 supervise/mission CLI tests, Ruff, mypy over 169 source files,
  376-row requirement coverage, and plan validation. No product remote or gated master section
  was changed.

- [level-8 wave-0 candidate-artifact disposition] **2026-07-23 — Five pre-existing plan
  candidates were classified and preserved without implicit acceptance.** Recorded current
  hashes, Git provenance, full diff findings, layout classification, and exact disposition for
  `plans/idea.md`, `plans/master.md`, `plans/changelog.md`, `plans/roadmap.md`, and
  `plans/status.md`. The master candidate contains valuable history but remains behind its
  section-specific approval gate; the untracked roadmap is a stale parallel program and the
  changelog/status files are outside the current allowed layout. All originals remain byte-for-byte
  recoverable and unstaged.

- [level-8 wave-0 semantic closure] **2026-07-23 — All 85 high-confidence semantic
  closure findings were consumed without bulk status laundering.** Preserved 76 implemented rows
  using requirement-specific focused selectors or checksum-addressed committed references; 224
  focused tests passed. Corrected nine literal overclaims to `PARTIAL`: `GOV-009`, `RDM-001`,
  `CORE-001`, `CORE-006`, `OPS-003`, `SAFE-005`, `MEM-002`, `PRL-001`, and `AUTH-003`.
  The regenerated matrix now has zero high-confidence findings across 141 implemented rows.
  `--matrix-only` refresh support preserves the separately gated untracked status candidate
  byte-for-byte; plan validation remains clean with the same 51 pre-existing warnings.

- [level-8 wave-0 master-consolidation governance] **2026-07-23 — Installed the approved Level-8 Waves 0-8 master program** The user supplied fresh explicit approval naming Mission, Status, Decision Ledger, Architecture, Build Checklist, and Verification Checklist. The preserved master candidate was verified against its Wave 0 SHA-256 before surgery. Useful decisions #55-72 were retained; decisions #26/#32/#33/#37 were amended in place; decisions #73-76 fix the canonical runtime, lifecycle/checkpoint, verified-proposal, and maturity contracts. Status no longer points to the disallowed untracked status/roadmap candidates, Architecture now describes the target production flow, and the obsolete phase/sprint tracks were reduced to historical context rather than a parallel checklist. Requirements gained L8-001 through L8-013 and a Waves 0-8 traceability table. No product remote was written.

- [master requirements wave1 level8] **2026-07-23 — Closed Level-8 Wave 1 canonical correctness and safety spine.** Fresh approval covered only the Status, Build Checklist, and Verification Checklist sections of plans/master.md. Wave 1 closure is backed by commits 44fa7d8 and 0169964 plus the checksum-complete level8-wave1-heterogeneous-fail-closed-2026-07-23 evidence bundle. The final committed source passed 1,211 non-live tests, Ruff, formatting, mypy, actionlint, plan validation, live preflight, historical 4/4 false-success replay, non-zero live .NET and Go blocked outcomes, explicit unsupported Rust, and the canonical mutation-path audit. Seven requirement rows were reconciled to IMPLEMENTED from this evidence. No product repository write occurred, and unrelated working-tree candidates remain untouched.

- [wave2 reconciliation level8] **2026-07-23 — Opened Level-8 Wave 2 after verified Wave-1 reconciliation.** The required preceding-wave reconciliation compared Wave 1's committed code, full official checks, heterogeneous evidence bundle, seven requirement statuses, master Status/Build/Verification sections, generated task coverage, and durable mission state. It found no remaining Wave-1 overclaim: commit e74a3a2 is green, the task transitioned through VERIFIED and SCORED to CLOSED at durable state version 48, and L8-WAVE2-RESTARTABLE-ACTIONS-RUNTIME was the sole eligible successor. The controller claimed Wave 2 at state version 49. No product repository write occurred.

- [requirements runtime evidence] **2026-07-23 — Implemented the local Wave-2 restartable
  Actions runtime without claiming production closure.** Added the sole scheduled/reusable
  production workflow, versioned trigger/lifecycle/checkpoint/state contracts, explicit V1-to-V2
  migration, eleven real producer checkpoints, recovery and health commands, `RunManifestV3`,
  strict GitHub-profile state behavior, read-only GitHub App authentication, and six distinct
  Tenacity-backed retry policies.

  Verification found and repaired four material defects before handoff: replayed checkpoint
  timestamps falsely collided; the first recovery sweep exposed stale work but did not resume its
  original durable trigger; unmarked permanent GitHub 403s were initially retryable; and the
  requirement extractor silently omitted digit-bearing `L8-*` IDs and rejected `BACKLOG`.
  Recovery now emits a repository matrix and uses `--resume-trigger-key`; every state-CAS loop
  shares the bounded jitter policy; the inventory/task graph/coverage evidence was regenerated at
  390 unique requirements, 366 mandatory, and zero reopened implemented rows.

  Requirement rows `RUN-002/005/006/007/009/010`, `EVID-004`, and
  `L8-001/003/004/005/013` remain honestly `PARTIAL`. Local gates and the production-like `act`
  recovery/plan reproduction pass, but the control repository has no configured GitHub App
  variable/private-key secret or dead-man endpoint, the new workflow is not published, and hosted
  interruption/absence proofs cannot be produced locally. Evidence:
  `plans/investigations/evidence/level8-wave2-restartable-actions-2026-07-23/`. No product
  repository write occurred; the unrelated dirty plan/AGENTS candidates remain preserved.

- [requirements incident] **2026-07-23 — Wave-2 independent-audit repairs.** Reordered terminal lifecycle persistence behind Manifest V3 checksum validation, made retryable/SLA-aged backlog explicitly unhealthy, and narrowed checkpoint recovery claims to same-trigger canonical restart rather than unproven stage skipping — independent review found false-success and false-green boundaries in the first local evidence pass.

- [requirements incident] **2026-07-23 — Healed stale requirement-coverage producers after full-suite rejection.** The requirements extractor now keys its source commit to the requirements file, the classifier handles every governed status, both producers expose non-mutating checks, all 390-row dependent outputs were regenerated, and an enforced consumer test prevents recurrence.

- [requirements incident] **2026-07-23 — Migrated hosted GitHub App token input to the current client-ID contract.** The final workflow now uses actions/create-github-app-token@v3 client-id with GH_APP_CLIENT_ID, regression coverage rejects app-id, and the hosted blocker remains missing external App registration/configuration rather than local workflow syntax.

- [external blockers continuation] **2026-07-23 — Recorded the Wave-2 external boundary and
  continued safe independent work.** The sole mission controller transitioned
  `L8-WAVE2-RESTARTABLE-ACTIONS-RUNTIME` to `BLOCKED_EXTERNAL` at durable state version 52 and
  reevaluated the mission at version 53. Local implementation head `14aceef` and evidence commit
  `8f739f1` remain unpublished because this execution has no publication authority. Remote
  `main` remains `3c81a1e`.

  Three materially different checks exhausted repository-local recovery: configuration inspection
  found no remote `GH_APP_CLIENT_ID`, `GH_APP_PRIVATE_KEY`, `LLM_BASE_URL`, or `LLM_API_KEY`;
  hosted run `30023067487` proved recovery/matrix execution but failed closed before the supervisor
  when the App identity resolved empty; and the workflow/action contract was repaired from legacy
  `app-id` to `client-id`, independently verified, and still cannot manufacture an installed App,
  secret, or external heartbeat. The hosted failure performed no target-repository write, while
  the governed health path remained active and opened visible issue #1.

  Exact external actions and resume conditions are:

  1. Grant fresh publication authority for local commits `14aceef` and `8f739f1` to
     `babar-raza/foss-readme-optimizer` remote `origin`, branch `main`.
  2. Register/install the GitHub App and configure `GH_APP_CLIENT_ID`,
     `GH_APP_PRIVATE_KEY`, `LLM_BASE_URL`, and `LLM_API_KEY`.
  3. Configure an external dead-man heartbeat endpoint and prove its absence alert.
  4. Run the hosted workflow through the supervisor, then inject and observe production recovery.
  5. After those proofs exist, obtain fresh approval to update `plans/master.md` Status,
     Build Checklist, and Verification Checklist for Wave-2 closure.

  The checksum-complete evidence remains at
  `plans/investigations/evidence/level8-wave2-restartable-actions-2026-07-23/`. The blocked
  production-proof lane does not authorize Wave-2 closure or Wave-3 production effects; it does
  permit repository-local, no-effect Wave-3 preparation through the same authoritative graph.

- [level-8 wave3 product-truth foundation] **2026-07-23 — Completed the repository-local
  ProductFactsV2 and ownership foundation without bypassing Wave 2.** Reconciled the immediately
  preceding wave first, left its hosted GitHub App/recovery proof `BLOCKED_EXTERNAL`, and added one
  bounded child to the same authoritative mission graph so independent local safety work could
  proceed. Built the provenance-complete V2 contract and explicit V1 migration, source precedence
  and conflict recording, dependent-surface gating, the five-class ownership map, Markdown-aware
  protected-content fingerprints, and a bounded secret-free example-execution boundary. Metadata
  proposals now block generic replacements when their facts are missing, and README candidates
  must pass a separate live acquisition/factuality gate before independent verification or an
  effect can be reached.

  The real read-only Cells/Java proof rejected the still-present
  `org.aspose:aspose-cells-foss` claim after Maven Central returned `NOT_PUBLISHED`, while a
  missing audience/problem pair blocked only the description and left the independently
  fact-backed homepage eligible. Prompt-injection prose could not verify itself; removing a
  limitation, command, example, or maintainer region failed closed. Official static gates passed,
  the focused contract lane passed, and the full non-live suite passed 1,297 tests with 18 live
  tests excluded. Eighteen requirement rows were reconciled conservatively to local truth; none
  was promoted to `IMPLEMENTED`. Evidence:
  `plans/investigations/evidence/level8-wave3-local-product-truth-foundation-2026-07-23/`.
  Parent Wave 3 remains blocked by Wave-2 hosted proof, full-field fact ingestion, real isolated
  example execution, and an external product-owner conflict handoff. No product repository or
  gated `plans/master.md` section was changed.

- [level-8 wave3 independent-audit repairs] **2026-07-23 — Independent rejection closed three
  defects before local acceptance.** The first review proved that a lower-precedence or same-rank
  losing fact remained citable and that direct metadata-capability callers could forge
  eligibility/citations. Citation validation now requires the selected fact for its field, with
  both losing-fact negative controls. The metadata capability now accepts only `org_repo`,
  independently re-derives facts through `facts/provider.py`, and rejects forged extra arguments
  at dispatch. A final P2 review found the older specialist module-map row still described the
  removed two-capability flow; a guarded retrofit replaced it with the current explicit registry.

  The final independent verdict is `accepted_no_blocking_findings`; all three findings are marked
  resolved. The refreshed bundle records 46 focused checks, 1,300 passing non-live tests with 18
  live tests excluded, clean static/plan gates, the real read-only Cells/Java rejection, and seven
  verified bundle checksums. The mission controller advanced the local child through
  `IMPLEMENTED`, `VERIFIED`, `SCORED`, and `CLOSED` at durable state version 60; parent Wave 3
  and Wave 2 retain their external/production blockers. No product repository, publication
  remote, or gated `plans/master.md` section was changed.
