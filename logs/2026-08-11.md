# Project log — 2026-08-11

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-11 | master supervisor local-poc acceptance | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Repaired `deterministic_manifest_dependency_key_mismatch` at its causal owner in `write_local_poc_readme_candidate`. |
| 2026-08-11 | master mermaid capability-semantics | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Repaired PDF Mermaid capability-normalization duplicate ("PDF file editing operations"). |
| 2026-08-11 | master backlog candidate | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Logged unrelated pre-existing `test_portfolio.py` failure found opportunistically; not filed as a catalog row. |
| 2026-08-11 | master supervisor claim-recovery | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Repaired expired-claim recovery being discarded when the requested task stayed unclaimable. |
| 2026-08-11 | master evidence pdf | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Re-promoted PDF against the rebound dependency key; closed L8-VPY-03A (1/13). |
| 2026-08-11 | master evidence page | — | — | `L8-VPY-03C-PAGE-CURRENT-REFRESH` | Promoted and closed Page reusing PDF's fixed acceptance-chain code (2/13). |
| 2026-08-11 | master evidence note incident | — | — | `L8-VPY-03D-NOTE-CURRENT-REFRESH` | Recovered Note from a pre-handover checksum-inventory corruption; promoted and closed (3/13). |
| 2026-08-11 | master evidence 3d | — | — | `L8-VPY-03E-3D-CURRENT-REFRESH` | Promoted and closed Aspose.3D; 4/13 Python repositories closed. |
| 2026-08-11 | master supervisor platform-scope | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired `platform:<name>` cohort-scope tokens being rejected as literal repository membership. |
| 2026-08-11 | master readme limitation-semantics | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired distinct per-symbology BarCode limitations colliding as duplicates. |
| 2026-08-11 | master readme capability-semantics | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired noun-first capability descriptions echoing their own title. |
| 2026-08-11 | master evidence pdf | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Re-promoted and re-closed PDF (cycle 3) after the three BarCode-driven shared-code fixes reopened it. |
| 2026-08-11 | master evidence page | — | — | `L8-VPY-03C-PAGE-CURRENT-REFRESH` | Re-promoted and re-closed Page (cycle 2) after the same shared-code reopen. |
| 2026-08-11 | master evidence note | — | — | `L8-VPY-03D-NOTE-CURRENT-REFRESH` | Re-promoted and re-closed Note (cycle 2) after the same shared-code reopen. |
| 2026-08-11 | master evidence 3d | — | — | `L8-VPY-03E-3D-CURRENT-REFRESH` | Re-promoted and re-closed Aspose.3D (cycle 2); closes the four-repo shared-code-reopen pass. |
| 2026-08-11 | master evidence barcode | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Promoted BarCode (5/13) after its three content bugs were fixed. |
| 2026-08-11 | master supervisor claim-recovery | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired expired-claim recovery discarding already-recorded material narrowing. |
| 2026-08-11 | master facts cells | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired Cells `product.formats` block: no corroborator was registered for family="cells". |
| 2026-08-11 | master evidence pdf | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Re-closed PDF (cycle 4) after the Cells shared-code reopen. |
| 2026-08-11 | master evidence page | — | — | `L8-VPY-03C-PAGE-CURRENT-REFRESH` | Re-closed Page (cycle 3) after the same shared-code reopen. |
| 2026-08-11 | master evidence note | — | — | `L8-VPY-03D-NOTE-CURRENT-REFRESH` | Re-closed Note (cycle 3) after the same shared-code reopen. |
| 2026-08-11 | master evidence 3d | — | — | `L8-VPY-03E-3D-CURRENT-REFRESH` | Re-closed Aspose.3D (cycle 3); closes the Cells-shared-code-reopen pass. |
| 2026-08-11 | master evidence promotion | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Single clean promotion rebuild for Cells/PDF/Page/Note/3D/BarCode (6/13). |
| 2026-08-11 | master facts email | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Repaired Email `product.formats` block: corroborator could only upgrade pre-seeded entries, never prove new ones. |
| 2026-08-11 | master facts font slides tex words | — | — | `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` | Registered corroborators for Font, Slides, TeX, Words; confirmed PSD (no source) and TeX (upstream indentation corruption) as genuine external blockers. |
| 2026-08-11 | master evidence pdf | — | — | `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES` | Re-closed PDF (cycle 6) after the corroborator-batch reopen. |
| 2026-08-11 | master evidence page | — | — | `L8-VPY-03C-PAGE-CURRENT-REFRESH` | Re-closed Page (cycle 4) after the same reopen. |
| 2026-08-11 | master evidence note | — | — | `L8-VPY-03D-NOTE-CURRENT-REFRESH` | Re-closed Note (cycle 4) after the same reopen. |
| 2026-08-11 | master evidence 3d | — | — | `L8-VPY-03E-3D-CURRENT-REFRESH` | Re-closed Aspose.3D (cycle 4); closes the corroborator-batch reopen pass. |

## Entries

- [master supervisor local-poc acceptance] **2026-08-11 — Repaired `deterministic_manifest_dependency_key_mismatch` at its causal owner in `write_local_poc_readme_candidate`.**
  Root cause: `same_candidate` in `local_poc_evidence.py` keyed only on `candidate_hash`. When a
  shared rendering/component dependency changes (for example commit `e0b26d8c3`'s Mermaid
  mixed-endpoint width fix) without altering the composed README bytes, the function preserved the
  prior `AGENT_APPROVED`/`NO_OP_PROVEN` `lifecycle_status`/`completed_stages` while still stamping
  the manifest's `candidate_stage_dependency_key` forward to the new value — leaving
  `review/deterministic-validation.json`, `independent-agent-review.json`, `final-verdict.json`, and
  `no-op-proof.json` bound to the superseded key with no signal that they needed to regenerate. This
  is exactly the `runs/multi-agent/L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES/independent-current/receipt.json`
  `FAIL` verdict from the Codex handover. Fix: `same_candidate` now also requires the manifest's
  stored `candidate_stage_dependency_key` to match the freshly computed one; a key change now
  correctly reopens the bundle at `CANDIDATE_GENERATED` (`complete: false`, review-stage markers
  dropped) regardless of whether the rendered bytes matched. Regression test:
  `tests/unit/test_local_poc_evidence.py::test_unchanged_candidate_bytes_with_new_dependency_key_reopens_stale_acceptance`
  (confirmed failing before the fix, passing after).

- [master mermaid capability-semantics] **2026-08-11 — Repaired PDF Mermaid capability-normalization duplicate found while verifying the PDF acceptance-chain repair.**
  `tests/unit/test_readme_header_visual.py::test_mermaid_uses_the_normalized_pdf_capability_view` was
  failing at handover because `"PDF file editing operations"` survived as a fourth semantic duplicate
  alongside `"Add and edit text and images, including text replacement and redaction"`.
  `same_public_capability()` in `capability_semantics.py` required exact domain-set equality before
  falling back to a generic-noun match; the specific phrase's domain set `{editing, text_images}` was
  a strict superset of the generic phrase's `{editing}`, so they never compared as equal and stayed
  distinct. Fix: `comparable_domains` now also accepts a strict-subset relation, but only when the
  narrower side's domains include at least one *operation* domain (verb-based:
  `document_lifecycle`/`editing`/`rendering`/`security`/`extraction`/`validation`) rather than a
  purely *object* domain (noun-based: `metadata`/`text_images`/`forms`/`annotations`/
  `resource_limits`/`pages`). Verified this does not regress the deliberately-distinct case in
  `tests/unit/test_public_semantic_reconciliation.py::test_capability_normalization_keeps_distinct_pdf_domains`
  (`"XMP metadata handling"` must stay separate from `"Extract text, images, attachments, metadata,
  and bookmarks"` — the shared domain there is the purely-descriptive `metadata`, not an operation
  domain, so it correctly still does not merge).

- [master backlog candidate] **2026-08-11 — Found unrelated pre-existing test failure, out of scope for this task; not filed as a formal `BACKLOG` catalog row.**
  `tests/unit/test_portfolio.py::test_completed_local_poc_status_advances_only_with_valid_bundle`
  fails (`completed_local_poc_status(...)` returns `None` instead of `"NO_OP_PROVEN"`) independent of
  and unaffected by both fixes above — confirmed identical failure with the `local_poc_evidence.py`
  change stashed. Not fixed here per `GOV-014` (does not block `L8-VPY-03A`'s correctness/safety/
  acceptance). Filing the required typed `BACKLOG` row in `plans/requirements/catalog.jsonl` needs a
  `legacy_row_sha256`/`legacy_line` derivation this session did not have tooling to compute safely
  without risking the catalog's fail-closed hash chain (`scripts/governance/query_requirement_catalog.py`
  fails closed on a stale catalog hash) — no `scripts/governance/*` authoring helper for new rows was
  found. Recorded here as the immediate, safe, non-blocking option instead of guessing the row's
  hash fields; still needs a proper `BACKLOG` catalog row filed by whoever next touches
  `local_poc_cache.py`/`test_portfolio.py`.

- [master supervisor claim-recovery] **2026-08-11 — Repaired expired-claim recovery being discarded when the requested task stayed unclaimable.**
  `claim_next_task()` computed the expired-claim recovery and the "requested task is not
  claimable" rejection inside the same all-or-nothing `_save_with_retry` mutator; raising
  `ConfigError` for the rejection discarded the just-computed recovery too, since a raising
  mutator persists nothing. Every subsequent claim attempt against `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES`
  recomputed and discarded the same recovery, leaving it permanently stuck under an expired
  `codex-coordinator` claim (`claim_expires_at` hours in the past). Fix: the mutator now always
  returns (never raises) for "not claimable this attempt", so recovery persists regardless of
  outcome; `claim_next_task()` reconstructs and raises the same two diagnostic messages from the
  persisted state afterward. Commit `e203c26f4`.

- [master evidence pdf] **2026-08-11 — Re-promoted PDF against the rebound dependency key and closed `L8-VPY-03A-PAGE-PDF-VERIFIED-CANARIES`.**
  Promoted evidence tree's review artifacts rebound to the current dependency key
  (candidate bytes unchanged, `189b3321d...`); independently verified PASS; closed through the
  mission ladder (`IN_PROGRESS -> ... -> CLOSED`). 1/13 Python repositories closed. Commits
  `1c7245f0e`, `942e4f254`.

- [master evidence page] **2026-08-11 — Promoted and closed Page, reusing PDF's fixed acceptance-chain code.**
  Page reached `NO_OP_PROVEN`/complete under the same shared-code fix (`20faecced`) that closed
  PDF, with no Page-specific repair needed — both prior "ineffective" approach attempts were plain
  claim-lease expirations, not a technical defect in Page's pipeline. Independently verified PASS;
  closed. 2/13 Python repositories closed; mission graph advances to
  `L8-VPY-03D-NOTE-CURRENT-REFRESH`. Commits `d7054e321`, `d2f1b3283`.

- [master evidence note incident] **2026-08-11 — Recovered Note from a pre-handover checksum-inventory corruption, then promoted and closed it.**
  `candidate/README.md` was 0 bytes on disk while `manifest.json`/`sha256sums.txt` still recorded
  the correct 25,760-byte hash, predating this session. A first, broader recovery attempt archived
  the whole bundle and wrongly discarded valid, expensive-to-recompute `facts/` evidence (a
  compiled and executed code example), producing `BLOCKED_MISSING_EVIDENCE`; reverted. Correct
  recovery used the codebase's own narrow `archive_and_prune_downstream_artifacts()` to preserve
  the corrupted candidate as forensic `superseded/` evidence while invalidating only
  assessment/planning/candidate/review and keeping `facts/` intact. The canonical pipeline then
  regenerated candidate+review twice back to back (`AGENT_APPROVED -> NO_OP_PROVEN`, zero provider
  calls on the second run) once an unrelated Docker-daemon-unavailable blocker for isolated example
  verification was resolved (Docker Desktop started). Independently verified PASS, including a
  direct diff against the preserved pre-recovery facts snapshot confirming only timestamp fields
  changed. 3/13 Python repositories closed. Commits `f97e5c47e`, `9b7d013d5`.

- [master evidence 3d] **2026-08-11 — Promoted and closed Aspose.3D.**
  `AGENT_APPROVED` with 2 provider calls on the first transaction, then `NO_OP_PROVEN` with zero
  provider calls and one cache reuse on the identical second transaction. Independently verified
  PASS; closed. 4/13 Python repositories closed (3D, Page, PDF, Note); mission graph advances to
  `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` for the remaining 9-repository cohort. Commits `54bcf961b`,
  `2035744d8`.

- [master supervisor platform-scope] **2026-08-11 — Repaired `platform:<name>` cohort-scope tokens being rejected as literal repository membership.**
  `require_visible_execution_binding()` did a plain `repository not in repository_scope` membership
  check, so `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` (scoped to `['platform:Python']` by design, not a
  literal repository list) rejected every real Python repository under it with "is outside
  immediate goal" — discovered when the first bounded canary against this task
  (`aspose-barcode-foss/Aspose.BarCode-FOSS-for-Python`) hit it. Fix:
  `_repository_matches_scope()` now also expands `platform:<name>` tokens against the repository's
  registry-declared platform (`require_listed().platform`, case-insensitive), while literal
  org/repo scope entries keep working exactly as before. Commit `2471652df`.

- [master readme limitation-semantics] **2026-08-11 — Repaired distinct per-symbology BarCode limitations colliding as duplicates.**
  `content_words()` dropped every token of length <=2, including short digit-only identifiers like
  "39" (Code 39) while keeping longer ones like "128" (Code 128) — an incidental,
  digit-count-dependent asymmetry with no semantic basis. Combined with
  `capability_discriminators()` only recognizing a fixed document-format vocabulary, two real,
  source-derived, genuinely distinct BarCode limitations ("Only the SVG backend is implemented for
  Code 128." vs "...Code 39.") collided as one duplicate statement, blocking BarCode's presentation
  plan under `L8-VPY-03-ALL-PYTHON-VERIFIED-POC`. Fix: `content_words()` now keeps any token
  containing a digit regardless of length; `limitation_semantics.py` also gains a second,
  general-purpose discriminator check via the newly-exposed
  `capability_semantics.identifier_discriminators()`, so symbology numbers and other non-format
  identifiers now correctly keep otherwise near-identical limitations distinct. Commit `76c5bf1a6`.

- [master readme capability-semantics] **2026-08-11 — Repaired noun-first capability descriptions echoing their own title.**
  `_description()`'s branch selection only recognized an exact leading verb via
  `capability_action_verb()`. A capability phrased noun-first ("SVG rendering for all supported
  symbologies") produces no leading-verb match, and its SEO title gets a synthetic "Work with ..."
  prefix whose leading word "work" *is* a recognized verb but has no branch of its own — so both
  cases fell through to the generic branches that echo the title back almost verbatim, failing the
  `capability_description_repeats_title` presentation check. Found live blocking BarCode's
  presentation plan under `L8-VPY-03-ALL-PYTHON-VERIFIED-POC`. Fix: added `_embedded_action_verb()`,
  which recognizes a small domain-relevant set of nominalized action nouns (rendering->render,
  generation->generate, etc.) anywhere in the text, tried ahead of `capability_action_verb()` at
  both call sites. Commit `c905c04e6`.

- [master evidence pdf] **2026-08-11 — Re-promoted and re-closed PDF (cycle 3) after the three BarCode-driven shared-code fixes reopened it.**
  Commits `2471652df`, `76c5bf1a6`, `c905c04e6` each advanced the shared
  `candidate_stage_dependency_key` for every Python repository, correctly reopening PDF, Page,
  Note, and Aspose.3D's prior acceptance per the mission's staleness-reconciliation rule. PDF
  re-approved and re-proven no-op against the new key with byte-identical candidate content,
  independently verified PASS a third time, re-closed through the mission ladder. Page/Note/3D
  correctly excluded/removed from the promoted tree pending their own re-verification. Commits
  `80073b518`, `fbeb607c1`.

- [master evidence page] **2026-08-11 — Re-promoted and re-closed Page (cycle 2) after the same shared-code reopen.**
  Page re-approved and re-proven no-op against the current dependency key with byte-identical
  candidate content (`89846281a5...`), independently verified PASS a second time, re-closed through
  the mission ladder. Mission graph advances to `L8-VPY-03D-NOTE-CURRENT-REFRESH` (Note's second
  re-verification cycle). Commits `9ea3e5aff`, `51b11990a`.

- [master evidence note] **2026-08-11 — Re-promoted and re-closed Note (cycle 2) after the same shared-code reopen.**
  Note re-approved and re-proven no-op against the current dependency key with byte-identical
  candidate content (`2333e0c29565...`), independently verified PASS a second time, re-closed through
  the mission ladder. Mission graph advances to `L8-VPY-03E-3D-CURRENT-REFRESH`. Commits `9ab1cb87a`,
  `e7d25185f`.

- [master evidence 3d] **2026-08-11 — Re-promoted and re-closed Aspose.3D (cycle 2); closes the four-repository shared-code-reopen re-verification pass.**
  Aspose.3D re-approved and re-proven no-op against the current dependency key with byte-identical
  candidate content (`5735c3f66e57...`), independently verified PASS a second time, re-closed through
  the mission ladder. Mission graph advances to `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` for the remaining
  9-repository cohort. Commits `1f18f4cb4`, `08152b136`.

- [master evidence barcode] **2026-08-11 — Promoted verified BarCode README (5/13) after its three content bugs were fixed.**
  BarCode reached `NO_OP_PROVEN`/complete under all three shared-code fixes made earlier this
  session (`2471652df` platform-scope tokens, `76c5bf1a6` per-symbology limitation discrimination,
  `c905c04e6` capability-description title-echo), after twice failing `BLOCKED:specialist_failed`
  before those fixes existed. First transaction: `AGENT_APPROVED`, 3 provider calls. Second
  (identical) transaction: `NO_OP_PROVEN`, 0 provider calls, 1 cache reuse. Independently verified
  PASS by a non-authoring subagent, which directly read `candidate/README.md` and confirmed both
  content bugs were genuinely resolved. This task closes only once all 13/13 Python repositories are
  current and independently accepted, so BarCode is promoted here without its own per-repository
  mission transition. Commit `4f316b990`.

- [master supervisor claim-recovery] **2026-08-11 — Repaired expired-claim recovery discarding already-recorded material narrowing.**
  `_recover_expired_claim()` unconditionally closed the active approach-budget attempt as
  ineffective with `evidence_refs=["claim lease expired"]`, discarding any
  `last_material_narrowing_at`/`evidence_refs` a prior `record-narrowing` call had already stamped
  on that same in-progress attempt. The 30-minute claim lease is comfortably shorter than one real
  Python-repository cycle under this session's independent-verification protocol (two bounded-canary
  transactions, a non-authoring subagent verification pass, promotion, and a commit), so every cycle
  risked a lease timeout mid-flight regardless of whether real narrowing occurred — discovered live
  when `L8-VPY-03-ALL-PYTHON-VERIFIED-POC`'s claim expired during Cells' investigation immediately
  after a successful `record-narrowing` call, and the resulting "ineffective" closure (with the
  narrowing silently erased) combined with an earlier genuinely-stalled attempt to trip the
  two-equivalent-failures gate. Fix: recovery now reads the closing attempt's existing
  `last_material_narrowing_at` and `evidence_refs` before calling `finish_latest_attempt`, closes it
  as effective (not ineffective) when narrowing had already been recorded, and merges rather than
  replaces `evidence_refs`. A lease lapsing on an attempt that made zero recorded progress still
  counts as ineffective, unchanged. Regression test:
  `tests/unit/test_mission_control.py::test_expired_claim_recovery_preserves_prior_material_narrowing`.
  Commit `55d520aa7`.

- [master facts cells] **2026-08-11 — Repaired Cells `product.formats` block: no corroborator was registered for `family="cells"`.**
  Cells' `product.formats` fact reached `BLOCKED_MISSING_EVIDENCE`: "no input/output format survived
  isolated-consumer and native-extractor verification". Root cause was not a content gap or a
  transient failure (an initial hypothesis, disproven — a standalone reproduction of the wrong
  function, `aspose_org_format_adapter.extract_aspose_org_formats`, is not the code path the real
  pipeline uses). The real path is `extract_repository_format_directions` ->
  `corroborate_python_family_format_directions`, which dispatches by product family through
  `_CORROBORATORS`; that dict only registered `"3d"`, `"barcode"`, `"email"`, and `"html"` —
  `"cells"` (and every other remaining Python family) had no corroborator at all, so dispatch
  silently no-oped to an empty format list. Added `python_cells_format_functionality.py`, following
  the existing per-family AST-verification pattern: confirms the immutable source revision, parses
  `aspose/cells_foss/workbook.py` for the real `save`/`_load`/`save_as_csv`/`load_csv`/
  `save_as_json`/`save_as_markdown` methods, and cross-checks each against a real round-trip or
  export example in `examples/` that actually calls it. Regression tests:
  `tests/unit/test_python_cells_format_functionality.py`. Commit `93d41ebfd`.

- [master evidence pdf] **2026-08-11 — Re-closed PDF (cycle 4) after the Cells shared-code reopen.**
  PDF's acceptance was regressed to stale by two shared-code fixes made while processing Cells
  (`55d520aa7` expired-claim narrowing preservation, `93d41ebfd` Cells format corroborator), neither
  of which touches PDF's own format family. Re-approved and re-proven no-op with byte-identical
  candidate content, independently verified PASS across two verifier passes (the first correctly
  caught an unrelated working-tree hazard from a promotion-script run against the still-mid-reopen
  cohort, resolved by restoring the working tree to HEAD; the second confirmed clean), closed
  through the mission ladder again. Mission graph advances to `L8-VPY-03C-PAGE-CURRENT-REFRESH`.
  Commit `40ed7e3a3`.

- [master evidence page] **2026-08-11 — Re-closed Page (cycle 3) after the same shared-code reopen.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a third time — including a positive re-derivation of
  `current_fact_acceptance_contract(ecosystem='python', family='page')` confirming the bundle
  genuinely reflects post-fix code state, not a stale coincidence. Closed through the mission ladder
  again. Mission graph advances to `L8-VPY-03D-NOTE-CURRENT-REFRESH`. Commit `74aebebf5`.

- [master evidence note] **2026-08-11 — Re-closed Note (cycle 3) after the same shared-code reopen.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a third time, including the same positive contract-re-derivation check. Closed through the mission
  ladder again. Mission graph advances to `L8-VPY-03E-3D-CURRENT-REFRESH`. Commit `5eb38329f`.

- [master evidence 3d] **2026-08-11 — Re-closed Aspose.3D (cycle 3); closes the Cells-shared-code-reopen re-verification pass.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a third time, including the same positive contract-re-derivation check. Closes the
  five-repository Cells-shared-code-reopen pass for PDF, Page, Note, and Aspose.3D; BarCode's own
  re-verification remained outstanding. Commit `2bc3a1a0c`.

- [master evidence promotion] **2026-08-11 — Single clean promotion rebuild for Cells, PDF, Page, Note, 3D, and BarCode (6/13).**
  One rebuild of all six repositories reopened/added by this session's shared-code changes, run only
  after all six were back to fresh `NO_OP_PROVEN` durable state to avoid the repeated
  promotion-script churn a premature run caused earlier this session. PDF, Page, Note, and Aspose.3D
  were byte-identical to their prior promoted content (only bookkeeping/dependency-key fields in the
  JSON sidecars changed). Cells was its first promotion. BarCode was genuinely regenerated content
  (candidate hash `e07c62f6d072` -> `3e11eff78a14`): its fact-acceptance contract was legitimately
  invalidated this cycle (isolated to the `drafting_and_example_selection` component), triggering a
  full fresh generation-review-approval cycle rather than a byte-identical reuse — the correct,
  designed behavior, not a defect. The new opening paragraph was more specific (named Code 128, Code
  39, EAN/UPC, QR Code), cross-checked by the independent verifier against
  `facts/product-facts.json`'s static-API-verified entries. The Mermaid OUTPUTS node order (O1/O2
  PNG/SVG) swapped, corroborating an already-open, non-blocking backlog item (commit `83d662b77`)
  about diagram-endpoint-label-ordering instability across render sites — not a new defect, not fixed
  here per `GOV-014`. Independently verified PASS across two verifier passes. Commit `34bc3e7e2`.

- [master facts email] **2026-08-11 — Repaired Email `product.formats` block: corroborator could only upgrade pre-seeded entries, never prove new ones.**
  Email's `product.formats` fact reached the same `BLOCKED_MISSING_EVIDENCE` symptom as Cells, but a
  corroborator was already registered for `family="email"`. Root cause was structural:
  `corroborate_python_email_format_directions()` only ever *upgraded* `AsposeOrgFormatEvidenceV1`
  entries already present in its `formats` parameter (`functional=None -> True`), never adding new
  ones. Its only real call site, `extract_repository_format_directions()`, always invokes it with
  `formats=[]` (the repository-format path has no upstream extractor seed, unlike the aspose.org
  sibling-tool path other corroborators can also serve), so with an empty seed the function was
  structurally guaranteed to return an empty list forever regardless of whether its own proof helpers
  ever returned true. This bug predates this session entirely; unrelated to the Cells/claim-recovery
  fixes. Fixed by having the function independently append fresh evidence entries for each proven
  direction, mirroring the already-correct barcode/html/3d/cells corroborators' pattern, while still
  upgrading and sanitizing any pre-seeded records exactly as before. Verified directly against the
  real Email repository: now correctly emits CFB import/export, MSG import/export, and EML export
  (EML has no import capability, matching its source). Updated
  `test_unsafe_and_unrecognized_preapproved_records_are_sanitized`, which previously only passed
  because the function could never independently prove anything, to check the two specific
  still-unproven pre-seeded records by identity instead of asserting the whole result list stayed
  empty. Commit `a68ef2253`.

- [master facts font slides tex words] **2026-08-11 — Registered format corroborators for Font, Slides, TeX, Words; confirmed PSD and TeX as genuine external blockers.**
  Continued the pattern found for Cells (missing) and Email (structurally broken): none of the
  remaining Python families with real source had a registered corroborator. Investigated each
  repository's real, on-disk source and test suite, independently re-verified every cited
  class/method/test location directly against the checked-out source, and added:
  `python_font_format_functionality.py` (TTF/OTF/CFF/Type1(PFB)/Type1(PFA, import only)/WOFF/WOFF2/
  EOT, 14 directions, standalone OTF export and Type1 PFA export deliberately excluded as
  under-evidenced); `python_slides_format_functionality.py` (PPTX import+export, Markdown export
  only; every other declared SaveFormat/SourceFormat member confirmed absent, not merely
  unverified); `python_tex_format_functionality.py` (TEX import, DVI/PDF/SVG export — a
  one-directional pure-Python TeX engine); `python_words_format_functionality.py` (DOCX
  import+export, PDF export, Markdown import+export, DOC import only; DOC export/general RTF
  import/TXT confirmed absent or under-evidenced). While writing the TeX corroborator, direct
  `ast.parse()` against the real checked-out source repeatedly failed with `IndentationError` —
  root-caused to a genuine, repository-wide **upstream defect**, not a local artifact, confirmed via
  three independent methods (two independently-fetched local clones, a completely fresh `git clone`,
  and a raw HTTPS blob fetch bypassing git entirely, all byte-identical): every indentation level in
  the published source has collapsed to a uniform single space regardless of true nesting depth,
  making the repository genuinely unimportable as published. The corroborator's own fail-closed
  design already handles this safely (parse failures are caught as "no evidence", never crash) — no
  further code change needed; TeX is a genuine external/upstream blocker for this Python cohort, not
  an agent-fixable defect. Separately, PSD was found to have no source code at all in its current
  upstream state (a single README-only commit, confirmed via `git log`, `git ls-remote` against the
  live origin, and the pipeline's own prior `BLOCKED_MISSING_EVIDENCE` findings) — a second genuine
  external blocker. Commit `03d050442`.

- [master evidence pdf] **2026-08-11 — Re-closed PDF (cycle 6) after the corroborator-batch reopen.**
  PDF's acceptance was regressed to stale by commit `03d050442` (Font/Slides/TeX/Words format
  corroborators), which advanced the shared fact-acceptance contract for every Python repository
  even though none of the four new corroborators touch PDF's own format family. Re-approved and
  re-proven no-op with byte-identical candidate content, independently verified PASS a sixth time.
  Closed through the mission ladder again. Commit `fe28ca02c`.

- [master evidence page] **2026-08-11 — Re-closed Page (cycle 4) after the same corroborator-batch reopen.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a fourth time, including a positive re-derivation of
  `current_fact_acceptance_contract(ecosystem='python', family='page')` confirming the corroborator
  batch does not affect Page. Closed through the mission ladder again. Mission graph advances to
  `L8-VPY-03D-NOTE-CURRENT-REFRESH`. Commit `fd4394ec9`.

- [master evidence note] **2026-08-11 — Re-closed Note (cycle 4) after the same corroborator-batch reopen.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a fourth time, including the same positive contract-re-derivation check for Note. Closed through
  the mission ladder again. Mission graph advances to `L8-VPY-03E-3D-CURRENT-REFRESH`. Commit
  `32747aa12`.

- [master evidence 3d] **2026-08-11 — Re-closed Aspose.3D (cycle 4); closes the corroborator-batch reopen re-verification pass.**
  Re-approved and re-proven no-op with byte-identical candidate content, independently verified PASS
  a fourth time, including the same positive contract-re-derivation check for Aspose.3D. Closed
  through the mission ladder again. This closes the four-repository (PDF, Page, Note, Aspose.3D)
  corroborator-batch reopen re-verification pass; mission graph advances to
  `L8-VPY-03-ALL-PYTHON-VERIFIED-POC` for the remaining cohort (BarCode, Cells, Email already
  promoted but stale; Font, Slides, HTML, Words not yet processed; TeX and PSD confirmed external
  blockers). Commit `ce7050c3b`.
