# Project log — 2026-07-21

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-07-21 | master | #41, #42 | `AGT-007`, `EFF-004`, `GOV-018`, `MEM-005`, `ORC-005`, `VER-001`, `VER-002` | Wave 5, Wave 7, Wave 7b, Wave 8, Wave 8a, Wave 8b, Wave 9 | Full detail in decision 42 above; this entry is the Changelog's own at-a-glance record. Mid-design, the user shared a large external revi... |

| 2026-07-21 | master incident | — | `CAP-006`, `EFF-001`, `MEM-004`, `OPS-009`, `VER-001`, `VER-002`, `VER-003` | Wave 7, Wave 7g, Wave 8a, Wave 8e | (`aspose-cells-foss/Aspose.Cells-FOSS-for-Java`, `aspose-3d-foss/ Aspose.3D-FOSS-for-Java`), not the full 25-entry registry -- user direc... |

| 2026-07-21 | master incident | #9 | `OPS-009`, `ORC-005`, `VER-001`, `VER-002`, `VER-004` | Wave 7g, Wave 8 | (user directive: "prove against complete products.json," with a pilot rerun and a direct comparison against earlier runs, concrete eviden... |

| 2026-07-21 | master | — | `CAP-006`, `MEM-004`, `VER-001`, `VER-002`, `VER-005` | Wave 8, Wave 8d | **Result**: all 25 entries reached `CONVERGED_NO_CHANGE`, exit 0, on a single clean pass (`runs/full-registry-pass-2026-07-21/summary.tsv... |

| 2026-07-21 | master | #42, #43 | `GOV-010`, `GOV-019`, `GOV-020`, `GOV-022`, `VER-001`, `VER-002` | Wave 8, Wave 9 | New `GOV-022` (`GOVERNANCE.md` rule 11): before a new wave's work begins, the immediately preceding wave's actual delivered state MUST be... |

| 2026-07-21 | master requirements governance | #44 | `GOV-023` | — | Changelog relocated out of `master.md`/`requirements.md` into an indexed, sharded `logs/`; new `master.md` edit-confirmation gate (rule 12). |

| 2026-07-21 | master requirements | #40 | `SCL-004` | — | Decision #40 follow-up: the profiling cache made reachable, `SCL-004` built (`PARTIAL`), the "~1GB" byte-size claim corrected. |

| 2026-07-21 | master requirements | — | `OPS-009`, `OPS-010` | Wave 7h | `OPS-010` logged and partially fixed: bisected a real pytest hang (not the git-credential mechanism first suspected) to an unmocked GitHub-API branch in `get_product_facts`; fixed. A second, sequence-only hang in `create_work_clone()` found, not yet fixed. |
| 2026-07-21 | governance | #44 | — | — | `logs/` re-split from one monthly shard to daily shards after a blind-agent compliance test found it 3x over its own size guidance; split-threshold guidance recalibrated for how dense a single real day can get. |
| 2026-07-21 | requirements | #42 | `VER-004`, `VER-005` | Wave 8e | `VER-004`/`VER-005` (both found live during Wave 8e, logged `BACKLOG`) built, unit-proven, and live-proven; both move to `PARTIAL`. A first design draft went through two independent reviews (fact-check + adversarial) before shipping, both of which caught real bugs. |
| 2026-07-21 | requirements incident | — | `VER-004`, `VER-005` | Wave 8e | Asked to broaden pilot proof: `VER-004`'s self-heal direction proven live (second call hits the coarse shortcut, `STALE_NONCOMPLIANT` correctly carried forward) -- moves to `IMPLEMENTED`. `VER-005` repeated on a second real pilot with a different crash domain -- stays `PARTIAL`. A second `VER-004` pilot attempt was blocked by a recurring external origin-push-URL incident. |

## Entries

- [master] **2026-07-21 — Wave 8a-8d built and unit-proven; two immediate corrections landed first; an
  external whole-system review triaged rather than accepted wholesale (decision 42).** Full detail
  in decision 42 above; this entry is the Changelog's own at-a-glance record. Mid-design, the user
  shared a large external review of the whole project. Each checkable claim was verified against
  the actual repo rather than accepted on its own say-so: three of its seven P0 claims were refuted
  by direct evidence (a stale test-count claim -- the real `pytest -q` run was 551 passed, 0 failed;
  an idempotency-collision scenario already prevented by `commit_sha` inclusion in `facts_hash`; a
  governance-blocks-autonomy claim contradicted by every "managed remote" reference in this
  project's own docs being scoped to product repos, never this project's own state ref), two were
  already-tracked positions in this project's own history, and two were confirmed real and fixed
  immediately, standalone: an `org_repo` planner-trust gap (`supervisor/loop.py`'s `arguments.
  setdefault()` let a planner-supplied `org_repo` silently override the trusted active repo --
  now an unconditional override, `AGT-007`), and a CI git-identity gap (no shipped workflow
  configures one, confirmed no prior live proof ever ran on a genuinely fresh, hosted runner without
  one -- now set `--local` on every work clone).

  Wave 8's own first-round design (independent verifier, `VER-001`/`VER-002`) was then stress-tested
  the same way decision #41 stress-tested Wave 7's own first draft: two independent reviewers with
  no shared context, one fact-checking every claim against the real code, one adversarially hunting
  for rerun-consistency failures. **Both found defects serious enough that the first-round design,
  if built as written, would have crashed on every single run** -- `DomainStateV1.details` has no
  LangGraph merge reducer (a naively-written verify node would have erased `render_result` before
  `_commit_node`'s own assert ran, every run); `effect_ledger.py` wrote its pending entry before
  cheap validation checks ran (a pre-existing Wave 5 latent defect, `EFF-004`, that the new
  `verification_verdict` argument would have been especially likely to trigger); nothing bounded
  *repeated* failures across reruns (only within one run). All three fixed structurally, not
  patched around, before Wave 8b shipped.

  **Built and unit-proven this pass**: `state/domain_state.py::merge_details()`/`record_failure_or_
  reset()`/`save_domain_with_failure_tracking()`, `DomainStateV1.consecutive_failure_count`/`last_
  failure_reason`, `SupervisorStateV1.control_plane_fingerprint` + `compute_control_plane_
  fingerprint()` (closes a real `is_fresh()` blind spot to policy/prompt/capability-version
  changes), the `precheck()` mechanism, `MEM-005`'s partial fix, a `requirement_ids` drift test
  (8a); the ninth domain `independent_verification`, `verify_readme_candidate` capability,
  `readme_presentation`'s graph extended to `render -> verify -> commit -> record`, `commit_readme_
  write`'s new required `verification_verdict` argument -- closing the already-known defect (a
  `BLOCKED_VALIDATION_FAILED` candidate durably accepted unconditionally) as its own concrete,
  real-fixture regression proof, not a synthetic one (8b); evidence completeness, requirement
  mapping, adversarial cross-domain checks, failure-escalation visibility (8c); failure-escalation
  counting wired into `readme_presentation` (the other eight specialists are a named, deferred
  follow-up), planner-conversation surfacing, a distinct `escalation_alert` decision, and a
  run-level evidence-completeness gate (8d). `pytest -q` → **620 passed** (up from 551 before this
  whole pass: +2 for the two immediate corrections, +67 for Wave 8a-8d), 16 deselected (`live`,
  unchanged); `ruff check .`, `ruff format --check .`, `mypy src` all clean throughout.

  **Honestly unmet, not fabricated**: `VER-001`/`VER-002` reach `PARTIAL`, not `IMPLEMENTED` --
  `GOV-018` requires live proof, deferred to a consolidated pass (8e) alongside the explicit "full
  `data/products.json` pilot" ask and Wave 9's own portfolio proof, the same "one credential dance,
  not one per sub-wave" precedent Wave 7b-7e already established. The CLI path (`orchestrator.
  run_repo()`) commits with zero verification step -- `VER-001`'s guarantee covers the
  `supervise`-path specialist only, named explicitly as a new `BACKLOG` row (`ORC-005`) rather than
  implied closed. User directive throughout (the external-review triage, both adversarial-review
  passes, and every fix above were explicitly directed, not autonomously decided).


- [master] [incident] **2026-07-21 — A partial slice of Wave 8e's live-proof pass ran against the 2 real
  `mode: "full"` registry pilots** (`aspose-cells-foss/Aspose.Cells-FOSS-for-Java`, `aspose-3d-foss/
  Aspose.3D-FOSS-for-Java`), not the full 25-entry registry -- user directive (`AskUserQuestion`,
  "run the consolidated 8b/8c/8d live-proof pass now"). `readme-agent supervise --repo ... --durable-
  state` against both pilots, then a second call against `aspose-cells-foss` to exercise the
  freshness shortcut.

  **Confirmed live, real evidence**: (1) all 9 real domains (the new `independent_verification`
  ninth, alongside the 8 from Wave 7's own close) recorded `DomainStateV1` entries in
  `specialist_results.json` for both pilots with zero collision -- extends `CAP-006`/`MEM-004`'s
  existing 8-domain proof to 9, both rows' evidence text updated below. (2) `independent_
  verification`'s own audit ran for real on both pilots: `completeness: {}` (no missing evidence
  keys across any real domain), `requirement_map` populated for `CAP-006`/`EFF-001`/`VER-001`/
  `VER-002` with `exercised_without_error: true`, `checks_performed` confirming the full 8c scope
  (evidence completeness, requirement mapping, adversarial cross-domain checks) actually executed,
  not just registered. (3) `VER-003` convergence re-confirmed live with the new control-plane-
  fingerprint check active: the second call to `aspose-cells-foss` hit the coarse `is_fresh()`
  shortcut with zero planning calls -- direct proof the Wave 8a fingerprint fix does not introduce a
  false negative into the existing, already-live-proven fast path. (4) push-blocking safety
  (`origin` push URL `DISABLED`) confirmed intact on both real work clones touched; `git ls-remote
  origin "refs/readme-agent-state/*"` showed exactly the expected legitimate per-repo state refs,
  zero stray refs; the `OPS-009` credential workaround was applied once and removed immediately
  after (confirmed via re-check).

  **Honestly not proven by this pass, stated plainly rather than implied closed**: both pilots were
  already compliant from earlier Wave 7g proofs, so `render_readme_candidate` returned
  `needs_write=False` on both calls and `_verify_node`'s own by-design short-circuit meant
  `verify_readme_candidate` -- the actual pre-apply accept/reject gate `VER-001`'s core claim rests
  on -- was never dispatched. Neither a real accept nor a real reject of a genuinely new candidate
  has been observed live yet; the requirement_map's `exercised_without_error: true` for `VER-001`/
  `VER-002` reflects the design's own documented coarse, domain-level semantics (confirmed accurate
  by this pass), not proof the verify capability itself fired this run. The failure-escalation
  counter (8d) also saw no failures to count -- nothing to observe either way. The 3rd enabled
  registry entry (`mode: "dry_run"`) and the full 25-entry registry (8e's own full stated scope)
  were not attempted this pass. `VER-001`/`VER-002` stay `PARTIAL`; Wave 8e's Build Checklist box
  stays unchecked. User directive throughout; the GH_TOKEN exposure incident during this pass (a
  shell quoting mistake printed the token value once) was flagged immediately -- user chose to defer
  rotation until this work finishes rather than pause it.


- [master] [incident] **2026-07-21 — The exact gap the prior entry named ("neither a real accept nor a real reject
  observed live yet") closed for real, plus the explicit "full `data/products.json` pilot" ask,
  plus a direct before/after comparison against the pre-Wave-8b design** (user directive: "prove
  against complete products.json," with a pilot rerun and a direct comparison against earlier
  runs, concrete evidence, and an honest mixed-results summary if warranted).

  **Direct before/after proof of the `VER-001` gate itself, against a real registry pilot, not a
  synthetic fixture repo.** New harness (`scripts/retrofits/prove_verify_gate_live.py`, kept per
  `GOVERNANCE.md` placement rule 5): drives the REAL, imported (never reimplemented) `_verify_node`/
  `_commit_node`/`_record_node` from `specialists/readme_presentation.py` inside two hand-built
  graphs -- a "BEFORE" graph (`render -> commit -> record`, exactly Wave 7g's pre-8b shape, verify
  omitted) and the current "AFTER" graph (`render -> verify -> commit -> record`) -- against the
  same forced-real render (`render_readme_candidate`'s own already-declared `force_regenerate`
  input plus `llm_mode="fixture"`, both legitimate pre-existing capability inputs, not a hack; an
  in-memory `FakeStateBackend` so this probe never touches this project's real remote state ref).

  **Reject path, against `aspose-cells-foss/Aspose.Cells-FOSS-for-Java`'s real content**: a
  deliberately non-compliant fixture LLM response (containing the real policy's prohibited term
  "no bugs") produces a genuine `BLOCKED_VALIDATION_FAILED` from the real validation registry.
  **BEFORE**: the bad candidate is WRITTEN to the real work clone's `README.md` (confirmed on disk,
  10157 chars including the prohibited-term text) and `accepted_status` is set to a clean
  `'FIRST_OBSERVATION'` -- durably marking a rejected candidate as accepted, exactly the already-
  known defect this row describes, now reproduced live rather than only in a synthetic unit
  fixture. **AFTER**: `_verify_node` independently re-derives, rejects with `accepted_status =
  "ERROR:verification_rejected:needs_write is True but claimed status='BLOCKED_VALIDATION_FAILED'
  is not GENERATED"`, `_commit_node` never runs, and `README.md` is never written (confirmed absent
  on disk). No git commit was ever at risk either way (`commit_generated_readme()`'s own
  `status == "GENERATED"` gate already blocked that) -- the concrete harm the fix prevents is the
  silent durable-state corruption and the corrupted file write, not an errant commit.

  **Accept path, same pilot, a genuinely compliant forced regeneration**: `verify_readme_candidate`
  returns `verdict: "accept"`, `requirement_map: {license_mentioned: true, products_org_link: true,
  products_com_link: true, relationship_explained: true}` (all four populated, not empty, since this
  candidate actually reached `GENERATED`), `commit_readme_write` dispatches for real, and a REAL
  local git commit landed in the disposable work clone (`git log`: `readme-agent: close promotional
  gaps (11407c2e0372)`, authored by the Wave 8 CI-identity fix's own `readme-agent` identity) --
  confirmed `origin`'s push URL still `DISABLED` throughout, never pushed. This is the first time
  `verify_readme_candidate` has been observed live returning `accept` against a real, non-fixture
  candidate. `VER-001`/`VER-002` move to **`IMPLEMENTED`** on this evidence -- both the accept and
  reject paths of the one real write's independent gate are now proven live, not just unit-proven,
  against a real registry pilot; the CLI-path gap (`ORC-005`) is unchanged and still open.

  **A genuine, unplanned finding surfaced while diagnosing an initial false negative on
  `aspose-3d-foss`**: `aspose-3d-foss/Aspose.3D-FOSS-for-Java`'s actual, current real upstream
  README -- re-validated directly against this project's own validation registry for the first
  time in this pass -- fails `commercial_mention_discipline` (3 commercial links formatted as a
  bulleted list, decision #9). Every prior live-proof pass reported this repo as compliant
  (`CONVERGED_NO_CHANGE`) purely via `orchestrator.py::prepare_readme_candidate()`'s `durable_skip`
  fast path (a matching `accepted_facts_hash`/content fingerprint trusts the last accepted status
  without re-validating current content) -- this pass is the first time this repo's real content
  was ever actually re-run through the real validation registry since whatever point it was first
  accepted. **This is not a Wave 8 regression** (nothing in Wave 8 changed `durable_skip` or this
  repo's content) but a real, previously-invisible structural gap: `durable_skip` has no expiry and
  no periodic re-validation, so a repo's "compliant" status can go stale indefinitely relative to
  the current validation ruleset (e.g. after decision #9's own rule tightened) without ever being
  caught, for as long as upstream content and the control-plane fingerprint stay unchanged. Logged
  as a new `BACKLOG` row (`VER-004`) rather than fixed here -- out of scope for this pass, and a
  periodic-revalidation design deserves its own consideration, not a rushed fix under this pass's
  own time pressure.

  **Full-registry pass** (all 25 `data/products.json` entries, `--durable-state`, the `OPS-009`
  credential workaround applied once for the whole pass and removed immediately after): see the
  next Changelog entry for results, logged separately once the pass completed given its size.

  User directive throughout (`AskUserQuestion`/explicit instruction); the `GH_TOKEN` used remained
  the same one flagged as exposed earlier in this session and still not yet rotated at the time of
  this pass -- the user was reminded again after this pass completed.


- [master] **2026-07-21 — The explicit "full `data/products.json` pilot" ask closed: all 25 real registry
  entries run through `supervise --durable-state` for real, plus one real code fix found live and
  a set of operational incidents found, fixed, and honestly logged rather than smoothed over.**

  **Result**: all 25 entries reached `CONVERGED_NO_CHANGE`, exit 0, on a single clean pass
  (`runs/full-registry-pass-2026-07-21/summary.tsv`, 25 rows, no duplicates). The 3 non-`disabled`
  entries (`aspose-3d-foss/...Java`, `aspose-cells-foss/...Java`, `aspose-pdf-foss/...Java`, the
  `dry_run` entry) each recorded all 9 real domains with zero collision, extending `CAP-006`/
  `MEM-004`'s proof to its largest scale yet. The 22 `mode: "disabled"` entries correctly `ERROR`'d
  `metadata_presentation`/`visual_preparation` for the expected, documented `missing_policy_profile`
  reason on every entry, exactly as designed.

  **Getting a trustworthy result took three attempts, not one -- both real infrastructure problems
  found and fixed, not hidden**:
  1. The first attempt failed on every entry: this project's own `origin` remote had its **push**
     URL set to `DISABLED` -- the exact marker `gitsafety/neuter.py` uses on *work clones*, found
     instead on this project's own repo (`git config`: `pushurl = DISABLED`), blocking every
     durable-state push regardless of credentials. Root cause not conclusively pinned down (most
     likely an earlier test run or exploratory command in this session applying the work-clone-only
     mechanism to the wrong path) -- fixed by restoring the push URL to match the real fetch URL.
  2. The second attempt was stopped via `TaskStop` partway through (once the first problem was
     found) and restarted -- but `TaskStop` did not actually terminate the underlying `python.exe`
     child process on this platform: it kept running in true, uncoordinated parallel with the
     restarted pass, racing on the same baseline/work-clone directories and the same durable state.
     This produced corrupted, duplicate-entry results (confirmed via `runs/full-registry-pass-.../
     summary.tsv` showing 42 rows for 25 repos) and several spurious "crash" tracebacks
     (`force_rmtree`/git-clone collisions between the two processes touching the same directory) --
     all resolved as a self-inflicted concurrency artifact, not a real product defect, once both
     zombie `python.exe` processes were found via direct process inspection
     (`Get-CimInstance Win32_Process`) and killed. **Operational lesson, not a code fix**: this
     harness's `TaskStop` on a backgrounded shell command is not a reliable process-kill guarantee
     on this platform -- a genuinely stopped background task must be verified by inspecting the
     actual OS process list, not assumed from the tool's own "stopped" acknowledgment.
  3. The third attempt (a single, verified-clean process, log directory wiped first) produced the
     trustworthy 25/25 result reported above.

  **A real code defect found live and fixed during this pass**: `readme_presentation`'s
  failure-escalation counter (Wave 8d, `VER-002`) reached `consecutive_failure_count: 2` (of the
  threshold-3 `escalation_alert`) for `mode: "disabled"` entries across repeated runs this
  session -- one run away from a false alarm firing simultaneously across all 22 disabled entries.
  Root cause: `readme_presentation`'s own `require_permitted()` call raises `NotAllowlistedError`
  for a disabled-mode entry, a *different* message (`"...is not in data/products.json with an
  enabled mode..."`) than `missing_policy_profile`'s `ValueError`, so it fell through the existing
  `NON_ESCALATING_FAILURE_REASONS` carve-out uncaught -- exactly the false-escalation failure mode
  Wave 8's own plan named as this pass's required validation, just via a different real error class
  than the one first anticipated. **Fixed**: `state/domain_state.py::_classify_error_reason()`
  recognizes this exact wording (safe because, by the time a specialist's own `require_permitted()`
  raises it, `supervise_repo()`'s upstream `require_listed()` entry gate has already confirmed the
  repo IS listed -- the only remaining cause left is `mode == "disabled"`) as a new
  `NON_ESCALATING_FAILURE_REASONS` member, `"disabled_mode"`, alongside `missing_policy_profile`.
  Regression-tested (`tests/unit/test_domain_state.py::TestFailureEscalation::
  test_disabled_mode_never_escalates_regardless_of_run_count`,
  `TestClassifyErrorReason::test_recognizes_a_disabled_mode_registry_entry_by_content`). `pytest -q`
  → **622 passed** (up from 620), 16 deselected; `ruff check .`, `ruff format --check .`, `mypy src`
  all clean.

  **A genuine, unplanned structural finding, not fixed here**: per-repo `domain_states` coverage
  for several of the 22 `disabled`-mode entries is visibly incomplete beyond the expected
  `missing_policy_profile` two (e.g. `readme_reconciliation`/`readme_presentation`/
  `package_release_audit`/`community_files_presentation` absent for specific repos, verified by
  directly loading each repo's real durable state) -- consistent with the incident #2 concurrency
  collision above having caused a one-time failure to record for those specific domain/repo pairs,
  and the coarse `is_fresh()`/`CONVERGED_NO_CHANGE` shortcut (unchanged upstream + unchanged
  control-plane fingerprint) then never giving the specialist tier another chance to retry them on
  this pass's own later, clean runs -- the fast (4-8s) elapsed time recorded for exactly these
  repos in the clean pass is consistent with this explanation (a shortcut replay, not fresh work),
  though not independently re-confirmed by clearing state and re-running (a real, destructive
  action on this project's own durable state ref, not taken without separate explicit direction).
  **Logged as a new `BACKLOG` row (`VER-005`)**: the coarse freshness shortcut has no way to know
  whether every domain from a prior run actually succeeded in recording its own result -- a
  crash/collision affecting only some domains, followed by an unchanged upstream, can leave
  `domain_states` coverage permanently incomplete for a repo with no way to self-heal short of an
  upstream change or a manual state-ref reset. Not proven to be Wave 8's own defect (the collision
  cause was this session's own concurrency incident, not the shipped code path under normal single-
  process operation) but a real gap in what the coarse shortcut can see, worth its own design
  consideration rather than a rushed fix here. The 3 real pilots' full 9/9 coverage (unaffected by
  the collision, run early/cleanly each time) and the dedicated, isolated `prove_verify_gate_live.py`
  before/after proof (a fresh in-memory backend each run, immune to this durable-state residue
  entirely) remain the rigorous, uncontaminated evidence for `VER-001`/`VER-002`'s own core claims.

  User directive throughout (`AskUserQuestion`: "prove against complete products.json"); `pytest -q`/
  `ruff`/`mypy` re-verified clean after the `disabled_mode` fix, not assumed from the pre-fix
  baseline.


- [master] **2026-07-21 — Wave-entry reconciliation gate (decision 43).** New `GOV-022`
  (`GOVERNANCE.md` rule 11): before a new wave's work begins, the immediately preceding wave's
  actual delivered state MUST be checked against `plans/master.md` (its Decision Ledger entry,
  Status, Build Checklist line) and `plans/requirements.md` (every requirement row it touches),
  with any overclaim corrected or logged as `BACKLOG` before the new wave extends the affected
  surface. Distinct from `GOV-010`'s existing phase-close review — this is an entry gate on the
  *next* wave. Motivated by this project's own accumulated coverage gaps at wave boundaries
  (`GOV-019`, `GOV-020`) and Wave 8's late-caught design defects (decision #42) that an earlier
  reconciliation would have surfaced sooner. First application: Wave 9 must open by reconciling
  Wave 8's `PARTIAL` `VER-001`/`VER-002`, the logged CLI zero-verification-step gap, and the
  `GOV-019`/`GOV-020` coverage gaps, before any Wave 9 code is written. `AGENTS.md` carries the
  working-rule summary. User directive.


- [master] [requirements] [governance] **2026-07-21 — Changelog relocated to `logs/`; `master.md`
  gated behind an edit-confirmation rule (decision #44).** `master.md` had grown to 3,761 lines
  (1,451 pure historical narrative) and `requirements.md`'s own 660-line Changelog cross-referenced
  it constantly. Both relocated verbatim into this indexed, dated-shard `logs/` directory
  (`logs/README.md` + `logs/<YYYY-MM>.md`), merged chronologically and source-tagged
  (`scripts/retrofits/relocate_changelogs_to_logs_dir.py`, self-verifying entry counts and content
  before writing anything — nothing lost). New `GOVERNANCE.md` rule 12 / `GOV-023`: `master.md`
  edits now require the same explicit, per-instance user confirmation already required for pushes;
  `logs/` stays freely, immediately agent-appendable, no gate. Two rounds of user feedback shaped
  the design: a flat log file would just become the next 3,000-line file (sharded instead), and
  the log lives at repo-root `logs/`, not nested under `plans/` (required amending GOVERNANCE.md's
  closed root-set table first, per its own placement rule 1). User directive.


- [master] [requirements] **2026-07-21 — Decision #40 follow-up: the profiling cache made
  reachable, `SCL-004` built (`PARTIAL`), the "~1GB" byte-size claim corrected.** A live pilot-proof
  rerun (isolated worktrees, `c25008d` vs `2086841`, real clones against `aspose-page-foss`, kept
  deliberately separate from a second, concurrent Claude Code session's own in-flight edits to this
  same working tree) confirmed decision #40's mode-gate split and SHA-freshness cache both work as
  designed -- 0 regressions, +14 tests, a 182s→0.99s cache-hit reduction, reproduced twice -- and
  surfaced two follow-up items, both closed this pass.

  **Byte-size correction**: the worktrees' actual shallow-clone payload was 5.2MB/25MB, not the
  "~1GB" `master.md`/`requirements.md` cited -- that figure is GitHub's total `.git` history size,
  never touched by a `--depth 1` clone. `SCL-004`'s text and decision #40's own text both corrected;
  the real cost driver is far more likely GitHub server-side shallow-pack computation and
  connection/protocol overhead, which better explains the wide variance observed for the identical
  repo across independent runs (158s-211s this session, 199.2s in an earlier one, 1004s in one
  externally reported run) than a bandwidth theory would.

  **Cache made reachable** (decision #26(b) compliance): `profile.cached.get_or_build_profile()`'s
  prior `state_backend` parameter was refactored to plain `prior_upstream_revision`/
  `prior_profile_result` values, matching `render_readme_candidate.py`'s own established
  stateless-capability convention exactly (it predated that convention being this explicit). New
  `orchestrator.py::profile_repo_with_cache()` owns the load-before/write-back-after a capability
  may no longer do itself -- a real bug (persisting the wrong SHA on a cache miss) was found and
  fixed during this refactor, before it ever shipped. New `orchestrator.py::
  run_registry_profiling_sweep()` plus CLI verb `profile-registry` are the first thing that actually
  loops the whole registry calling profiling with the cache engaged -- nothing did before.

  **`SCL-004` built, not yet `IMPLEMENTED`**: `github_api.client.get_tree()`/`get_file_content()`,
  `inspection/tree_paths.py`'s git-tree-based manifest matcher (sharing `file_inventory.py`'s
  matching rule via new `resolve_manifest_candidates()`), and `get_or_build_profile()` trying this
  API-first path before falling back to the proven clone path on any failure/truncation/no-token.
  Unit-tested; live proof against a real large repo's actual cold-call cost remains outstanding,
  honestly left `PARTIAL` rather than claimed early.

  Also: `plans/investigations/tools/measure_profile_latency.py` (relocated + generalized from
  `scripts/retrofits/verify_profile_cache_against_aspose_page_foss.py`, which was in the wrong
  placement-rule home for a repeatable diagnostic) and a new `workflow_dispatch`-only
  `.github/workflows/measure-clone-latency.yml` for gathering real GitHub Actions runner variance
  data, deliberately not scheduled (`GOVERNANCE.md` rule 10). `ruff check .`, `ruff format --check
  .`, `mypy src`, `pytest -q` clean throughout. User directive.


- [master] [requirements] **2026-07-21 — `OPS-010` logged and partially fixed: a real, bisected
  `pytest -q` hang, not the git-credential mechanism first suspected.** Verifying the `logs/`
  relocation (decision #44) surfaced `pytest -q` giving inconsistent pass counts (622/623) and one
  run hanging 4+ minutes with no error. Initial hypothesis -- ambient `GH_TOKEN`/`GITHUB_PAT`
  triggering the same silent-hang mechanism `OPS-009` already documents for a real `git push` --
  was tested empirically (`tests/unit/test_gitsafety.py` alone, 3/3 clean) and refuted, not assumed.
  Bisected for real via `faulthandler.dump_traceback_later()` stack dumps (pytest's own fd-capture
  swallows the dump unless run with `-s`) across `tests/unit/test_supervisor_loop.py`'s 26 tests.

  **Root cause 1, confirmed and fixed**: `visual_preparation` (Wave 7h) dispatches
  `get_product_facts`, whose `profile.cached.get_or_build_profile()` takes a real GitHub-API branch
  whenever `env.gh_token()` is truthy (`SCL-004`, decision #40 Part F) -- both `test_supervisor_
  loop.py`'s `project` fixture and `test_specialists.py`'s `TestVisualPreparationSpecialist` fake
  every *other* specialist's network call (7b-7g) but never wired this one, so on any machine with
  a real token already set (routine after this project's own live-proof sessions) these
  documented-"no network" tests silently hit the real network against a fake `org_repo`, hanging at
  DNS/connect with no timeout ever reached. Fixed both files with the exact
  `monkeypatch.setattr(cached.env, "gh_token", lambda: None)` pattern `test_profile_cached.py`
  already established for the identical problem -- reused, not reinvented (`GOV-015`). Confirmed:
  the specific test that first hung now passes standalone and as part of its class; `test_
  specialists.py` (46 tests) passes clean.

  **Root cause 2, found, not yet fixed, honestly left open**: a second, distinct hang -- a real
  `subprocess.run()` inside `gitsafety.clone.create_work_clone()`'s local-to-local `git clone`,
  stuck in `communicate()`'s reader thread well short of its own 120s timeout -- reproduces only
  when the full `test_supervisor_loop.py` file runs as a sequence; the exact same test passes clean
  and fast in true isolation every time it was tried. Not a git-credential issue (both source and
  destination are local paths); candidates are Windows-specific (antivirus/file-lock contention
  accumulating across many real git subprocesses in one process, or an orphaned grandchild process
  outliving a `Popen.kill()` and keeping the pipe open). Logged as `OPS-010` (`PARTIAL`, not
  `IMPLEMENTED`) rather than claimed fully resolved -- per this session's own `GOV-014` discipline,
  not chased into an open-ended platform-level investigation under this pass's own scope. User
  directive ("add it as backlog and bisect the hang").

- [governance] **2026-07-21 — `logs/` re-split from one monthly shard to daily shards (decision
  #44 revised in place).** The blind-agent compliance test run to prove decision #44 found
  `logs/2026-07.md` already 2,344 entry-lines -- 3x its own `logs/README.md`'s ~800-line split
  guidance -- and the user asked for exact `YYYY-MM-DD` granularity instead of monthly. Split via
  `scripts/retrofits/split_logs_shard_into_daily_files.py` (same self-verifying pattern as the
  original relocation: entry and index-row counts asserted equal before writing, nothing reworded
  or reordered) into `logs/2026-07-17.md` through `logs/2026-07-21.md` (5/26/43/22/9 entries).
  `logs/README.md` rewritten for day granularity and its split-threshold guidance recalibrated:
  ~800 lines is the *typical-day* target, not a hard cap -- 2026-07-19 alone is 974 lines of real,
  coherent single-day narrative and is documented as an accepted, named exception rather than
  force-split across an arbitrary sub-day boundary; only a day clearing roughly double that
  (~1,500) gets a further `-a`/`-b` split. Cross-references fixed to match: `GOVERNANCE.md` (Fixed
  homes clause + repository-layout table row), `AGENTS.md` (repo-layout line + Spec governance
  section), `docs/architecture.md` (the 2026-07-18 sprint-reset pointer, now `logs/2026-07-18.md`),
  and `plans/master.md`'s own decision #44 text -- edited under the very edit-confirmation gate
  that decision created, per this plan's own explicit up-front approval (GOVERNANCE.md rule 12).
  User directive.

- [requirements] **2026-07-21 — `VER-004`/`VER-005` built, unit-proven, and live-proven; both move
  `BACKLOG` → `PARTIAL`.** Both rows were logged during Wave 8e's live-proof pass, deliberately not
  fixed under that pass's own time pressure. Asked to design a fix ("do your best effort"), then to
  execute it. Design went through two independent reviewers before any code was written -- one
  fact-checking every claim against the real code, one adversarially hunting for rerun-consistency
  breaks -- mirroring this project's own established review discipline (decision #42's own two-pass
  precedent). **Both reviewers found real, concrete bugs in the first design draft, not stylistic
  nits**: a proposed new `RunStateV1` field for `VER-004` would have silently missed the
  `readme_presentation` specialist's own stateless durable-skip path entirely (it round-trips
  through `DomainStateV1.details`, not `RunStateV1`'s flat fields); a proposed unconditional
  per-domain persist retrofit for `VER-005` would have added up to 8 new `acquire_lock`/fetch/push
  cycles per run to every structurally out-of-scope repo (22/25 of the registry); and computing
  `VER-005`'s own completeness signal from the in-memory `specialist_results` dict (rather than a
  fresh backend reload) would have made the fix tautologically always-true, silently defeating
  itself while looking correct on paper -- both reviewers converged on this exact same finding
  independently. The corrected designs avoid all three.

  **`VER-004`** (`durable_skip` blind to a validation rule-code change): rather than invent a new
  field/hash/plumbing, reuses the existing `GENERATION_SCHEMA_VERSION` manually-bumped-constant
  convention -- a new `validation/registry.py::VALIDATION_RULESET_VERSION`, folded into
  `readme/facts.py::RepositoryFacts`/`_HASH_FIELDS` (rides the already-correct, already-unconditional
  `accepted_facts_hash` round-trip, so it self-heals in exactly one recheck on both the CLI path and
  the `supervise`-path specialist) and into `supervisor/convergence.py::compute_control_plane_
  fingerprint()` (closing a related gap found while implementing: without this, `is_fresh()`'s own
  coarse shortcut would keep skipping the specialist tier after a rule-code change, never giving
  `durable_skip` a chance to notice at all). A forget-proofing test tripwire
  (`_RULES_SOURCE_HASH_AT_VERSION`, `tests/unit/test_validation_registry.py`) fails loudly if a rule
  module's source changes without a conscious version-bump decision -- deliberately the one place in
  this design where an over-sensitive content hash (rejected as a *production* mechanism by both
  reviewers) is exactly the right tool, since it's a one-time, human-reviewed CI gate, not a live
  per-request comparison.

  **`VER-005`** (coarse shortcut blind to incomplete `domain_states` coverage): new
  `SupervisorStateV1.domain_coverage_complete`, computed from a fresh `state_backend.load()` after
  the specialist tier completes, checked by a new opt-in `check_domain_coverage` parameter on
  `is_fresh()` (mirrors exactly how the Wave 8a control-plane fingerprint was added). Cost-mitigated:
  a new `state/domain_state.py::merge_unrecorded_failures()` folds only the domains that never
  durably recorded themselves this run into the *existing* `SupervisorStateV1` write, rather than
  giving all 8 non-`readme_presentation` specialists their own unconditional per-domain persist --
  zero added cost on a healthy run, one enriched existing write on a run with failures. Also fixed:
  `supervisor/loop.py`'s own crash placeholder used a bare `"ERROR"` (no colon), which would have
  made `record_failure_or_reset()` treat a genuine crash as a clean accept once folded through this
  same mechanism.

  **Live-proven, both fixes, against real registry pilots**: `VER-004` -- a real `supervise
  --durable-state` run against `aspose-3d-foss/Aspose.3D-FOSS-for-Java`, immediately after the fix
  shipped (the field's mere introduction was enough to force one honest recheck, no artificial
  version bump needed), correctly re-validated the real content and recorded `readme_presentation`'s
  `details.render_status` as `STALE_NONCOMPLIANT` -- the real, previously-masked defect Wave 8e found,
  now durably visible instead of silently trusted. `VER-005` -- a real, deliberately-injected
  specialist crash against `aspose-cells-foss/Aspose.Cells-FOSS-for-Java`'s real clone
  (`scripts/retrofits/prove_domain_coverage_live.py`, an in-memory backend so the probe never touches
  this project's real remote state) was correctly captured, classified with the colon convention, and
  durably recorded rather than silently lost.

  **Honestly unmet, not fabricated**: both stay `PARTIAL`, not `IMPLEMENTED`. `VER-004`'s
  self-healing loop (does the fix stay correctly classified on a *subsequent* unchanged rerun) has
  not been re-proven with a second live call. `VER-005`'s "domain never even attempted" scenario (a
  killed process, as opposed to a crash the loop's own except-block catches) is proven only via
  direct state corruption in `tests/unit/test_supervisor_loop.py`, not reproduced live -- doing so
  safely would require killing a real process mid-run, not attempted here. `pytest -q` → 657 passed
  (up from 622 before this pass), 16 deselected; `ruff check .`, `ruff format --check .`, `mypy src`
  all clean throughout. User directive.

- [requirements incident] **2026-07-21 — `VER-004`/`VER-005` proof broadened to a second real pilot
  and a second direction; `VER-004` moves `PARTIAL` → `IMPLEMENTED`.** Asked to "prove your last
  enhancements with pilots" (plural) -- the single-pilot, single-direction proofs from the prior
  entry were real but left two things unconfirmed: whether `VER-004`'s forced recheck actually
  self-heals (or re-validates forever), and whether either fix generalizes past the one pilot each
  had been shown on.

  **`VER-004` self-heal, proven live**: a second `supervise --durable-state` call against
  `aspose-3d-foss/Aspose.3D-FOSS-for-Java` (now durably recording `STALE_NONCOMPLIANT` from the
  first call) correctly hit the coarse `is_fresh()` shortcut --
  `"upstream unchanged since last converged run... zero planning calls"`, zero specialist dispatch
  -- rather than re-validating on every call forever. Checked the real durable state directly
  afterward: `readme_presentation.details.render_status` still reads `STALE_NONCOMPLIANT`, correctly
  carried forward rather than silently reverting to a false "compliant" reading once the shortcut
  took over. This was the specific gap the prior entry left open; `VER-004` moves to `IMPLEMENTED`.

  **`VER-005` broadened to a second pilot**: `scripts/retrofits/prove_domain_coverage_live.py`
  re-run against `aspose-3d-foss` (the first run used `aspose-cells-foss`) with a different domain
  forced to crash (`package_release_audit` instead of `readme_reconciliation`) -- identical, correct
  result (colon-convention classification, durable recording, no silent loss). Stays `PARTIAL`: the
  one specific named gap (a killed-process scenario, as opposed to a crash the loop's own
  except-block catches) remains unproven live, by design -- not safely reproducible without
  literally killing a real process mid-run.

  **A second `VER-004` pilot attempt (`aspose-cells-foss`) was blocked, not fixed around**: this
  project's own `origin` remote had its push URL reset to `DISABLED` -- the same symptom as the
  incident logged during Wave 8e's full-registry pass, but this time recurring *twice* within
  seconds of being fixed, including once immediately after a fix applied in the same command
  invocation as the retry. No python/pytest process was found running against this repo at the
  moments checked (`Get-CimInstance Win32_Process`), so the responsible process had already exited
  each time -- consistent with a concurrent, unrelated process (outside this session's own control)
  touching this same shared git config, not a defect in this session's own code (`gitsafety/
  neuter.py::neuter_push()` is only ever called with an explicit work-clone path, never this
  project's own `cwd`, confirmed by direct trace both times). Restored the correct push URL each
  time; did not keep contesting it once the pattern was clear, since actively fighting another
  process for shared, mutable state is not productive -- the `aspose-cells-foss` `VER-004` repeat
  was set aside rather than forced through. `VER-005`'s second-pilot proof was unaffected, since its
  harness uses an in-memory backend with no `origin` push at all. `OPS-009` credential workaround
  applied once for this pass and removed immediately after; `git ls-remote` confirmed no stray refs
  (25, matching the full registry, unchanged). User directive ("prove your last enhancements with
  pilots").
