# Project log — 2026-08-19

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-19 | master | — | — | — | 55 commits; page-python BLOCKED->AGENT_APPROVED, slides S5 closed, E1/E2/E3 live-confirmed, E5 honestly mixed, lockfile hash-pinned; boundary suite 4021/1. |
| 2026-08-19 | master | — | — | — | Lane A/B/C merged (dev-deps H3, Key-Capabilities soft-wrap+cap fix, note fixture-substitution); two-gate finding surfaced a real upstream binding gap (api-surface-evidence lane) and a selection-ranking gap (Quick Start), both honestly scoped out, not absorbed. |
| 2026-08-19 | master | — | — | — | Lane D/E merged (function-level API-surface claim binding; targeted runtime example verification). Barcode's dense capability claim now mechanically binds where it previously returned None. Note-python's blocking claim traced to a genuine upstream README defect (`page.Title` is `None`), correctly left unpromoted, not forced closed. |
| 2026-08-19 | master | — | — | — | Mission-recovery resumed: docs caught up, Docker restarted to unblock isolated-execution verification, runner-readiness Finding #2 redaction half closed, a 4th pre-existing plan-hash characterization drift baselined. |
| 2026-08-19 | master | — | — | — | Runner-readiness Finding #3 closed (candidate upload); Q2 closed live (cells-python API Reference 56->includes Workbook/Worksheet again); redundant PyPI Requires badge suppressed; S12 signature and a vendored-scripts upstream-drift coverage gap documented. |
| 2026-08-19 | master | — | — | — | Lane F merged (`9f656b380` -> main, rebase+ff): resolves font-python's `font.convert(target)` instance-style-reference and import-statement gaps Lane D flagged as needing a different mechanism; live-verified against real cached facts. |
| 2026-08-19 | master | — | — | — | S12 blocked-diagnostics persistence bug found and fixed: claim_accountability/source_claim_resolutions/candidate_sha256 were read from the wrong nested dict (always null); fix also adds the full composition_ledger, closing the "no way to inspect a blocked attempt's ledger" gap. Bounded retry-blocked portfolio pass started to force fresh evidence. |
| 2026-08-19 | master | — | — | — | S12 root-caused and fixed: fresh evidence showed the "unbound" segments are the three real Dependencies H3 sub-headings Lane A/F added today without registering them as governed template structure. Added to `_GOVERNED_STRUCTURAL_LINES`; regression test proven to fail-then-pass; full suite 5/4084 (all pre-existing baseline, zero new regressions). |
| 2026-08-19 | master | — | — | — | S12 fix corrected: a second retry-blocked pass proved the first fix real but incomplete (cells-python 3->1 unbound); two fixed lead-in sentences also needed governing, plus a third proactively. Regression test strengthened to the real segment shape. barcode-python confirmed advancing past S12 to a later, unrelated stage. |
| 2026-08-19 | master | — | — | — | Full disposition-context wiring audit (5 call sites, gates 2/3 + idea_candidate.py fixed, 1 lead documented) closes the chain S12 opened. Live-confirmed: barcode-python reaches AGENT_APPROVED. 3D-Python re-proving after the shared verification-contract change (expected, not a regression). Six real defects fixed and regression-tested this session. |
| 2026-08-19 | master | — | — | — | Shared claim-disposition ratchet backfill fix lands and is confirmed working; surfaces a distinct, deeper equivalence-linkage gap between source-stage and candidate-stage claim records (not fixed, precisely documented). Terminal confirmation: 3/33 Python repos (3D, barcode, cells) reach NO_OP_PROVEN, up from 1/33 at session start. Seven real defects fixed and regression-tested this session. |
| 2026-08-19 | master | 106 | KNOW-001..012 | — | Native knowledge-application layer: imported aspose.org claim corpus (all 12 kinds, 97,303 claims) now indexed, freshness-gated, and bound-selected into ProductFactsV2; checksum-bound corpus manifest feeds a new fact-acceptance component; knowledge-application.json evidence artifact; vendored 89-check battery classified, 10 checks promoted to blocking with zero regressions verified against the 3 accepted candidates and the full relevant unit-test suite. |
| 2026-08-19 | governance | 107 | — | — | Control-repo auto-push: a `post-commit` hook (`scripts/governance/post_commit_push.py`) now pushes this repository's own landed commits to its own `origin` automatically, no separate confirmation, never force-pushing; GOVERNANCE.md rules 10/19 carved out and extended accordingly; the product-repo write path (`open_presentation_pr`, `AUTH-004`) is entirely unchanged. |
| 2026-08-19 | governance | — | — | — | Decision #107's post-commit push hung indefinitely on this environment's default git-credential-manager (no cached credential, falls back to an unanswerable interactive askpass prompt); fixed with a local-only `credential.helper` override to `gh auth git-credential`, verified with `git ls-remote`, protects every session sharing this checkout's `.git/config`. |

## Entries

- [master] **2026-08-19 — Mission recovery closeout: 4 parallel-lane merges, live-verified fixes, Decision #105** Mission-recovery session closeout (55 commits, `93fbe707c..f4fd365bc`). Full report:
`plans/investigations/evidence/mission-recovery-2026-08-18/final-report.md`.

Four parallel-lane implementations landed via isolated worktrees, each rebased onto the latest
main, reviewed diff-by-diff, gate-tested in its own venv, and merged one at a time with a
post-merge boundary check — per explicit operator instruction that main always hold only
verified, working state:

1. **Mission-graph loader fix** (`1a7992e27`) — closes the L8-HORIZON-01 deferral finding: a
   deferred-resolved dependency must carry terminal `CLOSED` status, not just membership.
2. **E5 slice 1** (`ead067890`/`8478f5a3c`/`7a1070076`) — closes the `verified_against_source`
   README-circularity soundness hole; adds a corroborated `excluded_with_reason` classification;
   adds a portfolio-shared read-through ratchet. Live-verified as claim-shape-dependent: closed
   barcode-python's dev-tooling claim live, did not close note-python's fixture-dependent
   code-block claim (the model reached for a different, plausible-but-inexact classification
   that corroboration correctly refused) — both outcomes honestly documented, including a
   self-correction after an initial flawed reproduction (used the source README as a stand-in
   candidate, discovered and retracted).
3. **Lane A/B deterministic fixes** (`138dfb3f4`/`79c6fb4b7`/`a39b3547c`) — renders the
   empty-Dependencies section; admits properties into the API Method Index slot; fixes the
   `Documentation and Resources` → `Documentation & Resources` heading divergence, including a
   real dead-anchor-key bug the heading fix surfaced. Live-verified: page-python fully unblocked
   BLOCKED→AGENT_APPROVED; slides-python's protected-content-loss class eliminated entirely.
4. **Hash-pinned lockfile** (`7bbe1832a`) — closes runner-audit finding #1; `pip-compile
   --generate-hashes` against real `pyproject.toml` declarations, conflict-free, independently
   re-verified by the coordinator via a second fresh-venv install.

Also landed standalone this session (pre-dating the parallel-lane phase): the blocked-decision
skip cache (E1, live-proven ending the 22-repo throughput starvation), the claim-disposition
ratchet (E2, live-proven stable cross-family on .NET), the mission-state backend routing fix
(closed the "durable mission state is unavailable" canary-killer), tex-python's `product_truth`
policy plus the cached-facts invalidation gap it exposed, the acquisition-shaped
`infra_external` classification fix (E3, live-confirmed on a fresh email-.NET derivation), the
Gate A driver's unique-log-directory fix, exact processed-slice LLM accounting, the aspose.org
upstream-drift detector, three new qwen3-next capability probes (proving temp-0 tool-argument
nondeterminism — the mechanistic root cause of the historically observed claim-count
fluctuation), the compaction-safe mission resume capsule with its `--check` staleness gate, and
four workflow-YAML mechanical gaps (timeouts, artifact retention, a missing concurrency group,
a step-summary health digest).

Decision #105 appended (dependency-bound blocked decisions; ratcheting acceptances). Decision
#97's 15-minute material-narrowing gate fired mid-session and was honored via
`--mission-action record-narrowing`, not bypassed.

Final boundary suite: 4,021 passed / 1 failed (the one pre-existing, documented stale-fixture
data drift, unrelated to any change this session) — down from the 5-10 failures recorded at
session start. `validate_compact_authority` and `validate_plan_structure` both clean.

Honest gaps carried forward, all recorded with clearing conditions in the failure-signature
ledger and the runner-readiness audit: E5's live model-choice reliability for fixture-dependent
code-block claims; slides' remaining 4 Key-Capabilities bullets (Lane B enrichment, unimplemented);
the `product_truth_blocked_category([])` empty-findings ambiguity; a standing GitHub-runner
clean-checkout proof (workflow mechanical gaps closed, full end-to-end dispatch not attempted,
correctly out of scope for a no-push session); `--require-hashes` enforcement in CI workflows
(deliberately left as a separate follow-up). No target repository was modified at any point.

- [master] **2026-08-19 — Two-gate architectural finding; Lane A completion merged; Lane B/C dispatched**
  Structural code-path analysis (`architectural-finding-two-gate-claim-accountability.md`)
  proved `evaluate_candidate_factuality()` (gate 2, `specialists/readme_factuality.py`) never
  supplies `llm_disposition_client`/`repository_root`/`disposition_ratchet_path` to
  `build_readme_document_candidate()` — so E5/api-surface-member disposition acceptance can
  *never* close gate 2 on its own, in any case, by construction; only claims whose assertion is
  literally present in the rendered candidate close both gates. This reframes the remaining
  residue queue: every still-blocked claim needs a rendering fix, not further disposition tuning.

  **Lane A part B merged** (`28d8c53d0` -> main, ff-only): `development_dependency_markdown()`
  renders the `### Development Dependencies` H3 from PEP 621 `[project.optional-dependencies]`
  and PEP 735 `[dependency-groups]` dev/test/lint/ci groups (verified-empty vs.
  unverified-absent distinction, mirroring the existing required-deps discipline). 15 new/changed
  unit tests pass; the 5 full-suite failures encountered were independently reproduced against
  unmodified main (a Windows temp-file race in `test_trusted_transform_review.py` and 3
  pre-existing Java-repo plan-hash characterization drifts) — confirmed unrelated to this branch
  before merging. Post-merge mypy/ruff clean on the changed files. Worktree and branch cleaned up.

  **Lane B merged** (`0c544f96d` -> main, ff-only): an Explore agent traced the "shortest
  passing claim wins" effect in `s1-residue-closure-map.md`'s Lane B section to
  `verified_template_capabilities.py::_richer_fact_bound_source_capability()`. Live diagnosis
  against real cached facts found the true first-order blocker for all 6 named claims
  (barcode/font/slides) was an undocumented bug, not the two suspected filters: `if "\n" in
  source_claim.strip(): continue` rejected every claim purely for containing aspose.org's
  routine Markdown soft-wrap, before the 160-char cap or 0.6 `semantically_repeats` floor ever
  ran. Fixed to disqualify only genuine multi-paragraph blocks (blank-line boundary) while
  normalizing and admitting soft-wrapped single-sentence claims; the 160-char cap was
  independently confirmed too tight (real claims run 162-473 chars) and raised to 480 with
  headroom, justified against the richest observed example. Verb/discriminator/domain/
  semantic-overlap safety filters left untouched — confirmed firing correctly, not the actual
  blocker. Live canary against real cached facts proved the fix live (a different font-python
  claim now substitutes through this path that didn't before) but also proved, honestly, that
  none of the 6 originally-named claims render yet: all 6 fail *upstream* of this function, in
  `complete_source_claim_fact_binding()` (`readme.source_claim_fact_binding`), which has no path
  from Key-Capabilities claims to function-level `api.public_surface` facts (only class names,
  only under "## API" headings) — a distinct, already-scoped gap belonging to the separate
  `api-surface-evidence` lane, deliberately not absorbed here to avoid scope creep. 7 new tests
  pass; full suite 4041/5 (identical pre-existing failure set, +7 vs. Lane A's 4034 baseline).
  Post-merge mypy/ruff clean. Worktree and branch cleaned up.

  **Lane C merged** (`8611c15c5` -> main, ff-only): a second Explore agent traced why
  note-python's `Document("SimpleTable.one")` claim never gets composed into Quick Start to
  `python_consumer_fixtures.py::_input_paths_from_example()` only setting
  `allow_substitution=True` for calls named in `_INPUT_CALL_NAMES` (`from_file`/`load`/`open`/
  `read`) — a class constructor call is recognized (via `imported_constructors`) but never
  qualified for the fuzzy on-disk fixture match, so `testfiles/SimpleTable.one` never resolved
  and the example failed with a real `FileNotFoundError` inside its isolated execution copy. The
  implementing agent's first-pass fix (extending the same suffix-based fuzzy match to
  constructors) broke an existing regression test (`test_consumer_does_not_substitute_an_unrelated_fixture_for_constructor_input`,
  from a prior session) that deliberately locks down that a constructor's string argument isn't
  guaranteed to be a file path — caught and corrected before commit with a narrower, dedicated
  exact-filename-only match strategy for constructors (`_select_repository_fixture_by_name`),
  gated on the call naming a real, statically-verified public class (`known_constructor_names`,
  threaded from `prove_python_consumer`'s already-resolved `PublicSymbolV1` surface — reusing
  existing extraction, not a new heuristic). Ordinary `_INPUT_CALL_NAMES` calls untouched. A live
  canary against a real clone of note-python confirmed the fixture now stages byte-identically;
  static ranking analysis found the fix is necessary but likely not alone sufficient to close
  claim `0d4d28fef68b38fd` specifically — a different, higher-scoring `Document("SimpleTable.one")`
  sibling example ("Export to PDF") currently outranks the claim's exact block in
  `select_verified_repository_example`'s preference order, so a companion
  selection/disposition step is still needed, honestly flagged rather than overclaimed. 6 new
  tests pass; full suite 4041/5 (identical pre-existing failures). Post-merge mypy/ruff clean.
  Worktree and branch cleaned up.

- [master] **2026-08-19 — Lane D/E: mechanical API-surface binding gap closed; targeted example
  verification built; note-python's real upstream defect surfaced honestly**

  **Lane D merged** (`c63d24b55` -> main, ff-only): an Explore agent traced Lane B's flagged
  "upstream binding gap" precisely — `claim_accountability_api_index.py::_build` indexed
  `catalog.get("classes")`/`catalog.get("modules")` but never `catalog.get("functions")`, even
  though `api.public_surface` facts always carry a sibling top-level `functions` list
  (`curated_python_api_projection.py`). `source_claim_api_detail_binding.py::_api_reference_known`
  and `source_claim_capability_binding.py::feature_detail_fact_ids` therefore had no path from a
  bare, backtick-coded function reference (e.g. barcode's `` `generate()` ``) to any fact at all.
  Fixed by adding `functions_by_name` to `ApiCoordinateIndexV1` and two narrow matchers, both
  deliberately restricted to `coded_references(text)` (backtick-fenced inline code) — never a
  bare-prose scan, mirroring the existing class-name safety discipline exactly; heading-ancestry
  gates left untouched. Live-verified: `complete_source_claim_fact_binding()` for barcode's real
  claim (`source:claim:2626:54e67ed0a47ae8af`) now returns
  `fact_ids={'api.public_surface:python-exports'}`, previously `None`. Traced the call path
  confirming this is the exact mechanism gate 2 consumes. Honestly flagged: this removes a hard
  blocker but doesn't by itself prove `AGENT_APPROVED` (composition/Lane B still has to select
  the claim verbatim); font-python's `font.convert(target)` claim needs a different mechanism
  (instance-attribute-style reference) not covered by this fix. 3 new tests; full suite 4051/5
  (identical pre-existing failures). Post-merge mypy/ruff clean. Worktree and branch cleaned up.

  **Lane E merged** (`f5446bbed` -> main, ff-only): a second Explore agent found the real reason
  note-python's fixed-fixture-staging Quick Start example still didn't close its claim: the block
  fails `curated_python_readme`'s static AST check outright (`unknown_product_member:Document.Title`
  — the validator can't track the `for page in doc:` loop-variable's type), landing in
  `withheld_inline_examples`, never `inline_examples`; separately, `example.minimal`'s ranked
  selector prefers a different, higher-scoring sibling example regardless. Built a new, narrowly
  bounded mechanism (`curated_python_evidence._runtime_verify_quick_start_examples`, "Quick
  Start" heading only, max 3 real isolated-execution attempts) that can PROVE a withheld example
  correct via actual execution — strictly stronger than the static check — and promote it,
  reusing the exact same `prove_python_consumer` machinery `example.minimal` already uses via
  dependency injection; wired through a pre-existing-but-unused `runtime_verified` field
  (`example_assurance_validation.py` already referenced it, confirming this was an anticipated,
  not-yet-wired extension point). `verified_repository_example_code()` now accepts
  `static_api_verified OR runtime_verified`, exact-text-match requirement untouched. **Live
  canary against a real note-python clone found a genuine upstream defect**: the claim's exact
  quoted code crashes with `AttributeError: 'NoneType' object has no attribute 'TitleText'` —
  `page.Title` is really `None` for at least one page of the real `testfiles/SimpleTable.one`
  fixture. Confirmed the verification machinery itself is correct (a guarded variant passes
  against the identical fixture). The mechanism correctly refused to promote broken code — this
  is fail-closed behavior working as designed, not a defect in this session's work — recorded in
  `note-python-quick-start-upstream-defect.md` since no target-repository writes are permitted
  during this mission. 8 new tests; full suite 4056/5 (identical pre-existing failures).
  Post-merge mypy/ruff clean. Worktree and branch cleaned up.

- [master] **2026-08-19 — Mission-recovery resumed: docs caught up, Docker restarted, runner-
  readiness Finding #2 redaction half closed, a 4th pre-existing hash-drift baselined**

  Live-state reconstruction (`14ff932c0`-series) found no destructive drift: main at `f5446bbed`
  as the resume capsule expected, only trailing documentation uncommitted (now landed in
  `f64c24707`). A separate, unrelated `freshness-service` workstream is live in its own worktree
  (`.fro-worktrees/coord`) — confirmed out of scope, left untouched.

  Started a Gate A portfolio re-verification pass (13 repos went `stale_fact_contract` from
  today's earlier template/rendering merges) and a background lane on font-python's
  `font.convert(target)` claim (the one gap Lane D explicitly left open — needs an
  instance-attribute-style reference matcher, not the bare function/class matcher Lane D built).
  The portfolio pass's first member hit `SYSTEM_FAILURE: IsolatedExecutionError: container
  registry acquisition remained unavailable` — root-caused to Docker Desktop's backend not
  running (`docker info`: named-pipe connect failure), not a code defect; restarted it.

  **Runner-readiness Finding #2 (`14ff932c0`), redaction half only**: `env.secret_values()` now
  includes `LLM_BASE_URL`/`GPT_OSS_ENDPOINT` when explicitly overridden via the environment — an
  operator-supplied gateway URL can carry an embedded credential (query-string key, signed URL)
  and previously reached evidence unredacted. `DEFAULT_LLM_BASE_URL` is a source constant, never
  env-sourced, so the common (no-override) case is unaffected. The bundled "fail-closed vs.
  documented-default" question was deliberately left open — a real behavior change nothing has
  evidenced a need for, against a default every invocation this mission has made already relies
  on. 3 new tests (`TestSecretValues`); the existing opportunistic evidence-scan test picks the
  new names up for free since it already iterates `secret_values()` generically.

  The full boundary run this surfaced (before the fix's tests were confirmed) showed 5 failures,
  not the expected 1: 3 were the already-documented Java plan-hash drifts, but a 4th —
  `test_agentic_plan_is_source_and_fact_bound_and_changes_the_candidate` (Cells-Java, agentic
  path) — wasn't previously named. Reproduced in a scratch `git worktree` at unmodified
  `f64c24707` (this session's own ambient `GPT_OSS_ENDPOINT=https://llm.professionalize.com/v1/`
  made causation from the redaction change plausible enough to verify directly rather than
  assume) — identical hash mismatch with the fix completely absent, confirming it predates this
  change. Recorded in `plans/backlog-post-poc.md` (GOV-014): the live pre-existing baseline is
  now 5 failures, not 1; a future session should re-diagnose all 4 plan-hash drifts together
  (same Java/characterization shape) rather than as four separate incidents. Full suite,
  ruff, mypy clean on the changed files after the fix was restored.

- [master] **2026-08-19 — Runner-readiness Finding #3 closed; Q2 closed live; redundant badge
  suppressed; new signature and upstream-drift gap documented**

  **Runner-readiness Finding #3** (`482a3915e`): `resume`/`analyze` (the two jobs that actually
  run `--execution-profile github_observe`) uploaded `runs/evidence/**` but never the real
  candidate directory. Added a second `actions/upload-artifact@v4` step to each, mirroring the
  existing evidence step's `retention-days`/`if: always()` convention, uploading
  `runs/readme-poc/${{ matrix.owner }}__${{ matrix.name }}/`. Relies on the action's default
  `if-no-files-found: warn` since most matrix repos in a given run are still `BLOCKED` before
  reaching candidate generation. `actionlint` clean.

  **Q2 closed live** (`a45f416f3`): cells-python's API Reference documented 56 public types
  against a real curated `__all__` of 63, silently dropping core classes `Workbook`/`Worksheet`
  among 6 others. Traced empirically against real cached facts (not guessed): the renderer
  sourced its per-namespace export list from the bounded planning projection
  (`value["modules"]`), a strict subset (48) of the complete evidence catalog's list (63) for
  the *same already-included* namespace — `_complete_catalog()`'s own docstring already stated
  the intended rule but it was only applied to class/function detail lookups, not export
  selection. Fix: prefer the complete catalog's export list for a namespace already selected,
  never adds a namespace the bounded projection didn't include (internal implementation modules
  stay excluded). Verified live: `Workbook`/`Worksheet` now render; `FormulaEvaluator` correctly
  still doesn't (genuinely exported only at the submodule level — a separate, distinct gap, Q3,
  not addressed here). New regression test isolates the exact mechanism.

  **Redundant Requires badge suppressed** (`cd2031ffd`): a separate "Requires: Python >=X" badge
  restated what the dynamic PyPI "Python versions" badge already showed — confirmed real
  aspose.org candidates never carry both. Narrowly scoped to the PyPI+Python case only; every
  other ecosystem (Java, .NET, ...) is unaffected (verified via the existing Java-fixture test
  plus two new regression tests covering both directions). Verified live against real
  cells-python facts: badge list now matches aspose.org's own convention exactly.

  **New signature + upstream-drift gap documented** (`b492251d0`): S12
  (`composition.segment.NNNN: substantive generated bytes lack exact candidate authority`,
  observed live on cells-python during the fresh portfolio pass, not yet root-caused) added to
  the failure-signature ledger. Re-ran `detect_aspose_upstream_drift.py` (fresh evidence, newer
  upstream HEAD, confirms the already-known 18/18 bundle / 28 data-file drift persists) and
  found a real coverage gap the tool doesn't reach: `src/readme_agent/vendored_asposeorg/**`
  (the actively-imported readme-refresh check battery, not vestigial) has itself drifted from
  live upstream, including a documented upstream incident fix (MT041) whose relationship to our
  vendored copy is unverified. Checked `api_reference_class_exclusions.json` directly (the one
  drifted data file most relevant to the Q2 fix above) and confirmed it is byte-identical to
  upstream — closes that specific question rather than leaving it open.

  All fixes independently full-suite-verified: an initial concurrent-run result (8 failed, 54
  errored, mostly `RepositorySnapshotError: cannot capture repository revision`) was traced to
  genuine resource contention (16 python processes, ~48% sustained CPU from the concurrent
  portfolio pass + a background agent's own suite run), not a regression — confirmed by
  re-running the same erroring test files in isolation (124/124 passed). No target repository
  was modified.

- [master] **2026-08-19 — Lane F merged: font-python's instance-style-reference
  and import-statement claim-accountability gap**

  Continued the thread Lane D's commit explicitly flagged as unresolved: font-python's
  `font.convert(target)` claim (`source:claim:3096:1fd567b607a0b92d`) needs "a different
  mechanism (instance-attribute-style reference)". Traced it live against font-python's real
  cached facts and source README (revision `7971880964143c86ffd4baa7c8ea1dd959fea04e`, pulled
  from the shared `runs/readme-poc/` history): the claim's Key-Capabilities bullet carries three
  backtick-fenced coded references — `font.convert(target)`, `FontConverter.convert(font,
  target)`, and `` `from aspose_font.converter import FontConverter` `` — and
  `feature_detail_fact_ids`'s final gate (`_api_references_are_known`) requires every one to
  resolve or the whole claim is discarded. `FontConverter.convert(...)` already resolved;
  the other two did not. Confirmed `convert` is unsafe to resolve by name-uniqueness alone (8
  different real classes declare a `convert` method), so the fix instead resolves the lowercase
  instance variable to its class only via exact case-fold identity (`font` → `Font`, the one
  real class both named that way and actually owning the matching `convert(target)` signature) —
  never a fuzzy match, and rejecting outright if two real classes' names collide under
  case-folding. A second, independent gap in the same claim — no existing matcher recognized a
  `from <module> import <Name>` shape at all — got a dedicated matcher that validates both the
  imported name and its exact module path against the real extracted surface, fail-closed against
  a hallucinated import path. Both paths fire only through `coded_references(text)` (backtick
  spans), mirroring Lane D's discipline exactly; heading-ancestry gates untouched.

  Live-verified: `complete_source_claim_fact_binding()` for the real claim now returns
  `fact_ids={'api.public_surface:python-exports', 'product.capabilities:repository-evidence',
  'product.formats:native-direction-evidence'}`, previously `None`. Honestly out of scope, left
  untouched: font-python's other two blocking claims from the same portfolio snapshot
  (`3523960e3ec2b571`, blocked by a bare non-call parameter name `font_type` in backticks;
  `f76ee53ac612c3f9`, blocked under the Dependencies heading gate by the private-submodule
  reference `aspose_font._brotli`) — both are different, unscoped mechanisms.

  A genuine end-to-end portfolio-level canary could not be completed in the isolated worktree:
  a bounded-verified-canary run (twice, reproducibly) blocks earlier than claim accountability,
  at `product_truth_not_ready:BLOCKED_MISSING_EVIDENCE` — several product-truth fields fall back
  to a checksum-valid "current-revision salvage candidate" that only exists in the main
  repository's long-accumulated `runs/` history, which a fresh worktree structurally lacks
  (confirmed via `findings.json`: "no checksum-valid current-revision salvage candidate was
  available"). This is an unrelated pre-existing bootstrap characteristic of a fresh single-repo
  canary, not a defect in this fix — the main portfolio snapshot at session start already had
  font-python passing product_truth and reaching claim accountability with exactly this claim
  blocking. Live proof was instead built directly against the real, already-accepted
  `product-facts.json` for this exact revision, via the same `complete_source_claim_fact_binding()`
  call path Lane D traced.

  7 new tests: coded instance-style method reference binds; unknown instance owner stays
  fail-closed; known-class-by-instance-name but unproven member call stays fail-closed; ambiguous
  case-fold collision between two real classes stays fail-closed; coded import statement binds;
  import statement with a wrong module path stays fail-closed. Full unit suite: 4070 passed, 11
  failed, 1 skipped — confirmed via a stash-and-rerun comparison that all 11 failures are
  pre-existing on unmodified main (identical test identities with and without this diff): 3
  Java-repo plan-hash characterization drifts, 1 agentic-composition-plan characterization drift,
  and 7 Windows temp-path races in `test_trusted_cohort_runtime.py` (`shutil.copytree` WinError
  3) — none newly introduced. mypy/ruff clean.

  Committed to the worktree branch (`worktree-agent-a070857ca5b248eb9` @ `9f656b380`); the
  agent's own sandbox refused every git operation targeting the shared main checkout (`git -C
  <main>`, `cd` into it, and `EnterWorktree` on the main tree were all rejected as out-of-scope
  for a worktree-isolated agent) — a harder isolation boundary than prior same-day lanes
  operated under. The coordinating session rebased the fix commit onto current main (clean, no
  conflicts) and fast-forward merged it; only this log entry itself needed conflict resolution
  against the coordinating session's own concurrent log entries above.

- [master] **2026-08-19 — S12 blocked-diagnostics persistence bug found and fixed; bounded
  retry-blocked pass started for fresh cohort evidence**

  Mission reconciliation session resumed against `L8-PORT-01-LOCAL-README-PORTFOLIO-ASPOSE-PARITY`
  (mission resume capsule, `plans/investigations/evidence/mission-recovery-2026-08-18/
  failure-signature-ledger.md`). S12's own last entry ("do not attempt a fix without a genuine,
  live-matching repro") named a real, disclosed gap as its next diagnostic: a BLOCKED
  presentation-plan attempt has no persisted composition ledger to inspect after the fact.

  Traced `_persist_blocked_plan_diagnostics` (`specialists/readme_presentation.py`) precisely:
  it read `claim_accountability`/`source_claim_resolutions`/`candidate_sha256` from
  `presentation_plan_record["presentation_plan"]`, but that key holds
  `RepositoryPresentationPlanV1.model_dump()`, which has no such fields — a real pre-existing bug
  (independent of S12) that always persisted `null` for the first two. The correct source is the
  sibling `readme_document_plan` key (`ReadmeDocumentPlanV1.model_dump()`), which carries all
  three correctly plus the full `composition_ledger` (every segment's exact `content_text`,
  `authority`, byte range) — already computed and in memory at this exact call site, never read.
  Fixed by switching the extraction source and adding `composition_ledger` to the persisted
  payload; no new computation, model, or pipeline change needed. Updated
  `test_blocked_plan_diagnostics_write_claims_and_candidate` to assert against the real
  `readme_document_plan` nesting instead of the buggy shape it previously asserted against (which
  is exactly why the bug went unnoticed). Focused suite green (6/6 in
  `tests/unit/test_blocked_plan_diagnostics.py`), ruff/mypy clean on both changed files.

  A `--bounded-verified-canary` rerun for cells-python to capture live evidence did not exercise
  the new code: the pipeline's own no-unnecessary-work convergence (VER-003) reused the prior
  terminal outcome (recorded ~06:18Z, ~90 minutes earlier) since no tracked dependency fingerprint
  changed — correct behavior, a diagnostics-only edit shouldn't invalidate a candidate. Confirmed
  `--retry-blocked` (the sanctioned lever for exactly this situation: same fingerprint, new reason
  to recheck) is portfolio-only per its own `--help` text, not available to a single-repo canary.
  Started a bounded `--registry data/products.json --retry-blocked
  --portfolio-time-budget-seconds 1200` pass instead, deliberately justified by the real code
  change just landed (not a blind re-run), to force fresh, complete diagnostics across the whole
  currently-blocked cohort (S1/S5/S12 members alike) in one pass. Whether segments
  `0032`/`0034`/`0036` are trivial whitespace or substantive dropped prose remains open pending
  that pass's results.

- [master] **2026-08-19 — S12 root-caused and fixed: ungoverned Dependencies H3 sub-headings**

  The retry-blocked pass finished and produced real fresh evidence (diagnostics files
  ~13KB -> ~487KB, confirming the persistence fix works): segments 0032/0034/0036
  (cells-python) and their barcode-python equivalents are **not whitespace** — they are the
  literal H3 sub-heading strings `### Required Package Dependencies`, `### Optional
  Dependencies`, `### Development Dependencies`, confirmed present in the real rendered
  candidate under `## Dependencies`.

  Root cause: today's own earlier Lane A part B / Lane F work added these three
  template-mandated Dependencies H3 headings, but never registered them in
  `composition_lineage.py`'s `_GOVERNED_STRUCTURAL_LINES` — the exact set `### Tests` is
  already in, for the identical reason (a template-shell heading carrying no fact/standard
  binding of its own). A real, same-day regression, not a pre-existing gap.

  Fixed by adding the three headings to `_GOVERNED_STRUCTURAL_LINES`. New regression test
  (`test_dependencies_subsection_headings_are_governed_mechanical_structure`) confirmed to fail
  without the fix (reproduces the exact live error) and pass with it. Full composition-lineage +
  diagnostics suites green (27/27). Full governed suite: 5 failed / 4084 passed — all 5 match the
  already-documented pre-existing baseline (`plans/backlog-post-poc.md` GOV-014: 3 Java
  plan-hash characterization drifts, 1 agentic-composition-plan characterization drift, 1
  note-python stale-fixture drift) exactly; zero new regressions. ruff/mypy clean.

  Expected to unblock cells-python, page-python (S12's named repos) and barcode-python
  (confirmed same live signature); any other repo whose sole remaining block was this exact gap
  clears too. A fresh portfolio/canary pass is needed to confirm actual promotion past
  `DETERMINISTIC_VALIDATED`. S1 (claim accountability) remains separately open for repos that
  fail there independently of S12. No target repository was modified.

- [master] **2026-08-19 — S12 fix corrected: two fixed lead-in sentences also needed governing**

  A bounded `--retry-blocked` pass proved the first S12 fix real but incomplete: cells-python's
  unbound segments went 3->1 (0032/0036 cleared, 0034 remained), and fresh evidence from
  font-python/note-python showed why — `### Optional Dependencies`/`### Required Package
  Dependencies` are each followed, in the same contiguous unbound byte range, by a fixed,
  non-fact-derived sentence (`verified_template_sections.py`): `"Install optional dependencies by
  scenario:"` and `"No required third-party package dependencies."` (verified-empty case).
  Registered both, plus the identical sibling sentence for Development Dependencies (`"No
  development dependencies declared in \`pyproject.toml\`."`) — same function family, not yet
  observed live but certain to recur, fixed proactively. Regression test strengthened to the real
  segment shape (heading + sentence together); confirmed it fails on the heading-only version and
  passes on the complete fix. Full composition-lineage suite 27/27, ruff/mypy clean.

  Caught by rerunning immediately against fresh live data rather than trusting the first green
  test in isolation — a partial live improvement is evidence the mechanism is right, not evidence
  the fix is complete. Same pass separately confirmed real forward progress: barcode-python
  advanced past S12 entirely to `CANDIDATE_GENERATED`, now blocked at a later, unrelated stage
  (`factuality_rejected`, one protected-content loss) — a different, already-known signature
  class (S5), not a regression from this work.

- [master] **2026-08-19 — Disposition-context wiring audit closes the chain S12 opened;
  barcode-python live-confirmed AGENT_APPROVED**

  Barcode/cells-python's `factuality_rejected` block traced precisely to a pre-existing
  architectural finding (`architectural-finding-two-gate-claim-accountability.md`): the
  independent factuality gate (`readme_factuality.py`) never received the disposition client/
  repository_root/ratchet path gate 1 already resolves, so an accepted `excluded_with_reason`
  claim could reappear as a fresh block. Fixed (`b32d4998c`) via a new shared
  `resolve_claim_disposition_context()` helper, wired into both real callers.

  Live-verifying it surfaced a THIRD gate with the identical gap: `verification/checks.py`'s
  independent verifier (`_verify_presentation_candidate`) also independently rebuilds the document
  plan without disposition context — barcode-python re-cited the exact same claim
  (`source:claim:3930:c5ac180c4dd86b4f`) under a new `verification_rejected` classification. Fixed
  (`5acf011b3`), simpler shape than gate 2 since `entry` was already resolved in the enclosing
  function. A full audit of every `build_readme_document_candidate()` caller then found two more:
  `idea_candidate.py` (fixed, `e33e631ed`, same easy shape as gate 3) and
  `readme_proposal_bundle.py` (left as a precisely-scoped lead — harder shape, no direct live
  failure evidence).

  A clean re-verification pass (after ruling out a transient GitHub clone/rate-limit hiccup on the
  prior attempt via a direct `git ls-remote` check, unrelated to any code change) confirmed the
  complete fix chain live: **barcode-python reaches `AGENT_APPROVED`** — S12 → gate 2 → gate 3 →
  agent review, all cleared. 3D-Python is re-proving through the same lifecycle stages after the
  shared verification-contract change (Decision #90's component-versioned invalidation working
  correctly, not data loss). cells-python was not reached in this pass's time budget; a future
  pass should confirm the same chain reaches it. Six real defects fixed and regression-tested this
  session (5,4084 -> clean composition suites throughout; 27+148+142 tests across the
  disposition-wiring changes). No target repository was modified.

- [master] **2026-08-19 — Shared-ratchet backfill fix confirmed; terminal proof: 3/33 Python
  repos reach NO_OP_PROVEN**

  Found and fixed a seventh real defect: the portfolio-wide shared claim-disposition ratchet
  (`shared_claim_disposition_ratchet_path()`) exists specifically so an identical claim's
  already-corroborated disposition propagates across repos, but the write to both stores only
  happened after a fresh model acceptance — a verdict replayed from a repo's own ratchet returned
  early and never backfilled the shared store. Live-observed: note-python's own ratchet already
  held an accepted `redundant_with_candidate` verdict for a boilerplate claim (content hash
  `7ff54c1da64deecb`) that page-python's real source also carries verbatim, but the shared store
  never had it. Fixed (`56a5f09c8`); a new regression test confirmed to fail without the fix
  (shared store file never created) and pass with it.

  Live-verifying it surfaced a genuinely deeper, separate finding rather than a full close: the
  fix works exactly as designed (the shared store now holds the backfilled entry, confirmed by
  direct inspection), but note-python's and page-python's blocking claims for this content stayed
  blocked regardless. Traced precisely: this claim is tracked as **two separate records** sharing
  one content hash — a source-stage claim (`expected_disposition: unjustified_loss`, what the
  ratchet resolves) and a byte-identical candidate-stage claim (`expected_disposition:
  unbound_generated`, the composer's generated replacement text, needing its own fact binding).
  Both carry `equivalent_candidate_claims`/`equivalence_group_id` fields — exactly the schema
  support an intended linkage would need — currently empty on both, populated by nothing in the
  current disposition path. Recorded precisely in the failure-signature ledger as a distinct,
  not-yet-fixed gap rather than guessed at further; confirmed pre-existing, not a regression from
  this fix.

  A follow-up pass confirmed cells-python reaching the same terminal state barcode-python already
  had. Official numerator: `candidate_generated=3, deterministic_validated=3, agent_approved=3,
  no_op_proven=3` — an exact, clean set (3D, barcode, cells), up from 1/33 no-op-proven at session
  start. Remaining Python blockers are now precisely characterized: email/pdf/slides (one
  unresolved S1 claim-accountability block each), font (two claims needing genuinely new
  extraction/matching logic, not a registration fix), note/page (the equivalence-linkage gap
  above), html/psd/tex (confirmed genuine upstream `infra_external` defects). Seven real defects
  fixed and regression-tested this session; zero speculative fixes attempted without live
  evidence. No target repository was modified.

- [master] **2026-08-19 — Native knowledge-application layer: the imported aspose.org claim
  corpus is now indexed, freshness-gated, and bound-selected into `ProductFactsV2`; 10 vendored
  checks promoted to blocking (Decision #106)**

  Closed the largest gap in the imported-knowledge audit: production read only `dependency`-kind
  claims (21 of 97,303) from `data/imported/knowledge/`, plus the structural `api_surface.json`;
  the other 10 claim kinds (feature, format_support, format, install, license, limitation,
  troubleshoot, plus api/api_class/api_method/api_field already covered structurally) were
  imported but never consumed. Extended the existing `aspose_detectors.py`/`composer_factpack.py`
  lineage (a prior "freshness-service" session's work, discovered mid-audit) rather than building
  a parallel system: `facts/aspose_knowledge_claims.py`/`aspose_knowledge_selection.py` load and
  index all 12 kinds, apply bounded confidence-ranked selection per fact field (never first-N
  truncation), and produce a complete per-claim disposition (considered/selected/rejected + reason)
  every run. Current repository evidence always wins on conflict; a claim whose bundle `repo_sha`
  does not match the revision this run actually observed is capped at reduced, unverified
  confidence unless independently corroborated (today: a live LICENSE-file check for `license`
  claims) — never silently promoted to truth. The whole corpus is one deterministic, checksum-bound
  unit (`data/imported/knowledge_manifest.json`, 3,050 files/31 bundles) feeding a new
  `imported_knowledge` `facts/acceptance_contract.py` component, so a knowledge-content change now
  invalidates dependent fact stages — previously the corpus data itself was hashed nowhere, only
  the two detector modules reading it.

  A per-run `knowledge-application.json` (`facts/knowledge_application_evidence.py`, wired into
  `supervisor/product_truth.py`/`local_poc_evidence.py`) records exactly which claims and SEO
  keywords were considered, selected, or rejected and why. `facts/aspose_detectors.py::
  detect_relevant_seo_keywords()` adapts the vendored, incident-hardened
  `filter_relevant_seo_keywords()` (platform-mismatch + stuffing guard, capped at 6) into a new
  `aspose.relevant_seo_keywords` fact, additive alongside the existing raw field.

  Separately: the vendored 89-check battery (`validation/aspose_checks_bridge.py`) was recorded
  but never blocking. `scripts/data-refresh/classify_aspose_checks.py` classifies every check
  (severity + real-parameter-satisfiability + empirical non-regression against the 3 currently
  accepted candidates) into `data/aspose_check_classification.json`; an initial 14-check blocking
  set was cut to 10 after this repo's own full relevant unit-test suite (not just the 3-repo
  sample) surfaced 3 real false positives — `check_process_narration_smells` (flags this repo's
  own legitimate install-verification prose), `check_scope_limitations_format` (expects an
  Enterprise Edition closing-paragraph convention this repo's template doesn't produce), and
  `check_no_excluded_domain_links` (fired on a real captured NET fixture — logged as `KNOW-012`,
  a likely genuine `document_link_hygiene.py` gap, rather than forced through by breaking an
  existing test). `document_validation.py` now appends the classified-blocking subset to `errors`.

  Verified zero regressions: the 3 currently accepted candidates plus 671/672 of the full unit
  suite pass (the one failure is the pre-documented Windows MAX_PATH issue, unrelated). Twelve new
  requirement rows (`KNOW-001`..`KNOW-012`) record what's implemented vs. explicitly deferred —
  editorial-plan expansion, cross-ecosystem dependency evidence, and the remaining 51
  input-blocked checks are real, logged gaps, not silently dropped. Bounded governed calibration
  (facts/evidence-only, no target-repository write) follows this entry.

- [governance] **2026-08-19 — Control-repo auto-push: mechanical `post-commit` enforcement
  (Decision #107)**

  Requested as a full first-principles production assessment ("remove the push block, agent
  pushes automatically after commit"). Investigation (two parallel deep-trace agents) found "push"
  names two structurally unrelated systems here: this control repository's own push to its own
  `origin`, and the `readme-agent` product's `open_presentation_pr` write path against real
  third-party FOSS repos. Scoped explicitly by the user, after being asked directly, to the former
  only — the latter's human-filed-authorization posture (`AUTH-004`, `GOV-018`, decision #69's
  deliberate "build the mechanism only, no real record" choice, reaffirmed as an absolute invariant
  as recently as the 2026-08-09 POC freeze) is untouched by this change.

  For this control repository's own remote: `origin` was already un-neutered and `main`/`origin/main`
  were already at parity (routine pushes already happened, just via a separate manual step each
  time, no durable mechanism — confirmed live via `git remote -v`/`git rev-list --left-right --count`).
  Closed the gap the same way this project closes every other safety-relevant gap: paired the
  stated rule with mechanical, tested enforcement, not a policy sentence alone.

  Added `scripts/governance/post_commit_push.py` (standalone, no `src/readme_agent` imports,
  mirroring `scripts/`'s existing isolation convention) and a new `post-commit` hook installed
  alongside the existing `pre-commit` hook by `scripts/governance/install_hooks.py`. Pushes the
  current branch to `origin` immediately after every commit; skips cleanly on detached HEAD;
  refuses to push if `origin`'s URL ever carried the product-repo neuter marker (`DISABLED`); on a
  non-fast-forward rejection (the real, already-observed concurrent-session case — see
  `concurrent-session-doc-edits-2026-08-19`) reports the rejection with a rebase remedy and never
  force-pushes — a commit is never lost, only its push delayed until the next successful attempt.

  `plans/GOVERNANCE.md` rule 10 gained an explicitly-scoped carve-out paragraph immediately after
  its existing, verbatim-preserved product-repo clause; rule 19's standing-authority grant was
  extended from "commits" to "commits and pushes to this repository's own `origin`". Decision #107
  appended to `plans/decisions/catalog.jsonl`; condensed bullet added to `master.md`'s Decision
  Ledger index. A pre-existing, unrelated documentation gap was surfaced during investigation and
  deliberately left unfixed here (logged separately): rule 10's product-repo clause text has not
  been updated to reflect Decision #33's own two 2026-07-23 amendments (the `AuthorizationRecordV1`
  supersession) — out of scope for this change, not silently bundled into it.

- [master] **2026-08-19 — Decision #107's post-commit push hangs indefinitely in this environment;
  fixed with a local-only credential-helper override**

  First real commit made after the Decision #107 `post-commit` hook was installed
  (`eaf5eef62`, restoring the hermetic composition test baseline) hung: `post_commit_push.py`'s
  `git push` blocked on an interactive `git-askpass.exe "Username for 'https://github.com'"`
  prompt that nothing could answer, orphaning a `git push`/`git-remote-https`/`git-askpass` process
  chain after the parent shell timed out. Root cause: this checkout's configured
  `credential.helper` is `git-credential-manager.exe` (both global and, until now, unoverridden
  locally), which has no cached credential for this remote and falls back to an interactive
  prompt rather than failing closed — `5e9aeebf3`'s "must not crash on a slow/hung push" fix
  prevents a crash but does not prevent this specific hang, since GCM never returns control at
  all. Killed the orphaned process chain (safe — it was this session's own leaked child, not
  another session's), pushed the pending commit manually via `gh auth git-credential` scoped to
  that one invocation, then set a **local-only** (`git config --local`, not `--global`)
  `credential.helper` override — `""` (reset) followed by `!gh auth git-credential` — so future
  automatic pushes from this hook (or from any git operation in this checkout) authenticate via
  the already-logged-in `gh` CLI instead of hitting the interactive prompt. Verified with
  `git ls-remote origin` resolving instantly post-fix. This lives in the shared `.git/config` for
  the primary checkout, so it protects every session working in this exact working directory, not
  just this one. Not yet root-caused *why* GCM has no cached credential here (out of scope for
  this fix — the local override is sufficient and does not depend on diagnosing GCM further).
