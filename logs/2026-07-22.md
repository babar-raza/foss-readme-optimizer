# Project log — 2026-07-22

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-07-22 | requirements | — | `SCL-008`, `PIL-012`, `PIL-013`, `PIL-014`, `RUN-005`, `RUN-006`, `RUN-007` | — | Full-project truth audit: corrected `SCL-008` to `PARTIAL` and added `PIL-012`–`014`/`RUN-005`–`007` objective gates -- actual runtime lacks fail-closed state, durable trigger intake, PR lifecycle, and a complete three-repository acceptance bundle. See `plans/investigations/full-project-truth-audit-2026-07-22.md`. |
| 2026-07-22 | master requirements governance incident | #46 | `GOV-009`, `PIL-012`, `PIL-013`, `PIL-014`, `RUN-005`, `RUN-006`, `RUN-007`, `SCL-008` | Wave 9 | Foreign-section incident triaged and removed; Decision 46 anchors it plus the full-project truth audit; GOV-009's CI/hook validator built and wired; Status/docs refreshed with a fresh 834-test count. |
| 2026-07-22 | master requirements governance incident | #47, #48 | `SCL-007`, `EFF-005`, `AGT-006`, `VER-005`, `PRL-001`, `PRL-002`, `PRL-008`, `GOV-009` | Wave 9 | 7 taskcards built with real tests (EFF-005 lock revalidation, F3 verifier-token fix, stop capability, VER-005 extension, PRL groundwork); decisions 47/48 recorded; a 4th concurrent session's data/products.json broadening (3->25 enabled) found and deliberately left for owner review. |
| 2026-07-22 | registry self-heal runtime sprint | #47 | `CORE-034`, `CORE-023`, `CORE-032`, `OPS-005`, `OPS-008`, `ONB-004` | — | Extracted discovery core to registry/discovery.py, added supervise-time self-heal (fail-open, TTL, evidence, PR-not-push CI), live-proven on aspose-words-foss/Aspose.Words-FOSS-for-.NET; CORE-034 IMPLEMENTED. |
| 2026-07-22 | requirements governance | #48 | `GOV-004`, `GOV-005` | Wave 9 | User confirmed the 3->25 enabled-entry broadening is intentional; 3 tests rewritten to a drift-proof invariant instead of a hardcoded count; fresh full-suite run: 867 passed, 0 failed, 18 deselected. |
| 2026-07-22 | registry policy-profile clone-reliability production-hardening | #49 | `ONB-004`, `SCL-009`, `SCL-004` | — | Verified/corrected 25 policy profiles (9 wrong .com URLs), built policy_facts.py + scaffold-policy CLI, added an aspose.org link surface, fixed clone_baseline's 9-17x within-run redundancy with memoization + retry + tunable timeout + graceful BLOCKED degradation; words/net fully onboarded live. |
| 2026-07-22 | plan-hardening governance verification supervisor | #50 | `GOV-022`, `VER-001`, `SCL-006`, `AGT-006`, `OPS-009`, `OPS-010` | Wave 9 | TC-14/18/19/28/29/30 + TC-01 push executed from the plan file's Phase 17 register; 919 passed, 0 failed after reconciling with a concurrent session's own decision 49. |
| 2026-07-22 | capability remote-write governance | #51 | `PRL-001`, `PRL-002`, `PRL-004`, `PRL-007`, `PRL-008`, `EFF-001`, `VER-001` | Wave 9 | New write-verb GitHub API layer, dedicated never-neutered clone/remote path, 17 new tests; unit-proven, not yet live-proven. |
| 2026-07-22 | capability remote-write live-proof | #52 | `PRL-001`, `PRL-004`, `PRL-008`, `OPS-009` | Wave 9 | Real Basic-vs-Bearer auth bug found and fixed live; real PR #1 opened, independently re-verified open and unmerged. |
| 2026-07-22 | requirements supervisor incident | #53 | `ORC-006`, `ORC-003` | Wave 8.7 | SHA-probe shortcut gains a real evidence trail (`sha_probe_shortcut` decision kind); `ORC-006`/`ORC-003` corrected from stale `BACKLOG`/rejected text to reflect what Waves 8.5/8.6 already built and this session live-proved. STALE_NONCOMPLIANT/turn-budget investigation for `aspose-3d-foss` closed as a non-reproducing, self-resolved condition -- no fix shipped. |
| 2026-07-22 | full-project truth audit repeat | #51, #52 | `PIL-001`, `PIL-003`, `PIL-011`, `PIL-014`, `ONB-002`, `PRL-002` | Wave 9 | Re-audit credits the real Cells/Java PR while retaining `NOT PRESENTABLE`/Level 3; corrects stale remote-write, pilot-mode, and proposal-state claims. |
| 2026-07-22 | requirements governance runtime | — | `OPS-010`, `GOV-024`, `GOV-025` | Wave 9.1 | Autonomous execution of the 2026-07-22 plan begins; Wave 9.1 closed: 3x clean full-suite baseline, lockfile+bootstrap proof, 5 malformed requirements.md rows fixed, GOV-024 found stale and reclassified IMPLEMENTED. |
| 2026-07-22 | requirements tooling | — | `GOV-007` | Wave 9.2 | Wave 9.2: built plans/investigations/tools/traceability_matrix.py, ran it against all 111 IMPLEMENTED rows -- 0 cite a nonexistent path, 74 have informational-only (no-test-cited) findings, 37 fully clean. |

## Entries

- [requirements] **2026-07-22 — Full-project truth audit added the evidence-backed execution
  handoff.** Added `plans/investigations/full-project-truth-audit-2026-07-22.md`: verdict `NOT
  PRESENTABLE` (Level 3 POC), live `gh api` checks confirming push/admin access on all three
  enabled pilot repositories, a fresh clean `pytest -q` run (824 passed, 18 deselected,
  410.22s -- superseded same-day, see the next entry), and a complete material-gap register
  (`AUD-001`-`014`). Corrected `SCL-008` to `PARTIAL` with an honest evidence note on
  `readme-agent-portfolio.yml`'s real (uncommitted) capabilities and real remaining gaps; added
  `PIL-012`-`014` (pilot-heterogeneity honesty, one committed evidence bundle, reviewable PR
  lifecycle without pushing to a product default branch) and `RUN-005`-`007` (fail-closed durable
  state, durable trigger-intake dedup, health/backlog observability) as new `BACKLOG` rows. Its
  drastic-action recommendation: freeze new specialist/domain abstractions until `AUD-001`-`007`
  close; retain the registry, safety model, state/CAS primitives, validators, and supervisor; use
  `supervise` as the sole pilot path and label `run` compatibility-only during the pilot.

- [master requirements governance incident] **2026-07-22 — Removed 10 foreign sections from master.md, added Decision 46, built the mechanical governance validator (pre-commit hook + CI).** Removed ten foreign top-level sections a concurrent session appended after `## Changelog`
(violating GOVERNANCE.md rule 2), triaging each claim: four already tracked (`GOV-009`,
`OWN-014`/`SURF-005`/`VAL-009`/`SAFE-010`/`NFR-008`, `DOC-007`, decision #43/`GOV-022`), one
factually wrong (`VER-004` is `IMPLEMENTED`, not unimplemented). Added Decision #46 formally
anchoring both the incident and the independent 2026-07-22 full-project truth audit (verdict
`NOT PRESENTABLE`, Level 3; live `gh api` confirmation of push/admin access on all three pilots,
resolving the fork-vs-direct-branch question; adopted its drastic-action recommendation to freeze
new specialist abstractions and use `supervise` as the sole pilot path). Refreshed the Status
section with a freshly-verified `pytest -q` count (834 passed, 18 deselected) and decisions
#43-46. Refreshed `docs/architecture.md`'s module map with the two previously-undocumented
specialists (`independent_verification`, `presentation_benchmarking`). Built the mechanical
backstop this incident's own root cause calls for: `scripts/governance/validate_plan_structure.py`
(section order, requirement-row validity, logs/ index consistency, specialist/module-map
completeness), installed as both a local pre-commit hook (`install_hooks.py`) and a required
`ci.yml` step -- `GOV-009` moves to `IMPLEMENTED`. Also built `append_log_entry.py` so future
entries are machine-generated and self-verified rather than hand-authored (the hand-authored
2026-07-22 shard was itself found non-canonical -- missing its index table and Entries header --
and was repaired as part of this pass). Caught and corrected one of this session's own errors
before it was written into the permanent record: an earlier claim that six new requirements.md
rows (`PIL-012`-`014`, `RUN-005`-`007`) mis-cited "Decision 45" was itself false on re-verification
(they never cited decision 45 at all); Decision #46's text was corrected in place rather than left
standing.

- [master requirements governance incident] **2026-07-22 — Executed the plan end-to-end: TC-05/06/07/15/16/17/23 built and tested; decision-number collision resolved; registry-broadening risk found and flagged.** Executed the approved plan end-to-end per user directive. Re-verified live state before touching
anything (git status, fresh pytest run, validator) rather than trusting prior summaries. Confirmed
TC-24-27 already done from the prior turn; triaged all remaining taskcards (TC-01-23) against
current reality; executed 7 (TC-05, TC-06, TC-07, TC-15, TC-16, TC-17, TC-23), all Critical/High-
severity or direct groundwork, each with new passing tests, not just code changes:
- SCL-007 -> IMPLEMENTED: deterministic release-list ordering (audit_package_release_surfaces.py).
- EFF-005 (new) -> IMPLEMENTED: effect-ledger lock holder-identity revalidation before the terminal
  applied write (state/backend.py, state/git_backend.py, capabilities/effect_ledger.py) -- closes
  Phase 13's F4 finding with a real test proving the exact break scenario, not just prose.
- F3 closed: commit_readme_write.py::precheck() no longer a plain string comparison against
  "accept" -- verification/checks.py::compute_verification_token() derives a re-derivable token from
  the real verify node's own accept, proven by a test that the exact old hardcoded-literal
  vulnerability is now rejected.
- AGT-006 -> PARTIAL: a real, registered "stop" capability (capabilities/stop.py) makes a genuine
  stop call telemetrically distinct from an unrecognized/hallucinated name.
- VER-005 extended: SupervisorStateV1.in_flight_run_id/in_flight_domains close the residual
  process-kill blind spot (state/domain_state.py::mark_specialist_tier_started()/
  effective_domain_coverage_complete()).
- PRL-001..008 (new subgroup within existing requirements.md §19, correcting the original plan's
  own mistaken "new §20" -- that number was already taken) and OpenProposalV1 (state/schema.py)
  added as groundwork for the not-yet-built remote-write capability (TC-08), deliberately not
  attempted this pass given its own stated prerequisites and risk profile.
Found and fixed, in passing: a pre-existing mypy error in registry/discovery.py (a str-typed
parameter reassigned to None inside its own loop) and a pre-existing ruff line-length issue in a
concurrently-added registry/self_heal.py. Found and resolved live: a decision-number collision
(both this session and a third concurrent session had informally reserved "#47" in comments only,
for two different topics) -- resolved by crediting the other session's real, shipped registry
self-heal feature as #47 and renumbering this session's own work to #48. Found during final
verification: a fourth concurrent session broadened data/products.json from 3 to 25 non-disabled
entries (still only 2 mode:"full" -- the original pilot boundary is intact), breaking 3 pre-existing
test_registry_loader.py assertions -- deliberately left unfixed and flagged for owner review rather
than silently patched, since ratifying a registry-scope change is not this pass's call to make.
Final state: 851 passed / 3 failed (the registry-scope tests above, unrelated to this pass's own
work) / 18 deselected; ruff/mypy/governance-validator all clean. TC-01 (commit/push) and TC-02
(token rotation) deliberately not executed -- both require actions (an explicit commit confirmation,
a human credential rotation) outside a single "execute the plan" directive's authority.

- [registry self-heal runtime sprint] **2026-07-22 — Runtime registry self-heal built and live-proven: drift becomes a self-healed class (CORE-034); Words/.NET healed into the registry as disabled.** Sprint trigger: `aspose-words-foss/Aspose.Words-FOSS-for-.NET` published upstream 2026-07-21 and
absent from `data/products.json`; the user directed the onboarding review sprint to "also be
healing" — registry drift became a self-healed runtime class (`CORE-034`, decision #47 + its
addendum) instead of a one-off hand-run of the refresh script.

**Built this session:**

- **Extraction (one implementation)**: `scripts/update_products_registry.py`'s discovery/merge
  core moved to `src/readme_agent/registry/discovery.py` (GOVERNANCE placement rule 2); the CLI
  became the thin wrapper `scripts/data-refresh/update_products_registry.py` (registry row's own
  "move to `data-refresh/` when next touched"); invariant tests migrated name-intact to
  `tests/unit/test_registry_discovery.py` plus two new ones (403-wait-cap raises instead of
  sleeping; `discover()` returns org failures as data). Extracted core adds
  `max_rate_limit_wait_seconds` → `RegistryScanRateLimited` (cron keeps wait-forever; the heal
  caps at 60s — without the cap, "fail-open" would silently sleep out a rate-limit reset inside
  supervise).
- **Heal module**: `src/readme_agent/registry/self_heal.py::heal_registry_drift()` — never
  raises (`HEALED`/`NO_DRIFT`/`SKIPPED_*` statuses), fail-closed on the write itself
  (`ProductEntry` re-validation before `write_atomic()`), no-token skip without network, 6h TTL
  marker (`runs/registry-heal/last_heal.json`, `paths.registry_heal_marker_path()`) so a
  sequential portfolio pass scans once, evidence (`registry_heal.json` + `manifest.json
  kind:"registry_heal"`) per real attempt. Wired as `cmd_supervise()`'s first statement, before
  preflight and `require_listed()`; `--no-registry-heal` opts out.
- **CI**: `readme-agent-supervise.yml` surfaces a healed registry as a PR on the same branch as
  the weekly cron (`chore/update-products-registry`) so diffs dedupe; `readme-agent-portfolio.yml`
  matrix jobs run `--no-registry-heal` with one dedicated `heal-registry` job per pass;
  `update-products-registry.yml` path updated. Never a push to `main` (`OPS-008` posture).
- **Tests**: `tests/unit/test_registry_self_heal.py` (8 tests: insert-disabled +
  owned-fields-untouched, fail-open on scan explosion with evidence, no-token no-network, TTL
  skip, NO_DRIFT byte-identical, evidence content, org-failure isolation, never-bootstrap-missing
  file) and `tests/unit/test_cli.py::TestSuperviseRegistryHealHook` (once-per-invocation before
  preflight, flag disable, heal error never changes exit code) + an autouse heal stub keeping
  every CLI test offline.

**Live proof (CORE-034 → IMPLEMENTED per GOV-018):** a supervise-invoked heal at
2026-07-22T10:11:04Z scanned all 26 orgs and healed `words/net` (plus other newly published
upstream repos, every one `mode:"disabled"`, owned fields null) — evidence
`runs/evidence/20260722-101104-ea9f/registry_heal.json`, which also recorded the nonexistent
`aspose-imaging-foss` org as a non-fatal `org_failures` entry. The `SKIPPED_RECENT` TTL leg
reproduced through a real second `supervise` invocation ("last heal attempt 935s ago"); the
marker-deleted rerun returned `NO_DRIFT` with `data/products.json` byte-identical.

**Concurrency notes (three sessions in these files today):** (1) master.md's Decision Ledger
already contained entry #47 for this mechanism (written by a concurrent session from the shipped
module) — this session appended a scoped addendum instead of a duplicate entry, resolving the
requirement-ID collision it flagged: the mechanism's row is `CORE-034` (`CORE-033` belongs to
decision #40's gate split); all citations renumbered. (2) Pre-existing, not this sprint's
breakage, left for their owning session: `tests/unit/test_registry_loader.py` has 4 failures
against the working tree because a concurrent session's uncommitted `data/products.json` changes
(31 entries, 25 enabled via `dry_run` flips) invalidate its 25-entries/3-enabled assertions —
those assertions match HEAD; this session did not touch them. (3) A `supervise` baseline clone of
the new Words/.NET repo timed out at 300s on this machine (environment condition, same class as
`OPS-010`) — the heal and gates ran correctly before it; noted, not fixed here.

Docs: `data/README.md` (runtime self-heal section + quick-reference), `docs/safety-model.md`
(allow-list currency paragraph incl. the read-eligibility widening stated plainly),
`docs/architecture.md` (registry module-map row), `docs/policy-authoring.md` (paths).
`validate_plan_structure.py` clean (pre-existing row-length warnings only).

- [requirements governance] **2026-07-22 — Registry broadening confirmed intentional; test_registry_loader.py rewritten to assert the real invariant; full suite verified clean (867 passed, 0 failed).** Per user decision, the data/products.json broadening (3->25 non-disabled entries) was accepted as
intentional (consistent with decision #24/PIL-011). Rewrote the 3 broken test_registry_loader.py
tests to assert the real invariant (enabled == every non-disabled entry, computed fresh from the
file) plus a floor asserting the original 3-repo pilot subset, rather than a point-in-time count --
so they won't break the next time the registry legitimately grows via the weekly discovery
workflow. Ran the full suite fresh (not reusing an earlier estimate): 867 passed, 0 failed, 18
deselected, 557s. ruff check/format and mypy all clean. Corrected an earlier unverified test-count
guess (862) in master.md's Status section with this real, measured number. Proceeding to commit
this session's work directly to main per explicit user instruction.

- [registry policy-profile clone-reliability production-hardening] **2026-07-22 — Production hardening: fixed a policy-profile fabrication incident with real verification tooling (ONB-004), and the within-run clone-redundancy root cause behind a live timeout (SCL-009).** Triggered by two problems surfacing during the prior sprint's live proof of `CORE-034`'s
Words/.NET self-heal: 4 failing `test_registry_loader.py` tests, and a 300s baseline-clone timeout
on that same repo. User asked for a deep, production-grade reassessment of both rather than local
patches — separating symptom from root cause.

**Registry-readiness incident, found and fixed.** The 4 failing tests were correct: a concurrent
session's `disabled`→`dry_run` broadening (22 entries) was already committed to `main` (user
separately confirmed intentional, tests already rewritten to a drift-proof invariant by a sibling
session) — but a *further*, still-uncommitted session had then mass-generated 22
`config/policies/*.yml` files by pattern substitution, violating `docs/policy-authoring.md`'s own
rule against string-formatted URLs and decision #4's business-facts posture: identical copy-pasted
`MIT` license claims, and `products.aspose.org` URLs guessed from a pattern with **zero**
verification data anywhere in this repo to check against. Live verification of all 25 non-disabled
entries found 9 of the 22 guessed `.com` platform URLs were genuinely wrong.

Fixed by building the verification tool this project always needed:
`src/readme_agent/registry/policy_facts.py::verify_repo_facts()` (GitHub's own license classifier +
README-text fallback; real HTTP GETs, never a guess, for every `.org`/`.com` URL). All 25 profiles
corrected against it (9 `.com` URLs fixed; every license reconfirmed genuinely MIT).
`data/aspose_com_links.json` gained a real `products.aspose.org` surface (11 families/25
platforms, live-probed, explicitly scoped not comprehensive) plus 3 corrected/2 added `.com`
platform entries found along the way. `readme-agent scaffold-policy` (new CLI) +
`scripts/data-refresh/policy_profile_coverage_report.py` are `ONB-004`'s long-designed scaffolding,
finally built, using the same verification module. New hard regression control:
`test_every_non_disabled_entry_has_ecosystem_and_policy_profile_configured` — makes the original
triggering condition (mode flip with no profile) a loud CI failure permanently.
`aspose-words-foss/Aspose.Words-FOSS-for-.NET` (this investigation's own trigger repo) was
scaffolded, fully verified with zero unverifiable fields, and wired to `mode: "dry_run"` — the live
proof. Remaining backlog: 5 entries still `disabled` (`cells/go`, `cells/rust`, `html/python`,
`pdf/cpp`, `pdf/python`), needing a human to run `scaffold-policy` and review. `ONB-004` is
`PARTIAL`, not `IMPLEMENTED`, on that basis.

**Clone-reliability redesign (`SCL-009`, new row).** The 300s timeout was a symptom: tracing one
`supervise_repo()` run found `clone_baseline()` is called independently by ~7-9 stateless
capabilities with no shared per-run repo view (decision #26(b)) — the same repo was cloned over
the network ~9-17 times per run, each an independent roll against `SCL-004`'s own measured
158s-1004s clone-time variance. This is the structural cause of cross-rerun inconsistency and a
latent correctness gap (nothing guaranteed all ~9 clones saw the same commit if upstream was
pushed to mid-run). Preserved unchanged: push-blocked clone model, `--depth 1`, decision #40's gate
split, the git-tree-API profiling path (`SCL-004`) as a named, not-this-pass, future
generalization. Fixed: an in-process memo in `clone.py` collapses the within-run redundancy to one
real clone; a new `invalidate_baseline_clone()`, called by `supervise_repo()` before its own
top-level clone, keeps every *call* (not merely every OS process) genuinely fresh —
`test_supervisor_loop.py`'s own consecutive-run tests proved a process-scoped-only memo was wrong.
`env.py::git_clone_timeout_seconds()` (default 300s→600s, evidence-informed, not a guarantee);
bounded transient-only retry (2 attempts, 5s/15s backoff; real errors fail fast); `supervise_repo()`
now catches its own clone's `GitSafetyError` and returns `BLOCKED` with written evidence instead of
an uncaught abort.

Tests: `tests/unit/test_gitsafety.py` (memoization reuse/cross-call invalidation/self-heal, retry
succeeds on synthetic timeout, no retry on real not-found, gives up after max attempts, timeout
reads env var), `tests/unit/test_supervisor_loop.py::TestBaselineCloneFailureDegradesGracefully`,
`tests/unit/test_scaffold_policy.py`, `tests/unit/test_registry_loader.py`'s new invariant. Full
suite run clean before and after. `validate_plan_structure.py` clean.

- [plan-hardening governance verification supervisor] **2026-07-22 — Plan-file hardening executed: GOV-022 check, verification-token nonce, graduated retry, deterministic backstop, hardened pre-commit gate, TC-01 push complete** Executed the Claude-Code plan file's own "PLAN FILE HARDENING" Phase 17 taskcard register:

- **TC-14 remainder** (`GOV-022`): `scripts/governance/validate_plan_structure.py::check_wave_reconciliation_gate()` fails the pre-commit hook/CI if a Build Checklist wave item flips `[ ]` -> `[x]` in the same change with no matching `logs/*.md` Wave/Phase entry. Diff-aware (compares against `git show HEAD:plans/master.md`) so Waves 0-8 are never retroactively flagged. First-ever test file for this script added (`tests/unit/test_validate_plan_structure.py`, 9 new tests plus smoke coverage for the 4 pre-existing checks).
- **TC-30**: pre-commit hook now also runs `ruff check`/`ruff format --check`/`mypy src`. Full `pytest` deliberately excluded (a ~9-minute hook would get bypassed with `--no-verify` routinely) -- stays a required CI step.
- **TC-28** (`VER-001` hardening): `compute_verification_token()` gains a `nonce` argument, minted once per `_verify_node` call, closing the cross-run token-replay gap TC-15/decision #48 named but left open.
- **TC-19** (`SCL-006` -> `PARTIAL`): a domain reporting `ERROR:` gets one immediate re-classify retry before the `CONVERGED_NO_TRACKED_CHANGE` shortcut's `all(...)` check, isolating one transient flake instead of falling through to the full planner loop.
- **TC-18** (`AGT-006` further progress): `NO_PROGRESS_TURN_LIMIT` (3) ends a run deterministically once a turn is `SUPERSEDED`/`BLOCKED` 3 times in a row, rather than burning every turn toward `max_turns`/`repair_exhausted`.
- **TC-29**: reviewed `golden_set/` (`OPS-011`) -- real, tested, but scoped to planner capability-selection scoring, not the README-content-drift weekly-replay TC-22 originally envisioned. TC-22 stays `not_attempted` for its own scope.
- **TC-01 remainder**: the 3 commits from decisions #46-48 pushed to `origin/main` for real. A live `git-credential-manager.exe` hang on the first attempt is a second corroborating data point for `OPS-009`.
- Independently duplicated part of decision #49's own finding: found and fixed the identical `test_specialists.py` clone-staleness bug via the identical mechanism before reading #49's own text -- left in place as harmless defense-in-depth per #49's own characterization, since its probe-based redesign supersedes it as the actual correctness mechanism.
- A one-off flake (`test_gitsafety.py` + `test_supervisor_loop.py` together, 9 failures once, 79/79 clean on immediate rerun) investigated and root-caused to substantial unrelated concurrent host load (other repositories' daemons plus another concurrent `readme_agent.cli supervise` process) -- logged as new evidence on the existing `OPS-010` row, not a code bug.
- A pre-existing, unrelated mypy error in `gitsafety/clone.py` fixed with an `assert` (before that file's own probe-based redesign landed).

Fresh, this-session-verified full suite: `pytest -q` -> 919 passed, 0 failed, 18 deselected (421s). `ruff check .`, `ruff format --check .`, `mypy src`, `scripts/governance/validate_plan_structure.py` all clean.

Not done this pass, still open: TC-02 (token rotation, human-only), TC-08 (the PR-opener capability -- now genuinely unblocked, all four prerequisites closed), TC-09/TC-13 (blocked on TC-08), TC-20 (deliberately deferred), TC-21/TC-22's own live-gateway measurement (no `LLM_BASE_URL`/`LLM_API_KEY` in this environment).

- [capability remote-write governance] **2026-07-22 — TC-08 built: open_presentation_pr, the one real remote_write capability** TC-08 built: `open_presentation_pr`, the one real `remote_write` capability this project registers.

- New `github_api/write_client.py` (deliberately separate from `client.py`, which stays read-only per its own explicit, pre-existing convention -- a design refinement over this plan's earlier draft): `find_open_pr()`, `create_pull_request()`.
- New `paths.pr_work_dir()` + `gitsafety.clone.create_pr_clone()`/`push_branch()` (`PRL-007`): the one clone in this codebase deliberately never neutered, structurally separate from `create_work_clone()`. Token travels via a per-invocation `http.extraheader`, never written to the remote URL or persisted config.
- Branch name deterministic (`readme-agent/presentation-update-{facts_hash[:12]}`, `PRL-001`): `find_open_pr()` checked before ever cloning or pushing; `reconciliation_check()` answers the same question for the effect ledger's `EFF-001` lifecycle.
- `precheck()` reuses `compute_verification_token()` unchanged (TC-15/TC-28's hardening, inherited for free). Gated on `entry.mode == "full"` inside `execute()` itself, matching `commit_readme_write`'s own pattern.
- `EFF-005`'s lock-holder-identity revalidation (`PRL-008`) inherited for free via the same `dispatch_gated_effect()` path.
- 17 new tests (`tests/unit/test_open_presentation_pr.py`): GitHub API mocked; `create_pr_clone()`/`push_branch()` exercised for real against a local bare repo (proves the clone is genuinely never neutered and a real branch lands); `execute()`'s own orchestration (mode gate, existing-PR short-circuit, argument threading) tested with clone/push monkeypatched.
- Deliberately NOT wired into any specialist's automatic dispatch path this pass -- building/proving the capability was the scope; deciding when the autonomous loop should call it is a separate decision.
- New `scripts/retrofits/prove_open_presentation_pr_live.py`: real render -> real verify -> real `dispatch_gated_effect()` against the real `GitStateBackend`. Refuses to run without an explicit `--confirm-real-pr` flag.
- `plans/requirements.md`: PRL-001 (PARTIAL), PRL-002/004/007 (IMPLEMENTED), PRL-008 (re-confirmed against a real remote_write capability) updated with this evidence.

Full suite: `pytest -q` -> 938 passed, 0 failed, 18 deselected (287s). `ruff check .`, `ruff format --check .`, `mypy src`, `scripts/governance/validate_plan_structure.py` all clean.

Not yet live-proven: no real PR has been opened against a real pilot yet -- that is a separate, explicitly-confirmed action, not implied by this build.

- [capability remote-write live-proof] **2026-07-22 — TC-08 live-proven: real PR opened against aspose-cells-foss/Aspose.Cells-FOSS-for-Java** TC-08 live-proven: a real, open, unmerged PR opened against `aspose-cells-foss/Aspose.Cells-FOSS-for-Java`.

- User confirmed the specific target repo before running (`GOV-018`, per-instance confirmation).
- First attempt failed: `gitsafety/clone.py::push_branch()`'s original `AUTHORIZATION: bearer {token}` header authenticates against GitHub's REST API but NOT its git-http backend -- silently fell through to normal credential-helper resolution, which correctly fast-failed on decision #51's own `GCM_INTERACTIVE=never` fix (working exactly as intended) rather than hanging.
- Fixed by switching to Basic auth (`x-access-token:{token}`, base64-encoded) -- matching this project's own already-documented, already-live-proven precedent (`test_state_git_backend_live.py`'s own `OPS-009` prerequisite) exactly. Logged on `OPS-009` as a real miss, not a design gap.
- The failed first attempt correctly left a `pending` effect-ledger entry. Confirmed via direct `GitStateBackend.load()` inspection that nothing had reached GitHub, then cleared it by hand (a real CAS save) before retrying -- `blocked_pending_reconciliation` firing on the immediate retry is this project's own designed behavior working correctly.
- Second attempt succeeded end to end: `render_readme_candidate` (real live LLM call) -> `verify_readme_candidate` (real independent accept) -> `open_presentation_pr` (real branch push + real PR create), all through the real `dispatch_gated_effect()` against the real `GitStateBackend`.
- Independently re-verified afterward via a plain read-only `GET`: `state=open`, `merged=false`, `head=readme-agent/presentation-update-15f811d4cf8e`, `base=master`.
- `plans/requirements.md`: PRL-001 -> IMPLEMENTED, PRL-004/008 evidence updated with the live proof, OPS-009 gets a third documented variant.

This is the live 3-repo proof's own first repo, not the whole of `TC-13` -- one PR against one pilot, not yet the full 10-domain, 3-repo pass `TC-13` itself calls for. `TC-08`'s own taskcard is now `completed_verified` in full.

- [FULL-PROJECT-TRUTH-AUDIT-REPEAT] **2026-07-22** — Repeated the whole-project audit after TC-08: credited the real Cells/Java PR while retaining `NOT PRESENTABLE`/Level 3; corrected stale `PIL-001/003/011/014`, `ONB-002`, and `PRL-002` requirements plus current architecture/schema text — the PR path is real but manual, non-draft, unrecorded in proposal state, and unproven across all three Java targets.

- [requirements supervisor incident] **2026-07-22 — decision #53 — SHA-probe evidence trail closed; `ORC-006`/`ORC-003` corrected from stale text; `aspose-3d-foss` STALE_NONCOMPLIANT investigation closed, no fix shipped.** A separate concurrent session this same day, working from its own live-proof-then-hardening pass against Waves 8.5/8.6, found two documentation gaps and one real, small remaining code gap; this entry records all three plus what was deliberately *not* fixed.

- **Documentation correction, not new code**: `ORC-006`'s row had sat `BACKLOG` since decision 45 even though the pre-clone SHA-probe shortcut it describes was actually built in Wave 8.5 and has been live-proven repeatedly since (this session re-confirmed it against `aspose-cells-foss/...Java` and `aspose-3d-foss/...Java`: a reconvergence pass completes near-instantly with zero clone and zero specialist dispatch, `durable_state.last_run_id` correctly unchanged). Corrected to `PARTIAL` with the real evidence cited -- not `IMPLEMENTED`, since the row's own `OPS-011`-measured revisit trigger has no accumulated measurement history yet. `ORC-003` had a matching problem: its text still described dynamic specialist selection as merely "evaluated ... rejected" (2026-07-21), with no mention that this session's own earlier work reversed that rejection for real in Wave 8.6 (the prerequisite safety fixes -- revision-stamping every domain via `save_domain(current_revision=...)`, a revision-aware `compute_domain_coverage_complete()` -- built and unit-tested *before* the skip mechanism itself, per the original rejection's own stated objection; the skip mechanism restricted to 4 domains with a real diff-correlated signal, a forced-run floor at 3 consecutive skips, final enforcement intersecting the LLM's request against the pre-computed eligible set, defaulting fully off). Live-proven, 2026-07-22, across 3 real pilots: `domain_coverage_complete` now correctly computes `True` with all 10 domains genuinely revision-stamped to the same real SHA, versus the pre-fix state where only `readme_reconciliation` was ever stamped and coverage was a false positive by construction (only 1 of 9 domains stamped, `domain_coverage_complete: None`, confirmed by direct durable-state inspection before the fix). Residual risk (the diff heuristic's `community_files_presentation` blind spot) stays disclosed, not claimed closed.

- **Real, small code gap closed**: the SHA-probe shortcut itself wrote no evidence at all before this -- only a console line -- unlike its post-clone sibling (`CONVERGED_NO_TRACKED_CHANGE`), which has written a full evidence bundle since decision #41. A human debugging months later could not tell, from evidence alone, that this specific (cheapest, zero-clone) shortcut fired versus the run never happening. `supervisor/loop.py` now writes a real `manifest.json`/`decisions.json` pair via the existing `_write_supervise_evidence()`/`_assert_evidence_complete()` machinery on this path too, with a new `kind="sha_probe_shortcut"` decision distinguishing it from the post-clone shortcut's own `"stop"` kind. `test_supervisor_loop.py::TestPreCloneShortcut` extended to assert the evidence bundle now exists with the right decision kind; live-confirmed via a real `aspose-3d-foss` rerun hitting exactly this path.

- **Investigated, not fixed**: an earlier live pilot pass against `aspose-3d-foss/...Java` had hit `render_readme_candidate` -> `STALE_NONCOMPLIANT` (a correct, by-design signal, decision 16 -- durably-accepted content fails validation, nothing changed upstream) without the planner ever retrying `render_readme_candidate(force_regenerate=true)`, instead exploring other capabilities until `BLOCKED/repair_exhausted` at the 8-turn cap. Re-run twice, live, specifically to check whether this reproduces: it does not -- the same repo now converges cleanly (`detect_readme_gaps` reports zero gaps against the real, current upstream content), and a third rerun correctly hit the newly-evidenced `sha_probe_shortcut` path. `durable_state.accepted_status` for this repo is `None` (no commit was ever made through this mechanism), so the most likely explanation is the real, upstream README changed independently between the original incident and this investigation -- not a persistent bug in this codebase. No fix was designed or shipped for this reason: building a stronger force_regenerate hint, or raising the turn budget, against a condition that no longer reproduces would be solving a problem this evidence does not show still exists. If it recurs, the next steps remain: inspect the dossier/turn-context content the planner actually saw, then choose between a stronger prompt hint, a larger turn budget, or accepting `BLOCKED` as the correct posture for a genuinely stuck case.

- Full suite: `pytest -q` -> 938 passed, 0 failed, 18 deselected. `ruff check .`, `ruff format --check .`, `mypy src` clean.

- [requirements governance runtime] **2026-07-22 — Convergence-sprint execution begins: Wave 9.1 verification baseline, lockfile, malformed-table-row fix, GOV-024 reclassified** Began autonomous execution of the 2026-07-22 convergence-sprint plan (Waves 9-15) per explicit user
direction ("execute the plan e2e autonomously. keep docs/logs updated"). This entry covers Wave 9.1
(Verification Baseline & Clean-Environment Bootstrap).

Established a real baseline before touching anything: `ruff check`/`ruff format --check`/`mypy src`
all clean; ran the full `pytest -q` suite 3 consecutive times in the existing `.venv` -- 938 passed,
0 failed each time, zero orphaned `git`/`python` processes after each run (`tasklist`-confirmed).
Built `requirements-lock.txt` (`pip freeze`-based, chosen over `uv`/pip-tools per GOV-015 -- zero new
tooling dependency, works in any Python environment) and proved it end to end: a genuinely fresh venv
built only from the lockfile plus `pip install --no-deps -e .` reproduces a clean import, clean
ruff/mypy, and a clean full pytest run (944 passed, including 6 new tests added this session).
Evidence: `plans/investigations/evidence/verification-baseline-bootstrap/bootstrap-proof-2026-07-22.json`.

Built `scripts/governance/run_official_checks.py` -- the single scripted command for all 6 official
checks (ruff/ruff-format/mypy/pytest/validate_plan_structure/actionlint), run and proven end to end.

Independently found and fixed a real, previously-undetected defect: 5 rows in `plans/requirements.md`
(`OPS-009`, `EFF-004`, `ORC-006`, `VER-005`, `SCL-005`) had a broken Markdown table structure --
verified by direct pipe-count (7 expected per row for 6 columns; these had 8 or 6) -- caused by
unescaped `|` characters inside inline code (shell pipes, Python `X | None` type unions) or one
missing separator. Any naive `.split("|")` parser, including this project's own
`validate_plan_structure.py::_requirement_rows()`, silently misreads these rows. Fixed all 5 by
escaping the offending pipes (`\|`) or restoring the missing separator; extended
`validate_plan_structure.py` with `_split_table_row()` (a real, escape-aware cell splitter) and a new
`check_requirement_row_column_counts()` gate so this class of defect fails CI/pre-commit going
forward -- 6 new tests added (`tests/unit/test_validate_plan_structure.py`), all passing.

Independently found and fixed a stale requirement row in the opposite direction (an underclaim, not
an overclaim): `GOV-024` claimed the supervisor's planner prompt was "still a Python string literal,
never migrated." Direct source verification (zero `SYSTEM_PROMPT` matches in `src/`) found the
migration is already fully done -- `supervisor/loop.py:846` loads it via
`prompt_registry.get("supervisor_turn")`, and `supervisor/convergence.py::compute_control_plane_
fingerprint()` already hashes it via `prompt_registry.content_hash()`, its own docstring explicitly
citing "GOV-024/Wave 8.5" by name. Reclassified `GOV-024` to `IMPLEMENTED` -- no new code needed,
just a corrected status, per this same session's own discipline against trusting the register over
live source.

Updated `OPS-010` with new corroborating evidence (4 consecutive clean full-suite runs this session,
zero hangs, zero orphaned processes) -- explicitly NOT reclassified to `IMPLEMENTED` on this basis
alone, since a clean small sample does not establish a root cause for a previously intermittent,
load-correlated issue; stays `PARTIAL`.

New row: `GOV-025` (`IMPLEMENTED`) for the scripted official-checks suite + lockfile.

Full suite re-verified clean after all changes: `run_official_checks.py` itself passed end to end
(ruff/ruff-format/mypy/pytest/validate_plan_structure/actionlint all green).

- [requirements tooling] **2026-07-22 — Traceability-matrix tool built and run: 111 IMPLEMENTED rows checked, 0 high-confidence overclaims found** Wave 9.2 (Implementation-Truth Matrix & Requirement Reclassification) of the 2026-07-22
convergence-sprint plan.

Built `plans/investigations/tools/traceability_matrix.py`: for every `plans/requirements.md` row
currently marked `IMPLEMENTED` (111 rows), mechanically extracts every backtick-quoted file path its
Acceptance-evidence cell cites and checks it actually exists on disk, whether it cites at least one
real `tests/` path, and whether it uses live-proof language. Deliberately downgrade-only and honest
about its own limits: this is not a semantic re-verification of every claim (that needs a human/agent
reading the actual test bodies) -- it is the mechanical floor `GOV-007` already requires. Findings are
split by confidence: a cited path that does not exist is high-confidence/actionable; "no test path
cited" is informational-only, since many Phase 0-15 rows legitimately use terse prose evidence that
predates this project's later per-row citation convention -- treating that as an equal-weight finding
would have drowned the real signal in noise. Checked no existing test convention applies to
`plans/investigations/tools/*.py` (no sibling tool, e.g. `extract_requirements.py`, has one) before
deciding not to add one here, matching the existing pattern rather than introducing an inconsistent
new one.

Result, live-run 2026-07-22: 111 `IMPLEMENTED` rows checked, 0 with a high-confidence finding (no row
cites a path that does not exist -- a genuinely clean result, not assumed), 74 with informational-only
findings (no `tests/` path cited, mostly older Phase 0-15 rows), 37 fully clean. Evidence:
`plans/investigations/evidence/implementation-truth-matrix-2026/matrix.json`.

This closes the mechanical half of Wave 9.2. The deeper semantic reclassification sweep across the
~170 still-`PLANNED`/`BACKLOG`/`PARTIAL` rows the 2026-07-22 truth audit inventoried (requiring
per-row code reading, not just path-existence checking) remains open for subsequent sessions, per the
plan's own design.
