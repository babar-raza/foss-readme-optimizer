# Project log — 2026-07-31

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-07-31 | verified-python golden-template trusted-preservation caching anti-stall | `88` | `L8-041`, `L8-042`, `TRP-023` | Verified Python POC | Preserved trusted-lane machinery/evidence but suspended its remaining goals; promoted the accepted Note reference; made reusable-template, Note, Page/PDF, and all-Python verified milestones the executable critical path. |
| 2026-07-31 | verified-python golden-template closure | `88` | `L8-042` | `L8-VPY-00` | Compiled the accepted Note structure into the reusable assurance-neutral contract and closed it with clean full-suite evidence. |

## Entries

- [verified-python golden-template trusted-preservation caching anti-stall] **2026-07-31 —
  The trusted lane is preserved but no longer executes; verified Python is now the immediate
  POC.** The owner accepted the corrected Aspose.Note FOSS for Python golden sample as the
  structural reference. Its exact bytes were promoted to
  `plans/investigations/evidence/readme-presentation-contract-v1/` with source revision and
  SHA-256 provenance. Decision #88 requires the first executable task to compile that reference
  into `RepositoryPresentationTemplateV1`, a fact-slot-driven structural contract with
  compact/standard/extended density profiles rather than a universal prose template.

  The sole mission graph now defines `GOAL-V0-VERIFIED-PYTHON-POC` and four ordered tasks:
  template, verified Note, verified Page/PDF, and the complete dynamic Python cohort. Remaining
  trusted TP/T0R/T1/T2/T3 goals stay inspectable with `execution_required: false`, retaining
  reusable extraction, LLM composition, repair, cache, retry, lease, workflow, staging, App, and
  effect proof without allowing them to regain task/effect authority or count as verified
  evidence.

  Requirement `L8-042` promotes the trusted anti-stall lesson and dependency-complete caching to
  verified work. Two ineffective same-fingerprint attempts or 15 minutes without material
  narrowing prevent another equivalent attempt; a first-principles review precedes a third
  approach. Stage reuse binds source, facts, template, prompts, policy, validators, reviewer
  standard, protected-content fingerprint, and runtime, so only downstream work is reopened.
  Mission-control tests and requirement/task coverage are updated to make the route executable
  through the goal interface rather than narrative-only.

- [verified-python golden-template closure] **2026-07-31 — The accepted Note structure now
  compiles through one reusable assurance-neutral contract.** Commit `5f463e75` adds the governed
  `repository-presentation` template, compact/standard/extended profiles, explicit fact/configured
  standard/inherited/omitted slot provenance, `ProductFactsV2` and inherited-reference adapters,
  a prose-free compiler, complete dependency cache keys, and a separate deterministic verifier.
  The golden reference recompiles byte-for-byte; a different verified-facts product uses the same
  structure without Note prose. Missing facts, extra badges, cross-product identity, comments,
  emoji, copyright, shallow or directional Mermaid, and every stale cache dependency fail closed.
  A clean commit-bound four-worker run passed all 2,458 non-live tests with zero leaked processes.
  Evidence and reproduction are under
  `plans/investigations/evidence/readme-presentation-contract-v1/`. This closes only
  `L8-VPY-00`; repository-verified Note approval remains `L8-VPY-01`.
