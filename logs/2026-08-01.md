# Project log — 2026-08-01

Back to [index](README.md).

## Index

| Date | Tags | Decisions | Requirements | Wave/Phase | Summary |
|---|---|---|---|---|---|
| 2026-08-01 | master requirements evidence intake | — | `L8-037` | L8-INTAKE-02-READONLY-PREFLIGHT-ENROLLMENT | One supervisor run proved unseen discovery, disabled admission, and one intake; closed L8-INTAKE-02/L8-037. |
| 2026-08-01 | master requirements registry revision | — | `L8-036`, `L8-037`, `L8-038`, `L8-039` | L8-INTAKE-03-REGISTRY-REVISION-QUEUE-AND-HEALTH | Gated portfolios on immutable RegistryRevisionV1 and admitted the 32nd registry entry, PDF Go MCP, disabled. |
| 2026-08-01 | master requirements evidence act | — | `L8-039` | L8-INTAKE-03-REGISTRY-REVISION-QUEUE-AND-HEALTH | Proved the new registry workflow job under ACT; its fixture revision is rejected as evidence outside ACT. |
| 2026-08-01 | requirements evidence act outage | — | `L8-039` | L8-INTAKE-03-REGISTRY-REVISION-QUEUE-AND-HEALTH | Proved all four workflow triggers and a fail-closed source-outage negative control for the registry job under ACT. |
| 2026-08-01 | requirements evidence act recovery | — | `L8-038`, `L8-039` | L8-INTAKE-03-REGISTRY-REVISION-QUEUE-AND-HEALTH | Proved the full registry-to-health workflow chain under ACT: happy path, matrix isolation, and trigger recovery. |
| 2026-08-01 | requirements evidence registry closure | — | `L8-038`, `L8-039` | L8-INTAKE-03-REGISTRY-REVISION-QUEUE-AND-HEALTH | Closed L8-INTAKE-03/L8-038 via a live 33-repository scan that admitted private CSSForge and fixed a stale-retry bug. |

## Entries

- [master requirements evidence intake] **2026-08-01 — Closed L8-INTAKE-02/L8-037 by proving one public-supervisor run performs unseen discovery, disabled admission, and exactly one durable intake.** L8-INTAKE-02's last missing-proof item required one public-supervisor integration to start from an unseen discovery observation, admit the repository disabled, and execute exactly one intake in the same logical run — discovery admission and durable intake had previously only been proven separately. Implementation commit `ff462ceea` (earlier the same day) added `tests/integration/test_readonly_intake_portfolio.py::test_public_supervisor_admits_unseen_repository_and_runs_exactly_one_intake`, driving a real `readme-agent supervise` invocation against an unseen real-local-Git observation (`aspose-widget-foss/Aspose.Widget-FOSS-for-Python`) that admits it in disabled mode, reloads the allow-list, and completes one durable intake ending in a truthful `BLOCKED_CLASSIFICATION` (missing ecosystem/policy ownership) with zero target effects. This commit records that run as `public-supervisor-proof.json`, flips the L8-INTAKE-02/L8-037 verdict from `PARTIAL` to `VERIFIED`/`IMPLEMENTED` in `plans/master.md` and `plans/requirements.md`, and confirms the affected 127-test regression set plus Ruff/format/mypy/diff checks pass. Commit `d48a352c4`.

- [master requirements registry revision] **2026-08-01 — Gated portfolio campaigns on immutable, content-addressed RegistryRevisionV1 receipts and admitted the newly discovered PDF Go MCP repository disabled.** Added `registry/revision.py`, which builds content-addressed `RegistryRevisionV1` receipts from authorized-source observations, reconciliation, the admitted denominator, source failures, and intake-delivery state; `registry/revision_gate.py`, which evaluates freshness, source completeness, intake settlement, unexplained observations, and denominator drift before any portfolio fan-out; and `registry/revision_store.py`, which persists immutable/current revisions and binds one exact revision to every campaign member's manifest. `supervisor/registry_revision_preflight.py` and `supervisor/registry_intake_queue.py` now own the sole pre-fan-out discovery boundary, mapping one provider-ID-plus-observation-revision change to one existing durable trigger and intake attempt. A new `readme-agent registry-preflight` CLI command (`--no-refresh`/`--force-refresh`/`--output`) and a new `registry` job in `.github/workflows/readme-agent-production.yml` run before `recover`/`plan`/`analyze`, uploading the revision artifact those jobs download; a weekly Sunday 04:43 UTC cron was added alongside the existing daily 05:17 UTC schedule for full-source audit. The same generic-variant reconciliation path admitted the previously-unmatched `aspose-pdf-foss/Aspose-PDF-FOSS-for-Go-MCP` observation as the registry's 32nd entry in `data/products.json`, `mode: disabled`. The live denominator moved 31 to 32; `L8-INTAKE-03`/`L8-038`/`L8-039` move from `PLANNED` to `PARTIAL` bound to live revision `93c718b4...968f3`, correctly rejecting Gate-A because `aspose-imaging-foss` returns HTTP 404 to the current credential. 247 new tests land in `tests/unit/test_registry_revision.py` alone, across roughly 40 changed files. Commit `d56ca3651`.

- [master requirements evidence act] **2026-08-01 — Proved the new workflow's registry job passes under Docker/ACT and bound its fixture revision as non-admissible outside ACT.** The real reusable workflow's new `registry` job (added in `d56ca3651`) passed under Docker/ACT at control commit `dc47169aa` with exit 0, producing a complete 32-member fixture `RegistryRevisionV1` with zero source failures, pending intake, or unexplained observations. The fixture is explicitly tagged `proof_scope: act_fixture`, and evaluating the persisted revision outside ACT correctly fails closed with `act_fixture_not_admissible`, so a synthetic ACT source cannot impersonate live GitHub enumeration. `act-registry-job-summary.json` binds the raw 104,032-byte disposable log and the uploaded 31,360-byte artifact by SHA-256. `L8-INTAKE-03` and `L8-039` stay `PARTIAL` — the amended workflow's downstream jobs beyond `registry`, and live all-source visibility for `aspose-imaging-foss`, remain open. Commit `f5a733e59`.

- [requirements evidence act outage] **2026-08-01 — Proved all four workflow trigger variants at the ACT registry boundary plus a fail-closed injected source-outage negative control.** Extended the ACT proof of the `registry` job to all four real trigger shapes — `workflow_dispatch`, `schedule`, `repository_dispatch`, and `workflow_call` — each exiting 0 and uploading its own revision artifact. A separate run injecting an `aspose-imaging-foss` source outage exits 1, correctly records both `source_scan_incomplete` and `source_failures_present` gate reasons, preserves the existing 32-entry registry rather than corrupting it, and still uploads diagnostic evidence through the workflow's `always()` upload step. This closes trigger-parity and outage behavior for the registry job specifically; the downstream recovery and matrix jobs after this new boundary remain unproven under ACT. Commit `afb405556`.

- [requirements evidence act recovery] **2026-08-01 — Proved the full registry-to-health workflow chain under ACT: happy path, matrix isolation, and expired-trigger recovery.** At control commit `845bbaa31` (which had just fixed health to surface workflow matrix failures), three ACT runs exercised the new registry-gated boundary end to end. Run 42 reached `INTAKE_READY` for the newly admitted Go MCP repository with a `RunManifestV3` bound to all 32 admitted repositories, zero LLM calls, and exit 0. Run 43 injected a pre-supervisor Go MCP matrix failure and confirmed the unrelated `aspose-note-foss/Aspose.Note-FOSS-for-Python` job still completed with its own registry-bound evidence, while health correctly recorded `workflow_failures: [{job: analyze, result: failure}]` and the workflow exited 1. Run 44 recovered a deliberately expired `processing`-status trigger, incrementing its recovery count and resuming the exact same dedup key to reach `completed`/`INTAKE_READY` with exit 0. `act-registry-intake-workflow-summary.json` binds all raw disposable artifacts by SHA-256; this closes local workflow-fixture proof for downstream intake, isolation, and recovery, leaving only a source-complete live scan with all-visibility credentials for `aspose-imaging-foss` open. Commit `815350ad2`.

- [requirements evidence registry closure] **2026-08-01 — Closed L8-INTAKE-03/L8-038 with a live all-visibility scan that discovered and admitted a 33rd private repository, CSSForge, and repaired a stale-retry lifecycle bug.** The 2026-08-01 all-visibility scan (control commit `1f4f8a98f`) enumerated public and private repositories across every enabled source into a complete 33-member `RegistryRevisionV1` (`d610fc75...22bb7e`) with zero unexplained observations, source failures, or pending intake, and one explicit exclusion recorded for the now-nonexistent `aspose-imaging-foss` source (the separate non-FOSS `aspose-imaging` organization was deliberately not substituted). Discovery admitted one newly found private repository, `aspose-html-foss/CSSForge` (GitHub repository ID `1319039143`), cloned over a process-local GitHub HTTPS credential header that is never written to Git config, command arguments, or evidence, with Git LFS smudging disabled for source inspection. The first CSSForge intake attempt exposed a real bug: a successful intake receipt was leaving the lifecycle stuck reporting stale `SYSTEM_FAILURE`; commit `1f4f8a98f` permanently repaired both future retries and historical reconciliation, after which the retried intake correctly classified CSSForge `READY_FULL_PIPELINE` at its immutable source revision, and a clean identical replay produced no new queue entries or effects. This commit binds that live result (`live-registry-all-visibility-summary.json`), the prior ACT happy-path/isolation/recovery runs (42-44), and a fresh clean 2,604-test suite together, closing `L8-INTAKE-03`/`L8-038` from `PARTIAL` to `IMPLEMENTED`; `L8-039` stays `PARTIAL` because hosted schedule/dead-man operation is deferred to later trusted-T3/hosted-runtime proof. Commit `f43f9f48c`.
